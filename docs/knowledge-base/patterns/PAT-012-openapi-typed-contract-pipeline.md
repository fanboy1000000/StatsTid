# PAT-012 — OpenAPI typed-contract pipeline (fork B)

| Field | Value |
|-------|-------|
| **Type** | PAT (pattern) |
| **Status** | active |
| **Introduced** | S111 (Phase 0 of the long-term typed-contract commitment) |
| **Relates to** | PAT-010 (endpoint-response-contract — the wire-byte anchor), PAT-011 (design-sync-drift-gate — the generate-and-gate sibling) |

## Problem
The FE↔backend response **shape** was hand-copied on both sides (`apiClient.get<T>` casts `res.json() as T` with a hand-written `T`; the backend hand-shapes the JSON). A FE list-hook reading a shape the backend doesn't serve passed its own mock test (green) but broke in prod — the "fetchEnheder" class, shipped 3× (S97/S99/S100). PAT-010 added a regression guard (the backend shape stays stable); it did NOT guarantee FE↔backend AGREEMENT.

## Pattern — the pipeline + 4 gates
**One generated source of truth, gated at every hand-off so drift is a CI failure, not a prod incident.**

```
backend Contracts/ records + .Produces<T>/.Accepts<T>
        │  (--openapi no-DB entrypoint; Swashbuckle, camelCase)
        ▼
   docs/api/openapi.json   ── committed
        │  (npm run gen:api; openapi-typescript)
        ▼
 frontend/src/lib/api-types.ts  ── committed
        │  (the typed apiClient: get(pathKey, {params,query}))
        ▼
        FE hooks (tsc)
```

**The 4 gates (each closes one hand-off; complementary, not redundant):**
1. **spec≡runtime** (`OpenApiSpecRuntimeTests` + `SpecRuntimeMatcher`, xUnit, build-and-test) — the committed schema matches the REAL serialized response PER ROUTE: root/array-ness, property presence, camelCase, declared-success-STATUS fidelity (S112), explicit 204, **required-fidelity** (a schema-`required` member absent at runtime = RED; present-but-null SATISFIES required — S113) and **enum-fidelity** (a non-null value outside a declared `enum` set = RED; **`required` and `nullable` are ORTHOGONAL claims — the enum set never arbitrates null**; null is admissible iff `nullable` — S113). `.Produces<T>` is a *convention* that can lie (esp. array-ness — author a bare array as `.Produces<IEnumerable<T>>`); THIS is the closer.
2. **spec drift** (`tools/check_openapi_sync.py`, build-and-test — needs .NET) — the committed `openapi.json` == what `--openapi` regenerates today.
3. **types freshness** (`npm run gen:api && git diff --exit-code`, frontend-build — needs Node) — the committed `api-types.ts` == what the spec regenerates. (Closes the spec→TS hand-off; without it the bug class reappears one level up.)
4. **convention** (`tools/check_openapi_convention.py`, docs job — pure Python) — every NEW/non-grandfathered operation in `openapi.json` carries a non-empty response (+ requestBody for body verbs) schema, **or declares `204` as its ONLY success response with no content (`.Produces(204)` — the typed no-body statement; S112 owner-ratified amendment)**; an untyped `Results.Ok(new {…})` lands with empty content → caught; a mixed inferred-empty-200 + 204 op or a no-success-code op still fails (the "only" formulation is load-bearing — `--selftest` proves both directions). The grandfather manifest (`tools/openapi-convention-exempt.txt`) only SHRINKS (a stale entry FAILS). **This is the durability keystone** — it forces every new endpoint typed while the retrofit drains the existing.

Plus the no-`as` lint (`npm run lint`, frontend-build) — the typed-read hooks carry no `as` / no hand-written `get<T>` type arg.

## The paved road (authoring a new admin endpoint — S112-amended)
1. Define a named RESPONSE record in `Contracts/`. **Sibling handlers emitting the identical shape share ONE record** (org create/update/move → one `OrganizationResponse`) — it makes "same wire contract" structural instead of coincidental and halves the strict-types retrofit surface.
2. Return it (`Results.Ok/Created(theRecord)`) + declare `.Produces<T>(code)` with the CORRECT collection-ness (`IEnumerable<T>` for a bare array) AND the code the handler REALLY returns. A body-less mutation: `Results.NoContent()` + `.Produces(204)` — no record. **An explicit `.Produces` REPLACES Swashbuckle's inferred empty 200 on an `IResult` handler** (S112-verified), so declared status codes become honest as a side effect of typing. **Keep ONE declared 2xx per endpoint — or a HOMOGENEOUS conditional-status set (S115): an op that genuinely returns different 2xx codes from ONE shape (e.g. 201 first-assign / 200 reassign) declares each status with the SAME record; the gate resolves the set IFF every declared 2xx carries one shared `$ref` (204 never a set member; inline schemas rejected conservatively) and asserts the runtime status ∈ the declared set.** A per-status branch proof requires ONE DEDICATED SEED STATE PER DECLARED STATUS (in separate Organisations for reporting-tree ops), with the actual status equality-asserted before the matcher call — set-membership alone cannot prove branch coverage. Shape-copy the HANDLER's anonymous object, never the repo model (enriched projection rows are often deliberate SUBSETS — S115's tree row has no `effectiveTo`); and never let a hand-written FE interface `extends` a sibling response type — it inherits phantom fields (the S115 `DirectReport` lie, the program's 4th).
3. **Request side: let Swashbuckle INFERENCE speak** — the inferred requestBody schema is generated FROM the bound parameter type, truthful by construction. `.Accepts<T>` OVERRIDES inference and can lie exactly like `.Produces`; if it is ever used, it MUST match the bound parameter type. **A route-param-only action POST that binds NO request DTO (S116, owner-ratified — the request-side analog of the declared-204 rule): declare it in `tools/openapi-bodyless-declared.txt`.** A listed op passes the convention gate IFF its response is typed; an UNLISTED body verb with no requestBody schema still FAILS (the "forgot the DTO" tripwire is preserved); a listed op with an untyped response still FAILS (liveness); a declaration whose op grew a body or disappeared turns STALE → FAIL (selftest covers all four directions). First members: `POST /api/approval/{periodId}/approve` + `/employee-approve`. The FE typed form for such an op sends NO body — a legacy `{}` body was a never-read delta, named per the S112 precedent. **(S113 correction of the S112 framing: request-body `required` IS emitted where the C# class declares `required` members — and that subset is binder-ENFORCED [absence → 400]; the S112 sentence "generated request-body types are all-optional" was FALSE — S112's own createUser finding rode exactly this mechanism. The honest boundary: only C#-`required` members are strict; non-`required` NRT/value members stay optional in the generated type because the binder tolerates their absence — the `ResponseStrictTypesFilter` deliberately does NOT touch request schemas, which would over-claim.)**
4. Regenerate: `dotnet run --project src/Backend/StatsTid.Backend.Api -- --openapi` → commit `docs/api/openapi.json`.
5. FE-consumed? `npm run gen:api` → commit `api-types.ts`; call via the typed forms — `apiClient.get/post/put/delete(pathKey, {params, query, body})` or `apiFetchWithEtag(pathKey, {method, params, ifMatch, body})` for If-Match routes (no `as`, no type args). **Verify by CALL FORM, not compilation: a raw-body/explicit-`T` string-path call SILENTLY falls through to the untyped fallback — tsc will NOT flag a missed switch** (S112-verified empirically).
6. Add a per-route spec≡runtime assertion (the S112 per-family fixture pattern; a mutation assertion gets its OWN dedicated seeded row — xUnit intra-class order is not guaranteed).
7. The 4 gates + the lint enforce all of the above in CI. (Do NOT add the endpoint to the grandfather manifest — it only shrinks.)

## Retrofit recipe (Pass ≥1, per drained op)
Response record (+`.Produces`, or `.Produces(204)` for body-less) as an EXACT shape-copy — deliver a field-mapping table (anonymous↔record member) for byte-identity review; NO request-class changes; regen spec+types; switch the FE call-site to the typed form; DELETE the manifest line; add the per-route assertion. The FE switch is audited by call form per route (see step 5). Typed-client overload extensions follow the S112 pattern: typed overloads FIRST constrained to literal path-key unions; explicit-`T`/template-URL callers fall through by constraint/arity; runtime structured-vs-legacy discrimination is key-subset-based and must behavior-coincide on every ambiguous overlap; success-status-aware derivation (200/201 JSON, declared-204 → `undefined`, undeclared-content 200s EXCLUDED); proven by synthetic `paths`-shaped fixtures + `@ts-expect-error` tripwires.

**The request-side lie detector (S118, the compile-refusal rule):** when the spec-derived body type REFUSES to compile against the current FE payload (a C#-`required`, binder-enforced member the FE never sent), the mismatch is a LIVE request-side contract defect — that write flow is ALREADY dead in prod (binder 400). Do NOT reshape the type, add the field ad hoc, or silence with `Omit`-typing alone: report it as a FINDING, keep the call on the legacy form route-helper-pinned (partial lint tier — the S115/S116 sanction boundary), wire-pin the CURRENT bytes as a named deferred defect, and fix payload + typed-form graduation in ONE later deliberate change (an `Omit` alias is permitted only as the documentation of that pinned deferral). This is the request-side analog of "a hand-written FE interface is a mock" — tsc refusal is the first COMPILE-TIME detection of a dead write flow (the response side needed prod bugs to surface). First firings: the wage-type-mapping update PUT and the child-entitlement update PUT (both omit binder-required `effectiveFrom` — every such edit 400s in prod; S118 named deferred defects #1/#2).

**The transcription-slip class (S118, 4th instance — the shape-copy rule wins):** planned member COUNTS in fact sheets and task descriptions are transcription-prone (S118 alone: "43 fields", the position mapper's site count, the 26/4 byte-identity split — all corrected at Step-0b — and the position record planned as "13 members" but correctly shipped as 14). The record is ALWAYS an exact shape-copy of the emission site; when a planned count disagrees with the code, the code wins and the implementing agent DECLARES the discrepancy rather than trimming the shape to match the plan. A count in a plan is a checksum, not a specification.

**The repo-test-masking smell (S118):** a repo-level test that HAND-CORRECTS an endpoint's invalid inputs (e.g. "bare strings are not valid JSON, so we pass proper JSON" — a literal comment in the pre-S118 audit-repo suites) masks an endpoint-level failure indefinitely; the per-route spec≡runtime gate is often the FIRST honest HTTP driver of a success path. Treat such workaround comments in repo tests as endpoint-bug smells and audit their endpoint counterparts. First firing: agreement-config archive + supersession-publish pass bare strings into `::jsonb` audit params → 22P02 → rollback → 500 on every archive and every re-publish in prod (S118 named deferred defect #3, covered by a labeled tripwire test that asserts the defective signature and goes RED on the fix). Test-hosting corollary: any flag Program.cs reads off `builder.Configuration` pre-`Build()` (e.g. `Auth:UseDatabase`) must be injected via HOST configuration (`CreateHost`) in test factories — the TASK-3001 connection-string timing generalizes to all builder-time flags.

## Vindication (S112 + S113, recorded evidence for the program's premise)
The Pass-1 FE switch surfaced the predicted class in NON-test code: **a hand-written FE interface is itself a mock.** Two live prod bugs fell out at compile time — `RoleAssignment` claimed `grantedBy`/`grantedAt` (backend: `assignedBy`/`assignedAt`) → two admin columns rendered blank in prod; `updateUser` claimed a phantom `username` response field → every save silently dropped it from drawer state. Plus three contract lies (phantom fields both directions, an out-of-contract request field, spec-required fields marked optional). **S113 extended the lesson to the NULLABILITY axis: a field-NAME-only drift guard cannot see nullability — `RoleAssignment.assignedBy` claimed non-null `string` while the grant path serves `string | null`; the lie survived S111 AND S112 and surfaced only under strict direct assignment.** Convention from the fix: undeclared error payloads (e.g. 412 bodies) narrow via a runtime type GUARD on the no-`as` surface, never a cast.

## Known residuals
**CLOSED by the S113 strict-types phase:** ~~required-strictness~~ and ~~same-name-TYPE-change~~ — the `ResponseStrictTypesFilter` emits `required` = all members for every schema in the **response-reachable closure** (transitive from 2xx `.Produces` refs; the truthfulness basis: the null-emitting serializer always writes every record member — empirically confirmed against all 26 real per-route responses), so the generated response types are directly strict and the coercion scaffolding (`apiNarrow.ts` / `coerceApiResponse` / `AssertFieldsInSpec`) is DELETED — a type flip now fails `tsc` directly. Enum-widening resolved via **`[property: AllowedValues(...)]` on closed-set response discriminators** (unit type / orgType / periodStatus / vikar reason / scopeType — each set citing its authority: a DB CHECK or a total projection; OPEN config-keyed sets like `employmentCategory` must NOT be declared) → the filter emits `enum: [...]` → generated literal unions + the enum-fidelity gate. **S122 added TWO more (the compensation vocabularies, closing the P6 authority gap S120 flagged): `compensationModel` (AFSPADSERING\|UDBETALING) graduated from refused-to-declared citing the NEW DB CHECK `agreement_configs_default_compensation_model_check` / `overtime_balances_compensation_model_check` (S122 created the authority the S120 refusal was waiting for), and `compensationType` (PAYOUT\|AFSPADSERING) declared under a NEW "handler-enforced" authority class — a first precedent expansion: its authority is the endpoint's inline validator (`OvertimeEndpoints.cs:531-532`), NOT a DB CHECK or a total projection, because the value never lands in a column but the response echoes the validated request (owner-ruled 2026-07-24). `source` stays refused — no authority.** Exclusions that keep `required` truthful: `[JsonIgnore(WhenWritingNull/Default)]` members (the `VacationSettlementSnapshot` future, test-pinned) and the nullable-`$ref` case below. Overlap policy: a schema reachable from both request and response gets response-truth `required` (over-strict toward the request side, fails SAFE at compile time).
- **~~The nullable-`$ref` exception~~ CLOSED (S117 — the escalation FIRED and RESOLVED).** The S113 exclusion existed because OpenAPI 3.0 cannot express `nullable` on a bare `$ref`. The trigger fired at the would-be 3rd member (`SettlementReversalResponse.Successor`, Pass 4): the `ResponseStrictTypesFilter` now emits EVERY CLR-nullable COMPLEX member as **`type: object` + `allOf: [$ref]` + `nullable: true`** AND `required` (the OAS-3.0.3-legal form; `openapi-typescript` renders it `T | null`, NOT optional); `SpecRuntimeMatcher` resolves THROUGH the pure wrapper (inner required/enum fidelity executes behind it — proven by inner-violation RED cases, unit + Docker-live) and the structural-null policy TIGHTENED: a bare-`$ref` member serving null = RED (truthful nullability always carries the wrapper now). The 2 legacy members (`outgoingVikar`, `activeVikar`) were retro-closed; their FE workaround scaffolding (the `Omit`-override, the `?? null` normalization) is DELETED. **Residual members: 0. Authoring guidance: a CLR-nullable complex response member is now simply fine — declare it `T?` on the record and the pipeline is truthful end-to-end (scope: "complex" = a NAMED RECORD emitting a `$ref`; a nullable COLLECTION [`IReadOnlyList<T>?`] emits inline `type: array`, is never wrapped, and the matcher's array-null policy stays permissive — zero such members exist today; treat a first one as a design decision, not a free ride).** **The mechanism rule (load-bearing): a spec-emission change that restructures a schema node MUST land with its matcher-resolution counterpart in ONE coordinated change, proven by an inner-violation (required/enum) RED — an incomplete matcher half is SILENTLY GREEN** (the wrapper walks as an empty object; no existing suite can catch it).
- **The flag-and-defer rule's firings (2):** **S115** — `GET /api/admin/employees/{employeeId}/entitlement-eligibility/{entitlementType}` (the no-row branch OMITS keys); the FE keeps ONE sanctioned explicit-T call, lint-narrowed by route helper. **S117** — `POST /api/vacation-settlements/{…}/resolve` (4 success branches with genuinely DIFFERENT key sets: `hint` DEFER-only; `claimDispositionDays` WAIVED-only; `feriehindringTransferDays`/`feriehindringReason`/`carryoverIn` FERIEHINDRING-only; `forfeitDays` absent in DEFER). Both stay grandfathered with in-manifest comments; a `oneOf` phase (an unbuilt mechanism) is the eventual fix for the class — at a 3rd firing, weigh building it (the S117 escalation precedent: numbered triggers beat silent accumulation).
- **~~The nullable-ITEMS class~~ CLOSED (S120 — instance #1 fixed at FIRST firing).** Distinct from BOTH prior nullability axes: not a nullable member, but a collection member whose ELEMENTS are CLR-nullable (`IReadOnlyList<decimal?>` — Swashbuckle 6.6.2 emits `items: {type: number}` and DROPS the element nullability). Instance #1 = `YearOverviewCategory.saldo` (the year-overview empty-config branch serves an all-null 12-array), exposed by the S120 ruling-#2 runtime pin REDding on the truthful wire — which is why this class skipped the S113-style numbered deferral: with a runtime pin RED there is no honest way to defer. Fix (per the mechanism rule below — one coordinated change, both halves): the `ResponseStrictTypesFilter` `ApplyElementNullability` (scalar nullable element → `items.nullable: true`; complex nullable element → the S117 wrapper on Items, defensive/zero members today; nested arrays descended; fail-conservative) + the `SpecRuntimeMatcher` element-position null admission (the S113 rule at element position: a null ELEMENT is admitted iff `items` carries `nullable: true` directly or through the wrapper; non-nullable items keep every existing RED). The pre-existing caveat above about a nullable collection MEMBER (`IReadOnlyList<T>?` — the member itself serving null) is UNCHANGED by this: that axis stays permissive with zero members, still a design decision on first arrival.
- **The typed-etag option surface is a compatibility contract (S115):** the structured `apiFetchWithEtag` options are `{method, params, query, ifMatch, ifNoneMatch, body}` — `ifNoneMatch` admits ONLY the literal `'*'` (the create-only precondition). Any future option key must be grep-verified absent from legacy `RequestInit` callers before admission (discrimination must never flip). Likewise `SuccessContract`'s single-status surface (constructor + `StatusCode`) is consumed by the S112 injected-lie tests — a compatibility contract for future matcher changes.
- **The spec≡runtime gate covers only the typed routes** — a future typed endpoint that lies passes convention+drift until a per-route assertion is added. Fold into the retrofit. (S112: status fidelity + declared-204; S113: required + enum fidelity; all 26 typed ops carry per-route assertions.)
- **Request-side declaration-vs-binder boundary** (documented, accepted): spec `required` on request bodies = the C#-`required` subset only (binder-enforced); other members stay optional by design.

## Scope status
Phase 0 (S111) typed the proof surface + installed the pipeline + the 4 gates. **Pass 1 (S112)** drained the 20-op merged-admin mutation surface + the typed body-verb/If-Match overloads + the declared-204 policy + status fidelity. **Strict-types (S113)** made generated types directly strict + deleted the FE coercion scaffolding. **Pass 2 (S115)** drained the 23-op admin-core remainder + the homogeneous-multi-2xx convention + the `ifNoneMatch` option + the flag-and-defer first firing. **Pass 3 (S116) drained the 17-op approval bucket (approval 10 + the delegate trio + the overtime pre-approval quartet) — 67 typed / 70 grandfathered / 2 declared body-less** — adding the declared-bodyless-POST gate rule (above), the program's FIRST born-typed product op (`GET /api/overtime/pre-approvals`, a scope-bounded + active-only admin list authored on the paved road — it repaired a pre-existing dead-route admin page that 404'd on every load), and the partial-lint-tier pattern's second firing (`useSkema.ts` — a switched file hosting residual grandfathered-op calls takes the ROUTE-HELPER-pinned partial tier: S115 `ELIGIBILITY_PATH`, S116 `SKEMA_MONTH_PATH`/`SKEMA_SAVE_PATH`; the helper pin is the sanction boundary, never a URL regex). **Pass 4 (S117) drained 6 of the 7 settlement ops — 73 typed / 64 grandfathered / 3 declared body-less — and FIRED the nullable-`$ref` escalation** (the residual class CLOSED above; the old candidate line's "+2 employee settlement POSTs" were `settlement-reversal` + `termination-payout-request` — DRAINED in this pass, not a separate bucket; `/resolve` = the flag-and-defer second firing; `reconcile-payout` = the declared-bodyless third member; the bucket had ZERO FE callers — a greenfield pass, no lie-tally growth). **Pass 5 (S118) drained 30 ops — the program's largest pass: bucket A COMPLETE (the 29-op agreement-rule config admin surface: agreement-configs 12 incl. the 4 child entitlement ops, entitlement-configs 5, position-overrides 7, wage-type-mappings 5) + login — 103 typed / 34 grandfathered / 8 declared body-less** (the 5 body-less lifecycle POSTs — clone/publish/archive/deactivate/activate — joined the declared list). It carried the program's first TWO owner-ruled wire changes, each first-of-its-kind, NAMED, self-standing, and explicitly NOT precedent for less-degenerate cases: **(1) the dead-branch class** — the 3 re-read-fallback two-shape 201s (agreement-config create + clone, position-override create) eliminated via `INSERT … RETURNING *` `CreateReturningAsync` siblings (the null branch was defensive dead code the FE would break on; the 201 is now STRUCTURALLY always the full entity, test-pinned by full-key-set assertions); **(2) the drift-repair class** — the entitlement child DTO's three mapper copies (one drifted, omitting `fullDayOnly`) collapsed to ONE record; the by-id embedded rows gained `fullDayOnly` ADDITIVELY, display-only (the write-side wiring explicitly barred and deferred). Pass 5 also fired the request-side lie detector twice and the repo-test-masking smell once (see the recipe sections above) — leaving THREE named deferred defects (dead WTM edit, dead child-entitlement edit, archive/re-publish 500), all live prod dead-ends on the config admin surface caught by this pass and none fixable inside its exclusions. **Pass 6 (S119) drained bucket B COMPLETE — the 14-op org-config + projects surface (config 7 + projects 7) — 117 typed / 20 grandfathered / 9 declared body-less** (the select POST joined the declared list), **the program's first ZERO-wire-delta pass since S116**: every response an exact shape-copy; the only request-side delta the S112-accepted-class drop of the never-bound `projectCode` key on the project PUT. It carried the program's first live `ifNoneMatch: '*'` use (the profile PUT's flexible create-or-update precondition, both branches wire-pinned), the two-sibling resolution of the hand-maintained 13/14-member constraint-shape drift risk, and prod bug **#7**: the FE `Project.isActive` PHANTOM field — no endpoint ever emitted it, every read path filters `is_active = TRUE`, and the admin project table rendered "Inaktiv" for EVERY row (`undefined` → falsy; the S112 blank-columns class, masked by absence of coverage — the page had zero tests). Fixed FE-only: field + Status column removed, the page's first component tests added, the dead `projectCode` edit control (never persisted on update — S91 dead-button class) disabled. Regen-pipeline note (standing practice, back-annotated per the S119 Step-0b review): the backend drain agent is cross-domain AUTHORIZED for the pipeline outputs `docs/api/openapi.json` + the generated FE `api-types.ts` — no other docs/ or frontend/ file. The remaining **20 grandfathered operations** = bucket C (17, employee-facing; the skema drain graduates `useSkema.ts` to the full lint tier) + `GET /health` (deferred-with-cause: infra probe) + the 2 standing flag-and-defer ops — Pass 7 = bucket C, after which only the deliberate residue remains. Every newly-typed op lands strict automatically; nullable-complex members are first-class. **FE-lie tally across the program (honest counting): 7 live prod bugs + 11 contract lies, all caught by typing.** (S119: prod bug #7 = the `Project.isActive` phantom, above; the hand-written `ProfileSaveRequest`'s all-6-required claim vs the spec's `effectiveFrom`-only is a declared delta, not tallied — it was STRICTER than the contract and the wire bytes were identical.) S118's additions — prod bugs #4 `wage-type-mapping update` (binder-400 dead: `EffectiveFrom` required, never sent), #5 `child-entitlement update` (same class; found pre-code at Step-0b as the 422 dead-end, then found WIDER by the compile refusal: it 400s at binding before the 422 guard — one defect, counted once), #6 `archive + supersession-publish` (bare-string audit JSON → 22P02 → 500 on every archive/re-publish); lies #8 the 43-field `AgreementConfig` interface omitting 5 emitted compliance fields, #9 the `fullDayOnly` three-way mapper drift (the FE 15-field interface mirrored the drifted copy), #10 `LoginResponse` omitting `orgId`, #11 the false-optional `entitlements`/`entitlementsReadOnly` page-local type (an out-of-enum `normModel:'WEEKLY'` test-mock lie was also caught by the strict enums — test artifact, not tallied). See [[typed-api-contract-program]]. **Pass 7 (S120) drained bucket C COMPLETE — the FINAL 17-op employee-facing surface (time-entries 2, absences 1, flex 1, balance 3, compliance 2, overtime 5, skema 3) — 134 typed / 3 grandfathered / 9 declared body-less: THE BULK RETROFIT IS COMPLETE.** The residue is deliberate and named: `GET /health` (infra probe) + the 2 standing flag-and-defer ops (eligibility GET, settlement `/resolve`) — each carrying its cause comment in the manifest; the eventual `oneOf` phase remains the class fix, weighed at a 3rd firing. Pass 7 carried THREE owner-ruled wire changes (ruled 2026-07-21): the **branch-normalization class** (2 instances: the flex no-history branch → the ONE shape with null-filled history fields + the vestigial `message` dropped; the year-overview empty-config rows gain `settlement: null`) and the **dead-branch class 3rd instance** (the compliance/governance literal-null-200 defensive branch → 502 upstream-invalid at both sites — ONE ruling, TWO ops). The useSkema GRADUATION landed: both S116 carve-out lint rules + the partial-tier entry DELETED — **the partial-tier pattern is CLOSED (0 members; the S115 eligibility route-helper pin remains as the flag-and-defer mechanism, not a tier)**. The nullable-ITEMS class fired and closed at first instance (see Known residuals). **Prod bug #8 (the integer-enum class — a NEW mechanism: a wrong-TYPED hand-written union):** the FE compared the compliance `violationType`/`severity` wire INTEGERS against STRINGS — `severity === 'VIOLATION'` could never match (every item rendered as a WARNING "Advarsel") and type labels rendered as raw integers; repaired FE-only via CLR-order constants (making the wire strings would be an unruled wire change + SharedKernel touch — declared, deferred). Flagged for a future sprint (P6 authority gap): the TWO overtime compensation vocabularies (PAYOUT/AFSPADSERING vs AFSPADSERING/UDBETALING) are raw inline-validated strings with NO DB CHECK. **Tally after Pass 7: 8 live prod bugs + 15 contract lies** (S120 adds bug #8 above; lies #12 the false-exhaustive `violationType` union — 4 of 6 CLR values; #13 the false-non-null `tiles.ferieRemaining`/`careDayRemaining` claims; #14 the settlement-blind summary/year-overview types — one class, two surfaces; #15 the spec-side saldo item-nullability drop, caught by the runtime pin not the FE switch). **S121 (the post-program follow-up): the three S118 named-deferred-defect ledger entries are CLOSED** — the WTM-update + child-entitlement dead ends repaired via TWO further owner-ruled wire-change classes (2026-07-23): the **request-loosening class** (the 3-PUT family `effectiveFrom` → server-defaulted optional, compute-once `?? today`, the same-day 422 retained — ruled family-wide so the primary surface's client-clock midnight race died too) and the **request-tightening class** (`FullDayOnly` → `required` on the 4 entitlement DTOs — the structural closure of the absent-deserializes-to-guard-rejected trap the binder-based lie detector was blind to); the archive/re-publish 22P02 repaired with honest `{"status":…}` audit JSON (the locked-row `PreviousStatus` carried out of the repo — the DRAFT-archive shape would otherwise have produced a LYING audit row, the Step-4 Codex catch). The FE side of the program is now FULLY drained: the last 2 legacy explicit-T calls graduated, all carve-outs deleted, FE-lint residue = the single S115 eligibility pin. Recipe note from the S121 blast-radius discipline: a request-TIGHTENING's test blast radius must be swept and NAMED in the plan before code (the Step-0b Reviewer enumerated all 5 affected tests up front — zero mid-sprint discoveries). **S122 (a second post-program follow-up) closed the P6 compensation-vocabulary authority gap S120 flagged: the model vocabulary got its DB CHECK + graduated to a declared spec-enum, and `compensationType` was declared under the new handler-enforced authority class (above) — plus the S17 default-trap eradication that rode along (see [[typed-api-contract-program]]). Reusable recipe note: `generate_db_schema.py` captures ONLY `CREATE TABLE`-inline constraints, NOT `ALTER TABLE ADD CONSTRAINT` — a named CHECK that must both reach legacy DBs (guarded ALTER) AND be captured by the docs gate (inline) needs BOTH forms, exactly as `entitlement_configs_full_day_only_types` already carries both. And hand-rolled test-fixture DDL (`CREATE TABLE` blocks that don't source init.sql) is a cross-process schema contract — a new CHECK/DEFAULT must be mirrored there or CHECK-violation pins false-green.**
