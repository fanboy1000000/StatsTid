# Model-routing register — is the delegation framework holding?

<!-- anchor-sprint: 141 -->

**Purpose.** Since 2026-09-07 planning and review run on the most capable model and implementation on
cheaper ones (`docs/WORKFLOW.md`, section "Model Routing"). This register is the thin monitor the owner
asked for: one row per sprint, filled at close from artifacts the close guard already requires, so that if
the routing starts producing errors the trend shows it. It is deliberately small — no per-task attribution,
no score, no gate. **Read it for direction over three sprints, never for a verdict from one row**: a sprint
has roughly ten tasks and the numbers cannot carry more than that.

**Refined 2026-09-24 (owner ruling):** *"I want Fable to plan and review and Opus to implement. I want to make
sure we always use the newest model."* Tiers are fixed by model **family** (they do not move when a lower-tier
model out-benchmarks a higher one), and each tier runs the **newest version** of its family. Aliases can lag a
release — on 2026-09-24 `model: opus` resolved to `claude-opus-5`, three days after Opus 5.5 shipped — so from
S144 on, the *Agent spawns by model* column records the **resolved model id per tier** (e.g. `backend-infrastructure
→ claude-opus-5-5`), not the alias, taken from the reviewer's `reviewed-by-model:` line and the subagent
transcripts. The reference list of newest ids lives in `docs/WORKFLOW.md`, "Version check".

**Where the numbers come from.**
- *Spawns by model* and *guard blocks*: `.claude/telemetry/model-routing.log` (local, gitignored; one line
  per Agent spawn written by `model-routing-guard.ps1`, plus one `close` line per sprint written by
  `sprint-close-guard.ps1`). Count a sprint's lines by date range.
- *Review findings*: the Step-7a artifacts under `.claude/reviews/` and the Step-5a subsections of the
  sprint log — counts the close guard already requires to exist, never a self-filled field.
- *CI iterations to green* and *post-close defects*: the sprint log's CI sections and any post-close
  review section.
- *Tokens by model* (on demand, when a row looks odd — not every sprint): the session transcript under
  `%USERPROFILE%\.claude\projects\C--Users-b200895-source-repos-StatsTid\<session>.jsonl` carries a
  `"model"` and a `"usage"` object per assistant message, and each subagent transcript under the session's
  `tasks\` folder does the same. Three commands answer "which model spent what":

  ```bash
  J=~/.claude/projects/C--Users-b200895-source-repos-StatsTid/<session>.jsonl
  grep -o '"model":"claude-[a-z0-9-]*"' "$J" | sort | uniq -c                       # messages per model
  grep -o '"model":"claude-[a-z0-9-]*"\|"output_tokens":[0-9]*' "$J" | paste - - | awk -F'[":]' '{t[$4]+=$NF} END {for (m in t) print m, t[m]}'   # output tokens per model
  grep -o '"name":"Agent","input":{[^}]*' "$J" | grep -o '"subagent_type":"[^"]*"\|"model":"[^"]*"' | sort | uniq -c   # spawns as requested
  ```
  If the transcript format changes and these stop working, record "not measured" in the row rather than a
  stale number, and decide then whether a script is worth writing.

**When to reopen the routing.** Any of these, sustained over the trailing three sprints, puts the routing
on the entropy-scan agenda for the owner's decision (nothing moves automatically):
1. a defect found AFTER close that traces to a cheaper-tier implementer's output;
2. CI needing more than two runs to go green;
3. any reviewer refusal (`verdict: REFUSED`) or any guard BLOCK on a reviewer spawn;
4. *(added 2026-09-24)* any tier found running an older version than the newest of its family — this one is
   acted on at once (pin the full id in the role's definition file and re-dispatch), not only after three sprints, because the owner's rule is
   "always the newest".

| Sprint | Orchestrator model by phase | Agent spawns by model (guard blocks) | Review findings — 5a B/W · 7a B/W (internal lens) | CI runs to green | Post-close defects | Notes |
|--------|-----------------------------|--------------------------------------|---------------------------------------------------|------------------|--------------------|-------|
| **S138 (baseline, pre-routing)** | Fable through refinement, plan and wave 1; **Opus from mid-sprint** (owner switched for token reasons) through close and CI remediation | 19 spawns, all `general-purpose`, **all inherited the session model** (Fable or Opus; no routing existed). Guard blocks: n/a. Session profile: 704 tool calls, 65 CI polls, ~240 build/test invocations, 100 direct Orchestrator edits | 5a: **2 B** (create-POST audit gap; profile GET refused terminated employees) / 0 W · 7a: 0 B / **5 W** + 6 N (boundary `<`→`<=` + concept rename; ADR stated the replaced rule; asymmetry comment wrong; follow-up registered nowhere; diagnostic read could destroy the correction). Codex 5a 1 W, 7a 1 W | **3** (6 red → 2 red → green; 1 product defect = worklist order by UUID, 5 test defects incl. 2 calendar-dependent pins) | Post-close external review: 1 W (weak `NotEqual` pin), 1 N that exposed a small product gap (`reversedSince` answered false when unknown); Orchestrator pass: 1 phantom method name in ADR-040, 7 rename leftovers | The control row. Every defect here was produced on Fable or Opus — capability did not prevent them; review caught them. `QUAL-153` (calendar-floating pins) registered. |
| **S139 (first live run)** | **Fable throughout** — the owner declined the Opus switch at dispatch ("dispatch wave 1" on Fable); the switch points were offered and recorded, not taken | **19 spawns + 5 resumes, all by role name, zero `model` overrides:** 4 `trace` (Sonnet), 2 `test-qa` (Sonnet; one resumed three times), 1 `backend-infrastructure` (Opus; resumed twice), 1 `sweep` (Haiku), 2 `constraint-validator` (Sonnet), 9 `reviewer` (Fable). **Guard blocks: 0. Reviewer refusals: 0.** One Sonnet resume lost to a rate limit — tree verified clean, restarted | 5a: **0 B / 2 W** (the soft-delete read the clock twice; the probe was blind to the repository guards) + cycle 2 **0 B / 2 W** (RED comments overclaiming; a conditional pin) · 7a: **0 B / 2 W** (doc-vs-code cite drift after concurrent edits) → cycle 2 verified. Codex: 5a CLEAN ×2; 7a 1 W (the projection read twice — an Orchestrator one-liner) → CLEAN at cycle 3. The register (Part A): Codex 2 B / 4 W → 0; Reviewer 0 B / 5 W → 0 | **1** — green on the first run (`34148997832`, all 7 jobs; S138 needed 3) | none yet | **Every defect this sprint was caught by review, none by CI so far; every code defect sat on the cheaper tier or in the Orchestrator's own one-liners** — consistent with the S138 control row: capability did not prevent them, review caught them. The Fable-seat cost was the one number the routing could not lower (owner's choice). Signals 1–3 (a post-close defect from a cheaper implementer; CI > 2 runs; a reviewer refusal or a guard block on a reviewer): none tripped at close |
| **S140 (second live run — the first with the Orchestrator switch actually taken)** | **Fable** for refinement (4 Codex cycles / 2 Reviewer cycles), Steps 0a / 0b / 1, and every ruling · **Opus 5 from "dispatch wave 1"** — the switch the owner declined in S139 was **taken** here, and held through dispatch, merging, conflict resolution, contract regeneration, all verification runs and close bookkeeping. So this is the first sprint where the routing rule ran end to end, including the seat that cannot be hooked | **17 spawns + 11 resumes by role name, zero `model` overrides:** 5 `reviewer` (Fable) + 2 resumes · 4 `backend-infrastructure` (Opus) + 1 resume · 1 `test-qa` (Sonnet) + 3 resumes · 1 `ux` (Sonnet) + 5 resumes · 3 `constraint-validator` (Sonnet) · 1 `sweep` (Haiku) · 2 `Explore` (read-only built-in). **Guard blocks: 0. Reviewer refusals: 0.** One `reviewer` spawn died mid-review on a Fable **session rate limit** and resumed cleanly from its transcript with no lost work — worth knowing that the review lens is interruptible without restarting it | **Refinement Step 4:** Reviewer 2 B / 6 W / 7 N → 0 B / 5 W / 7 N; Codex 3 B / 5 W → 2 B / 2 W → 1 B / 1 W → READY (4 cycles) · **Step 0b:** Reviewer 1 B / 4 W / 7 N → 1 B / 1 W / 4 N → 0 B / 1 W / 1 N; Codex 0 B / 3 W → 0 B / 1 W → clean · **Step 5a wave 1:** Codex CLEAN; Reviewer 0 B / 3 W / 5 N → 0 B / 1 W / 4 N · **Step 5a wave 2:** Codex **1 B** / 2 W / 6 N; Constraint Validator PASS ×3 | pending (backfilled at close) | pending | **The finding that matters for the routing question: five of the review layer's catches landed on the ORCHESTRATOR'S own work, not on a cheaper agent's** — a test that could not fail (it asserted its own recomputed arithmetic instead of calling the product), a spec field that never existed (transplanted from another endpoint's error body), a claimed exact partition that was not one, a ruled role rule enforced in only one of two places, and a type-check failure misreported as a pass because `$?` was read through a pipe. The cheaper tiers' output was, by contrast, repeatedly *better than its spec*: agents added pins nobody asked for (a blocked-row × role composition case), declined twice to make a metric green (refusing to manufacture a fixture that would reintroduce the degenerate case a test exists to avoid; refusing to invent a suppression rule the owner had not made), and one caught the missing `reversalEndpoint` field by checking both the generated type and the C# record rather than trusting the brief. **Read against the routing hypothesis:** capability at the implementation tier was not the constraint — spec quality and coordinator discipline were. Signals 1–3 (a post-close defect from a cheaper implementer; CI > 2 runs; a reviewer refusal or guard block): none tripped at close. Cost note: the `ux` agent burned **5 resumes** in a Monitor wait-loop for its own test run, which the Orchestrator broke by owning the run — a harness lesson, not a model one |
| **S141 (third live run — the largest, and the first where the routing question got a clear answer)** | **Opus 5 throughout** — refinement revs 1–5, Steps 0a / 0b, every owner ruling, all dispatch, merging, conflict resolution, two contract regenerations, every verification run and the close. **Disclosed deviation, recorded rather than absorbed:** owner ruling OQ-2 was a *domain-correctness* ruling (which agreement version governs a mid-year hire's first holiday year) taken on Opus, not the Fable review floor the routing rule reserves for that class. The *reviews* ran on the floor throughout (hook-enforced), so the review was unaffected; the *proposal* was not | **24 spawns + 4 resumes this session, all by role name, zero `model` overrides:** 6 `reviewer` (Fable) · 6 `backend-infrastructure` (Opus) · 6 `ux` (Sonnet) · 3 `test-qa` (Sonnet) · 1 `payroll-integration` (Opus) · 1 `trace` (Sonnet, read-only) · 1 `Explore` (read-only built-in). **Guard blocks: 0. Reviewer refusals: 0.** Refinement cycles 1–2 ran in an earlier session and are not in this count | **Refinement Step 4:** three dual-lens cycles, ending Codex 1 B / 1 W / 5 N and Reviewer APPROVED-WITH-WARNINGS 0 B / 5 W / 5 N · **Step 0b: THREE cycles, four blockers** — Codex 1 B / 3 W / 2 N → 2 B / 1 W → clean; Reviewer 1 B / 7 W / 6 N → 2 B / 5 W / 6 N → APPROVED-WITH-WARNINGS · **Step 5a wave 1:** Codex **2 B**; Reviewer 0 B / 4 W / 5 N · Step 7a: see the sprint log | pending (backfilled at close) | pending | **The routing question got its clearest answer yet, and it is the same answer as S140 only louder. THIRTY claims in this sprint's planning were contradicted by the code; TEN were the Orchestrator's own** — including an instruction that pointed five line numbers at *entirely the wrong kind of code* (defence-in-depth throws inside write helpers, described as fail-closed readers), an acceptance criterion that **mandated a domain-wrong result** (suppressing a revaluation, which would have left booked holiday valued at a superseded part-time fraction), a criterion that could not fail, a count of refusal sites that was 4 when it was 7 — the three missed ones would have shipped the sprint's headline feature **dead** — and a quality finding I registered that was actually the owner's ruling under-applied, delaying a payroll-key correctness fix behind a decision already made. **Meanwhile the cheaper tiers repeatedly exceeded their spec:** a Sonnet test agent refused to write a pin it could not construct honestly and explained why; another found the standard fixture pins the clock at the one instant where the two candidate clocks *agree*, so a naive pin would prove nothing, and moved it to 23:30 UTC; an Opus implementer **proved a live data-protection violation by reverting its own fix and watching a hire date appear in the output**, then found the same leak in the sibling file it had been told to copy; another chose an aggregate over a sort so the join cannot fan out *even on malformed data*, and proved its key pin by deliberately breaking what the pin guards. **Two defects were found by neither lens across six review passes and came from the OWNER instead** — "should a scheduled change be visible to HR?", which reframed two separate defects into one requirement, and "why not use the Danish clock?", which exposed a live user-facing wrong-date bug that traces to a single frontend call. **Read against the routing hypothesis: implementation-tier capability was never the constraint. Coordinator precision was, and the product question was.** Signals 1–3: none tripped at close. Harness lesson, and the sprint's biggest process cost: **nine agents were handed worktrees missing the entire sprint** — root-caused to worktrees branching from `origin/master`, which lags by design because this project pushes only at close. Now a standing instruction in `docs/AGENTS.md` |
| **S142 (fourth live run — the largest agent count so far, and the first where every agent contradicted its brief)** | **Opus 5 throughout** — refinement revs 1–9, Steps 0a/0b, all twelve owner rulings (OQ-1…OQ-12), every dispatch, all merging, the wave-2 conflict resolution, the CI remediation and the close. **Same disclosed deviation as S141:** the rulings were *proposed* on Opus rather than the Fable review floor; both Step-4 lenses, both Step-0b cycles, all Step-5a and Step-7a reviews ran on Fable (hook-enforced), so the reviews that checked the rulings were on the floor even where the proposals were not | **14 spawns + 3 resumes, all by role name, zero `model` overrides:** 7 `backend-infrastructure` (Opus), 2 `test-qa` (Sonnet), 1 `ux` (Sonnet), 4 `reviewer` (Fable). **Guard blocks: 0. Reviewer refusals: 0.** One reviewer spawn died mid-review on a Fable **session rate limit** and was re-run after reset — the only routing-related interruption | 5a (wave 1): 0 B / 2 W both lenses · 0b: **4 B cycle 1** (internal: 9 of 64 rows unassigned, the OQ-4 deletes unassigned, **the sprint-wide carve-out itself wrong**, 4 coupled pairs split; external: test ownership unprovable, validator↔picker "ordered" ≠ atomic), 1 B cycle 2 · 7a: **five passes** — cycle 1 (0B/2W both lenses), cycle 2 (**1 BLOCKER**: the new guard was half vacuous), cycle 3 (owner-authorised past the cap; 3W internal, and the external lens reviewed by INLINED SOURCE because its shell was blocked), a fourth pass (the interpolation false pass), and a final confirmation — **internal `CLOSE`, external `CLOSE-WITH-WARNINGS`, both against the close sha `7ce69b8`** | **CI: 3 red → all fixed.** Both reds were the SAME six new pins failing on an `InvalidCastException` at the database boundary (Npgsql boxes `DATE` as `DateTime`), **zero product defects**; plus one frontend flake that passed on the next run | **★ The routing question this sprint answers is not about models but about briefs.** Every one of the fourteen agents contradicted its instructions, and **every contradiction was correct**: a pinned test instant I specified that could not detect the bug it was specified for; a caller mapping that would have produced a test covering nothing; three failure-mode descriptions that tracing refuted, each *understating* severity; two "sharpest lead" claims that proved inert; and comment lists incomplete every single time. The cheaper implementation models (Sonnet) produced findings indistinguishable in quality from the Opus ones — **the differentiator was the instruction "contradicting this brief is valuable", not the model tier.** Two harness defects were the Orchestrator's: `git stash` is repository-global and collided between two agents mid-sprint, and the census both review lenses had *blocked the plan over* until it was written to disk sat under a gitignored path, invisible to all eleven agents told to read it |
| **S143 (fifth live run — the first where per-task review found a defect in every single task)** | **Opus 5 throughout** — refinement revs 1–5, Steps 0a/0b, all owner rulings (OQ-1a/1b/1c/1d, OQ-3, OQ-4), every dispatch, all sixteen merges, every verification run and the close. **Same disclosed deviation as S141/S142:** rulings were *proposed* on Opus rather than Fable, which the routing rule reserves for planning and review. Recorded rather than absorbed. | **19 spawns + 14 resumes, all by role name, zero `model` overrides:** 11 `ux` (Sonnet), 3 `backend-infrastructure` (Opus), 1 `rule-engine` (Opus), 1 `test-qa` (Sonnet), 1 `sweep` (Haiku, **dispatched then re-routed** — see below), plus 8 `reviewer` (Fable, never overridden). | **The review tiers earned their cost, measurably.** Step 0b ran 2 internal cycles and 3 external (the third owner-authorised past the cap) and every cycle found a blocker, including two in the Orchestrator's own corrections. **Step 5a ran on every substantive task and found a defect in each — no exceptions.** Three of those were invisible to the task's own green suite: an Npgsql `-infinity` sentinel a `DateOnly` round trip hid, a boot loop measured at 26 reads / 25 reloads, and a conversion that changed which code branch a test took. Step 7a then found the **sprint's headline claim false** — six sites still took a stored date from the device clock through the approved helper — which no per-task review could have seen. | **One re-route, and it was a scope-shape error not a model error:** TASK-14306a was planned for `sweep` (Haiku, defined as ONE pattern-shaped change with no improvisation) and moved to `test-qa` at Step 0b, because ~30 heterogeneous judgment items across 13 files is not a sweep. The cheapest tier was wrong for the *shape* of the work, not its difficulty. **The clearest signal for the rule itself:** the two review lenses independently converged on the same escaped mutant (a refresh 401 that silently stops retrying) from different methods — one by reading, one by probing extracted copies. Two Fable lenses disagreeing on *method* and agreeing on *finding* is the strongest evidence yet that review is where capability pays. |
