# S64 Regression-Debt Census — COMPLETED

> **Status: COMPLETE** (TASK-6401 agent run 2026-06-04, ~33 min, full suite + per-class isolation ×2 for every deterministic class; landed by the Orchestrator). Zero unknown rows. Supersedes the provisional skeleton. **Orchestrator addendum:** F4-3's casing question was fact-checked post-census — `EventSerializer` has used `JsonNamingPolicy.CamelCase` **since Sprint 3 (`0cb4ced`), never changed**: there was no casing flip; the PascalCase-asserting tests were wrong from birth.

## Headline corrections vs the skeleton
1. **The "47 deterministic core" was wrong.** Full-suite today = 83 failed / 424; the **true deterministic core is ~45 tests across 24 classes**; ~36 of the run failures are **parallel-contention FLAKY** (9–13 WAF-booting classes, isolated-green ×2). The 88→57→47→83 run variance IS the flaky signal, not a converging core.
2. `ReportingLineRepositoryTests` ×37: **green with the stack up** — INFRA confirmed; CI-side fix only (TASK-6405, done).
3. Three cited root causes explain most of the core: **S53/TASK-5306** removed `employee_profiles.weekly_norm_hours`; **S59/ADR-029** added `users.birth_date`; **S55** approval-flow columns + **S25** `agreement_configs.version` (fixture DDL drift). Plus **S37** seed growth (30→50 entitlement configs; users 7→19 by S51–S63) and **S35** admin-strict If-Match (428).

## Census table (deterministic core; per-test counts = isolated-run)

| Class (fails) | Family | Signature (raising frame) | Citation / root cause | Verdict | Owner |
|---|---|---|---|---|---|
| `ReportingLine.ReportingLineRepositoryTests` (0 with stack; ×37 without) | INFRA | `Npgsql: Failed to connect 127.0.0.1:5432` at `InitializeAsync` | Compose-coupled by design (S48 era) | pass w/ stack | 6405 (CI services-postgres — DONE) |
| `Infrastructure.TxContractTests` (×7) | F2 | `IndexOutOfRangeException: Field not found in row: version` | class `SchemaDdl` `agreement_configs` lacks S25 `version BIGINT` (ends `description TEXT`, ~L164) | test-drift | 6402 |
| `Outbox.ApprovalAtomicTests` (×3: Reopen/Reject/ManagerApprove; Submit+EmployeeApprove pass) | F2 | `42703: column "designated_approver_id"` / `"explicit_fallback_confirmation"` of `approval_periods` does not exist — SUT `ApprovalPeriodRepository.UpdateStatusAsync` :399 | `ForcedRollbackHarness` `approval_periods` DDL predates **S55** (NOT `agreement_configs.version` — that table has it) | test-drift | 6402 |
| `PhaseE.AuditProjectionFamilyCutoverTests` (×3) + `CrossProcessTests` (×1) + `SyncInTxTests` (×1) | F1 | `23503: FK audit_projection_target_org_id_fkey` at `AuditProjectionRepository.InsertAsync` :92 | tests set `target_org_id='ORG_S44B_TEST'`, never seed that org row | test-drift | 6402 |
| `PhaseE.AuditProjectionBackfillIdempotencyTests` (×1) | F2 | `42P01: relation "users" does not exist` at `AuditProjectionBackfillService.LoadUserOrgLookupAsync` :283 | `AuditProjectionTestSchema.Ddl` never CREATEs `users`; the backfill SUT now does a user→org lookup | test-drift | 6402 |
| `CalculateAndExportScopeTests` (×3) | F2 | `Field not found in row: birth_date` at `UserRepository.ReadUser` :290 | fixture `users` DDL predates **S59/ADR-029** `birth_date` | test-drift | 6402 |
| `Payroll.EmployeeProfileMarqueeTests` (×4) | F1 | `EmployeeProfileNotFoundException 'EMP-MARQUEE-EP'` (resolver `GetByUserIdAtAsync`→null) | seeds users+profiles, never `user_agreement_codes` (S34 fail-loud) — skeleton confirmed verbatim | test-drift | 6402 |
| `Config.EmployeeProfileEndpointTests` (×4) | F3+F4 mixed | (a) `Bootstrap` `Assert 7 vs 19` **:384 = outbox-EVENT count** (the :373 IN-list passes); (b)(c) `weeklyNormHours` KeyNotFound :86/:130 + `42703 weekly_norm_hours` :229 | (b)(c)=**S53/TASK-5306** (`a7aee58`); (a)=seed growth to 19 users (S51–S63) — **F4-2** | mixed | **6404** (whole class; F4-2 leads) |
| `EmployeeProfile.EmployeeProfileLifecycleTests` (×2) | F3 | `42703 weekly_norm_hours` :141/:323 (test SQL) | **S53/TASK-5306** | test-drift | 6403 |
| `Outbox.EmployeeProfileAtomicTests` (×1) | F3 | `KeyNotFoundException` GetProperty (weeklyNormHours response field) | **S53/TASK-5306** | test-drift | 6403 |
| `Outbox.AdminUserCreateAtomicTests` (×1) | F3 | `42703 weekly_norm_hours` | **S53/TASK-5306** | test-drift | 6403 |
| `Payroll.AgreementCodeMarqueeTests` (×1) | F3 (NOT F1 as skeleton suspected) | `42703 weekly_norm_hours of employee_profiles` at test seed :338 | **S53/TASK-5306** | test-drift | 6403 |
| `Config.EntitlementConfigEndpointTests` (×3) | F3 + 1 F4-suspect | `Get_List` `30 vs 50` :78; `BalanceSummary…` `OK vs 422` :409 | 30→50 = **S37/TASK-3701** (AC_RESEARCH+AC_TEACHING ⇒ 5×5×2=50); BalanceSummary = accrual guard (**F4-5**) | mixed | **6403 owns the file**; 6404 supplies the F4-5 ruling for 6403 to apply |
| `Config.EntitlementConfigSupersessionTests` (×1) | F3 | `SeedIdempotency… 30 vs 50` :229 | **S37/TASK-3701** | test-drift | 6403 |
| `EmployeeProfile.AdminUserAgreementCodeChangedTests` (×2) | F3 | `OK vs PreconditionRequired (428)` :82/:166 | **S35/TASK-3506** (`a5e3ce0`) admin-strict If-Match (ADR-019 D2); tests PUT without If-Match | test-drift | 6403 |
| `UserAgreementCode.UserAgreementCodeRepositoryTests` (×1) | F3 | `OK vs 428` :315 | **S35/TASK-3506** | test-drift | 6403 |
| `Segmentation.{ManifestReplay,ReplayDeterminism,ManifestProjectionRebuild,ManifestCreation,BoundaryScenario.OkTransition}` (×5) + `Payroll.MixedVersionExportTests` (×1) | **F4-1** | `PlannerInvariantViolation: 'OVERTIME_CALC' AlignedWindow … interior boundaries, AllowUpstreamAlignment false (ADR-016 D4)` at `PeriodPlanner.ApplyAlignmentPolicy` :268 | planner unchanged since S29; fixture since S53; the invariant rejects this input since S20 — tests could not have been green on this path | **adjudicated: test-side** (see register) | 6404 |
| `Concurrency.AgreementConfigConcurrencyTests` (×1) + `Outbox.AgreementConfigAtomicTests` (×2) | **F4-4** | `22P02 invalid input syntax for type json` at `AgreementConfigRepository.AppendAuditAsync` :753 | tests pass bare strings (`"ACTIVE"`) as JSONB params; product correctly rejects | test-data defect | 6404 |
| `Outbox.ProjectionParityTests` (×2) + `Outbox.OvertimeApproveRejectAtomicTests` (×1) | **F4-3** | `'?column?' is null` for `data->>'Date'` :237 / `Assert.Contains "ApprovedBy"` :125 — payload is camelCase | **Orchestrator fact-check: camelCase since Sprint 3 (`0cb4ced`), never flipped** — tests asserted PascalCase from birth | test-wrong-from-birth | 6404 |
| `UserAgreementCode.UserAgreementCodeBackfillSeederTests` (×1) | **F4-6** | `Assert.True firstBootRows>0` :79 → 0 rows at `effective_from='0001-01-01'` read | WAF backfill async-on-boot vs test read timing (deterministic ×2 isolated) — drain-await missing OR seeder predicate changed (verify seeder's effective_from) | adjudicate-then-fix | 6404 |
| `Smoke.SmokeTests.Backend_RegisterAndRetrieveTimeEntry` (×1) | test-drift | `Expected Created, Actual Forbidden` — target `SMOKE002` never seeded; `OrgScopeValidator.cs:48-50` denies unknown target before the `:61` GLOBAL short-circuit | unknown-target deny is by-design; GlobalAdmin-for-existing pinned elsewhere | test-drift | 6404 |

## FLAKY margin (isolated-green ×2; fail only under parallel full-suite load)
`Admin.AdminUserVersioningTests` (5), `Concurrency.PositionOverrideConcurrencyTests` (7), `Config.EndExclusiveMigrationTests` (4), `Config.WageTypeMappingEndpointTests` (8), `Outbox.AllocationGateTests` (7), `Outbox.OutboxPublisherTests` (9), `Outbox.SkemaMonthlyAccrualGuardTests` (7), `Outbox.SkemaEntitlementEligibilityGuardTests` (13), `Outbox.SkemaWorkTimeDayBoundsGuardTests` (6), `EmployeeProfile.BalanceSeriesTests` (12), `PhaseE.AuditProjectionCutoverTests` (12), `PhaseE.AuditProjectionQueryTests` (6), `Outbox.WorkTimeBackfillTests` (2). Suspected shared state: concurrent WAF hosts + Testcontainers provisioning + the live :5432 under xUnit parallelism (Docker/host-port/connection pressure).

## Overrides register (PROPOSED — applied only with census documentation)
| Override | Scope | Rationale | Status |
|---|---|---|---|
| `xunit.runner.json` `parallelizeTestCollections: false` (assembly-wide, Regression project) | whole Regression assembly | individually green; only failure mode is cross-class parallel resource contention (concurrent WAF hosts + Testcontainers + shared :5432 — run counts varied 88/57/47/83 on identical inputs); serializing collections removes the flaky margin; runtime cost accepted; revisit if the suite is partitioned into resource-isolated collections | **APPLIED** (TASK-6406, 2026-06-04; + csproj copy item) |
| ~~Backfill-drain await in `UserAgreementCodeBackfillSeederTests`~~ | — | F4-6 ruling REFUTED the timing hypothesis: the defect was the test's `'0001-01-01'` literal vs Npgsql persisting `DateOnly.MinValue` as `DATE '-infinity'` — fixed as a query-literal correction, no drain-await needed (seeder predicate unchanged since S34 `65d4889`, no escalation) | CLOSED (not an override) |

## F4 adjudication register (ADR-024 D3 — decided BEFORE code)
| # | Item | Lean | Decision | Decided by | Date |
|---|------|------|----------|------------|------|
| F4-1 | Segmentation OK-straddle ×6 — planner correct per ADR-016 D4; tests never-green-as-written | test-side fix (align `PlannerOptions`/period to the invariant) | **INVESTIGATE PRODUCT FIRST → INVESTIGATION COMPLETE (verdict a + latent b).** (a) Tests broken-since-birth: the AlignedWindow+straddle combination is S20-original (`97881dd`); rejection logic byte-identical since S20; the SAME file's `_Invalid_AlignedWindowRejects` twins assert THE THROW for the identical input, and its `_Valid_` planner-only tests deliberately use `SegmentSafeCalc` — the six PCS-round-trip tests wrongly reuse the shared AlignedWindow-bearing `RuleSet`. S53-fixture hypothesis REFUTED (single-line `WeeklyNormHours` deletion, unrelated). Fix shape: straddle-specific rule set WITHOUT the AlignedWindow rule (mirror `SegmentSafeCalc`), cite S20+ADR-016 D4; do NOT flip `AllowUpstreamAlignment` (unimplemented S20 stub — bypasses the guard without shrinking). (b) **LATENT PRODUCT GAP, separate item:** `/api/payroll/calculate-and-export` (the last [Obsolete]-shim caller) passes raw periods → `BuildPlanForLegacyCallersAsync` → live ruleset (incl. AlignedWindow OVERTIME_CALC) → `PlannerOptions.Default`, no alignment preprocessing ⇒ a real OK-straddling monthly period THROWS and fails the export (P4/P6). `WeeklyCalculationPipeline` unaffected (bypasses the planner). **RULED (user, 2026-06-04): (a) PROCEED** with the test-side fix per the investigation's fix shape; **(b) TRACK AS DEFERRED FOLLOW-UP** — an ADR-016-D4 follow-up item (options: upstream period-snap / complete the `AllowUpstreamAlignment` shrink stub / accepted-limitation pre-split contract), to be decided when the obsolete `/calculate-and-export` shim's retirement is designed; recorded in ROADMAP at S64 close; no S64 scope change. | user (product owner) + investigation | 2026-06-04 |
| F4-2 | `Bootstrap` outbox-event count 7→19 (seed growth S51–S63) | re-pin to the actual seed-set (19) | **APPROVED test-side** (cited) | user | 2026-06-04 |
| F4-3 | Event-JSON casing — fact-checked: camelCase since S3 (`0cb4ced`), no flip | test-side fix (camelCase keys) | **AUDIT REPLAY PARITY FIRST → SWEEP RAN: CLEAN, fix cleared.** Exhaustive reader inventory (same date): SQL-level — only `SegmentManifestProjectionRebuilder` (+ its ops-SQL mirror), all-camelCase, doc-cited; C#-level — all typed via the symmetric case-sensitive camelCase `EventSerializer` (52 audit mappers read C# properties, not JSON; the `GetProperty("OrgId")` hits are reflection on deserialized objects); outbox publisher payload-opaque; tools typed. The ONLY PascalCase event-JSON reads in the repo are the 3 test assertions. Fix guidance: also align the hand-built PascalCase synthetic payload (`OvertimeApproveRejectAtomicTests.cs:96`) + delete the stale comment at `ProjectionParityTests.cs:225-226`. | user (gate) + sweep (clean) | 2026-06-04 |
| F4-4 | Bare-string JSONB audit literals ×3 | trivial test-data fix; product correct | **APPROVED test-side** | user | 2026-06-04 |
| F4-5 | `BalanceSummary_AfterSupersession` 422 | accrual guard (S60/S62) correct → test needs earned-balance precondition | **APPROVED test-side** (cite ADR-030/S60+S62 in the fix) | user | 2026-06-04 |
| F4-6 | Backfill seeder firstBootRows=0 | verify seeder's `effective_from` predicate first; then drain-await (test) | **APPROVED conditional** — if the seeder's predicate changed, ESCALATE back to user instead of fixing | user | 2026-06-04 |

## Owner partitions (file-disjoint)
- **6402** (F1/F2 fixture seed+DDL; no assertion changes): TxContractTests, ApprovalAtomicTests(ForcedRollbackHarness DDL), CalculateAndExportScopeTests, AuditProjectionBackfillIdempotencyTests + the 3 FK-org PhaseE classes (AuditProjectionTestSchema/seeds), EmployeeProfileMarqueeTests.
- **6403** (F3 citation-gated): EmployeeProfileLifecycleTests, EmployeeProfileAtomicTests, AdminUserCreateAtomicTests, AgreementCodeMarqueeTests, EntitlementConfigEndpointTests (incl. applying 6404's F4-5 ruling), EntitlementConfigSupersessionTests, AdminUserAgreementCodeChangedTests, UserAgreementCodeRepositoryTests.
- **6404** (F4 post-adjudication + smoke + mixed): EmployeeProfileEndpointTests (whole), the 6 Segmentation/MixedVersionExport classes, AgreementConfigConcurrencyTests, AgreementConfigAtomicTests, ProjectionParityTests, OvertimeApproveRejectAtomicTests, UserAgreementCodeBackfillSeederTests, SmokeTests.
- **6405** (INFRA/CI): done.

Key fixture paths: `TxContractTests.cs` (SchemaDdl ~L112-164), `ForcedRollbackHarness.cs` (approval_periods ~L130-151), `PhaseE/AuditProjectionTestSchema.cs` (~L24-76), `Segmentation/TestFixtures.cs` (RuleSet ~L85-92, OkStraddleSources ~L70-78), `docker/postgres/init.sql` (employee_profiles ~L481; entitlement_configs L1418-1485; users seed = 19).
