<!-- anchor-sprint: 139 -->
# QUAL — Code-Quality Finding Register

**Status**: LIVE — **S131 sweep COMPLETE (TASK-C/D/E 2026-08-19)**; now the durable cross-session quality
register. (This file is TASK-E output — the register rows, cross-register updates, gate packet, and
below-floor appendix.) **Owner**: Orchestrator + PM. **Sweep baseline SHA**: `7e4bb1b` (S130 close). **Registration
floor (owner-ruled)**: **Medium+** — would plausibly change behavior, block or mislead a future change,
or misinform a reader. Below-floor items live ONLY in the inventory appendix.


> ## S139 status (2026-09-07) — HR follow-up register + the QUAL-153 fixed clock
> Per-fix detail in `docs/sprints/SPRINT-139.md`. **FIXED:** **QUAL-153** (a fixed test clock the PRODUCT shares —
> shared fixture + `WithFixedToday` + a falsifiability probe; the clock SOURCE on the profile, agreement-code and
> approval-projection paths behind `TimeProvider`, incl. the soft-delete `NOW()::date` write and the `CURRENT_DATE`
> compare as bound `@today`; 8 suites on constant anchors; the weekday nudges deleted). **FIXED in passing, never
> registered — found by the Step-5a lenses:** the admin create POST read "today" THREE times beneath S137's
> "Computed ONCE so the three can never disagree" comment (a midnight straddle would have left a new employee's
> first day without an agreement code or an approver) — now one value; and the profile soft-delete stamped its row
> and its event from two separate clock reads — now one value passed in (ADR-023 D8's shape). **NEW (10):**
> **QUAL-154** (second-tranche conversion: 6 suites whose paths need seams the OQ-1 (a) ruling did not name + the
> ~65-file unverified bucket) · **QUAL-155** (108 raw C# clock reads in 47 `src/` files; the business-date ones are
> the debt) · **QUAL-156** (six SQL business-date sites still on the DB clock; UTC-session assumption in the runbook)
> · **QUAL-157** (UTC-day vs Copenhagen-day split — owner ruling OQ-3 (a) keeps it, its own task) · from the HR
> sweep: **QUAL-158** (dead compensatory-rest feature) · **QUAL-159** (reconcile-payout: no outbox event) ·
> **QUAL-160** (transfer agreement: no event, no read) · **QUAL-161** (role-assignment expiry: silent state) ·
> **QUAL-162** (a finished overtime pre-approval page never routed) · **QUAL-163** ("efter frist" tile never reads
> the deadline; +2/+5 are unratified developer defaults).

> ## S137 status (2026-09-02) — time-control Increment 2 (calculation correctness)
> Per-fix detail in `docs/sprints/SPRINT-137.md`. **FIXED:** **QUAL-147** (OK-version date-derived INSIDE
> `EmploymentProfileResolver` — structural, every consumer at once; + `BalanceEndpoints:152-157`).
> **NOTED:** **QUAL-146** (partial reversal — the windowless segment byte-shape is now pinned per writer).
> **NEW:** **QUAL-148** (the `user.OkVersion` live-fallback FAMILY + the 11th indirect accrual caller —
> adjudicated sweep) · **QUAL-149** (ADR-016 D4 refusal at a genuine split in the live rule set — the S64
> F4-1(b) gap re-registered with its S137 scope; hire/leave truncations cleared by owner ruling, the
> fraction-change split still refuses and is pinned as such) · **QUAL-150** (the wage-type-mapping key is snapshotted per PLAN from the caller profile — wrong position mapping after a mid-period position change; latent, guarded by QUAL-149, COUPLED to it — Step-5a Codex) · **QUAL-151** (segment-date-bearing Payroll-host diagnostics, D7 class, server-side only — the two client-triggerable fail-closed exceptions were re-anchored in S137; the residual routed to the hardening pass).

> ## S134 remediation status (2026-08-25) — increment 3 / FINAL of the fix-next program (AUDIT-SCOPE + OBSERVABILITY + DOCS)
> The audit/observability/docs increment is remediated + merged (uncommitted pending the sprint-close commit);
> dual-lens-reviewed (Step-0b 3 Codex + 2 Reviewer cycles; Step-5a on QUAL-003; Step-7a at close). Per-fix
> detail in `docs/sprints/SPRINT-134.md`. **No production behaviour changed** — additive audit/log emission +
> doc accuracy. **This closes the three-increment S131 fix-next PROGRAM.**
> - **FIXED (S134):** **QUAL-003** (Payroll-host audit middleware + the manifest-id/ADR-016-D10 linkage;
>   byte-identical export by construction; Step-5a-cleared) · **QUAL-008** (correlation id ambient within a
>   request + `IncludeScopes` tree-wide) · **QUAL-065** (the cross-service propagation half — pulled in per
>   owner OQ-2; Backend→rule-engine forwarder carries the ambient id) · **QUAL-009 / SEC-038** (policy-denial
>   trace: a decorating result-handler + a denial-gated admin/mutating audit-row middleware) · **QUAL-010**
>   (særlige-feriedage reset Sep→Jan) · **QUAL-011 / SEC-042** (SECURITY.md → the ADR-038/S105 as-built
>   authority model) · **QUAL-012** (upgrade-runbook pointers → durable grep refs) · **QUAL-080**
>   (ARCHITECTURE folders + the renamed class) · **QUAL-090** (ALL stale `init.sql` "authority" comment
>   anchors — the drift was systematic, not the finding's "3-4") · **QUAL-093** (the paired balance comment).
> - **ROUTED OUT (not S134):** **QUAL-145** (WeeklyCalculationPipeline/TaskDispatcher normalize-or-retire) →
>   the domain-semantics track (it is OK-version domain behaviour, not audit/obs/docs).
> - **S134-SURFACED FOLLOW-UPS (register + route, don't lose):** (1) **`LogSanitizer` consolidation** — S134
>   added a shared `src/Auth/LogSanitizer` that duplicates SEC-040's `private SanitizeForLog` in Backend
>   `AuthEndpoints.cs`; consolidate the Backend one onto the shared helper (new QUAL, low). (2) **QUAL-090
>   root-cause** — today's re-sync/durable-ref fix should be backed by a `check_docs.py` CI check that
>   resolves any `init.sql:NNN` (or named-anchor) reference in source comments + fails on drift (new QUAL/gate).
> - **PROGRAM RESIDUALS (on their tracks):** QUAL-016 bespoke atomic-outbox increment · QUAL-036 residual 12
>   overloads (retire/repoint test seeders) · QUAL-123 + day-attribution (domain track) · the 8 S132-discovered
>   follow-ups (QUAL-142–146 + SEC-043–045, already registered).

> ## S133 remediation status (2026-08-24) — increment 2 of the fix-next program (TEST INTEGRITY)
> The test-integrity increment is remediated + merged (uncommitted pending the sprint-close commit); each fix
> was dual-lens-reviewed at Step-0b and (for the auditability-boundary hoist) Step-5a; per-fix detail in
> `docs/sprints/SPRINT-133.md`. **No production bug was hiding behind the theater** — all defects were test-side.
> - **FIXED (S133):** **QUAL-013/014/015/017/018/019/020/022/095/110/111** (the 11 non-outbox test-integrity
>   rows — each rewired to the SHIPPED path + proven falsifiable) · **QUAL-016** (atomic-outbox — the **cheap
>   cluster** of 15 tests converted wire-driven per **PAT-019**; the mechanism is the shared `WithThrowingOutbox()`
>   on `StatsTidWebApplicationFactory`) · **QUAL-027** (audit JSON options hoisted to SharedKernel; byte-format
>   pinned by the golden pin; dual-lens Step-5a cleared) · **QUAL-096/072/073/074/121** (the five CI gates) ·
>   **QUAL-026** (mock hosts now in the solution, resolved by QUAL-074). QUAL-069 rationale comment corrected.
> - **PARTIAL (S133):** **QUAL-036** — 14 of 26 self-connection write overloads (write-with-`(conn,tx)`-sibling)
>   deleted; **residual 12** blocked on live test-seeding + one production seeder → FOLLOW-UP (retire/repoint the
>   test seeders onto a `(conn,tx)` helper, then delete the rest). (Register's prior "33/14" over-counted — it
>   folded in READ overloads + no-sibling writes; QUAL-036's precise target = 26 write-with-sibling / 9 repos.)
> - **DEFERRED:** **QUAL-016 bespoke cluster** (~16 tests: Skema/WorkTime/Approval/Overtime — org-scope tokens +
>   heavy seed + rule-engine stub) → its own increment per the owner's **OQ-3 SPLIT** ruling (mechanism proven +
>   shared, so pure copy-work). **QUAL-069** (payroll warn-gate) stays deferred (legacy CS0618 unretired).
> - **REGISTER CORRECTIONS (re-attack findings):** QUAL-111's "three endpoints have no 428 test" → actually **six**
>   (AgreementConfig ×3 + PositionOverride ×3; only WageTypeMapping's 2 were covered). QUAL-096's "~82 DemoSeed
>   tests" → actually **153**. QUAL-036's "33/14" → **26/9** (see PARTIAL above).
> - **8 S132-discovered follow-ups registered** at S133 open: QUAL-142–146 (new rows below) + SEC-043–045.

> ## S132 remediation status (2026-08-21) — increment 1 of the fix-next program
> The S132 correctness-and-safety core is remediated + merged (uncommitted pending the sprint-close commit);
> each fix was dual-lens-reviewed. Per-fix detail + verification in `docs/sprints/SPRINT-132.md`.
> - **FIXED (S132):** **QUAL-001** (Critical daily-rest miscalc + day-attribution + compliance period-edge +
>   the payroll fail-closed guard; ADR-039) · **QUAL-021** (its RED-on-old) · **QUAL-114** (threshold
>   fixtures) · **QUAL-133** (§15 reversal probe — the go-live precondition guard) · **QUAL-002** (manifest
>   encoding) · **QUAL-004** (settlement recovery) · **QUAL-005** (Copenhagen date helper) · **QUAL-006**
>   (idempotency atomicity) · **QUAL-007** (weekly-pipeline swallow) · **QUAL-141** (the doc-freshness HARD
>   gate — landed). Mirrored SEC rows **SEC-039/040/041** fixed + **SEC-004** confirming test (SEC register).
> - **DEFERRED:** **QUAL-069** (payroll warn-gate) — per OQ-5, the payroll project's baseline CS0618 (legacy
>   `CalculateAsync` overload) is not cleared by S132; lands when that overload retires.
> - **ROUTED (unchanged by S132):** test-integrity rows (QUAL-013…022, 095/110/111, 096/121, 072/073/074)
>   **+ the D4 dead-code batch QUAL-027/036** → **S133** (per the ratified ROADMAP program record `ROADMAP.md:198`
>   + the SPRINT-132 OUT-set; **corrected at S133 open 2026-08-24** — an earlier draft of this block mis-routed
>   QUAL-027/036 to S134, which contradicted ROADMAP; the dual-lens S133 Step-0b caught it); audit-scope
>   (QUAL-003/009) + observability (QUAL-008) + doc pass (QUAL-010/011/012/090/093 + D7) → **S134**; QUAL-123
>   + day-attribution → **domain-semantics track**.
> - **S132-DISCOVERED follow-ups (8):** enumerated in `SPRINT-132.md` §S132-discovered follow-ups (rule-eval
>   swallow · adjacent-interval false-gap · guard⇄payroll-widen go-live coupling · WeeklyCalculationPipeline
>   + TaskDispatcher normalize-or-retire · login timing side-channel · InvalidOperationException detail echo ·
>   app-wide username-log CR/LF audit · universal manifest byte-identity). Register as new rows at S133 open.
> - **Legacy-overload retirement + QUAL-069 landing** — a tracked follow-up (unblocks the payroll warn-gate).

**Semantics** (mirrors the SEC register): rows are a pointer-index — the durable "what + where + status."
Each row carries enough to **re-attack it** at any later audit: the plain-language claim, the code
`Source of truth` loci (tracked ground truth), the class, and the disposition. **Where the deeper
evidence lives**: for the **Critical + 21 High** rows, per-row verdict provenance (which refute batch,
what the panel changed, the Codex tag) is in the tracked adjudication record
**`docs/sprints/SPRINT-131-adjudication.md`**; for the **118 Medium** rows the adjudication carries
dimension-level provenance plus every refuted/merged/dedupe decision, and per-row panel history sits in
the regenerable sweep working papers (`.claude/sweeps/S131-*`, gitignored per the S129 raw-evidence
hygiene precedent — "hygiene, not secrecy"). **Revisit, not shield**: a Medium row is re-attackable from
its tracked loci + claim + the code, independent of the working papers. **Dedupe rule**: anything
already tracked in SEC / F / ROADMAP gets a cross-reference here at most (**two** candidates were retired
that way — the dev-JWT-key literal → SEC-015 and the KB-index freeze → ROADMAP; a third class, the
scope-denial log noise, was re-observed under SEC-012 but was never a candidate row — see the
adjudication record §external-dedupe).

**Verification chain**: every row survived (1) a read-only dimension sweep at the pinned baseline, (2) an
adversarial internal refute panel that re-derived the evidence fresh (6 rows refuted, ~20 merged,
severities corrected both directions), and — for the Critical + High rows — (3) the Codex external lens:
**the 21 High rows got 19 CONFIRM / 2 ADJUST / 0 REFUTE**, and **the Critical (QUAL-001) got Codex PR-1
with boundary opinion BQ-1 recommending Critical** (a severity recommendation, which with the panel's own
Critical filing is why its status reads `CONFIRMED(dual-lens)`). Calibration: 3 withheld items; round-1
score 1/3 (honest record); both misses recovered unseeded by corrected-method passes; all three hit
chains verified code-derived.

## Owner adjudication — DECIDED 2026-08-20 (all 9 packet items ruled)

1. **QUAL-001 = Critical, ratified.** 2. **QUAL-133 = fix now in S132** (not left gated). 3. **Systemic
rule = family-alone-reaches-High (broader than the panel line)** → **6 rows promoted Medium→High:
QUAL-027, QUAL-036, QUAL-090, QUAL-095, QUAL-110, QUAL-111** (the replication families — the defect IS
the repetition). *Borderline consistency-clusters kept Medium and named for a possible later pull-up:
QUAL-028, QUAL-029, QUAL-050, QUAL-061, QUAL-064, QUAL-109 — each is N sites independently diverging from
a norm, not a copied family.* 4. **All six gate proposals adopted for S132.** 5a. **All five candidate SEC
rows mirrored** → SEC-038 (QUAL-009), SEC-039 (QUAL-061), SEC-040 (QUAL-063), SEC-041 (QUAL-049), SEC-042
(QUAL-011). 5b. **SEC-004 CLOSED** (premise retired by S92–S95; + a one-line exact-Organisation verify
test). 6. **QUAL-123 routed to the domain-semantics track, severity held** (leaves the S132 rest bundle).
7. **The consolidated findings index is now tracked** (`docs/sprints/SPRINT-131-consolidated-findings.md`).
8. **Read-only deviation-of-letter ratified.** 9. **S132 = the full fix-next set** (Critical + all High +
the six gates; Mediums stay register-tracked for themed follow-ups).
**Revised totals after the promotions: 1 Critical + 27 High + 112 Medium = 140 findings (+ QUAL-141 gate
proposal).** Full rationale in `docs/sprints/SPRINT-131-adjudication.md` §Owner rulings.

**Statuses**: `CONFIRMED(dual-lens)` (panel + Codex) · `CONFIRMED(panel)` · `(L)` = confidence Likely · `↑H` = owner-promoted M→H (2026-08-20) ·
`GP` = gate proposal (owner rules per gate; the audit changed no CI behavior) · `⚖` = was an owner-reserved call — **all now DECIDED 2026-08-20** (see the Owner-adjudication section above); the marker is retained on rows for provenance
on a reserved call. All dispositions are proposals until owner adjudication (→ S132).

## Findings

### Critical (1)
| QUAL | What it means (plain language) | Title | Dim | Sev | Status | Source of truth | Adjudication |
|------|--------------------------------|-------|-----|-----|--------|-----------------|--------------|
| QUAL-001 | The statutory 11-hour daily-rest check assumes no shift crosses midnight: a 23:00–02:00 night shift counts as ~29h of rest (breach reported compliant), and a night callout beside a day shift becomes invisible to the check. The shape is the system's canonical one; nothing splits or validates it. Bounded to the POST /api/time-entries surface. | Daily-rest check treats a night shift as 29h rest | rule-engine (product) | **Critical** ⚖ | CONFIRMED(dual-lens; both lenses recommend Critical) | `RestPeriodRule.cs:218-278` | →SPRINT-131-adjudication §QUAL-001; owner ratifies severity |

### High (21 as-swept + 6 owner-promoted M→H = 27; the 6 promoted rows [QUAL-027/036/090/095/110/111] remain physically in the Medium table below with `↑H` Sev markers, not relocated)
| QUAL | What it means (plain language) | Title | Dim | Sev | Status | Source of truth | Adjudication |
|------|--------------------------------|-------|-----|-----|--------|-----------------|--------------|
| QUAL-002 | The live calculation and the projection rebuilder write the same JSONB column in two enum encodings; the only reader parses one — after any rebuild the projection read path is permanently dead behind a generic fallback log, and the documented recovery restores rows the reader can't parse. | Two writers disagree on segment_manifests enum encoding | D4 (product) | High | CONFIRMED(dual-lens) | `PeriodCalculationService.cs:993,1056,1070`; `SegmentManifestProjectionRebuilder.cs:110` | →adjudication §QUAL-002 |
| QUAL-003 | The only method that stamps a calculation's manifest-id into the audit trail is never called, so the audit linkage ADR-016 promises cannot exist — and the Payroll host registers no audit middleware at all (audit rows are Backend-only). | Manifest-id audit enrichment has no caller | D4+D7 | High | CONFIRMED(dual-lens) | `PeriodCalculationService.cs:756`; `ADR-016:134,:224` | →adjudication §QUAL-003; ⚖ Backend-only-audit design question |
| QUAL-004 | Four copy-pasted settlement race-recovery blocks: three hardened to fail loudly, the fourth still fabricates an "already settled" outcome from a row that was never written — and claims to mirror the others. | Settlement duplicate-key recovery diverges across copies | D4 | High | CONFIRMED(dual-lens) | `VacationSettlementService.cs:388-408` vs 3 siblings | →adjudication §QUAL-004 |
| QUAL-005 | The "today in Copenhagen" rule is implemented six times; the copy deciding the statutory §21 stk.2 transfer deadline diverges three ways (fixed +01:00 fallback wrong in summer, narrower catch, no injectable clock). | Copenhagen date helper: six copies, one diverges on a statutory guard | D4 | High | CONFIRMED(dual-lens) | `VacationSettlementEndpoints.cs:1511-1523,:177-184` | →adjudication §QUAL-005 |
| QUAL-006 | If writing the payroll-correction duplicate-prevention marker fails, the failure is swallowed with no log — the guard silently stops working and a same-token retry emits a phantom second correction event and audit row. | Payroll idempotency-mark failure swallowed unlogged | D5 | High | CONFIRMED(dual-lens) | `Payroll Program.cs:399-415` | →adjudication §QUAL-006 |
| QUAL-007 | The weekly calculation pipeline never checks whether its Backend fetches succeeded and reports success unconditionally — a 403 with a JSON body yields an empty calculation persisted and returned as completed. | Weekly pipeline persists a failed fetch as completed | D5 | High | CONFIRMED(dual-lens) | `WeeklyCalculationPipeline.cs:67-75,:140` | →adjudication §QUAL-007 |
| QUAL-008 | The correlation id the system carefully mints and propagates appears in 1 of 185 log lines, and no ambient mechanism substitutes — one user action cannot be traced across services from the logs. | Correlation id absent from 184 of 185 log sites | D8 | High | CONFIRMED(dual-lens) | census; sole emitter `OrgScopeValidator.cs:451-453` | →adjudication §QUAL-008 |
| QUAL-009 | Authorization denials decided at the policy layer produce no application log and no audit row (the audit middleware runs after authorization — and only the Backend has it), so a 403's cause and occurrence can't be established afterwards. | Policy-level authorization denials leave no trace | D8 | High (L) | CONFIRMED(dual-lens) | `ScopeAuthorizationHandler.cs`; `Backend Program.cs:484-487` | →adjudication §QUAL-009; ⚖ SEC-routing |
| QUAL-010 | The agreement reference doc — prompt input for the Rule Engine Agent — states the særlige-feriedage reset month as September; both sources it cites say January, and the September model is the recorded pre-S80 defect. | Agreement reference states wrong reset month | D7 | High | CONFIRMED(dual-lens) | `danish-agreements.md:117` | →adjudication §QUAL-010 |
| QUAL-011 | SECURITY.md asserts the unit dimension grants no approval authority — false since S105 deliberately wired the unit-leader edge — and pins the claim to a test and tables that no longer exist. | SECURITY.md asserts a false authority model | D7 | High | CONFIRMED(dual-lens) | `SECURITY.md:134-138` | →adjudication §QUAL-011; ⚖ SEC-routing (strongest) |
| QUAL-012 | The legacy-database upgrade runbook's per-sprint pointers land on unrelated schema lines in ~11 of 14 rows (one names dropped tables); followed as written it cannot complete the migration. | Upgrade runbook pointers resolve to unrelated DDL | D7 | High | CONFIRMED(dual-lens) | `legacy-db-upgrade-runbook.md:44-60,:149` | →adjudication §QUAL-012 |
| QUAL-013 | The one unit-test file named for the payroll wage-type mapping never touches the shipped resolver — it asserts values it just assigned and re-implements the lookup in the test body. | Payroll wage-type mapping tests assert nothing | D3 | High | CONFIRMED(dual-lens) | `PayrollMappingTests.cs:5-59` | →adjudication §QUAL-013 |
| QUAL-014 | Four legacy-migration tests apply migration SQL pasted into the test instead of extracting it from the shipped init.sql — they verify their own copies, which have already drifted in their provenance citations. | Migration tests run pasted DDL copies | D3 | High | CONFIRMED(dual-lens) | S25/S35/AuditProjection/LegacyProfileSchema test loci | →adjudication §QUAL-014 |
| QUAL-015 | Both payroll replay-determinism "marquee" tests mutate a field that neither the test stub nor the calculation service ever reads — the byte-identity assertion cannot fail on the regression the tests exist for. | Replay marquees cannot fail on resolver regression | D3 | High | CONFIRMED(dual-lens) | `EmployeeProfileMarqueeTests.cs:121-212` | →adjudication §QUAL-015 |
| QUAL-016 | ~45 "atomic-outbox" tests across 15 files re-implement endpoint orchestration inside the test (a stated harness convention), so they keep passing when the endpoints they are named for change or disappear — a failure mode this repo has recorded twice. | Atomic-outbox suite pins repo contracts, not endpoints | D3 | High | CONFIRMED(dual-lens) | `ForcedRollbackHarness.cs:49-58` + member list | →adjudication §QUAL-016 |
| QUAL-017 | The sole test of "a soft-deleted profile must not silently default on the Compliance path" has one arm that asserts nothing and another any-5xx satisfies — and the harness's unstubbed rule-engine hop produces a 5xx regardless. | Compliance fail-closed test cannot verify its claim | D3 | High | CONFIRMED(dual-lens) | `EmployeeProfileLifecycleTests.cs:702-755` | →adjudication §QUAL-017 |
| QUAL-018 | The role-revoke endpoint carries three separate 403 guards; no test anywhere exercises any of them — only the happy path is covered. | Role-revoke authorization has no deny test | D3 | High | CONFIRMED(dual-lens) | `AdminEndpoints.cs:2468-2486` | →adjudication §QUAL-018 |
| QUAL-019 | No test reads the production rule-classification registry — every segmentation test hand-builds its own tuples, so mis-registering a real rule's OK-version split behavior breaks nothing. | Rule-classification registry pinned by no test | D3 | High | CONFIRMED(dual-lens) | `RuleRegistry.cs:53-93` | →adjudication §QUAL-019 |
| QUAL-020 | The wage-type-mapping supersession suite writes its own outbox row and its own reopen UPDATE and asserts them back — the real emitter and its audit mapper are exercised by nothing. | WTM supersession pins assert the test's own SQL | D3 | High | CONFIRMED(dual-lens) | `WageTypeMappingSupersessionTests.cs`; emitter `WageTypeMappingEndpoints.cs:465-525` | →adjudication §QUAL-020 |
| QUAL-021 | The daily-rest rule is never tested with a midnight-crossing shift — the canonical shape that exposes QUAL-001 — so the defect had no guard. | Daily-rest untested for midnight-crossing shifts | D3 | High | CONFIRMED(dual-lens) | `RestPeriodRuleTests.cs:79-139` | →adjudication §QUAL-021 |
| QUAL-022 | A registered, invoked compliance rule has zero tests anywhere — while the file named for it is a byte-identical copy of a different rule's tests, adding ten phantom tests to the suite count. | OvertimeGovernanceRule untested; its test file is a copy | D3 | High | CONFIRMED(dual-lens) | `RuleRegistry.cs:85-87`; `Sprint17OvertimeGovernanceTests.cs` | →adjudication §QUAL-022 |

### Medium (118 as-swept − 6 owner-promoted to High = 112; the 6 promoted rows carry an `↑H` Sev marker in place) — pointer rows (re-attackable from the in-row loci + claim + code; per-row verdict provenance now in the tracked `docs/sprints/SPRINT-131-consolidated-findings.md`, dimension-level in the adjudication record)
| QUAL | What it means (plain language) | Title | Dim | Sev | Status | Source of truth | Adjudication |
|------|--------------------------------|-------|-----|-----|--------|-----------------|--------------|
| QUAL-023 | The health page fetches seven services on hardcoded localhost ports, breaking the frontend-talks-to-Backend-only hard rule; off a dev machine all seven resolve to the viewer's host, so six of seven (all but a locally-run Backend) report unreachable. | Health page bypasses the Backend | D1 | M | CONFIRMED(panel; **H→M** by tier rubric) | `HealthDashboard.tsx:11-30` | →adjudication |
| QUAL-024 | Compose never sets the Backend's rule-engine URL (it silently falls back to a constant) while setting an orchestrator URL nothing reads and a startup dependency nothing calls. | Compose wiring inverted vs actual call graph | D1 | M | CONFIRMED(panel) | `docker-compose.yml:33-42` | →adjudication |
| QUAL-025 | The shared assembly carries the accrual formula, statutory period geometry and the segmentation planner across three folders the architecture doc doesn't list, under a "no business logic" rule. The code's placement rationale is sound; the doc is silent. | SharedKernel holds undocumented domain logic | D1 | M | CONFIRMED(panel) | `ARCHITECTURE.md:52-62` vs 9-folder census | →adjudication; document |
| QUAL-026 | Two declared services are .NET projects outside the solution, so the standard build gate compiles 14 of 16 projects (CI's compose jobs do compile them later, unconditionally). | Two mock hosts sit outside the solution build | D1 | M | CONFIRMED(panel) | `StatsTid.sln`; `ci.yml` | →adjudication; xref QUAL-072/074 |
| QUAL-027 | 17 of 79 audit mappers carry byte-identical private copies of the shared JSON options — changing the shared setting silently splits the audit payload format. | Seventeen audit mappers copy the shared JSON options | D4 | ↑H | CONFIRMED(panel); M→H owner 2026-08-20 | `AuditMapperJsonOptions.cs` + 17 files | →adjudication; hoist to SharedKernel |
| QUAL-028 | Four admin-config hooks share verbatim boilerplate; two lack the stale-response race guard although every mutation re-fetches — overlapping responses can paint stale rows. | Config hooks: race guard present in two of four | D4 | M (L) | CONFIRMED(panel) | `usePositionOverrides.ts:72-83`; `useWageTypeMappings.ts:74-86` | →adjudication; F2 register corrected |
| QUAL-029 | Four screens copy the version-conflict handler; two narrow errors safely, two use the unchecked cast their own hooks' convention forbids (and those hooks export no guard to call). | 412-handler diverges: type guard vs blind cast | D4 | M | CONFIRMED(panel) | `PositionOverrideManagement.tsx:52-59` +3 | →adjudication |
| QUAL-030 | The before/after audit payload is built by two hand-written 39-field lists that must be edited in lockstep with no compiler or test complaint. | Twin 39-field audit serializers | D4 | M | CONFIRMED(panel) | `AgreementConfigEndpoints.cs:992-1021` | →adjudication |
| QUAL-031 | Six decomposed compliance-check entry points are registered but unroutable and uncalled — editing one changes nothing (the combined entry point still runs every check). | Six rule entry points have no caller | D4 | M | CONFIRMED(panel) | `RestPeriodRule.cs` / `OvertimeGovernanceRule.cs` / `RuleRegistry.cs:213-232` | →adjudication |
| QUAL-032 | The retry-backoff helper is referenced nowhere — the live retry policy is a pair of fixed delays — yet it reads as the resilience infrastructure. | ExponentialBackoff is entirely unused | D4 | M | CONFIRMED(panel) | `ExponentialBackoff.cs` | →adjudication; delete |
| QUAL-033 | Three shared interfaces describing in-process service contracts have zero implementers, inviting a reader to treat HTTP-only boundaries as in-process seams. | Three contract interfaces have no implementer | D4 | M | CONFIRMED(panel) | `IRuleEngine/IPayrollMapper/IIntegrationGateway.cs` | →adjudication |
| QUAL-034 | The compensatory-rest feature has a read endpoint and write methods that nothing calls; its event is never constructed; only a test seeds the rows it reads. | Compensatory-rest write path has no producer | D4 | M | CONFIRMED(panel) | `CompensatoryRestRepository.cs:25-75` | →adjudication |
| QUAL-035 | Five plausible-looking repository read methods are called by nothing, in production or tests. | Five repository reads with zero callers | D4 | M | CONFIRMED(panel) | 5 loci (adjudication) | →adjudication |
| QUAL-036 | Across 14 repositories, every "opens its own transaction" write overload is production-unused — production always uses the sibling that joins the caller's transaction (the one the outbox+audit atomicity contract binds). 33 loaded traps for the next writer. | Self-connection write overloads are production-unused | D4 | ↑H ⚖ | CONFIRMED(panel) [QC-3]; M→H owner 2026-08-20 | family enumeration (adjudication) | →adjudication; ⚖ systemic-High revisit under BQ-2 |
| QUAL-037 | Both payroll replay entry points are unreachable from production — the byte-identical replay-determinism guarantee is proven about code production never runs. | Replay surface has no production caller | D4 | M | CONFIRMED(panel) | `PeriodCalculationService.cs:669-710` | →adjudication |
| QUAL-038 | An 889-line repository with seven public members is referenced by nothing and registered nowhere — deliberate ADR-024 dormancy that the code itself doesn't disclose. | Entire RoleConfigOverrideRepository is unreachable | D4 | M | CONFIRMED(panel) | `RoleConfigOverrideRepository.cs` | →adjudication; document dormancy |
| QUAL-039 | The 600-line legacy-profile migrator has no production entry point of any kind; five tests keep it compiling. Whether it is operational capability or scaffolding is unanswerable from the code. | Profile migrator is invoked only by tests | D4 | M | CONFIRMED(panel) | `LocalAgreementProfileMigrator.cs` | →adjudication; SEC-037 annotated |
| QUAL-040 | The role-unfloored variant of the accessible-orgs lookup has no production caller and is kept alive only by tests that demonstrate the leak it permits — a shorter, more obvious call for the next developer to pick wrongly. | Unfloored scope overload is a retained footgun | D4 | M | CONFIRMED(panel) | `OrgScopeValidator.cs:370-372,:384-385` | →adjudication; closes SEC-022 half 1 |
| QUAL-041 | The audit log has a production write path but neither of its two query methods is called by anything — the advertised audit read surface doesn't exist. | Both audit-log query methods are unreferenced | D4 | M | CONFIRMED(panel) | `AuditLogRepository.cs:42-81` | →adjudication |
| QUAL-042 | The two static central-config lookups are asserted by ~30 test lines and called by no production path. | Central-config lookups are test-only | D4 | M | CONFIRMED(panel) | `ConfigResolutionService.cs:414-429` | →adjudication |
| QUAL-043 | The shared request validator's absence method is unused while its time-entry twin is live — absence registration validates inline, so the "shared validator" pattern misleads. | ValidateAbsence unused beside its live twin | D4 | M | CONFIRMED(panel) | `RequestValidator.cs:26,41` | →adjudication |
| QUAL-044 | A public method that increases used leave with no quota check sits uncalled next to the guarded one production uses — and its ungated shape is already copied once. | Ungated balance mutator with no caller | D4 | M | CONFIRMED(panel) | `EntitlementBalanceRepository.cs:87-124` | →adjudication; delete |
| QUAL-045 | Two audit-projection counters, an overtime adjuster and the circuit-breaker reset exist only for tests — production cannot even invoke the manual recovery control. | Four members load-bearing to tests only | D4 | M | CONFIRMED(panel) | 4 loci (adjudication) | →adjudication |
| QUAL-046 | A legacy grid component has zero referrers (its documented referrer was deleted sprints ago) — dead by the full conservative checklist. | WeekGrid.tsx is dead | D4 | M | CONFIRMED(panel+Orchestrator checklist) | `WeekGrid.tsx` | →adjudication; xref QUAL-083 |
| QUAL-047 | Asking payroll to export an unapproved month returns 403 — the same code as a real permission denial ten lines above; a caller can't tell "not allowed" from "not ready". | Payroll export returns 403 for a state condition | D5 | M | CONFIRMED(panel) | `Payroll Program.cs:230-240` | →adjudication |
| QUAL-048 | The payroll service rejects with 422 in three structurally different body shapes under two different key conventions. | Payroll emits three different 422 shapes | D5 | M | CONFIRMED(panel) | `Payroll Program.cs:151-396` | →adjudication |
| QUAL-049 | The bulk reporting-line import turns any unexpected exception into the tree's only ad-hoc 500 — carrying the raw exception message to the client and logging nothing server-side. | Bulk-import 500 with raw exception text, unlogged | D5 | M | CONFIRMED(panel) | `ReportingLineEndpoints.cs:1144-1147` | →adjudication; ⚖ SEC flag (ex.Message in body) |
| QUAL-050 | Five access denials return an empty-body 403 while 128 sibling sites return {error, reason} — the UI renders the empty ones as the literal "HTTP 403". | Five 403s return an empty body | D5 | M | CONFIRMED(panel) | `TimeEndpoints.cs` ×4; `AuditEndpoints.cs:35` | →adjudication |
| QUAL-051 | The send-month allocation rejection is the only 4xx body in the API without an `error` key — clients reading `.error` get undefined on the gate's most common rejection. | Allocation-gate 422 lacks the error key | D5 | M | CONFIRMED(panel) | `ApprovalEndpoints.cs:2006-2013` | →adjudication |
| QUAL-052 | "Already decided/revoked" state conflicts return 400 (malformed input) at three sites where ten siblings return 409 (refresh and retry). | State conflicts returned as 400 not 409 | D5 | M | CONFIRMED(panel) | `OvertimeEndpoints.cs`; `AdminEndpoints.cs:2461` | →adjudication |
| QUAL-053 | Two overtime business-rule rejections return 400 against the codebase's own written rule (400 = out-of-range input; 422 = product-rule rejection). | Overtime rejections violate the code's own 422 rule | D5 | M | CONFIRMED(panel) | `OvertimeEndpoints.cs:540,707` | →adjudication |
| QUAL-054 | The local-profile effective-date rejection returns 400 where nine sibling surfaces return 422; the divergence is noted in-code with a partial rationale. | Effective-date rejection diverges from nine siblings | D5 | M | CONFIRMED(panel) | `ConfigEndpoints.cs:196-214` | →adjudication; document |
| QUAL-055 | The nested entitlement PUT answers the identical "URL id isn't the open row" predicate with 404 while its declared verbatim-mirror answers 409 — and its message describes the wrong check. | Sub-resource PUT contradicts its declared mirror | D5 | M | CONFIRMED(panel) | `AgreementEntitlementEndpoints.cs:357-366` | →adjudication |
| QUAL-056 | Two authority-granting unit writes skip the If-Match precondition their four siblings enforce, contradicting the file's own "every mutator" claim (no lost-update hazard — insert-only under a lock). | Unit-leader writes skip If-Match vs their own contract | D5 | M | CONFIRMED(panel) | `UnitEndpoints.cs:616-759,:20-26` | →adjudication |
| QUAL-057 | The settlement resolution endpoint returns four different success shapes and declares none — the only authorized route invisible to the generated typed client. | /resolve undeclared with four 200 shapes | D5 | M | CONFIRMED(panel) | `VacationSettlementEndpoints.cs:353-1048` | →adjudication |
| QUAL-058 | The one frontend HTTP client shows users the raw response text instead of the API's error field — raw JSON on screen, or "HTTP 403" when the body is empty. | Frontend client renders raw error bodies | D5 | M | CONFIRMED(panel) | `api.ts:68-78,576-593` | →adjudication |
| QUAL-059 | If the team-overview's away-today read fails, the exception is discarded and every row reports "not away" as fact — no log, no degradation signal (verified not a fail-open; declared fault isolation). | Team-overview degrades silently on read failure | D5 | M | CONFIRMED(panel) | `ApprovalEndpoints.cs:955-976` | →adjudication |
| QUAL-060 | The weekly pipeline's three rule-failure warnings carry no parameters at all, though the employee and correlation ids are in scope. | Pipeline failure logs carry no identifiers | D8 | M | CONFIRMED(panel) | `WeeklyCalculationPipeline.cs:152-186` | →adjudication |
| QUAL-061 | Failure paths log the rule engine's whole response body — employee id plus per-day hours/rates/balances — making the log stream an unmanaged copy of employment data. | Employment data logged verbatim on failure paths | D8 | M | CONFIRMED(panel) | `PeriodCalculationService.cs:1399-1485` +1 | →adjudication; ⚖ SEC-routing |
| QUAL-062 | A failed payroll delivery is logged at Information — invisible to anyone filtering Warning+ — while the same method's other failure mode logs Error (the DB envelope does flip to failed). | Failed payroll delivery logged at Information | D8 | M | CONFIRMED(panel) | `PayrollExportService.cs:229-238` | →adjudication; document |
| QUAL-063 | Failed logins produce no application log line and the attempted username reaches no store — unknown-user and wrong-password are indistinguishable (the audit row records the 401 + IP with a null actor). | Failed logins unlogged; username recorded nowhere | D8 | M | CONFIRMED(panel) | `AuthEndpoints.cs:23-31,90-91` | →adjudication; ⚖ SEC-routing |
| QUAL-064 | A Warning-level family fires on caller behavior the adjacent code documents as normal — alarms that always ring get ignored. | Warnings fire on documented-expected input | D8 | M | CONFIRMED(panel) | `TimeEndpoints.cs:83-85`; `PCS.cs:334-337`; `RCS.cs:179-188` | →adjudication |
| QUAL-065 | Correlation ids minted for header-less requests are stored only in Items and the response, so the Backend→RuleEngine forwarder finds nothing to forward — no id ever crosses that hop for frontend traffic. | Minted correlation ids never reach the RuleEngine | D8 | M | CONFIRMED(panel) | `CorrelationIdMiddleware.cs:16-34`; forwarding handler | →adjudication |
| QUAL-066 | When a settlement line exhausts its retry budget and flips to terminal DEAD_LETTER, no log records the transition — "will retry" and "abandoned" share one Warning template. | Terminal DEAD_LETTER is never logged | D8 | M | CONFIRMED(panel) | `SettlementExportEmitter.cs:930-1017` | →adjudication |
| QUAL-067 | When writing an audit row fails, the fallback log carries zero identifying fields — the lost entry cannot be reconstructed. | Dropped audit entry logged with no fields | D8 | M | CONFIRMED(panel) | `AuditLoggingMiddleware.cs:61-65` | →adjudication |
| QUAL-068 | 185 disciplined structured log templates feed a plain-text console — no structured sink exists anywhere, so none of the fields are queryable (design-target readiness). | Structured templates, no structured sink | D8 | M | CONFIRMED(panel) | 10 appsettings + 5 Program.cs | →adjudication; document |
| QUAL-069 | The payroll project's whole "warnings are errors" gate is off for one benign warning, and the justifying comment is wrong about both the location and the caller count — new warnings there pass CI silently. | Payroll warn-gate opt-out is project-wide, rationale false | D6 | M GP | CONFIRMED(panel) [QC-1 base] | `Directory.Build.props:48-58` | →adjudication; ⚖ gate |
| QUAL-070 | 18 deliberate breadcrumb warnings wait on a retirement that already happened — the endpoint they track is gone, so they can never clear and two comments promise work that's done. | Obsolete-API breadcrumbs outlived their trigger | D6 | M | CONFIRMED(panel) | `ConfigResolutionService.cs:317-328` | →adjudication |
| QUAL-071 | The comment justifying the test-project warning opt-outs explains 18 of 136 exempted warnings and gives no cover for the six that flag test correctness itself. | Opt-out rationale covers a fraction of what it exempts | D6 | M | CONFIRMED(panel) | `Directory.Build.props:60-68` | →adjudication; document |
| QUAL-072 | Two projects named in the warning-gate opt-out list can never be reached by it (outside the solution and the Docker build context) — the lines describe a regime that doesn't exist. | Mock-host opt-out entries are unreachable config | D6 | M GP | CONFIRMED(panel) | `Directory.Build.props:77-78` | →adjudication; ⚖ gate |
| QUAL-073 | 109 identical benign SQL-review warnings make warning #110 invisible; freeze the count and ratchet down. | Freeze CA2100 at 109 and ratchet | D6 | M GP | CONFIRMED(panel; benignity L) | census 109×71 files | →adjudication; ⚖ gate. **Agent guidance (S137):** the ratchet counts TEST projects too — a generic `Exec(string sql)`/`Scalar(string sql)` test helper adds one CA2100 site per `new NpgsqlCommand(sql, …)`; write one literal-SQL method per statement, and replicate locally with `dotnet build -c Release --no-incremental` + the ci.yml grep before submitting (TASK-13703 hit 118 vs 115 on a first draft). |
| QUAL-074 | Two C# projects are compiled by no gated build — they could stop compiling with CI green; bring them into the gate, then decide their opt-outs on evidence. | Bring the mock hosts into a compiled gate | D6 | M GP | CONFIRMED(panel) | `StatsTid.sln` + `ci.yml:121` | →adjudication; ⚖ gate |
| QUAL-075 | A permanent weak-hash suppression is justified by MD5 code deleted ~25 sprints ago — it suppresses nothing today and would hide a real weak-hash use tomorrow. | NoWarn CA5351 justified by deleted code | D6 | M | CONFIRMED(panel) | DemoSeed csproj:17-20 | →adjudication |
| QUAL-076 | A suppression rationale asserts every SQL string is a static literal; four queries interpolate an identifier with no parameters — the stated safety condition is false. | Suppression rationale states a false condition | D6 | M | CONFIRMED(panel) | DemoSeed csproj:15-16; `Loading/DemoVerifier.cs:339-365` | →adjudication; document |
| QUAL-077 | The payroll shim's deprecation texts misstate their own caller count (says one, has two — the second hidden under its own pragma) and frame a completed migration as in-flight, so indefinite debt reads as transitional. | Obsolete-shim rationale family misstates its state | D6 | M | CONFIRMED(panel) [QC-2 locus `RCS.cs:209`] | `PCS.cs:497-516` + `RCS.cs:203-213` | →adjudication; document |
| QUAL-078 | Three of seven frontend lint suppressions carry no reason at all — one in the authentication context — while every C# suppression in the tree is rationale-backed. | Three bare frontend lint suppressions | D6 | M | CONFIRMED(panel) | `AuthContext.tsx:94` +2 | →adjudication; suppress-with-reason |
| QUAL-079 | Two replay tests point their manifest-stamp explanation at production lines holding unrelated logic — the same file reads those lines correctly elsewhere. | Replay tests cite the wrong manifest-stamp lines | D6 | M | CONFIRMED(panel) | `EmployeeProfileMarqueeTests.cs:142`; `ReplayDeterminismTests.cs:205` | →adjudication; document |
| QUAL-080 | The architecture doc describes service layouts using three folders that don't exist and a repository class that was renamed. | ARCHITECTURE names nonexistent folders and a class | D7 | M | CONFIRMED(panel) | `ARCHITECTURE.md:81,107,109` | →adjudication |
| QUAL-081 | The doc's rule list omits three of eleven rule classes and its endpoint table lists 11 of 24 files, with one group's count at less than half the real number. | ARCHITECTURE's rule/endpoint inventories froze | D7 | M | CONFIRMED(panel; corrected) | `ARCHITECTURE.md:13,80,89-101` | →adjudication |
| QUAL-082 | The frontend reference lists four components/pages that were deleted — a UX task routed off it starts from a false map. | FRONTEND names deleted components | D7 | M | CONFIRMED(panel) | `FRONTEND.md:112,114,164,167` | →adjudication |
| QUAL-083 | The doc explains a legacy component's retention by naming a page the same doc says was deleted, and lists one global stylesheet where three exist (one imported at app entry). | FRONTEND misstates WeekGrid and the styles folder | D7 | M | CONFIRMED(panel) | `FRONTEND.md:116,244,310` | →adjudication; xref QUAL-046 |
| QUAL-084 | Three of six code pointers in the accepted-residuals map land on unrelated code — the map's substance holds but can't be confirmed from its citations. | Residual map's code pointers rotted | D7 | M | CONFIRMED(panel) | `SECURITY.md:155-156` | →adjudication |
| QUAL-085 | An owner-accepted authorization residual and its committed follow-up sprint are justified by a method and scope type that were removed — and by an org model the flat-authority reform retired; the hole may already be closed. | Accepted residual justified by retired constructs | D7 | M | CONFIRMED(panel) | `SECURITY.md:126,:132` | →adjudication; ⚖ SEC-004 close-or-rescope |
| QUAL-086 | The audit catalog's header still says "initial draft" against its own 74/78 status, its validation section speaks of shipped tests in the future tense, and two pointers rotted (the "never constructed" sub-claim was refuted — the event is emitted). | Audit catalog header and pointers stale | D7 | M | CONFIRMED(panel; adjusted) | `audit-projection-catalog.md:5,32,52,175-182` | →adjudication |
| QUAL-087 | The demo-seed README justifies bypassing the event-emitting grant API with a "500s on every call" defect that was fixed three sprints later. | DemoSeed README asserts a fixed defect as current | D7 | M | CONFIRMED(panel) | DemoSeed README:136-140 | →adjudication |
| QUAL-088 | Two failure-log entries are filed inside the patterns table; the failure table shows four of the six on disk — invisible to the link-presence CI gate. | FAIL entries misfiled in the PAT table | D7 | M | CONFIRMED(panel) | `INDEX.md:65,68,92-97` | →adjudication |
| QUAL-089 | The endpoint registry presents three deleted routes as "superseded" (implying they still answer) and names a hook that doesn't exist. | DEP-004 lists deleted endpoints as superseded | D7 | M | CONFIRMED(panel) | DEP-004:107-113,170 | →adjudication |
| QUAL-090 | Seventeen contract comments name specific schema lines as the "authority" for closed vocabularies; three of the four cited anchors land on unrelated tables. | "Authority: init.sql" comment family points wrong | D7 | ↑H | CONFIRMED(panel); M→H owner 2026-08-20 | Contracts/*Responses.cs (17 sites) | →adjudication |
| QUAL-091 | An accepted ADR asserts a "real, launch-blocking" determinism exposure that is closed — and refutes itself three lines later. | ADR-023 asserts a closed exposure as live | D7 | M | CONFIRMED(panel) | ADR-023:48 vs :51 | →adjudication; amendment note |
| QUAL-092 | The Security Agent's file scope names a directory that doesn't exist and omits the assembly holding JWT validation and the scope handler — under a rule forbidding out-of-scope edits. | Security-Agent scope names a dead folder, omits src/Auth | D7 | M | CONFIRMED(panel) | `AGENTS.md:30` | →adjudication; ⚖ SEC-routing |
| QUAL-093 | A comment in the balance code states the pre-correction September reset for særlige feriedage — the wrong fact of QUAL-010, inside the consuming code (behavior itself is correct). | Balance comment states the retired reset month | D7 | M | CONFIRMED(panel) | `BalanceEndpoints.cs:632-633` | →adjudication; pairs QUAL-010 |
| QUAL-094 | The schema file's own ordering-pin comments cite their own lines wrongly by ~443 and ~1,190 lines — the operator who most needs them can't follow them. | init.sql's self-references are stale | D7 | M | CONFIRMED(panel) | `init.sql:1730,1732` | →adjudication |
| QUAL-095 | A family of legacy unit tests re-implements the system under test inside the test file (a local routing table; literal echoes; authorization-named POCO round-trips) — inflating counts while real guards live elsewhere. | Legacy Unit tests assert their own setup | D3 | ↑H (family size L) | CONFIRMED(panel); M→H owner 2026-08-20 | `TaskDispatcherTests.cs`; `Sprint7ScopeTests.cs`; `Sprint9SkemaTests.cs`; `ReportingLineTests.cs` | →adjudication; ⚖ BQ-2 |
| QUAL-096 | A real 82-test project — including byte-exact golden pins — is compiled by CI but never executed by it; its green status is never established. | DemoSeed suite built but never run in CI | D3 | M GP | CONFIRMED(panel) | `ci.yml` dotnet-test census | →adjudication; ⚖ gate |
| QUAL-097 | The two components deciding page access have no test of their refusal paths — an inverted guard passes the whole frontend suite (backend enforces independently). | Route-guard deny branches untested | D3 | M | CONFIRMED(panel) | `RequireRole.tsx`; `RequireAuth.tsx` | →adjudication |
| QUAL-098 | The outbox restart test almost certainly leaves nothing for the second publisher to resume — the named resume path is never created (it does pin exactly-once-across-restart). | Restart test cannot observe a resume | D3 | M (L) | CONFIRMED(panel; narrowed) | `OutboxPublisherTests.cs:71-112` | →adjudication |
| QUAL-099 | The no-go-live dormancy test sleeps three seconds and asserts nothing settled, with no evidence the poller ran (dormancy is separately pinned elsewhere with a real witness). | Dormant-gate negative lacks a run witness | D3 | M (L) | CONFIRMED(panel) | `SettlementCloseServiceBoundaryTests.cs:153-168` | →adjudication |
| QUAL-100 | The "projection replay is idempotent" test re-inserts its own rows with its own conflict clause — production projection code never runs (a real backfill-idempotency suite exists). | Replay test re-inserts its own rows | D3 | M | CONFIRMED(panel) | `FeriehindringResolutionTests.cs:545-571` | →adjudication |
| QUAL-101 | The agreement-code marquee never asserts its discriminator is present — one routine edit from tautology — and the author's believed second channel is provably inert (the comment misinforms). | Marquee lacks a positive control; backup channel inert | D3 | M | CONFIRMED(panel; upgraded) | `AgreementCodeMarqueeTests.cs:118-165,266-269` | →adjudication |
| QUAL-102 | The §15 gate negative's poll witness returns nothing and exits silently on timeout — a green pass can mean the poller never ran (the named regression itself would still fail it). | Gate test's poll witness is silent on timeout | D3 | M | CONFIRMED(panel; reclassed) | `SpecialHolidaySettlementTests.cs:193-207,381-392` | →adjudication |
| QUAL-103 | Under the exact regression it guards, the boundary-day exclusion test detects only if a random id sorts favorably — order-dependent, not guaranteed. | Boundary-day pin is iteration-order-dependent | D3 | M (L) | CONFIRMED(panel; estimate withdrawn) | `SettlementCloseLeaverTests.cs:172-214` | →adjudication |
| QUAL-104 | The dated-Position-wins precedence rule is asserted by a comment and a resolver probe — no test observes what the calculation service actually used. | Position precedence asserted by code-reading | D3 | M | CONFIRMED(panel) | `EmployeeProfileMarqueeTests.cs:239-288` | →adjudication |
| QUAL-105 | The class documents an owner ruling that three reads stay open at LocalAdmin, then pins only one — two could be silently tightened. | Read floor pinned on one of three GETs | D3 | M | CONFIRMED(panel) | `SEC032PositionOverrideAuthorizationTests.cs:188-195` | →adjudication |
| QUAL-106 | Three nested arrays of the program's largest composite are empty in every spec-matched read, so their element schemas are never validated — a shape lie about them would pass. | Spec gate never walks three nested schemas | D3 | M | CONFIRMED(panel; upgraded) | `S120SkemaSpecRuntimeTests.cs:130-134` | →adjudication |
| QUAL-107 | A test named Unknown404 accepts 404 or 403 while its comment asserts 403 definitively — a three-way name/comment/assertion contradiction (both are denials). | Unknown-target status pinned three ways differently | D3 | M | CONFIRMED(panel; strengthened) | `EmploymentEndDateLifecycleTests.cs:463-475` | →adjudication |
| QUAL-108 | The half-timer settlement test compares two identical inputs to a pure function — and the property it claims to pin holds by construction (no fraction exists in that code). | Half-timer test compares identical inputs | D3 | M | CONFIRMED(panel; H→M) | `VacationSettlementPartitionTests.cs:151-162` | →adjudication |
| QUAL-109 | Four replay-determinism assertion arms iterate collections without proving them non-empty — an emptied pipeline slips past those arms (a fifth site was cleared). | Assert.All arms pass on empty results | D3 | M | CONFIRMED(panel; H→M, 1 site killed) | `ManifestReplayTests.cs:71,75` +2 | →adjudication |
| QUAL-110 | The wage-mapping drift guard's hand-copied schema cites the wrong source lines, omits the duplicate-history unique index, and adds a default production lacks — it accepts writes production rejects. | WTM schema-drift guard has itself drifted | D3 | ↑H ⚖ | CONFIRMED(panel; leg replaced); M→H owner 2026-08-20 | `WageTypeMappingRegressionTests.cs:32-55` | →adjudication; ⚖ family promotion under BQ-2 |
| QUAL-111 | Eight identically-bodied helper tests claim to mirror eight endpoints' missing-precondition paths; three of those endpoints have no 428 test at all. | Eight If-Match tests mask three untested endpoints | D3 | ↑H | CONFIRMED(panel; reclassed); M→H owner 2026-08-20 | Concurrency 3 files | →adjudication |
| QUAL-112 | The dual-emission test performs both audit writes and both outbox inserts itself, then asserts them — the endpoint's emission is not verified here (the repo-side enabler is). | Dual-emission test performs its own emissions | D3 | M | CONFIRMED(panel; trimmed) | `AgreementConfigConcurrencyTests.cs:215-292` | →adjudication |
| QUAL-113 | The headline "determinism proofs" call a pure function twice and 101 times with identical arguments — properties the language guarantees (the traceability leg was cleared). | Determinism proofs test language guarantees | D3 | M | CONFIRMED(panel; narrowed) | `RegressionTests.cs:36-174` | →adjudication |
| QUAL-114 | The rest-rule fixture sets a reference-period config the rule never reads, and none of the three thresholds is tested at its exact limit. | Rest-rule config ignored; thresholds unpinned at limits | D3 | M | CONFIRMED(panel) | `RestPeriodRuleTests.cs:27-48,233-250` | →adjudication; xref QUAL-123 |
| QUAL-115 | The only part-time overtime test states the expected split in a comment but asserts only that some overtime exists — removing the pro-rating passes (mutation re-derived exactly). | Part-time overtime arithmetic unpinned | D3 | M | CONFIRMED(panel) | `OvertimeRuleTests.cs:165-184` | →adjudication |
| QUAL-116 | The expiring-cap OK-version anchoring is pinned by a value both versions share — either anchoring passes (the file honestly declares the gap; the sibling dimension is discriminated). | carryoverMax anchoring pinned non-discriminatingly | D3 | M | CONFIRMED(panel) | `YearOverviewTests.cs:1107-1126` | →adjudication |
| QUAL-117 | The Balance fallback test asserts the norm field exists, not its value — a chain returning zero passes (pinned nowhere else, verified). | Fallback test asserts presence, not value | D3 | M | CONFIRMED(panel; upgraded) | `EmployeeProfileLifecycleTests.cs:766-794` | →adjudication |
| QUAL-118 | Two suites connect to the developer's own database instead of a throwaway container — and one runs an unscoped sweep that closes every expired delegation in whatever database is connected. | Two suites run against the ambient dev database | D3 | M | CONFIRMED(panel; disposition corrected) | `ManagerVikarEngineTests.cs:28-31,368` +1 | →adjudication |
| QUAL-119 | Two scale guards count queries by calling repositories directly while claiming to measure the endpoints — an extra per-row query in the handler (the exact guarded regression) wouldn't move the count. | Perf guards measure a copy of the endpoint's reads | D3 | M | CONFIRMED(panel) | `S106SeedScalePerfTests.cs:79-186` | →adjudication; F1 cross-note |
| QUAL-120 | The nav-redirect test declares the redirect inside its own router — deleting the real one leaves it green. | Redirect test declares what it verifies | D3 | M | CONFIRMED(panel) | `TeamOversigt.test.tsx:792-806` | →adjudication |
| QUAL-121 | Hermetic frontend fixtures can pin a wire shape the server stopped sending — the file documents exactly that having happened; nothing binds fixture nullability to the served contract. | FE fixtures can mask backend shape changes | D3 | M GP | CONFIRMED(panel; reclassed) | `TeamOversigt.test.tsx:72-96,285-289` | →adjudication; ⚖ gate |
| QUAL-122 | The admin settlement overview renders a hard-coded "Maj 2026" period label for every period (the served period is deferred work); the test correctly pins the placeholder and would fight the fix. | Hard-coded period label on an admin surface | D7/product | M | CONFIRMED(panel; re-routed) | `StrukturPanel.tsx:124-127` | →adjudication; ROADMAP item added |
| QUAL-123 | The 48-hour weekly ceiling hard-codes 48 over the caller's window and ignores the configured reference period entirely — the config is plumbed everywhere and read by no rule; correct semantics need a domain ruling. | 48h ceiling ignores its configured reference period | rule-engine | M (L) ⚖ | CONFIRMED(panel) | `RestPeriodRule.cs:345-375` | →adjudication; ⚖ semantics ruling before severity |
| QUAL-124 | A debt-census document is absent from the doc map entirely — unrouted for any reader. | s64 debt census unrouted in the doc map | D7 | M | pre-known (no sweep credit) | CLAUDE.md doc map | →adjudication |
| QUAL-125 | A point-in-time research verdict is absent from the doc map while all its siblings are routed. | Settlement-law research doc unrouted | D7 | M | pre-known (no sweep credit) | CLAUDE.md doc map | →adjudication |
| QUAL-126 | The one endpoint writing time, absences and consumption is a single 1,294-line lambda with ~115 decision points and nesting of seven. | Skema save handler is one 1,294-line lambda | D2 | M | CONFIRMED(panel) | `SkemaEndpoints.cs:559-1852` | →adjudication |
| QUAL-127 | The year-overview screen is one 675-line lambda (~≥66 decision points, four nested local functions) computing the exact figure settlement must reproduce. | Year-overview handler is one 675-line lambda | D2 | M | CONFIRMED(panel) | `BalanceEndpoints.cs:639-1313` | →adjudication |
| QUAL-128 | The settlement snapshot deliberately duplicates the year-overview valuation chain, kept in step only by prose anchors — two of which disagree with each other and all of which are stale. | Settlement snapshot mirrors the reader by prose | D2 | M | CONFIRMED(panel; anchors corrected) | `VacationSettlementService.cs:1236-1554` | →adjudication |
| QUAL-129 | One admin PUT is an 851-line lambda that also rewrites reporting lines, leaderships, edges and vikar closures in the same transaction. | Admin user PUT is one 851-line lambda | D2 | M | CONFIRMED(panel; tail read) | `AdminEndpoints.cs:1346-2196` | →adjudication |
| QUAL-130 | The manager team view ends in five per-field withholding conditionals — an 18-argument record where a new field needs a remembered sixth conditional; enforcement is documented as partial. | Team-overview withholds fields via per-field ternaries | D2 | M | CONFIRMED(panel; count corrected) | `ApprovalEndpoints.cs:786-1168` | →adjudication |
| QUAL-131 | The settlement resolve endpoint is a 696-line four-verb lambda whose tail carries the outbox and audit fan-out (one verb: two events, three audit rows). | Settlement resolve is one 696-line four-verb lambda | D2 | M | CONFIRMED(panel; tail read) | `VacationSettlementEndpoints.cs:353-1048` | →adjudication |
| QUAL-132 | The settlement reversal — the highest-consequence write — packs six refusal gates, a supersede leg and three typed catches into one 289-line transaction body. | Settlement reversal is one dense transaction method | D2 | M | CONFIRMED(panel) | `SettlementReversalService.cs:122-410` | →adjudication |
| QUAL-133 | Three near-identical payroll export handlers repeat an eight-step chain; the særlige-feriedage one omits the under-lock reversal probe both siblings carry — declared in-code as a go-live-blocking follow-up. Medium only while two verified gates keep the path dormant. | Export-handler family missing a reversal guard | D2/D4 | M ⚖ **conditional: Critical-class at §15 stk.1 go-live** | CONFIRMED(panel) | `SettlementExportEmitter.cs:643-796 (:679-686)` | →adjudication; **NAMED GO-LIVE PRECONDITION** |
| QUAL-134 | The one-shot profile migration decides permanent audit outcomes five nesting levels deep. | Profile migration nests five deep over audit outcomes | D2 | M | CONFIRMED(panel) | `LocalAgreementProfileMigrator.cs:108-274` | →adjudication; xref QUAL-039 |
| QUAL-135 | The payroll period calculation mixes per-segment OK resolution, profile overlays, merging and audit emission in one 239-line method. | Period calculation is one mixed 239-line method | D2 | M | CONFIRMED(panel) | `PeriodCalculationService.cs:257-495` | →adjudication |
| QUAL-136 | The projection rebuild — the mechanism making results reconstructable — is one 265-line method with four inline event branches and nine counters. | Projection replay is one 265-line method | D2 | M | CONFIRMED(panel) | `ProjectionBackfillService.cs:207-471` | →adjudication |
| QUAL-137 | The terminated-inclusive access gate has five clauses whose order is load-bearing, and its scope loop is the third near-identical copy in the file (no missing guard — the default-deny is factored into the shared helper, verified). | Terminated-access gate: ordered clauses, third loop copy | D2 | M | CONFIRMED(panel; wording corrected) | `OrgScopeValidator.cs:204-283` | →adjudication |
| QUAL-138 | Saving a person's edits fires five sequential writes, hand-threading row versions between them — a mistake silently 412s or overwrites. | saveEdit hand-threads versions across five writes | D2 | M | CONFIRMED(panel; count corrected) | `useEditPerson.ts:138-349` | →adjudication |
| QUAL-139 | The placement hook repeats the promote/demote ordering rule across cases with an asymmetric guard (verified harmless today) — a fourth caller copying the wrong arm drops a leadership. | savePlacement repeats the ordering rule | D2 | M | CONFIRMED(panel; corrected) | `usePlacement.ts:101-191` | →adjudication |
| QUAL-140 | The save hook picks which Danish error to show by testing body-field presence; the quota arm is purely shape-based with no string anchor — the mis-surfacing defect the file records once already. | saveMonth discriminates 422s by body shape | D2 | M | CONFIRMED(panel; sub-claim relocated) | `useSkema.ts:187-276` | →adjudication |

## S132-discovered follow-up findings (registered 2026-08-24 at S133 open)

These 5 QUAL rows were surfaced DURING the S132 remediation (not by the S131 sweep, so they sit outside the
001–140 sweep table above) and are recorded here so none is lost. Enumerated in `SPRINT-132.md`
§S132-discovered follow-ups; routed per the S132–S134 program. **S133 fixes NONE of them as domain fixes** —
they are registered + routed (S133's job is test integrity, not these behaviours). Same "revisit, not shield"
semantics as the sweep rows: each is re-attackable from its loci + claim + the code.

| QUAL | What it means (plain language) | Title | Dim | Sev | Status | Source of truth | Disposition |
|------|--------------------------------|-------|-----|-----|--------|-----------------|-------------|
| QUAL-142 | The weekly-pipeline rule-eval helpers swallow a non-success rule-engine response to `null` while the pipeline still reports `Success=true` — a compliance/calc result can be silently wrong. Same silent-failure class as QUAL-007, but for the rule-eval calls (QUAL-007 fixed only the data-fetch half). | Rule-eval helpers swallow non-success to null | D5 | **H** | REGISTERED (S132-discovered) | `WeeklyCalculationPipeline.cs` — `CallRuleEvaluateAsync`/`CallAbsenceEvaluateAsync`/`CallFlexEvaluateAsync` | → Backend/orchestrator track; fix candidate (QUAL-007 sibling). NOT S133. |
| QUAL-143 | Two separately-registered adjacent per-day intervals (distinct or absent `SourceStintId`) are read as two stints, producing a false 0-hour midnight gap in the rest-period checks — a spurious rest violation, or a real one missed. Edge of the QUAL-001 day-attribution fix. | Adjacent registered intervals read as a false rest gap | D-domain | M (correctness-adjacent) | REGISTERED (S132-discovered) | `RestPeriodRule.cs` (`ReconstructStints`); `TimeEntry.SourceStintId` | → domain/rule track; candidate = rejoin clock-adjacent zero-gap same-employee stints, or a one-crossing-entry input contract. NOT S133. |
| QUAL-144 | The PCS `AssertNoDroppedBoundaryCrossing` fail-closed guard (S132) must be reworked IN LOCKSTEP with the GAP-B payroll caller-widen (ADR-039 D5b) — otherwise widening the payroll caller would make the guard throw on valid input. A go-live sequencing constraint, not a standalone bug. | Boundary-crossing guard coupled to the payroll caller-widen | D-integration | M (go-live-coupled) | REGISTERED (S132-discovered) | `PeriodCalculationService.cs` (`AssertNoDroppedBoundaryCrossing`); ADR-039 D5b | → §15 / go-live precondition (rides the payroll-widen task). NOT S133. |
| QUAL-145 | `WeeklyCalculationPipeline` + `TaskDispatcher` ship un-normalized time entries to OK-version-sensitive calc rules (the ADR-039 D6a exclusions) — a midnight-crossing entry via these paths could be attributed to the wrong calendar day / OK-version. Excluded from S132 on their non-of-record status. | Un-normalized secondary rule-input boundaries (D6a exclusions) | D-domain | M | REGISTERED (S132-discovered) | `WeeklyCalculationPipeline.cs`; `TaskDispatcher.cs`; ADR-039 D6a | → S134-adjacent; normalize-or-retire; revisit if these paths become of-record. NOT S133. |
| QUAL-146 | QUAL-002 restored DESERIALIZED equivalence (the real ADR-018 contract) between a live-written and a rebuilt segment manifest, but a null-snapshot segment still byte-DIFFERS (PCS writes `"snapshot":null`; `EventSerializer` omits it via `WhenWritingNull`). Benign — no consumer keys on segment bytes; the ADR-016 D10 join is on `manifest_id`. | Null-snapshot segment byte-differs (deserialized-equivalence holds) | D-contract | L | REGISTERED (S132-discovered) | `PeriodCalculationService.cs` (shared `JsonOptions` / snapshot write); `Infrastructure/EventSerializer.cs` (`WhenWritingNull`) | → deferred: universal byte-identity needs `DefaultIgnoreCondition=WhenWritingNull` on PCS's shared JsonOptions — a wire-format change to the rule-engine HTTP payloads with its own RED test. NOT S133. **S137 note (partial reversal):** the "no consumer depends on segments_jsonb byte-identity" judgment is PARTIALLY reversed — the WINDOWLESS byte-shape is now pinned PER WRITER by frozen literals (`SegmentSerializationParityTests`, the ADR-040 D5 by-construction parity proof); the null-snapshot two-writer delta remains the deliberate residual. |

## Discovered by the employment-lifecycle time-control refinement recon (2026-08-25)

Surfaced by the arc-item-3 refinement recon (rev 2, dual-lens converged; this row's scope was
corrected by the internal Reviewer — the first draft over-attributed it to payroll). Registered
independent of that program's schedule; routed to its Increment 2. Same "revisit, not shield"
semantics as the sweep rows.

| QUAL | What it means (plain language) | Title | Dim | Sev | Status | Source of truth | Disposition |
|------|--------------------------------|-------|-----|-----|--------|-----------------|-------------|
| QUAL-147 | `EmploymentProfileResolver` hydrated `OkVersion` from the live `users.ok_version` column instead of resolving by date (the ADR-003 principle; the resolver's own documented "Phase 4e" tail). The payroll path was protected by a per-caller overlay in PCS, but the compliance check and historical balance summaries fed the live value into OK-version-sensitive config lookups, and the OK24→OK26 boundary (2026-04-01) is already crossed: a past-period read through those consumers could use the wrong agreement version. | Live `ok_version` reaches OK-sensitive consumers (compliance, balance history) | D-domain | **H** | **FIXED — S137 TASK-13703 (structural).** `EmploymentProfileResolver` now derives `OkVersion = OkVersionResolver.ResolveVersion(asOfDate)` and no longer selects `u.ok_version` — every resolver consumer is correct by construction; the one named non-resolver consumer `BalanceEndpoints:152-157` resolves by the month read. Pinned (Docker-gated, CI): `EmploymentProfileResolverDateOverlayTests` (OK24-month read for a live-OK26 row), the compliance `okVersion == "OK26"`-at-hire wire pin, the Balance OK24-norm mutation pin. PCS's per-segment overlay is KEPT as defense-in-depth for the null-resolver fixture path (comment corrected). | `EmploymentProfileResolver.cs` (date-derived); `BalanceEndpoints.cs:152-163`; `ComplianceEndpoints.cs` (resolver-fed) | → the wider `user.OkVersion` live-fallback family is its own row: **QUAL-148**. |
| QUAL-148 | **The `user.OkVersion` live-fallback FAMILY beyond QUAL-147's named consumers** — ~15 remaining reads of the live `users.ok_version` column outside the resolver: the `BalanceEndpoints` today-tile / live-open ResetMonth family (~L845/854/888/1226 — Reviewer-verified DELIBERATE live-open reads), `DatedEntitlementConfigResolver`'s live-fallback design, the settlement config fallbacks (`VacationSettlementService` `?? GetCurrentOpenAsync(…, user.OkVersion)`), and others. Each site is EITHER a deliberate "today" read OR a QUAL-147-class defect — not yet classified per site. **Also records the 11th, INDIRECT accrual caller** `RuleEngine/Rules/AccrualCalculator.cs:57` (rule-engine side; CANNOT take the ADR-040 D9 end-cap per D7 — employment dates never cross into the rule engine; idempotent post-Increment-1 because bookings are window-gated) so no later sweep re-finds it as "missed". | Live OK-version reads outside the resolver; the un-cappable rule-engine accrual wrapper | D-domain | M | REGISTERED (S137 refinement recon + Reviewer-N3) | `BalanceEndpoints.cs` (~845/854/888/1226); `DatedEntitlementConfigResolver.cs`; `VacationSettlementService.cs` (config fallbacks); `AccrualCalculator.cs:57` | → adjudicated sweep: classify each site deliberate-live vs date-resolve; the deliberate ones get a comment naming the ruling, the defects get QUAL-147's fix shape. Not a time-control increment item. |
| QUAL-149 | **ADR-016 D4 refusal at a GENUINE split in the live rule set — the S64 F4-1(b) deferred gap with its S137 scope.** The Payroll host plans with the live `RuleRegistry` classifications (four `AlignedWindow` rules: OVERTIME_CALC, weekly NORM_CHECK, daily/weekly rest) and `PlannerOptions.Default`; a period in which such a rule would be evaluated in ≥ 2 EMPLOYED segments is refused (`PlannerInvariantViolation`, unhandled → 500 at `/api/payroll/calculate-and-export`). S64 deferred this when interior boundaries were rare; S137's owner ruling (2026-09-02, TASK-13707) cleared hire/leave edges as TRUNCATIONS (≤ 1 EMPLOYED segment plans) — but a mid-month `part_time_fraction` / position change WHILE EMPLOYED, or two spells in one month, still refuses. Consequence: the increment's "mid-month fraction change pays per span" mechanism is proven under the straddle-safe test rule set and PINNED AS REFUSED under the live set (`EmploymentWindowLiveRulesetTests`), so a future reclassification flips it consciously. Sub-item: the unhandled exception should become a clean 4xx with a diagnosable body (counts + causes, no dates). | Fraction-change months refused in production wiring; raw 500 | D-domain | **H** | REGISTERED (S137 — re-registration of S64 F4-1(b), which had fallen out of ROADMAP) | `PeriodPlanner.ApplyAlignmentPolicy`; `RuleRegistry.cs:63-68`; `Payroll/Program.cs` (live provider); `PeriodCalculationService.BuildPlanForLegacyCallersAsync` | → options (S64, unchanged): (i) reclassify norm/overtime as `Mergeable` with a pro-rating merger — needs a DOMAIN decision (how does a weekly norm pro-rate across a mid-week fraction change? Phase-B-adjacent); (ii) complete the `AllowUpstreamAlignment` shrink stub; (iii) an upstream pre-split contract. Candidate for Increment 3 or its own increment; Rule Engine + Payroll. **S138 note:** the refusal is now VISIBLE where HR works — the backdate worklist derives `recalcBlockedBy: QUAL-149` on any PROFILE_CHANGE / EMPLOYMENT_CATEGORY_CHANGE trigger whose effective date is strictly inside an exported month (TASK-13803); fixing 149 flips those rows to actionable. |
| QUAL-150 | **The wage-type-mapping natural key is snapshotted ONCE PER PLAN from the CALLER profile (ADR-020 D1.5 "uniform-per-plan binding", LOCKED in the S29 refinement), while S137 makes per-segment DATED profiles real.** `BuildPlanForLegacyCallersAsync` registers the `WtmNaturalKey` hydrator against the caller-supplied profile; `MapSegmentToExportLinesAsync` reads `OkVersion / AgreementCode / Position` from that shared snapshot (only `asOfDate` is per-segment). Rules are evaluated with the correct per-segment dated `segmentProfile`, but a mid-period POSITION change (now an `EmployeeProfileChange` boundary) would map the post-change segment's export lines with the OLD position-specific mapping — wrong lønart codes. The same shape is latent for OK-straddles (never interior to a month) and agreement-code changes (not a boundary source at all). **Production wiring cannot reach it today:** a profile-change split refuses under the live rule set (QUAL-149) — the two rows are COUPLED: whichever increment makes profile-change splits plannable MUST land the per-segment key first. Found by Codex at Step-5a (S137, WARNING P1). | Export mapping keyed on plan-wide snapshot, not the segment's dated profile | D-domain | **H** (latent; guarded by QUAL-149) | REGISTERED (S137 Step-5a Codex) | `PeriodCalculationService.BuildPlanForLegacyCallersAsync` (hydrator registration, `WtmNaturalKey`); `MapSegmentToExportLinesAsync` (the three key reads); ADR-020 D1.5 | → fix shape (needs an ADR-020 D1.5 amendment + its own refinement, not a Step-5a bolt-on): key the lookup on the per-segment dated `segmentProfile` (Position/AgreementCode dated since ADR-023; OkVersion segment-resolved) — replay-stable BECAUSE the resolver is dated, which retires the S29 "caller profile may drift" rationale for the snapshot; then either drop the `WtmNaturalKey` snapshot contract or keep it as an audit artifact. Pin with a mid-month POSITION-change export test. Land WITH QUAL-149. **S138 note:** VISIBLE on the backdate worklist — `recalcBlockedBy: QUAL-150` on any AGREEMENT_CODE_CHANGE trigger whose effective date is strictly inside an exported month (TASK-13803); lands WITH QUAL-149 per the owner ruling. |
| QUAL-151 | **Segment-date-bearing diagnostics in the Payroll host (ADR-040 D7 class, server-side).** S137 made segment edges coincide with employment dates (a starter's first EMPLOYED segment starts on the hire date; a leaver's NOT_EMPLOYED suffix starts the day after the leave date). The two client-triggerable FAIL-CLOSED exceptions that embedded a segment as-of date were re-anchored on caller input in S137 (Compliance + the PCS per-segment resolver throw — Step-7a Codex cycles 1–2). REMAINING, pre-existing, INCLUDING (not exhaustive — Step-7a Reviewer final check): (1) `EmploymentProfileResolver.cs:197` — the resolver's OWN fail-loud throw (`EmployeeProfileNotFoundException(employeeId, asOfDate)` when a profile row exists but no agreement-code row covers the date — an ADR-023 D3 data-integrity fault) carries the caller's as-of, which is the hire date when reached from the compliance GET (`firstEmployedDay`, employee-self-triggerable on a seeding fault) or from PCS (`segment.StartDate`); (2) the `MapSegmentToExportLinesAsync` snapshot-missing `InvalidOperationException` messages carry "Segment range: [start, end]"; (3) the "No wage type mapping … at {AsOfDate}" `LogWarning` logs `plannedSegment.StartDate`; (4) the caller-supplied-OK mismatch `LogWarning` (PCS ~L487) logs `segment [{SegStart}..{SegEnd}]` and fires in production for single-segment plans. All Payroll-host / Backend logs or the unhandled-exception path: under the composed stack (no `ASPNETCORE_ENVIRONMENT` set → Production → bare 500, no exception handler registered) no client body echoes them; a LOCAL Development launch's developer exception page WOULD. D7's letter names DTOs, JWTs, exports and error BODIES; export-line `PeriodStart/PeriodEnd` stamps equal to a hire/leave date are the payroll boundary's LEGITIMATE content (ADR-040 D7 precision note). | Segment dates (= employment dates at window edges) in Payroll-host exception messages and warning logs | D-security | L | REGISTERED (S137 Step-7a class follow-through) | `PeriodCalculationService.MapSegmentToExportLinesAsync` (three throw messages; one LogWarning) | → assess under the production-hardening pass (log-redaction posture for employment dates); fix shapes: the resolver throws WITHOUT a date (or callers catch and re-anchor on their period), segment ranges → segment INDEX + period in messages, log lines drop the dates; if D7 is tightened to cover host logs this becomes Medium. Not a calculation defect. |
| QUAL-152 | **The omitted-`effectiveFrom` sentinel is caught in the endpoint instead of refused by the binder.** `EffectiveFrom` is a non-nullable `DateOnly`, so a request that omits the field binds `0001-01-01` — which, once any past date is legal (S138), would route as a SILENT correction covering all recorded history, and is also the backfill seeder's own anchor row. S138 shipped a defensive guard (`== default` → 422, date-free, on all three edit surfaces: profile PUT, admin agreement-code PUT, admin user create). That closes the hole but states the contract in the wrong place: the wire spec still advertises the field as optional, so a client cannot learn the rule from the OpenAPI document, and every future dated-edit surface must remember to re-add the same guard. Cost of the workaround: the `0001-01-01` anchor row can no longer be addressed through the edit surfaces (never a designed use case; still reachable through the repository/seeder paths). | Omitted-date sentinel guarded at the endpoint, not in the contract | D-contract | M | REGISTERED (S138 Step-7a Reviewer W4 — the S138 doc said "registered follow-up" but no register row existed) | `EmployeeProfileEndpoints.cs` / `AdminEndpoints.cs` (the three `== default` guards); `docs/api/openapi.json` (`required` array); `SPRINT-138.md` §"RULED — the `EffectiveFrom` presence guard" | → make the DTO members `required` in C# so the model binder refuses an omitted field with a 400 and the generated spec lists them in `required`. This is a WIRE-VISIBLE contract change (spec delta + regenerated `frontend/src/lib/api-types.ts` + the PAT-012 typed-contract pipeline), which is why S138 did not take it mid-sprint. Keep the `== default` guards as defense-in-depth even after the flip — they also catch a literal `"0001-01-01"` that a binder would happily accept. Land with the next dated-edit surface (Increment 4's date picker) so one spec delta covers both. |
| QUAL-153 | **Docker-gated pins whose dates float with the calendar pass or fail depending on which weekday CI runs.** `ProfileBackdatingEndpointTests` derives every date from `DateOnly.FromDateTime(DateTime.UtcNow)`, so a seeded absence at a fixed offset (`Today-60`, `Today+30`) lands on a Saturday or Sunday two days in seven. An entitlement-consuming absence cannot exist on a zero-norm day — the Skema save guard rejects one with a 422 (ADR-032 D3) and `DailyNormCalculator` returns 0 for weekends — so the revaluation divides by a zero divisor, gets no value, and skips the absence BEFORE it can notice the year is settled. The pin then tests a state the product forbids and fails for a reason unrelated to what it claims to prove. S138 hit this on a Thursday, the one weekday where BOTH settled-year pins fail at once; a third pin (`PUT_Backdate_RevaluesOnlyInsideTheWrittenInterval`) carries the same latent flake on Sundays and Mondays, and a fourth was passing for the wrong reason (its "outside the interval" assertion held because the day was a weekend, not because of the interval). Immediate fix applied: an `OnWeekday` nudge on all five seeded dates, mirroring the helper `Adr032RevaluationTests` already carries. | Calendar-floating dates make Docker-gated pins weekday-dependent | D-test | M | **FIXED (S139)** — the shared `FixedTimeProvider` fixture (`tests/…/Hosting/`), `WithFixedToday(DateOnly)`, the falsifiability probe, and the product clock SOURCE routed through the injected `TimeProvider` on the profile, agreement-code and approval-projection paths (C# and the two SQL business dates); the 8 Group-1 suites converted to constant anchors with RED conditions; the `OnWeekday` / `NextWeekday` nudges deleted. Second tranche → QUAL-154 | `ProfileBackdatingEndpointTests.cs` (`Today`, `OnWeekday`, the five seeded dates); `Adr032RevaluationTests.cs` (`NextWeekday`, the existing precedent); `DailyNormCalculator.cs` (weekend ⇒ 0); `SkemaEndpoints.cs` (the ADR-032 D3 422 guard) | → the nudge is a per-site guard, not the durable answer. The structural fix is to inject a FIXED `TimeProvider` into the WebApplicationFactory host for the date-sensitive regression suites, so a pin's meaning does not depend on the day CI runs, and to sweep the other suites for the same pattern. Sized as its own task, not a Step-7a bolt-on. The weekend rule itself needs no change — it is correct and deliberate. |
| QUAL-154 | **Six date-hazardous regression suites are still on the wall clock because their request paths need clock seams the S139 ruling did not name.** The S139 census marked 14 suite files hazardous (13 census entries — one entry covers two roster suites); 8 (profile, agreement-code and approval-projection paths) were converted. The other six each exercise a product clock read the fixed test clock cannot reach: `ManagerVikarEngineTests` calls `DelegationExpiryService.CloseExpiredDelegationsAsync` whose SQL `until_date < CURRENT_DATE` (`DelegationExpiryService.cs:86`) is the DATABASE clock — a fixed past "today" would make BOTH seeded rows expire; `DesignatedApproverAuthorityTests` reaches the authorizer's raw `DateTime.UtcNow` fallbacks (`DesignatedApproverAuthorizer.cs:147/322`), `SkemaEndpoints.cs:218` and the `ApprovalEndpoints.cs:319/505/1592` as-of reads; `AdminVikarOnBehalfTests`, `EmploymentDateGuardTests`, `EmploymentEndDateLifecycleTests`, `EntitlementQuotaCheckUsesYearStartTests` (hard-fails every 1 September: `today.Month >= ResetMonth` at `:97-105`) need a per-suite product-path trace first. Also: ~65 test files were classified H-none only by an offset-arithmetic proxy now KNOWN to miss hazards (it missed `EntitlementQuotaCheckUsesYearStartTests`); `FeriehindringResolutionTests` and `SiblingReadMonthGateTests` are the named suspects for a `.Month/.Year/.DayOfWeek` secondary scan. | Second-tranche fixed-clock conversion (6 suites + the unverified bucket) | D-test | M | **FIXED (S140)** — all six suites converted to the constant anchor `F = 2025-03-12` (Wednesday, OK24 side), every date derived from it; the three product seams widened (`DelegationExpiryService` takes a `TimeProvider` and binds `@today`; the `DesignatedApproverAuthorizer` fallbacks and `ReportingLineRepository:1043` read an injected provider; the approval / skema / reporting-line as-of reads adopt the seam) plus a fourth found in passing (the cross-org transfer date, TASK-14009, owner ruling OQ-5); four probe legs added, each with a stated red condition. `EntitlementQuotaCheckUsesYearStartTests`' annual 1-September hard failure is defused by a **separate** fact anchored at exactly 2025-09-01 that calls the real `EntitlementPeriodResolver.Resolve` — the first version of that fact asserted its own recomputed arithmetic and could not have failed, which the Step-5a review caught. **The secondary scan is closed by evidence, and the evidence corrected the method:** `DateOnly.FromDateTime(<local>)` is invisible to the pinned regex (the clock read is one assignment away), which is how a business date survived two conversion sprints — the widened pattern is now recorded in QUAL-155's reproduce step and found QUAL-164. Exactly three suites were calendar-dependent and all three are among the six; the two named suspects (`FeriehindringResolutionTests`, `SiblingReadMonthGateTests`) are calendar-invariant. Docker unavailable locally, so all converted pins and the four legs are **CI-verified at the S140 close** | `SPRINT-139.md` § "Orchestrator ruling — the conversion-set scope"; the census (`.claude/sweeps/S139/TASK-13905-clock-census.md`, session artifact); `DelegationExpiryService.cs:86`; `DesignatedApproverAuthorizer.cs:147/322`; `SkemaEndpoints.cs:218` | → one task: widen the seam per suite (DelegationExpiryService gets a `TimeProvider` + `@today`; the authorizer gets a provider field for its fallbacks; the as-of reads adopt the `SkemaEndpoints.cs:2052` form), convert the six with constant anchors + RED conditions, run the secondary scan over the ~65 files and convert what it surfaces. PAT-008 carries the rule. |
| QUAL-155 | **The product still reads the raw C# clock at 108 sites in 47 `src/` files (S139 post-seam census; was 117 in ~50).** Most are audit / token / `CreatedAt` stamps that stay on the real clock BY DESIGN (`AdminEndpoints.cs` 16, `ReportingLineEndpoints.cs` 12, `ApprovalEndpoints.cs` 8 — the latter incl. the business-date as-of reads at `:319/:505/:1592`). The BUSINESS-DATE reads still raw are the debt: `ApprovalPeriodRepository.cs:238` (`GetByMonthForDesignatedReportsAsync`) and `:488` (`GetTeamOverviewRosterAsync`), `SkemaEndpoints.cs:218` (designated-approver "today"), `DesignatedApproverAuthorizer.cs:147/322` and `ReportingLineRepository.cs:1043` (fallbacks when `asOf` is omitted), `ConfigEndpoints.cs:196` (self-declared "Phase-4 hardening" debt), the config-endpoint families (3–4 each). Why it matters: S139 found a REAL defect of this class hiding under a comment — the admin create POST computed "today" three separate times beneath S137's "Computed ONCE so the three can never disagree", so a midnight straddle would have left a new employee's first day with no agreement code or no approver. | Remaining raw business-date clock reads in `src/` | D-domain | M | REGISTERED (S139) | the pinned census command in `SPRINT-139.md` § TASK-13905 (re-run to reproduce: `rg -n -P 'DateTime\.(Today|UtcNow|Now)\b|DateOnly\.FromDateTime\(DateTime\.' src --glob '*.cs'` — **and, per S140 TASK-14009, ALSO** `rg -n -P 'DateOnly\.FromDateTime\((?!DateTime\.)' src --glob '*.cs'`, because the first regex is BLIND to a business date derived from a local (`DateOnly.FromDateTime(now)`): the clock read is one assignment away. That blind spot hid a business date through two conversion sprints — S139 converted a handler's sibling date and missed this one — and the Orchestrator's S140 sweep with the widened pattern found QUAL-164); `ApprovalPeriodRepository.cs:238/488`; `SkemaEndpoints.cs:218`; `DesignatedApproverAuthorizer.cs:147/322`; `ConfigEndpoints.cs:196` | → convert per path as its suites are pinned (QUAL-154 first); each converted method computes "today" ONCE and threads it; NEVER route token minting or audit stamps (a fixed provider under token minting mints tokens "in the past" against a real-clock validator). |
| QUAL-156 | **Business dates still taken from the DATABASE clock (`CURRENT_DATE` / `NOW()::date`) at six SQL sites; behaviour-preserving only while the Postgres session is UTC.** S139 parameterised the two on the converted paths (`EmployeeProfileRepository` soft-delete write; `ApprovalPeriodRepository.GetPeriodStatusProjectionForTreeAsync` compare). Remaining, with reach: `ReportingLineRepository.cs:1287` `SET effective_to = CURRENT_DATE` (WRITE — closes the old approver line; the same users PUT dates its other cells from the APP clock, so one request mixes two clocks), `DelegationExpiryService.cs:86` (compare, host-background poller), `LocalAgreementProfileMigrator.cs:385-386/430-431` (compare, one-time startup migrator), `docker/postgres/init.sql:4312` (WRITE inside a one-time backfill block, invisible to any C# grep), and two DEAD sites — `RoleConfigOverrideRepository.cs:444` (class never constructed or DI-registered) and `LocalConfigurationRepository.GetActiveByOrgAsync` (`:56-57`, zero callers). The four `expires_at > NOW()` compares (`RoleAssignmentRepository.cs:24`, `IAuthorityFactsSource.cs:130`, `DesignatedApproverAuthorizer.cs:557`, `ReportingLineEndpoints.cs:2682`) are TIMESTAMP compares for authorization, self-consistent with SQL-seeded expiries (`S105UnitLeaderApprovalTests` seeds `expires_at = NOW() - INTERVAL '1 day'` — the counter-example to keep). | SQL clock sites not parameterised (business dates from the DB clock) | D-domain | M | REGISTERED (S139) | `SPRINT-139.md` § TASK-13905 census §3 (pinned case-INSENSITIVE regex: `rg -n -i -P 'CURRENT_DATE|NOW\(\)|CURRENT_TIMESTAMP|LOCALTIMESTAMP' src docker/postgres --glob '*.cs' --glob '*.sql'`); `docs/operations/legacy-db-upgrade-runbook.md` § S139 | → parameterise `ReportingLineRepository.cs:1287` and `DelegationExpiryService.cs:86` with QUAL-154's seam widening; delete the two dead sites; leave the migrator/backfill (one-time) and the `expires_at` timestamp compares as they are; keep the runbook's `SHOW timezone = UTC` check. |
| QUAL-157 | **Two "today" conventions coexist on purpose and disagree for two hours a night in summer.** The profile and agreement-code writers use the UTC day (to match the frontend's `new Date().toISOString().slice(0,10)`, `EmployeeProfileEndpoints.cs:83-90`); settlement and the HR worklist use the Copenhagen day (`CopenhagenBusinessDate.Today`, `HrBackdateWorklistRepository.cs:685`). Between 22:00 and 24:00 UTC when CEST applies (00:00–02:00 local) an HR user dating an edit "today" by the local calendar is refused as future-dated by the UTC rule, and a profile PUT writes its worklist row under a different "today" than it validated against. S139 routed only the clock SOURCE and kept UTC-day by owner ruling (OQ-3 (a)); changing the day boundary is a domain-correctness change needing a frontend change and a midnight pin of its own. **EXTENDED S140 (Step-7a cycle 2) — the split now reaches two READ surfaces that answer the same question differently.** S140 built two places that decide "is this month past the manager's deadline", from one owner-ratified rule, on two different clocks: the organisation page's new `pendingPastDeadlineCountByManager` evaluates it against the **UTC** day (`ApprovalPeriodRepository.cs:498` → `:713`), while the HR landing page's "approver late" list evaluates the identical rule against the **Copenhagen** day (`HrFollowUpApprovalEndpoints.cs:103` → `HrFollowUpApprovalReadRepository.cs:304`). For roughly two hours a night the two tiles disagree about whether the same month is late. Nothing is *wrong* in either — each is internally consistent — but a user reading both sees a contradiction, and that is a new symptom of this row rather than a new finding. The cheap resolution is to make the roster subset read `CopenhagenBusinessDate` (one call, matching every other read in the HR family, which the Step-7a lens verified all use it); the correct resolution is this row's own question, ruled once. **Recorded here rather than as a new row** because it is the same defect: two conventions for "today" coexisting on purpose. | UTC-day vs Copenhagen-day split on the edit paths | D-domain | M | REGISTERED (S139, owner ruling OQ-3 (a)) | `EmployeeProfileEndpoints.cs:83-90`; `SkemaEndpoints.cs:2052`; `CopenhagenBusinessDate.cs:41-46`; `HrBackdateWorklistRepository.cs:685`; S139 refinement OQ-3 | → its own task: a domain ruling (which day is "today" for an HR edit — the user's local day is the natural answer), the frontend sends local dates, the writers adopt `CopenhagenBusinessDate`, and a 23:30-UTC pin proves both halves of one request agree. |
| QUAL-158 | **Compensatory rest (EU-WTD) is a dead feature that looks alive.** The table (`init.sql:1452-1466`, status PENDING / GRANTED / EXPIRED), the repository (`CompensatoryRestRepository.cs:16-77`), the GET (`ComplianceEndpoints.cs:269`) and the frontend hook `useCompensatoryRest` all exist, but `CreateAsync` and `GrantAsync` have ZERO callers in application code (only the DI registration at `ComplianceEndpoints.cs:252`); `RestPeriodRule` never creates a row when it finds a rest violation. The GET always returns an empty list; the hook is referenced only from a test. A reader (or Increment 4) would assume compensatory rest is tracked. | Dead compensatory-rest schema + repo + read API | D-contract | M | REGISTERED (S139 HR sweep — HRP-020) | `docs/operations/hr-follow-up-process-register.md` HRP-020; `CompensatoryRestRepository.cs:16-77`; `ComplianceEndpoints.cs:252/269`; `RestPeriodRule.cs:52-88` | → decide in Increment 4: wire `RestPeriodRule` violations to `CreateAsync` (then a human grants — a genuine HR hand-off, HRP-019/020) or remove table, repo, GET and hook together. |
| QUAL-159 | **The §24 payout reconciliation writes an audit row but emits NO outbox event, unlike every sibling mutation in its file.** `POST /api/vacation-settlements/{…}/reconcile-payout` (`VacationSettlementEndpoints.cs:1143-1361`) CAS-writes `payout_reconciled_at/by` and calls `AppendAuditAsync` (`:1337`), but `IOutboxEnqueue` is injected only into the resolve handler (`:365`) — the reconcile handler enqueues nothing. The audit trail is intact; the EVENT stream is not: a replay cannot see that a payout was reconciled. | Reconcile-payout has no outbox event | D-integration | M | REGISTERED (S139 HR sweep — HRP-006) | `VacationSettlementEndpoints.cs:1143-1361` (`:1337`); ADR-033 D5 event vocabulary | → add a `SettlementPayoutReconciled` event (DEP-003 registration + ADR-026 mapper) in the same tx, or record in ADR-033 why reconciliation is audit-only. |
| QUAL-160 | **A recorded §21 vacation-transfer agreement produces no domain event at all.** `POST` / `PUT /api/vacation-transfer-agreements/{employeeId}` (`VacationSettlementEndpoints.cs:344/347`; handler `:110-348`) call only `transferRepo.InsertAsync/UpdateAsync` (`:283/:301`); the repository writes its own `vacation_transfer_agreement_audit` table (`VacationTransferAgreementRepository.cs:213-222`). No `TransferAgreement*` event exists in `EventSerializer.EventTypeMap` or in the ADR-033:42 vocabulary (`FeriehindringTransferred` is the unrelated §22 case). There is also no GET — the agreement is write-only. | Transfer agreement: no event, no read | D-integration | M | REGISTERED (S139 HR sweep — HRP-010) | `VacationSettlementEndpoints.cs:110-348`; `VacationTransferAgreementRepository.cs:213-222`; `EventSerializer.cs:16-219`; ADR-033:42 | → define `VacationTransferAgreementRecorded` (DEP-003 + mapper) and a GET/list; both are Increment-4 inputs (the register's HRP-010 tile needs the read). |
| QUAL-161 | **A role assignment's expiry has no closer, no event, no audit row and no list — the row silently stops granting authority.** `role_assignments.expires_at` is written at grant (`AdminEndpoints.cs:2866` route; INSERT `:2960-2970`) and manually revocable (`:3049`); when the date passes, six authorization reads simply stop honouring it (`RoleAssignmentRepository.cs:24`, `DesignatedApproverAuthorizer.cs:557`, `IAuthorityFactsSource.cs:130`, `ReportingLineEndpoints.cs:2682`) while `is_active` stays TRUE forever. An org whose only HR assignment lapses gets no signal from the system. Contrast `manager_vikar`, whose expiry IS closed by `DelegationExpiryService` with an event + audit row (still telling nobody). | Role-assignment expiry is a silent state | D-domain | M | REGISTERED (S139 HR sweep — HRP-014 b) | `hr-follow-up-process-register.md` HRP-014; `AdminEndpoints.cs:2866/2960-2970/3049`; `RoleAssignmentRepository.cs:24` | → at minimum a list of assignments expiring within N days on the HR landing page (Increment 4); ideally an expiry closer mirroring `DelegationExpiryService` (event + audit); the owner must first name who acts. |
| QUAL-162 | **A finished, unit-tested overtime pre-approval worklist page has never been routed — no user in any role can reach it.** `frontend/src/pages/admin/OvertimePreApprovalManagement.tsx:1-167` (PENDING / APPROVED / REJECTED tabs, Godkend / Afvis actions wired to `GET /api/overtime/pre-approvals` and the `PUT …/approve|reject` endpoints, unit-tested S116) has no route in `frontend/src/App.tsx`, no sidebar link, and never had one (`git log -S OvertimePreApprovalManagement -- frontend/src/App.tsx` is empty). The APIs are `LeaderOrAbove` (`OvertimeEndpoints.cs:300-355/359-435/439-508`). | Unrouted overtime pre-approval page | D-contract | M | **FIXED (S140)** — routed under the leader tier with a `Sidebar.tsx` entry in "Godkend tid" (TASK-14007), pinned by a route test asserting it resolves for a leader and is refused for an employee. It had been finished and unit-tested since S116 and reachable by nobody | `frontend/src/App.tsx`; `OvertimePreApprovalManagement.tsx:1-167`; `OvertimeEndpoints.cs:355/435/508` | → route it under the leader tier (the backend gate), with a sidebar entry; an Increment-4 input. |
| QUAL-163 | **The organisation page's "godkendere efter frist" (approvers past deadline) tile claims an aging computation that does not exist.** `godkendCount = Object.keys(pendingCountByManager).length` (`StrukturPanel.tsx:941-943`) counts every manager with ANY pending SUBMITTED month; the stored `employee_deadline` / `manager_deadline` (hard-coded month-end + 2 / + 5 days at period creation, `SkemaEndpoints.cs:524-525`, persisted `ApprovalPeriodRepository.cs:1804`) are read by nothing but the Skema response DTO (`SkemaResponses.cs:86-87, 129-130`). A user reads "past deadline" where the data says "pending". The +2/+5 values are developer defaults no one has ratified (SYSTEM_TARGET §G:167 promises configurable "cutoff dates"). | "Past deadline" tile never reads the deadline | D-contract | M | **FIXED (S140)** — the roster read gains `pendingPastDeadlineCountByManager`, the SUBSET of the existing pending tally genuinely past the stored `manager_deadline` (computed fallback applied where the deadline is NULL), so the tile can read "Ikke godkendt N — heraf M efter frist" with both halves true; `pendingCountByManager` keeps its meaning. The tile relabel itself is TASK-14006 (wave 3), which reads the new field | `StrukturPanel.tsx:941-943, 1304-1328`; `SkemaEndpoints.cs:524-525`; `ApprovalPeriodRepository.cs:1804`; `hr-follow-up-process-register.md` HRP-012 | → **owner ruled 2026-09-08: +2 / +5 ratified as provisional institutional deadlines, export cutoff = the manager deadline** → compute the tile from the stored deadlines in Increment 4; make the values configurable per institution later. |
| QUAL-164 | **A stand-in (vikar) arrangement's start date is reported from two different clocks, so the create response and the read-back disagree across midnight.** The create endpoints compute `effectiveFrom` from the injected `TimeProvider` (`ReportingLineEndpoints.cs:1798` self-delegate, `:2287` admin-on-behalf) and echo it, then stamp `CreatedAt = DateTime.UtcNow` a few lines later (`:1971`, `:2414`; repository fallback `ManagerVikarRepository.cs:207`). The delegation GET does not read a stored start date — there is none — but DERIVES one: `EffectiveFrom: DateOnly.FromDateTime(vikar.CreatedAt)` (`:1750`). A request straddling UTC midnight therefore has the POST answer day D and the GET answer D+1 for one business fact. Two consequences: the reported start date is **unreachable by the fixed test clock** even after S140's seam work (a fixed-clock suite asserting the GET's field would read the real day), and `ManagerVikarCreated` carries no start date of its own, so the event stream cannot reconstruct it. Found by the Orchestrator's `DateOnly.FromDateTime(<local>)` sweep (the blind spot now recorded in QUAL-155's census command) and adjudicated by the S140 Step-5a cycle-2 review, which confirmed **no S140 pin asserts the field**, so nothing in that sprint passes for the wrong reason. **"Effective from the creation instant" IS the intended model today** — the POST validates only `effectiveTo > today`, there is no future-start input, the authorizer reads only `until_date >= today AND effective_to IS NULL`, and the DDL has no start column — so this is not a wrong result; it is one fact reported from two clocks. | Vikar start date derived from an audit stamp; POST echo and GET disagree at midnight | D-domain | M | **REGISTERED (S140) — owner ruled 2026-09-09: register now, the schema fix is its own task** (the review's option (ii); option (i), documenting it as an accepted known hole, was declined) | `ReportingLineEndpoints.cs:1750` (the GET derivation), `:1798` / `:2287` (the POST seam reads), `:1971` / `:2414` (the `CreatedAt` stamps); `ManagerVikarRepository.cs:207`; `docker/postgres/init.sql:4235-4250` (no start column); `SPRINT-140.md` § "Step-5a cycle 2" adjudication | → add `manager_vikar.effective_from DATE NOT NULL`, written from the seam at POST, carried on `ManagerVikarCreated`, read by the GET; then row, event, echo and read agree BY SOURCE (PAT-028) and the event becomes self-describing (auditability). Schema change: needs Orchestrator approval, `init.sql` + a regenerated `db-schema.md`, and its own fixed-clock pin. NOT folded into S140 (owner ruling). |
| QUAL-165 | **The "this row cannot be recalculated" rule exists only in the browser — the API accepts the claim anyway, for every role.** A backdate-worklist row whose triggering correction falls strictly inside the already-exported month is marked `recalcBlockedBy` (`HrBackdateWorklistRepository.cs:208-237`, reasons QUAL-149/150), because recalculating such a month would produce wrong wage-type codes. `WorklistList.tsx:283, 334` therefore refuses the *Recalculated* verb on those rows for **every role and every kind**. The backend has no equivalent check: `POST /api/hr/backdate-worklist/{id}/resolve` validates the verb, the row kind and (since S140) the actor's role, but never the blocked marker — so a direct call records "recalculated" on a month the system itself says must not be recalculated. **Demonstrated, not theorised:** the S140 gate pin seeds a row with `recalcBlockedBy = ["QUAL-149"]` and a Global Admin resolves it `RECALCULATED` → 200. Found by the TASK-14010 implementer while closing the sibling gap (the OQ-7 authorization gate), and deliberately left for a ruling rather than fixed unilaterally — because unlike that gap this one is **arguably not an authorization rule at all**: the verb records what an operator did *outside* the tool, and an operator may legitimately have re-planned the month correctly by hand, in which case recording the truth is right and the screen is merely discouraging a usually-wrong action. Related, minor: the screen has no dedicated 403 branch on resolve (`WorklistList.tsx` handles 412/409/428 explicitly, everything else falls to a generic save-failure), so S140's new authorization refusal renders as "Kunne ikke gemme" with the server text appended. | Blocked-row recalculation gate is UI-only; API accepts it | D-domain | M | **REGISTERED (S140) — owner ruled 2026-09-09: register now, decide in S141** alongside the four deferred HR write forms, since that is when the operator's real workflow is settled. Nothing is at risk meanwhile: the screen still refuses it | `WorklistList.tsx:283, 334`; `BackdateWorklistEndpoints.cs` (the resolve handler — verb, kind and role checked, marker not); `HrBackdateWorklistRepository.cs:208-237`; the demonstrating pin in `BackdateWorklistEndpointTests.cs`; `SPRINT-140.md` § TASK-14010 finding F1 | → three honest readings to choose between in S141: (a) refuse it server-side for every role, treating the marker as binding; (b) rule the marker advisory and let the API permit what the screen discourages, documenting the deliberate disagreement; (c) split the verb, so "recalculated in this tool" and "handled outside this tool" are different recorded facts — which is probably what the domain actually means. Add the frontend 403 branch whichever way it goes. |
| QUAL-166 | **The typed-contract programme's own generated artefacts under-describe two required inputs on the worklist resolve endpoint.** Two instances, same class, both found by the TASK-14007 UX implementer working *from* the generated types: (i) `ResolveBackdateWorklistRequest` comes out as `resolution?: string; reason?: string` — both optional — while the C# record requires both positionally and the handler returns 422 on a missing/unknown verb or a blank reason; (ii) the OpenAPI operation declares **no header parameter at all** (`header?: never`), so nothing in the contract records that `If-Match` is mandatory, even though the endpoint is admin-strict and answers 428 without it. A caller working only from `api-types.ts` — which is exactly what the typed-contract programme (PAT-012 / S111) tells them to do — would omit both and learn the truth from runtime errors. The S140 screen got them right only because the task text spelled them out. | Generated contract omits required fields and the mandatory If-Match header | D-contract | M | REGISTERED (S140) | `frontend/src/lib/api-types.ts` (the resolve operation); `Contracts/BackdateWorklistResponses.cs` (`ResolveBackdateWorklistRequest`); `BackdateWorklistEndpoints.cs` (the 422 and 428 branches); `SPRINT-140.md` § TASK-14007 findings 2–3 | → annotate the request record so the generator emits both properties as required, and declare the `If-Match` header on the operation. Then re-run the generator and check whether any other admin-strict endpoint in the family has the same silent gap — the two found here were found by accident, not by a sweep. |
| QUAL-167 | **An employee deactivated without an employment end date ages forever on the past-deadline list, with no way to close them out.** `is_active` governs login only (ADR-040 D3) and the **employment window** is what bounds a leaver, so S140's (employee × month) enumeration correctly ignores the flag — but an admin can deactivate a user while leaving `employment_end_date` NULL, which ADR-040 D2 reads as unbounded. That person's never-sent months then keep accruing on HRP-012's employee-late list, bounded only by the ruled 12-month lookback floor, and the only way to clear them is to set the end date. Raised by the TASK-14004 implementer, which declined to act on it on the grounds that **suppressing them would invent a rule the owner has not made** ("deactivated means excused") while excluding `is_active = FALSE` would hide exactly the leaver months HRP-011/012 exist to surface. A policy gap, not a code bug. | Deactivation without an end date accrues past-deadline months indefinitely | D-domain | M | REGISTERED (S140) | `HrFollowUpApprovalReadRepository.cs` (the enumeration's window predicate); ADR-040 D2 (NULL = unbounded) and D3 (`is_active` governs login); `SPRINT-140.md` § TASK-14004 finding 1 | → owner ruling wanted: does deactivation without an end date need a guard at the deactivation path, or its own data-quality tile (the HRP-015 shape, "employees whose record cannot answer when they left")? The tile reading is probably better — it surfaces the data gap to the person who can fix it instead of silently forgiving it. |

## Gate-promotion proposals (owner rules each; the audit changed no CI behavior)

*(Id-space note: the sweep produced **QUAL-001…140 findings**; **QUAL-141** is the pre-planned
gate-promotion proposal, numbered in the same id space but excluded from the 140 finding total —
1 Critical + 21 High + 118 Medium = 140.)*

| QUAL | Proposal | Cites |
|------|----------|-------|
| QUAL-141 | **Promote `check_docs.py`'s freshness warning to a hard failure for `docs/QUALITY.md`** — the doc refroze twice because freshness findings never reach the exit code (the FAIL-006 class). Pre-planned at sprint open. | FAIL-006; QUALITY.md history (anchor S111, matrix S64, warning ledger stale vs the measured 137) |
| (see rows) | QUAL-069 payroll warn gate · QUAL-072/074 mock hosts into the compiled gate · QUAL-073 CA2100 ratchet · QUAL-096 DemoSeed in CI · QUAL-121 FE fixture-contract binding | each row above |

## Below-floor inventory (appendix — counts + pointers; not adjudicated, never invisible)

Per-dimension counts as filed by the sweep (full pointer lists in the adjudication record's archive
references): D1 **28** items/6 classes (incl. the 18-file test-DDL census and the endpoint-local-SQL
census flagged for an owner ruling on the layering figure) · D2 **~36** regions (22 Tier-3 UI hotspots
incl. a CCN~30 tree-walker; flat guard chains; census artifacts) + **~30 census-identified unread
over-threshold regions (coverage residual → S132)** · D3 **~40** pointers (weak assertions, deliberate
empty-set pins, silent-timeout helpers, the 26-of-34 migration ledger ids with no upgrade test, the
Docker-trait census [masks nothing — CI runs unfiltered], 6 refuted-to-below-floor items) · D4 **~31**
(22 never-constructed events + 6 test-only-constructed [all serialization-contract-blocked at
"suspected"], /health duplication, SCD-2 lock template family [no divergence], the Deconstruct
compiler-dispatch suspect) · D5 **~34** (unshaped-500 absence of a global handler, bare-401/404
postures, 412-message variants, TZ-catch family, the per-episode unreachable branch, the refuted
superseded-generation row) · D6 the full **137-warning matrix** (109 CA2100 / 19 CS0618 / 4 xUnit1031 /
3 CS1998 / 2 xUnit2013; per-file census) + 12 rationale-current suppression sites verified clean ·
D7 **13** (below-floor stale pointers incl. the CLAUDE.md "docs job" name; judgment notes) · D8 **~18**
(noise/actionability/level singles; the OrgScopeValidator denial-noise class → SEC-012 note).
Also recorded: the S74 marker without a BEGIN (folds into QUAL-014's fix); the GoLiveDate
comment/config mismatch (quick-check owed); `SkemaEndpoints.cs:1490-1491` stale anchors (unverified).

## Cross-register updates applied from this sweep (2026-08-19)
SEC-022 status split (half fixed S130-incidental, verified; half open) · SEC-037 reachability annotation ·
SEC-015 ledger count 94 + inline-bootstrap subnote · SEC-012 re-observation note · SEC-004
reconciliation question ⚖ · F2 census-classification correction · F1 guard-quality note · ROADMAP: KB-index
facts appended; period-label backlog item; §15 stk.1 go-live precondition (QUAL-133).
| QUAL-168 | **The automated year-end holiday disposition never runs for anyone hired between January and August — silently.** `SettlementCloseService.cs:710` generates each employee's candidate ferieår years with `GREATEST(@floor, EXTRACT(YEAR FROM u.employment_start_date))` — the **raw calendar year** of the hire, with **no reset-month adjustment** — while the leaver *upper* bound three lines below (`:713-717`) applies the reset-month-9 mapping explicitly (`WHEN month >= 9 THEN year ELSE year - 1`). The hire path never got the equivalent, so the split is by hire month: a **September–December** hire's ferieår IS enumerated, the close attempts it, and it throws at the ferieår-start anchor (QUAL-169) — loud, on every five-minute poll, for ever; a **January–August** hire's actual ferieår (which began the previous September) falls *below* the lower bound and is **never enumerated at all**. For that second group the fifth week is never transferred under §21, never paid out under §24, and never forfeited under §34 — the disposition simply does not happen, and nothing reports that it did not. Found by the TASK-14012 implementer while tracing a one-line population fix; the Orchestrator confirmed the asymmetry in the SQL. **Consequence worth stating plainly: in a real institution most new hires start mid-ferieår, so this is a functional hole in the one process that has a statutory deadline.** Note also what it implies about the S140 HR tile: for the silent January–August shape, the tile's `cannotCompute` row is currently **the only signal anywhere in the system** that a disposition is owed — which is why S140 deliberately did NOT suppress that bucket along with the not-applicable noise. | Year-end settlement close never enumerates a Jan–Aug hire's first ferieår | D-domain | **M–H** | REGISTERED (S140) — reported, not fixed: its own task under a settlement/rule-engine owner | `SettlementCloseService.cs:710` (the lower bound) vs `:713-717` (the upper bound that DOES map); `SPRINT-140.md` § TASK-14012 finding 6 | → give the lower bound the same reset-month mapping the upper bound already has, and pin both hire shapes (a Sep–Dec and a Jan–Aug hire) with Docker-gated facts. The conclusion rests on `generate_series` lower-bound arithmetic read from source, **not executed** — reproduce against a database before fixing. Pairs with QUAL-169: fixing enumeration without the anchor turns the silent shape into the loud one. |
| QUAL-169 | **A fail-closed that no operator action can ever clear — the settlement values a closed holiday year against an agreement the employee did not yet have.** For a mid-ferieår hire, `VacationSettlementService.cs:1495-1498` asks "which agreement code was this person on, on the ferieår's START date?" — a date before they were employed. There is no answer and never will be: the only way HR could clear it is to back-date an agreement-code row to before the hire, i.e. falsify employment history. **A fail-closed state that no remediation can reach is not fail-closed; it is a dead end.** The *posture* is right and must stay (ADR-033 D7/D10 — never key a §24 payout off today's live code); the **anchor** is the defect. D7's requirement is satisfied just as well by `max(ferieårStart, employmentStartDate)`: still strictly dated, still deterministic, still replay-stable, and it is the agreement in force on the employee's first accruing day of that year. That this is an oversight rather than a decision shows in the asymmetry — the same method already moves the *valuation* cutoff for a mid-period **leaver** (the TERMINATION fork sets `valuationBoundary = terminationCutoff`) and `AccrualMath.EarnedToDate` already clamps elapsed months to the hire date, so the leaver case got proper treatment while the mid-period **hire** never did; the comment at `:1476-1479` then asserts the anchors "stay the strictly-dated ferieår START in BOTH cases". Raised by the TASK-14012 implementer, which **declined to fix it** and escalated instead. | Settlement anchors a closed ferieår at a date before the employee existed | D-domain | **M–H** | REGISTERED (S140) — **needs an OWNER RULING before any fix**, not just an implementer's judgment | `VacationSettlementService.cs:1495-1498` (the throw), `:1476-1479` (the comment asserting the anchor), the TERMINATION fork's `valuationBoundary`; `AccrualMath.EarnedToDate`; ADR-033 D7/D10; `SPRINT-140.md` § TASK-14012 finding 6 | → **why it needs a ruling, not an edit:** four anchor dates key off the closed ferieår's start in that method (dated agreement code, dated entitlement config, employee profile, and `OkVersionResolver.ResolveVersion`) and must move as a SET; moving the OK-version anchor changes which OK period governs a mid-period hire's first ferieår, which is an **OK-version-transition question** and therefore a domain-correctness invariant; and it changes a stored snapshot field against the recorded "VACATION snapshot unchanged byte-for-byte" invariant. Fix with QUAL-168, in that order of reasoning: anchor first, then enumeration. |
| QUAL-170 | **The same mixed-role over-grant S140 closed on the worklist gate still exists in the Orchestrator's own global-admin check, under the same false comment.** `OrchestratorScopeHelpers.IsGlobalAdmin` (`src/Orchestrator/StatsTid.Orchestrator/Services/OrchestratorScopeHelpers.cs:263-283`, consumed by the task-read gate at `Program.cs:96-99`) accepts a GLOBAL scope whose `Role == GlobalAdmin` as a secondary signal for a token "that carries the global scope under a legacy/other role string", and its comment says this "keeps this consistent with the role-based `GlobalAdminOnly` policy". **That clause is false**, in exactly the way S140's worklist gate was: `ScopeAuthorizationHandler` reads the primary role claim (`:12`), returns without succeeding when it is not allowed (`:16`), and — because `GlobalAdminOnly` sets `requireOrgScope: false` — succeeds at `:22-23` **before** the block that parses scopes. So the policy never consults the scopes array for the role decision, and accepting a scope-derived signal is a divergence from it, not consistency. Consequence: an actor whose primary role is below GlobalAdmin but who holds a GLOBAL GlobalAdmin scope gets the unrestricted orchestrator task-read bypass, skipping subject resolution and org scoping entirely. Found by the TASK-14011 implementer immediately after tightening the analogous worklist predicate. | Orchestrator global-admin check accepts a scope-derived signal the policy does not | D-security | M | REGISTERED (S140) — **reported, not fixed, deliberately:** unlike the worklist gate this is **not** obviously strictly-tightening | `OrchestratorScopeHelpers.cs:263-283`; `Program.cs:96-99`; `AuthorizationPolicies.cs:16-19` + `ScopeAuthorizationHandler.cs:12/:16/:22-23` (the authority on how the policy evaluates); existing pins in `tests/StatsTid.Tests.Regression/Security/OrchestratorTaskReadAuthorizationTests.cs` | → the fallback there defends a **deliberate design property** — a GlobalAdmin must be able to read a task whose subject employee no longer resolves (the SEC-021 terminated-subject fix) — so removing it needs an owner ruling on whether the "legacy/other role string" actor shape it protects is real. Read the existing pins before any change. Whatever the ruling, **correct the comment now**: a claim of consistency with a policy that does not behave that way is how S140's own gate went wrong. |
