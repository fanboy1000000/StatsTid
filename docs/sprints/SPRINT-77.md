# SPRINT 77 — Medarbejder administration, Phase 4: polish + PROGRAM CLOSE

Status: **CLOSED + PUSHED + SEALED 2026-06-15 (`1a59b58`; CI whole-workflow GREEN run `27550880898`, all 6 jobs FIRST-TRY no-flake — 15th consecutive CI-verified close). THE 4-PHASE PROGRAM (S74–S77) IS FEATURE-COMPLETE.** 2026-06-15.
Program: `.claude/plans/PLAN-s74-medarbejder-administration.md` (the 4-phase program S74→S77). S77 is the FINAL phase. S74 (backend foundations), S75 (read tree), S76 (write-flow backend), S76b (unified-editor FE) are CLOSED + CI-verified at master `f427c37`.
Refinement: `.claude/refinements/REFINEMENT-s77-program-close.md` (dual-lens: Reviewer CLEAN; Codex c1 BLOCKED(1)+5W → c2 "all 7 resolved" + 1 new BLOCKER + 1W → all absorbed; owner rulings OQ-1..OQ-5 recorded).

## Premise
A tight **program-close sprint** that PROVES the S74–S76b feature correct end-to-end (the UI-driven approver-through-vikar cross-flow + the composed backend lifecycle + a plan-pinned scale validation), retires the one genuinely-dead component, runs a light a11y audit, and records the 4-phase program as **feature-complete with a named open security residual**. NO new domain logic; NO schema/event/ADR-structural change — additive tests + a deletion + docs.

## Source-verified ground truth (drives the pins)
- `ReportingLineTree.tsx` is **DEAD** (route `admin/ledelseslinjer` → `MedarbejderAdministration`, `App.tsx:78`); `useReportingLines.ts` is **LIVE** (11/15 exports used by the S76b drawer) — only 4 exports dead.
- The **backend** approver-through-vikar APPROVE/read proof EXISTS (Phase 1, `DesignatedApproverAuthorityTests.cs:327`); **vikar+REJECT and vikar+REOPEN are absent full-stack**.
- The Phase-4 mandate (PLAN:41/65) is the **FE-driven** cross-flow (a vikar acting on `ApprovalDashboard`/Godkend tid), NOT the admin-config flow. No E2E/Playwright harness exists.
- `Genåbn` (reopen) is FE-gated `hasMinRole(userRole,'LocalHR')` (`ApprovalDashboard.tsx:132`); `Godkend`/`Afvis` are not → reopen-via-vikar is role-confounded in the UI (owner ruling OQ-5: HR-only-reopen is INTENDED, document it).
- The `/delegate` descendant-cycle-guard is **CLOSED** (S76/7601). The **approval-auth TOCTOU is OPEN** (`ApprovalEndpoints.cs:205/351/961`; the designated-EDGE half is program-introduced) and the cross-styrelse-transfer serialization is **deferred** (non-corrupting) — both stay the named post-program hardening track (owner ruling OQ-2).
- Perf: PLAN:14/43/73 pin a **2000+** per-styrelse target; the *solution* already shipped (bounded per-styrelse tree + the S74 server-side person-search) → Phase 4 = a bounded **validation**, not new optimization.

## Pinned requirements
- **R1 (Task A — the FE headline).** A FE vitest integration test on `ApprovalDashboard` (Godkend tid) where a **Leader-level vikar-actor** (deliberately NOT LocalHR — so the reopen gate stays hidden and no role short-circuits the flow) sees an away-manager's report on my-reports and drives **APPROVE + REJECT** through the UI. **Hooks (Codex correction):** the dashboard READS via the real approval hook, but the approve/reject MUTATIONS call `apiClient` directly (`ApprovalDashboard.tsx:228`) — NO reporting hook participates; use the **existing fetch-boundary harness** (`ApprovalDashboard.test.tsx:118`) with a network-boundary mock asserting the HTTP method + URL + payload per verb. **HONEST framing (Codex WARNING):** this FE test proves the **UI cross-flow** — that a vikar-actor's my-reports returns the away-manager's report AND the approve/reject buttons issue the correct HTTP calls — it does NOT by itself prove the designated EDGE grants authority (that is the backend's job: the Phase-1 vikar-approve proof + R2). Do not claim the FE test proves edge authority. **REOPEN is NOT in the FE proof** (role-gated, OQ-5/R6) — proven backend-only (R2).
- **R2 (Task B — supplemental backend).** Close the absent **vikar+REJECT and vikar+REOPEN** full-stack coverage in `DesignatedApproverAuthorityTests.cs` via the real HTTP endpoints, RESPECTING the real state machine (Codex BLOCKER): **`approve→reopen→reject` is an INVALID single-period path** — reopen yields `DRAFT`, but reject only accepts `SUBMITTED`/`EMPLOYEE_APPROVED` (`ApprovalEndpoints.cs:345/971/980`). So either (a) **two periods** — period-1 `SUBMITTED → vikar approve → APPROVED → vikar reopen → DRAFT` (proves vikar approve+reopen), period-2 `SUBMITTED → vikar reject → REJECTED` (proves vikar reject); or (b) **one period threading an employee re-submit** between reopen and reject. Drive transitions via the real HTTP lifecycle / seed the needed states — note `DesignatedApproverAuthorityTests.cs` has `CreateVikarAsync` + `InsertPeriodAsync` but **no transition helpers** (`:225`), so the task adds the needed submit/approve/reopen/reject HTTP steps or seeds states directly. Keep it minimal — no duplicate per-verb tests beyond covering both transitions.
- **R3 (perf — bounded ~2000 validation; FE half is optimize-THEN-validate).** **Backend half = validation:** the roster read + the server-side person-search return correctly within a sane bound at ~2000 employees (the server-side search shipped in S74; this validates it). **FE half = optimize then validate (Codex BLOCKER):** `medarbejderTree.ts` is currently **O(n²)** — `depthMap`/`visibleTreeRows` call full-array `filter` inside recursion and `defaultCollapsed` nests `childrenOf` + `isManager` scans (`medarbejderTree.ts:25/68/100/137`); at ~2000 nodes that is pathological. So PERMIT an **adjacency-index (precomputed children-map) O(n) optimization** of the derivation, THEN validate with PINNED fixtures + thresholds (Codex c2 — no "e.g."):
  - **Correctness fixture (target size):** a ~2000-node styrelse tree; assert the derivation (childrenOf/depthMap/visibleTreeRows/defaultCollapsed) produces the correct tree (rows, depths, collapse state) — proves the optimization is behavior-preserving at the product target.
  - **Perf-discriminator fixtures (test size n=5000, chosen ABOVE the 2000 target so a re-introduced O(n²) is unmistakable):** TWO worst-case topologies — (i) a **degenerate 5000-node deep chain** (stresses depth-recursion) and (ii) a **5000-node shallow star (1 root + 4999 direct children)** (stresses the per-level full-array `filter`). **Threshold:** the full derivation pipeline over EACH completes in **< 100 ms** on the test runner. (Post-adjacency-index O(n) at 5000 ≈ sub-millisecond → ~1000× headroom, non-flaky; the pre-fix O(n²) at 5000 ≈ 25M ops ≈ 100 ms+ would breach it → a real discriminator.) 2000 = the product target; 5000 = the test size that makes the budget meaningful.
  - The S75 cycle-safety / visited-set guards MUST be preserved by the optimization.
This is the one place S77 carries a real (bounded) code optimization, not just a test.
- **R4 (Task C — dead-code deletion, FE-ONLY).** Delete `ReportingLineTree.tsx` + `ReportingLineTree.module.css` + `__tests__/ReportingLineTree.test.tsx`; prune EXACTLY the 4 dead `useReportingLines` exports (`fetchTree`, `removeActingManager`, `importLines`, `assignActingManager`) + their now-unused internal callbacks/state + the hook-local `ReportingLineTreeEntry` type. Verify `tsc --noEmit` (`noUnusedLocals`) 0E; FE suite green; grep-confirm no surviving CSS `@import`/`composes` of the deleted module; zero orphaned imports. **KEEP all backend endpoints** (the ACTING-manager assign/remove `ReportingLineEndpoints.cs:511` + the HR bulk-import `:796` are intentional ADR/API surface with live coverage in `ReportingLineWriteLifecycleTests.cs`).
- **R5 (a11y — light audit).** Verify named checks on `MedarbejderAdministration` + `EditPersonDrawer` + `ApprovalDashboard`: keyboard traversal (tree + drawer + dialogs), focus trap + focus-return on the drawer/dialogs, accessible names on the toggle tiles + icon buttons. **Tooling (Codex WARNING — no `axe` dep exists, `package.json`):** use **manual `@testing-library` a11y assertions** (`getByRole`, accessible-name queries, `toHaveFocus`, tab-order) — **NO new dependency**; do NOT pin "axe" (adding `jest-axe`/`@axe-core` is out of scope unless the owner approves a dev-dep). **Fix ONLY if broken** — an audit, not a redesign/pixel pass (owner ruling OQ-1). **Known likely-breakage (Codex):** the `ApprovalDashboard` reject/enforcement dialogs (`ApprovalDashboard.tsx:381/417`) are plain `div`s without dialog semantics / focus-trap / Escape / focus-return — that is MORE than a one-line fix, so per OQ-1 record it as a **post-program a11y follow-up** (do NOT balloon 7700 by retrofitting full dialog semantics in a close sprint); the kit-based drawer/dialogs on the OTHER surfaces already carry the a11y set (S72/S76b) and only need verification. Any non-trivial breakage → recorded follow-up, not in-task.
- **R6 (reopen gate — document, NO behavior change).** The FE `Genåbn` LocalHR+ gate (`ApprovalDashboard.tsx:132`) is **INTENDED policy** (owner ruling OQ-5): reopen is HR-only in the UI even though the backend designated-edge grants reopen to a Leader-level approver. **Do NOT change the reopen GATE at `ApprovalDashboard.tsx:132`** (narrowed from "do not change the file" — Codex BLOCKER: R5's a11y audit MAY touch other parts of the same file, e.g. the dialog semantics, without violating this pin; only the line-132 reopen-role-gate is frozen). Document the HR-only-reopen posture in the close docs (the backend authority is exercised via R2).
- **R7 (Task D — program close docs).** Mark the 4-phase program **FEATURE-COMPLETE with a named open security-hardening residual** — NOT unqualified "complete"/"done"/"A". ROADMAP program-done row + the program-plan close note + a QUALITY program assessment: Reporting-Line held **A-** with the open approval-auth TOCTOU named (the edge-half program-introduced) as the post-program hardening track. **Frontend HELD at B** (Reviewer WARNING): S77 adds NO E2E harness (OQ-3 = vitest network-boundary mock), so the "no E2E / visual regression" A-ceiling persists — dead-code retirement is a minor positive, NOT a grade bump; reconcile the narrative-vs-matrix "A-/B" discrepancy honestly TOWARD the matrix B (do not claim an unsupported upgrade). The word "complete" travels WITH the A-/TOCTOU caveat everywhere. **Close docs must NOT claim security completion.**
- **R8 (no structural change).** NO schema / event / ADR-structural change. ADR-027 may get a small "program complete + reopen-gate-intended + residuals named" note. Additive tests + a deletion + docs only.
- **R9 (residuals deferred + named).** The approval-auth TOCTOU + the cross-styrelse-transfer serialization stay DEFERRED (owner ruling OQ-2); they are the named post-program in-lock-authorization-hardening track (a next-sprint candidate).
- **R10 (program-close validation — no coverage regression).** As a PROGRAM CLOSE, S77 must re-run the FULL pyramid (unit + regression + fresh-greenfield smoke + FE + tsc) and CI-verify. The R4 dead-code deletion must NOT drop *meaningful* coverage: confirm `ReportingLineTree.test.tsx`'s assertions are either obsolete (they exercised the now-deleted dead surface) OR already mirrored by `MedarbejderAdministration.test.tsx` — they must not be silently lost (Reviewer NOTE). Net FE test count may drop by the dead-surface tests; that drop must be explained in the Test Summary, not unremarked.

## Task decomposition
| Task | Title | Owns | Depends on | Review |
|------|-------|------|-----------|--------|
| **7700** | FE headline + a11y + FE-scale | R1 (the ApprovalDashboard vikar approve+reject cross-flow, real hooks + network-boundary mock) + R5 (the light a11y audit, fix-if-broken) + the FE half of R3 (medarbejderTree at ~2000) | — | Step-7a (rides) |
| **7701** | Backend vikar reject/reopen coverage + scale | R2 (close the vikar+REJECT and vikar+REOPEN full-stack gap via the **legal** two-periods-or-re-submit path — NOT a single approve→reopen→reject walk) + the backend half of R3 (roster read + person-search at ~2000) | — | discretionary Step-5a (test-only, auth-adjacent) + Step-7a |
| **7702** | Dead-code deletion (FE-only) | R4 (delete ReportingLineTree + prune the 4 dead hook exports) | — | Step-7a (rides) |
| **docs** | Program close | R6 + R7 + R8 note + R9 naming; the ADR-027 program-complete note | 7700, 7701, 7702 | program-level Step-7a (dual-lens) |

**Sequencing:** 7700 ‖ 7701 ‖ 7702 are independent (different files: ApprovalDashboard test / DesignatedApproverAuthorityTests + roster-read test / ReportingLineTree+hook). docs ← all three. The full pyramid + program-level Step-7a after.

## Step-0b plan-gate
- **Reviewer (internal): CLEAN** (1W + 3N, all advisory; every source-grounding spot-check passed). Absorbed: R7 → "Frontend HELD at B" (no E2E added → no bump); R5 → non-trivial a11y breakage is a recorded follow-up; **R10 added** (full-pyramid re-run + the dead-code deletion must drop no *meaningful* coverage).
- **Codex (external): cycle-1 BLOCKED(3) + 2W → all absorbed (cycle-2 pending).** [[review-lens-complementarity]] on the plan — the external lens caught three real defects the internal CLEAN missed:
  - **B1 (R2 invalid state path):** `approve→reopen→reject` is illegal (reopen→DRAFT; reject needs SUBMITTED/EMPLOYEE_APPROVED, `ApprovalEndpoints.cs:345/971/980`) → R2 reworked to two-periods-or-re-submit, respecting the state machine; noted no transition helpers exist.
  - **B2 (R3 perf premise unsound):** `medarbejderTree.ts` is O(n²) (`:25/68/100/137`) → R3 FE half reworked to **optimize (adjacency-index O(n)) THEN validate** at a named topology + threshold.
  - **B3 (R5↔R6 conflict):** R5 (fix broken a11y) vs R6 (don't touch the file) — the dashboard dialogs ARE broken (`:381/417`) → R6 narrowed to "don't change the reopen GATE at line 132"; the dialog-a11y retrofit recorded as a follow-up (R5).
  - W1 (R1 hooks): mutations use `apiClient` directly, no reporting hook; the FE proves the UI cross-flow, not edge authority → R1 corrected + honesty pin. W2 (no `axe` dep): R5 pinned to manual `@testing-library` assertions, no new dep.
  - Codex NOTEs: R4 dead-set exactly right (no extra orphan); R7 honest (no security-completion over-claim).
- **Codex cycle-2: BLOCKED(2) → absorbed (both mechanical, severity strictly decreasing 3→2):** (1) R2 was legal but the **7701 task row** still mandated the illegal single `approve→reopen→reject` walk → table row corrected to the legal two-periods-or-re-submit coverage; (2) R3 still said "e.g."/"a set bound" → pinned concrete fixtures (a 2000 correctness tree + 5000-deep-chain & 5000-star perf discriminators) + a concrete **< 100 ms** threshold. R1/R5/R6/R7/R10 confirmed sound; adjacency-index confirmed the correct optimization preserving visited-set guards. **Cycle-3 = a brief confirmation** (cap reached; convergence not thrash — escalate to owner only if anything substantive remains).
- **Codex cycle-3: R2/7701 confirmed resolved + legal; the ONLY residual was a cosmetic off-by-one** (a "1-root + 5000-children star" = 5001 nodes) → fixed to "5000-node star (1 root + 4999 children)". Immaterial to the discriminator. **Gate CONVERGED CLEAN** — no c4 (the cap is reached and the residual was cosmetic; looping on an off-by-one is exactly the thrash the cap guards against). The substantive design (R1–R10) is settled and dual-lens-verified.

## Implementation Record
- **7700 ✅ ACCEPTED 2026-06-15.** **R1 (FE headline):** 3 `ApprovalDashboard` cross-flow tests — a **Leader-level vikar-actor** (NOT LocalHR, so `Genåbn` stays hidden) sees an away-manager's report on my-reports and drives **approve + reject** through the UI; the tests assert the WIRE contract at the fetch boundary (`POST /api/approval/p-away-1/approve` empty body; `/reject` with `{reason}`), with honest UI-cross-flow framing (NOT edge authority — that's backend-proven). **Step-7a-c1 strengthened the fixture** (Codex BLOCKER: the my-reports and unrestricted `by-month` reads returned identical data → non-discriminating): the unrestricted "Alle" read now returns a DISTINCT `unrelatedReport` (EMP_OTHER) absent from the scoped read, and the test asserts EMP_OTHER is NOT on the default my-reports tab (Radix unmounts the inactive tab) — so the EMP_AWAY_MGR assertion now proves the SCOPED read feeds the visible tab. **R5 (a11y light audit):** 8 manual `@testing-library` a11y verification tests (no new dep) — `EditPersonDrawer` (role/aria-modal/name, Escape, focus-trap wrap, focus-return) + `MedarbejderAdministration` (tile `aria-pressed`, level `role="group"`, tree `aria-expanded`, search name, drawer focus-return); all already correct (verification-only, no fixes needed). The `ApprovalDashboard` reject/enforcement dialogs (plain `<div>`s, `:381/:417`) are **recorded as a post-program a11y follow-up** (full dialog semantics > a one-liner, OQ-1). The reopen gate (`ApprovalDashboard.tsx:132`) was NOT touched (R6). **R3 (FE — O(n) optimization):** `medarbejderTree.ts` made O(n) via a precomputed adjacency index (`buildChildIndex` → `Map<parentId, child[]>`, O(1) `childrenFromIndex`); the recursive `depthMap`/`visibleTreeRows`/`descendantsOf` walks converted to **iterative explicit-stack pre-order** (a real robustness gain — a 5000-deep chain overflowed the JS recursion stack; the old recursive version would crash on a deep real org chart too). The S75 cycle-safety `visited`/`out` guards preserved verbatim; reverse-push preserves the original child order → behavior-identical (all 22 existing `medarbejderTree` tests green; **both Step-7a lenses confirmed ordering/depths/cycle-guards/collapse/orphan semantics preserved**). New `medarbejderTree.perf.test.ts`: a ~2000-node correctness fixture + 5000-deep-chain & 5000-star (1 root + 4999 children) perf discriminators at a **< 100 ms** budget (post-fix O(n) ≈ sub-ms; the pre-fix O(n²) at 5000 ≈ 25M ops would breach it). FE suite 442 → **459** (+17).
- **7701 ✅ ACCEPTED 2026-06-15** (test-only; no production src/ change). **R2:** `Vikar_HoldsAuthority_Rejects_AndReopens_ViaEndpoints_NotViaRole` in `DesignatedApproverAuthorityTests.cs` — closes the absent vikar+REJECT and vikar+REOPEN full-stack coverage via the **legal two-period path** (period-1 SUBMITTED → vikar approve → APPROVED → vikar reopen → DRAFT; period-2 SUBMITTED → vikar reject → REJECTED — since `approve→reopen→reject` on one period is illegal: reopen→DRAFT, reject needs SUBMITTED/EMPLOYEE_APPROVED, `ApprovalEndpoints.cs:346/957-969/971/982`). The vikar is **LocalLeader (NOT LocalHR) + an AFD02 org-scope that does NOT cover the employee's AFD01** → authority is **edge-only, never role**; each verb asserts HTTP 200 + the status read-back. **R3 (backend half):** `Scale_2000Employees_RosterAndSearch_ReturnCorrectly_ScopedAndPaginated_WithinBudget` in `PeriodStatusAndPersonSearchReadsTests.cs` — 2000 employees bulk-inserted (set-based `INSERT…SELECT FROM unnest`, self-contained prefix teardown), asserting the roster read (2000 rows, < 15 s) + the scoped/paginated person-search (full first page, `total ≥ 2000`, deep-offset slice, cross-styrelse absence). Build 0E/0W; both classes 31/31 against live testcontainers.
- **7702 ✅ ACCEPTED 2026-06-15** (FE-only). **R4:** deleted `ReportingLineTree.tsx` + `.module.css` + `__tests__/ReportingLineTree.test.tsx` (confirmed dead — the route points at `MedarbejderAdministration`); pruned EXACTLY the 4 dead `useReportingLines` exports (`fetchTree`, `removeActingManager`, `importLines`, `assignActingManager`) + the orphaned `ReportingLineTreeEntry` type; the 11 LIVE exports (used by the S76b drawer sections + MedarbejderAdministration + PersonPickerDialog) intact. `tsc --noEmit` 0E; FE suite 452 → 442 (−10 = exactly the deleted `ReportingLineTree.test` cases — no meaningful coverage lost; the one surviving behavior [the enforcement toggle + 409] is mirrored in `MedarbejderAdministration.test.tsx`). KEPT all backend endpoints (ACTING-assign/remove + bulk-import are intentional ADR/API surface with live coverage in `ReportingLineWriteLifecycleTests.cs`). The stale `docs/FRONTEND.md:170` route reference was repointed to `MedarbejderAdministration` (Step-7a-c1 Codex catch).

## Step-7a — program-level dual-lens (the S77 diff)
- **Reviewer (internal): CLEAN** (1 cosmetic NOTE: `collapsedForLevel` builds the adjacency index then calls `depthMap` which rebuilds it — a redundant O(n) build, not a correctness issue). Verified the `medarbejderTree` rewrite behavior-preserving (reverse-push preserves pre-order; cycle guards verbatim; `idx.has` ≡ old `isManager`), the R1/R2/R5/perf tests prove their pins, the deletion drops no meaningful coverage, and the R7 close-framing matches QUALITY (A- / B held, no security-completion claim).
- **Codex (external): c1 BLOCKED(2) → c2 CLEAN.** [[review-lens-complementarity]] on the diff — the external lens caught two real items the internal CLEAN missed: (1) the R1 fixture was **non-discriminating** (both reads returned identical data) → strengthened (distinct unrestricted payload + the EMP_OTHER-absent assertion); (2) `docs/FRONTEND.md:170` still routed to the **deleted** `ReportingLineTree` → repointed. Codex CONFIRMED the `medarbejderTree` rewrite preserves ordering/depths/cycle-guards/collapse/orphan semantics, and R2/R3/R4/R5/R6 clean.

## Test Summary
824u + 900r (effective; single run 897/900 + 3 FAIL-002 testcontainer-start flakes isolation-cleared 21/21) + 6s (fresh greenfield volume) + 459fe = **2189 (+9)**. Baseline from S76b close: 824u + 898r + 6s + 452fe = 2180.

- **Unit:** 824/824.
- **Frontend:** 459/459 (36 files; `tsc --noEmit` 0E). Net vs S76b 452: **−10** (the deleted `ReportingLineTree.test` — no meaningful coverage lost, the surviving enforcement-toggle behavior is mirrored in `MedarbejderAdministration.test.tsx`) **+17** (R1 cross-flow ×3, R5 a11y ×8, R3 perf ×4, +2 misc) = **+7**.
- **Regression:** 900 effective (+2 vs S76b 898: the composed vikar reject/reopen test + the 2000-scale validation). The single full run reported 897/900; the **3 non-passes were FAIL-002 testcontainer-start socket-aborts** (`Npgsql … connection aborted` in `DockerHarness.StartAsync()` fixture init for `EmployeeProfileLifecycleTests` ×2 + `MedarbejderRosterReadTests` ×1 — Docker-Desktop shed-starts under the concurrent Codex/FE churn during the 47-min run; the assertions never ran). **Isolation-cleared:** re-running those 2 classes exclusively passed **21/21** (1m10s) — confirmed environmental flake, NOT a regression nor contamination from the 2000-seed (which is self-contained/prefix-torn-down). The 2 new 7701 tests passed in the full run.
- **Smoke:** 6/6 (fresh greenfield volume — `docker compose down -v` + `up -d --build`; init.sql byte-unchanged from S74 — S77 is test/FE/docs only, no schema/backend-production change; the smoke re-confirms the greenfield stack is healthy).
- **CI: whole-workflow GREEN run `27550880898` (`1a59b58`) — all 6 jobs (build-and-test / frontend-build / lizard / gitleaks / smoke / check_docs) FIRST-TRY, no flake.** **15th consecutive CI-verified close.** `build-and-test` passed clean on attempt 1 — the 3 local FAIL-002 testcontainer-start aborts were Docker-Desktop churn (concurrent Codex/FE runs), not reproduced in CI's clean environment.
