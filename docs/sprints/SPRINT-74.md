# SPRINT 74 — Medarbejder administration, Phase 1: Backend foundations (ADR-027 extension)

Status: **CLOSE IN PROGRESS — all 5 tasks ACCEPTED; Step-7a CLEAN both lenses; finalizing the count + fresh-volume smoke + close-out docs, then the (owner-gated) commit.** 2026-06-13.
Program: `.claude/plans/PLAN-s74-medarbejder-administration.md` (owner-approved 2026-06-13; the 4-phase program). This sprint = **Phase 1 (backend only, no FE)**.
Refinement: `.claude/refinements/REFINEMENT-s74-medarbejder-administration.md` (owner rulings OQ-1..OQ-5).
Handoff: `design_handoff_medarbejder_administration/`.

## Premise

Phase 1 lands the backend engine changes for the Medarbejder-administration redesign — the highest-risk slice, where the authorization surface (A3) and the vikar model (A2) live. NO frontend. Every change is additive over the existing ADR-027 reporting-line model; the org hierarchy (ADR-008), audit (ADR-026), settlement attribution, and the RBAC scope backbone are byte-unchanged. The owner-approved program rulings carry verbatim: OQ-1a (additive `enhed_label`), OQ-2a (atomic create+assign), OQ-3a (the edge GRANTS approve-authority within the styrelse — a P7 expansion, mandatory Step-5a), OQ-4a (phased), OQ-5a (approver-owned vikar covering future reports).

## Grounded current state (Explore sweep 2026-06-13 — all confirmed)

- `employee_profiles` (init.sql 485-495): temporal table (profile_id PK, effective_from/to, version, partial-unique-live + history-unique indexes); **NO `enhed_label`**.
- `reporting_lines` (init.sql 2343-2358 S48 + 2486-2489 S51): relationship CHECK {PRIMARY,ACTING}; source CHECK {MANUAL,HR_IMPORT,SELF_DELEGATION}; `scheduled_expiry`; `tree_root_org_id`; partial-unique live PRIMARY + live ACTING; `idx_reporting_lines_manager`. **No vikar table.**
- `ResolveDesignatedApproverAsync` (ReportingLineRepository.cs:445-480): returns `(string? ManagerId, string? ApprovalMethod, int Depth)`; ACTING-precedence → PRIMARY → inactive-manager escalation walk; `while depth<10`; opens its own connection. `ValidateSameTreeAsync` (389-431) + `ResolveTreeRootOrgIdAsync` (344-379) **open their own connections** (no conn/tx overload). Row-mapper reads `tree_root_org_id` at 671.
- Self-delegation (ReportingLineEndpoints.cs): POST `/api/reporting-lines/delegate` (1146-1379) fans out per-PRIMARY-report `SELF_DELEGATION` ACTING rows + emits ONE batch event **`ReportingLineSelfDelegated`** (ReportingLineEndpoints.cs:1347-1361; registered EventSerializer.cs:117; replay-tested ReportingLineTests.cs:588-647 — **NO per-line `ReportingLineAssigned`**; corrects the first grounding pass); the POST explicitly SKIPS a report that already has an admin ACTING (1295-1310). GET (1071-1140, body 1082-1138) reads `source='SELF_DELEGATION'` rows → `delegatedEmployees[]`; DELETE (1385-1482) closes them + emits `ReportingLineSuperseded`. `DelegationExpiryService.cs:43-127` closes them on `scheduled_expiry <= CURRENT_DATE` (line 58 — **exclusive of "til og med"**).
- Approval auth (ApprovalEndpoints.cs): approve (174-307) + reject (311-448) gate on `ValidateOrgAccessAsync(actor, period.OrgId)` (199-202) THEN call `ResolveDesignatedApproverAsync` for the method label only; reopen (~935, Leader+ org-scope); submit (60-170) + employee-approve (687-898) self/scope (**no designation routing — correctly excluded from A3**). Dashboards: GET `/api/approval/pending?my-reports=true` → `GetPendingForDesignatedReportsAsync` (468); GET `/api/approval/by-month` → `GetByMonthForDesignatedReportsAsync` (567).
- Dashboard repos (ApprovalPeriodRepository.cs): `GetPendingForDesignatedReportsAsync` (222-314) + `GetByMonthForDesignatedReportsAsync` (116-213) — **both** a RECURSIVE transitive-report CTE with inline ACTING-precedence, **intersected** with the actor's `{orgScopeClause}` AND-filter.
- Assign endpoints (ReportingLineEndpoints.cs): PRIMARY (22-176) + ACTING (447-597) — scope + same-tree validation, **NO descendant/cycle guard**. DELETE (182-317) has the multi-root 409 guard (213-255).
- User-create (AdminEndpoints.cs:376-763): 6-way atomic tx (users + employee_profiles + user_agreement_codes + 3 events/audit). Deactivation emits `ReportingLineManagerDeactivated` per direct report (1318-1339) but does NOT close the reports' edges.
- ADR-027 D9 (lines 86-91): ≤1 root per `tree_root_org_id`; Phase-1 allows zero roots, Phase-4 makes population mandatory; orphan-creation guarded by the DELETE 409. ADR-012 manager transitions = approve / reject / reopen (employee-approve is the employee's own gate).

## Pinned requirements (R1–R11) — cycle-1 Step-0b findings absorbed

- **R1 (A1 enhed_label — END-TO-END, owned by 7400):** `employee_profiles.enhed_label TEXT NULL`, additive, NULL backfill (FE shows the primary_org name when null). It rides the existing ADR-022 temporal profile versioning — a label change SUPERSEDES the live profile row like position/fraction do, NOT a separate event family (Reviewer-confirmed sound). **7400 threads it FULLY, not just the column** (Codex B1): the repo model + SQL (`EmployeeProfileRepository.cs:223, 368-443, 735-789`), the GET/PUT DTOs + temporal snapshot + audit (`EmployeeProfileEndpoints.cs:270-300, 360-453, 894-912`), the create-path insert + the `EmployeeProfileCreated` payload (`AdminEndpoints.cs:487-521, 636-646`), and all three profile events (Created/Updated/Superseded). The field is INERT for rules/payroll — display only.
- **R2 (A2 manager_vikar table — SELECTED, not deferred):** new table `manager_vikar (vikar_id UUID PK, absent_approver_id TEXT FK users, vikar_user_id TEXT FK users, until_date DATE NOT NULL /* INCLUSIVE */, reason TEXT CHECK {FERIE,SYGDOM,ORLOV,TJENESTEREJSE,ANDET}, tree_root_org_id TEXT FK organizations, version BIGINT, created_by TEXT, created_at TIMESTAMPTZ, effective_to DATE NULL /* close marker */)`; **partial-unique one-active-per-approver** WHERE effective_to IS NULL. CHECK `absent_approver_id <> vikar_user_id`. (Table DDL owned by 7400; repo/events owned by 7401.)
- **R3 (A2 resolver precedence — PINNED):** `ResolveDesignatedApproverAsync` gains the vikar consult. Single-winner order: **(1)** per-report admin-assigned ACTING (unchanged, highest) → **(2)** the resolved PRIMARY manager M's **active approver-owned `manager_vikar` V covering `asOf`, where V's user is itself ACTIVE** → return V (covers CURRENT + FUTURE reports of M automatically) → **(3)** M if active → **(4)** inactive-manager escalation walk. **Edge cases pinned:** (a) if M is INACTIVE but holds an active vikar V (V active), V wins over escalation (the explicit delegation beats the inactive-walk); (b) **if V's user is INACTIVE, the vikar is SKIPPED** — fall through to M-if-active else escalation (mirrors the existing ACTING/PRIMARY `IsUserActiveAsync` checks at `ReportingLineRepository.cs:460-472`; else escalation is suppressed without granting usable authority — Codex W2). **Signature: `asOf` is a parameter with DEFAULT = today** so the existing approve/reject callers (`ApprovalEndpoints.cs:204-206, 342-344`) compile UNCHANGED when 7401 lands before 7402 (Codex W1 — no breaking caller edits mid-chain); 7402 passes an explicit `asOf` only where it needs to.
- **R4 (A2 self-delegation cutover — STORAGE swap, CONTRACT stable):** the three `/api/reporting-lines/delegate` endpoints + `DelegationExpiryService` move their STORAGE from per-report `SELF_DELEGATION` ACTING fan-out to a SINGLE `manager_vikar` row owned by the delegating manager — **but the endpoint request/response CONTRACTS stay byte-stable** so the still-live S51 self-service UI (retired only in Phase 4) keeps working. POST creates one vikar; **GET re-derives `delegatedEmployees[]` dynamically = the reports for which the vikar is CURRENTLY effective, EXCLUDING any report already superseded by an admin ACTING** (matching the POST's existing skip at `ReportingLineEndpoints.cs:1295-1310` — Codex W3); DELETE closes it; expiry closes it the day AFTER `until_date` (**the inclusive "til og med" fix — R4a**). New go-forward event family `ManagerVikarCreated` / `ManagerVikarEnded`. **The old `ReportingLineSelfDelegated` event (NOT "DelegationCreatedBatch" — that name does not exist; corrected) is RETIRED FROM EMISSION ONLY: its event class + the `EventSerializer.cs:117` registration are RETAINED for historical replay** (replay-tested at `ReportingLineTests.cs:588-647` — dropping it breaks replay; Codex B4 / Reviewer B2). The POST never emitted per-line `ReportingLineAssigned`, so nothing per-line to retire. Existing open `SELF_DELEGATION` ACTING rows: a one-time migration in the S74 init.sql segment closes them + projects them into `manager_vikar`. **Admin-assigned per-report ACTING is NOT touched** (distinct, higher-precedence use-case).
- **R5 (A3 canonical predicate — THE load-bearing piece; ONE home):** define ONE predicate `IsEffectiveDesignatedApprover(actorId, employeeId, asOf=today)` ⟺ actor is **active + LeaderOrAbove** AND the **single resolved effective approver** of `employeeId` at `asOf` (per R3 precedence) **== actorId**. (The edge being intra-tree is an assign-time invariant via `ValidateSameTreeAsync`, so `actor == resolvedManager` already implies same `tree_root_org_id` — no period-org re-derivation.) **ONE authoritative implementation** (a single repo method) consumed by BOTH the dashboard reads (R6) and the three action endpoints (R7) — no second encoding, or the two surfaces drift (Reviewer W2). **NOT** a UNION of the recursive transitive-report set (that over-grants a grand-manager).
- **R6 (A3 dashboard reads — BOTH, REWRITTEN to single-immediate semantics — Codex B2 / Reviewer B1):** the cycle-1 "relax the orgScopeClause" framing was WRONG — both `GetPendingForDesignatedReportsAsync` (222-314) and `GetByMonthForDesignatedReportsAsync` (116-213) today recurse through ACTIVE managers (`ApprovalPeriodRepository.cs:161-188, 264-291`), so merely changing the org filter + adding vikar PRESERVES the grand-manager over-grant (sees a grandchild, 403 on approve — the exact incoherence R7 prevents, one level up). **FIX: replace the designated-path report set with per-employee single-immediate-effective-approver semantics** (`{employee : resolved effective approver(employee, today) == actor}`, per R3: admin-ACTING → vikar → PRIMARY → escalation), **tree-root bounded; recursion advances ONLY for inactive-manager escalation** (never to grant a grandchild whose own manager is active). The vikar-consult is part of the resolution (a vikar SEES the absent manager's reports). The default `/pending` pure-org-scope path is UNCHANGED. **See == act** at every level.
- **R7 (A3 action authority — approve + reject + reopen-LEADER-branch):** each authorizes if `(existing: actor's RoleScope covers period.OrgId) OR (new: IsEffectiveDesignatedApprover(actor, period.EmployeeId, today))`. **`asOf = today`** (action-authority = "who may act NOW"; the audit method-label may stay period-dated, but the authority check is today — pinned per the S73 OkVersion lesson; Codex W1 / Reviewer W1). **Edit sites differ — pinned:** approve (`204-206`) + reject (`342-344`) are flat `LeaderOrAbove`; **reopen is `EmployeeOrAbove` with an `if (isEmployee) … else …` split — the new OR-branch goes ONLY in the `else` (Leader+) arm (`ApprovalEndpoints.cs:902-941`), NEVER the employee arm (`921-932`)** (else over-grant; Codex+Reviewer W3). employee-approve + submit UNCHANGED (not manager transitions). Mandatory **Step-5a** + the pinned 3-case regression: sibling-afdeling edge → approvable AND reopenable; cross-styrelse edge → blocked; an edge the actor does NOT hold (or holds PRIMARY while a vikar supersedes) → not approvable; the vikar-holds-authority case (approve + SEES-on-dashboard); existing org-scope path unchanged. A **route→read→approve→reopen integration test lands in THIS sprint** (Phase 1), not Phase 4.
- **R8 (A4 cycle guard — tree-wide lock, BOTH assigns — Codex B3 / Reviewer W4):** the PRIMARY assign AND the ACTING assign REJECT (4xx) an approver that is the employee or any descendant. The existing `AssignAsync` `FOR UPDATE` locks only the target employee/relationship SLOT (`ReportingLineRepository.cs:213-214, 518-534`) — concurrent FIRST assignments find NO row to lock (phantom gap), and a descendant `SELECT … FOR UPDATE` alone has the same gap. **REQUIRE a stable tree-wide `pg_advisory_xact_lock(tree_root_org_id)` (the ADR-032 D4 advisory-lock precedent) OR SERIALIZABLE + retry, taken BEFORE the descendant cycle-walk, on EVERY assign path.** The descendant walk carries a stated termination bound (`depth < N`, mirroring the resolver's `while depth<10`) so a pre-existing cycle cannot hang it.
- **R9 (A4 atomic create+assign):** extend the existing AdminEndpoints `POST /api/admin/users` create transaction (`AdminEndpoints.cs:420-694` — users + employee_profiles + user_agreement_codes inserts + 3 audit rows + 3 outbox events) with an OPTIONAL `approverId` → in the SAME tx, create the PRIMARY reporting line via the repo's in-tx `AssignAsync` overload, **emitting `ReportingLineAssigned` + the reporting-line audit row exactly like the normal assign path (`ReportingLineEndpoints.cs:98-150`)** (Codex W4). `ValidateSameTreeAsync`/`ResolveTreeRootOrgIdAsync` gain **(conn, tx) overloads** (they open their own connections today — `ReportingLineRepository.cs:344-368, 389-421`) so the read sees the just-inserted user; the R8 tree-wide lock + cycle guard run in-tx here too. No new orphans from the UI create path.
- **R10 (A4 delete-with-reassignment — NO orphans, FULL closure matrix — Codex B5):** a new "Fjern medarbejder fra afgrænsning" op = **preflight-guarded**. If the person has active reports, return **409 + mutate nothing** until the caller supplies replacement approver(s); then commit atomically, closing the FULL edge matrix (`GetDirectReportsAsync` returns BOTH PRIMARY+ACTING — `ReportingLineRepository.cs:87-125`): **(1)** reassign INCOMING PRIMARY edges (`manager_id = removed`) to the supplied replacement; **(2)** close INCOMING ACTING edges (`manager_id = removed`); **(3)** close the removed person's OWN outgoing PRIMARY/ACTING edges; **(4)** close `manager_vikar` rows where the removed user is `absent_approver_id` **OR** `vikar_user_id`; **(5)** soft-deactivate the user + emit events/audit — all one tx. (Closing only outgoing edges leaves active references routing to an inactive user.) **No newly-created orphans, ever.** The legacy orphan card (Phase 2 FE) surfaces only pre-existing no-approver data.
- **R11 (A5-backend reads):** (a) a **period-status projection read** — per-styrelse, each employee's last-closed-month (`period_end < CURRENT_DATE`, greatest) `approval_periods.status` projected to OPEN(DRAFT/none/REJECTED) / SUBMITTED(SUBMITTED+EMPLOYEE_APPROVED) / APPROVED + the per-manager pending count, with a supporting index; (b) a **server-side person-search** endpoint (`?q=`, scope-filtered, excludes self+descendants server-side, paginated) for the 2000+ picker. Reads only — the FE consumes them in Phase 2/3.

## Task decomposition (dependency-corrected)

| Task | Title | Agent | Depends on | Review |
|------|-------|-------|-----------|--------|
| **7400** | Schema + **enhed_label END-TO-END** (R1: column + repo + DTO + snapshot + audit + 3 events) + the `manager_vikar` **table DDL** (table only) + the S74 init.sql segment (legacy ALTERs + the `SELF_DELEGATION`→`manager_vikar` close/project migration) + db-schema.md regen + caller census | Data Model | — | Step-5a discretionary (rides the schema precedent) |
| **7401** | Vikar engine + self-delegation storage cutover: `ManagerVikarRepository`, the R3 resolver precedence (dated `asOf=today` default, vikar-consult, inactive-vikar-skip), the `ManagerVikarCreated/Ended` event family + audit (RETAIN `ReportingLineSelfDelegated` for replay), the 3 delegate-endpoint storage swaps (contract-stable; GET dynamic-derivation excl. admin-ACTING) + the `DelegationExpiryService` cutover + the R4a inclusive-date fix | Rule Engine / Reporting | 7400 | **Step-5a** (touches the approval-routing resolver) |
| **7402** | Approval-authority expansion (A3): the R5 ONE-home canonical predicate; R6 both dashboard reads REWRITTEN to single-immediate semantics (recursion only for inactive-escalation) + vikar-consult; R7 approve+reject+reopen-Leader-branch authority (`asOf=today`) + the Phase-1 route→read→approve→reopen integration test + the 3-case regression | Security / Approval | 7401 | **Step-5a MANDATORY** (P7 authorization expansion) |
| **7403** | Write lifecycle (A4): R8 tree-wide-advisory-lock cycle guard on BOTH assigns (bounded walk); R9 atomic create+assign (tx extension + in-tx tree-root overloads + `ReportingLineAssigned` emission) ; R10 preflight-409 delete-with-reassignment + the FULL closure matrix (incl. `manager_vikar` rows) + its events | Reporting / Admin | **7401** (R10 closes `manager_vikar` rows owned by 7401) | **Step-5a** (create/delete + cycle invariant) |
| **7404** | Backend reads (A5): R11 period-status projection read + index; the server-side person-search endpoint | Approval / Data | 7400 | Step-5a discretionary |
| **docs** | ADR-027 amendment (**D4** routing→authority; **D5** resolver vikar-consult + `asOf`; **D9** delete-reassignment; **D10** `ManagerVikarCreated/Ended` family + `ReportingLineSelfDelegated` retirement-from-emission; the `manager_vikar` representation) + ADR-012 amendment (edge-grants-authority over approve/reject/reopen) | Orchestrator | **{7402, 7403}** | — |

**Sequencing (corrected — Codex B6):** `7400 → 7401 → { 7402 ‖ 7403 }` ‖ `7404 (from 7400)`; `docs ← {7402, 7403}`. **7403 depends on 7401** (R10's delete closes `manager_vikar` rows the 7401 repo owns) — NOT parallel-from-7400. 7402 and 7403 both depend on 7401 and run in parallel after it. 7404 is the only task parallel-from-7400.

**Sizing note:** heavy backend sprint (≈ S73 × 1.5; one mandatory + ≥2 discretionary Step-5a). Split point if execution runs long (the S73 session-death pattern): land **7400 + 7401 + 7402** (the coupled vikar+authority core, the highest-risk) as S74; carry **7403 + 7404** to an S74b. Clean because R10 is WHOLLY in 7403 (7401 leaves no half-done deletion cleanup) and 7403 is independent of 7402; 7404 depends only on 7400. Decided at execution time.

## Step-0b plan-gate (detailed decomposition)

**Cycle 1 (2026-06-13): Codex BLOCKED(6), Reviewer BLOCKED(2) — ALL absorbed above.** Strong convergence: B-R6/R5 dashboard-CTE-stays-transitive (Codex B2 = Reviewer B1) → R6 rewritten to single-immediate semantics; B-R4 event misnamed `DelegationCreatedBatch` (Codex B4 = Reviewer B2; I verified — the name does not exist; real event `ReportingLineSelfDelegated`, retain-for-replay) → R4 corrected; B-R8 slot-lock phantom gap on concurrent first-assigns (Codex B3, Reviewer W4) → tree-wide advisory lock; B-R1 enhed_label had no threading owner (Codex B1) → 7400 owns it end-to-end; B-R10 delete closure matrix under-pinned (Codex B5) → full 5-step matrix; B-dependency-graph 7403↛7401 invalid (Codex B6) → 7403 now depends on 7401, docs on {7402,7403}. Absorbed WARNINGs: asOf default-today compat (W1) + asOf=today action semantics; inactive-vikar-skip (W2); GET excludes admin-ACTING (W3); reopen Leader-branch-only (W3); 7403 owns ReportingLineAssigned emission (W4); predicate single-home (Reviewer W2). NOTEs: R9 tx description corrected (no "6-way" miscount); ADR amendment enumerates D5+D10 (Reviewer N).

**Cycle 2 (2026-06-13): Reviewer CLEAN (0B/0W/2N — verified all absorptions held against live code: both dashboard reads confirmed identical transitive CTEs; reopen structure confirmed isEmployee-split at 921/923, Leader else-arm 933-942; AcquireLockAsync slot-only phantom-gap confirmed real). Reviewer's 2 implementer NOTEs (→ Step-5a):** (N1) **7401** — pin the resolver vikar-lookup to key on the inactive M's id IN THE SAME loop iteration where M is found inactive, BEFORE `currentEmployeeId = primaryLine.ManagerId` advances (`ReportingLineRepository.cs:475`), so the "M-inactive-but-has-vikar → V wins" edge case (R3a) actually fires; (N2) **7404** — the period-status read is read-only/additive, `period_start`/`period_end` confirmed present — sound. **Codex cycle-2: CLEAN — no findings** (verified all 6 BLOCKER absorptions + the 6 WARNING absorptions held against live code: R1 ownership complete; R5/R6 single-effective-approver removes transitive grant; R8 tree-wide lock adequate; R4 ReportingLineSelfDelegated retain-for-replay; R10 all 5 closure categories atomic; dependency graph acyclic). **STEP-0b GATE CONVERGED 2026-06-13 — both lenses CLEAN cycle-2.** Artifacts: `.claude/s74-sprint-stepb-codex.log` (c1), `.claude/s74-sprint-stepb-codex-c2.log` (c2), the two Reviewer transcripts.

## Implementation Record

- **7400 ✅ ACCEPTED 2026-06-13.** Schema foundations + `enhed_label` end-to-end + `manager_vikar` table DDL. Files: `init.sql` (S74 segment @ ~3697: the enhed_label ALTER + manager_vikar CREATE + `uq_manager_vikar_active` partial-unique + `idx_manager_vikar_vikar` reverse-lookup + the ledger-guarded SELF_DELEGATION→manager_vikar projection/close DO-block); `EmploymentProfile.cs` (+`EnhedLabel`; NOTE the in-memory model is `EmploymentProfile` not `EmployeeProfile`); `EmployeeProfile{Created,Updated,Superseded}.cs` (+EnhedLabel, no new serializer registration — additive-safe); `EmployeeProfileRepository.cs` (mapper + 3 write paths + 3 request DTOs); `EmployeeProfileEndpoints.cs` (GET/PUT/DELETE + predecessor snapshot + audit ×2 + both events); `AdminEndpoints.cs` (create-tx profile audit + EmployeeProfileCreated, null); 3 audit mappers. db-schema.md regen → **65 tables**; `check_docs.py` green. Validated: `dotnet build` 0E/0W; **live-Postgres greenfield + legacy-projection (1 SELF_DELEGATION row → 1 manager_vikar row, 0 remaining) + idempotent re-apply + all CHECK/partial-unique invariants reject violations**. Discretionary Step-5a WAIVED (rides the schema precedent + live-DB validation). The `EmployeeProfileSeeder` correctly left null-by-default. **Caller surface for 7401/7402/7403 recorded in the agent's census** (7401 owns ManagerVikarRepository + events + resolver + the 3 delegate-endpoint swaps + DelegationExpiryService; the `idx_manager_vikar_vikar` index serves 7403's R10 reverse-lookup).

- **7401 ✅ ACCEPTED 2026-06-13.** Vikar engine + self-delegation storage cutover delivered (new `ManagerVikar` model + `ManagerVikarRepository` + `ManagerVikarCreated/Ended` events + 2 audit mappers; resolver R3 precedence with the vikar consult [verified fires at depth 0 for M-inactive-with-active-vikar; inactive-vikar-skip; `asOf` default-today so approve/reject callers compile unchanged; vikar→ApprovalMethod `ACTING_MANAGER`, no CHECK widen]; `ReportingLineSelfDelegated` RETAINED for replay, emission stopped; the 3 delegate-endpoint contract-stable cutovers; `DelegationExpiryService` R4a strict `until_date < CURRENT_DATE`). Build 0E/0W; unit 824 (+7); regression 12/12 + existing 37/37 + parity 3/3 green. **Step-5a DUAL-LENS: Reviewer CLEAN (0B/0W/5N — all benign/intended); Codex BLOCKED(1) — a REAL defect the Reviewer MISSED ([[review-lens-complementarity]] AGAIN): the 3 vikar writes (POST 1355, DELETE 1454, expiry) emit the outbox event but NEVER do the ADR-026 D2 sync-in-tx audit insert — the registered `ManagerVikar*AuditMapper`s are DEAD → vikar create/end produce ZERO audit rows.** (Verified by the Orchestrator: the reporting-line family audits via the dedicated `reporting_line_audit` table keyed on reporting_line_id, which manager_vikar CANNOT use — so audit_projection [the agent's mapper choice] is correct, just UNWIRED.) **FIX-FORWARD (done):** wired `EnqueueAndReturnIdAsync` + `mapper.Map` + `AuditProjectionRepository.InsertAsync` in-tx at all 3 sites (SYSTEM-actor ctx for the expiry, mirroring `SettlementCloseService`); moved the POST/DELETE count reads INTO the write tx (W2); added real-audit-path tests + a forced-rollback atomicity test. **Step-5a c2 (Codex): production fix VERIFIED CORRECT (all 3 writes/counts/shapes/SYSTEM-ctx), residual BLOCKER = the forced-rollback test was tautological (asserted state+audit absence but never queried `outbox_events` → didn't prove the "NoEvent" its name claims). Orchestrator DIRECT FIX (trivial, past 2-cycle cap, production verified): added `GetVikarOutboxCountAsync` + the `outbox_events==0` assertion → the rollback now provably discards state+audit+EVENT.** Final: build 0E/0W; ManagerVikar regression **15/15**; unit **+7**; existing reporting-line 37/37 + parity 3/3 green. **Step-5a lesson: lens complementarity AGAIN — Codex caught the dead-mapper audit gap (c1) AND the tautological atomicity test (c2) that the Reviewer's structural pass [CLEAN] missed both times.** **Catalog rows DONE: added the 2 `ManagerVikarCreated/Ended` rows to `docs/operations/audit-projection-catalog.md` (mapper_kind=`interface`, cross-process, target=`tree_root_org_id`; Total 64→66); `AuditProjectionParityTests` 3/3 green (catalog↔code lockstep holds, TBD count still 5).**

- **7402 ✅ ACCEPTED 2026-06-13.** A3 approve-authority expansion delivered: NEW `DesignatedApproverAuthorizer` (the R5 single-home predicate: active + LeaderOrAbove [role_assignments join, hierarchy_level≤4] AND single-resolved-effective-approver==actor); both dashboard reads rewritten to single-immediate semantics (a candidate-superset CTE seeded from actor + vikar-stood-for approvers, descending ONLY through inactive users for escalation, then filtered by the shared predicate — org-scope intersection removed; see==act); approve/reject/reopen-Leader-arm edge-authority OR-branch (asOf=today; employee-approve/submit untouched; reopen branch verified Leader-arm-only). 12 cross-org integration tests; build 0E/0W (0 new warnings in the touched production projects). **Step-5a DUAL-LENS — LENS DISAGREEMENT on a security defect ([[review-lens-complementarity]], the sharpest instance yet): Reviewer CLEAN (0B/0W/3N — claimed "D2 preserved STRUCTURALLY, a cross-tree actor can never be the resolved winner"); Codex BLOCKED(2). The Reviewer's structural trace was FALSE for the vikar path — Orchestrator verified against source.** **B1 (REAL, security, CONFIRMED): cross-styrelse hole via the VIKAR path.** The predicate trusts "resolver-winner==actor ⟹ same-tree" — true for PRIMARY/ACTING (assign-time `ValidateSameTreeAsync`) but the `/delegate` POST (`ReportingLineEndpoints.cs:1201-1300`) validates only the vikar's ROLE + ORG-SCOPE coverage, NEVER same-tree → a cross-tree vikar can become the resolver winner and gain cross-styrelse approve authority (ADR-027 D2 violation). The cross-styrelse test only covered an actor-with-no-edge, masking this route. **B1 FIX-FORWARD (background agent `a64ff68e463873949`): Layer-1 vikar-creation `ValidateSameTreeAsync(absentApprover, vikarUser)` + Layer-2 predicate structural same-tree(actor,employee) defense-in-depth + a cross-tree-vikar regression (denied on BOTH layers; legitimate cross-AFDELING same-styrelse still works).** **B2 (revocation TOCTOU): authority checked at request entry, OUTSIDE the action tx → a revoked edge/vikar/role in the sub-second window before commit isn't caught. PRE-EXISTING PATTERN (the existing org-scope auth has the identical window); 7402 follows it consistently. → OWNER RULED (2026-06-13): FOLLOW-UP TASK** — a dedicated "in-lock approval authorization hardening" that does it properly for BOTH the org-scope AND edge paths (re-evaluate authority inside the action tx with SELECT FOR UPDATE on the edge/vikar rows; the S70-B1 precedent). NOT fixed in 7402 (a partial narrowing wouldn't fully close it; pre-launch so acceptable). **→ ROADMAP follow-up at close. Bundle W1 into it.** Codex WARNINGs: **W1** the post-auth resolver recompute (`ApprovalEndpoints.cs:217,363`) uses a fresh implicit `today` → a UTC-midnight crossing between the auth check and the method-label recompute could change winner/method (capture `today` once) — **bundled into the B2 follow-up** (same code area). **W2** completeness tests omit direct-admin-ACTING / multi-level-escalation+vikar / the by-month read (the CTE is traced complete by the Reviewer + by inspection, but unproved by test). **Step-5a c2 (Codex): B1 authorization hole CONFIRMED CLOSED on both layers (Layer 2 denies planted cross-styrelse rows + permits same-styrelse cross-AFDELING); W2 DOWNGRADED to NOTE given the Reviewer's CTE trace; ONE residual robustness BLOCKER = Layer-1 left `InvalidOperationException` uncaught → 500 instead of 400 for an inactive/missing/org-broken vikar.** **Orchestrator DIRECT FIX (one-line, past cap, hole already closed): added `catch (InvalidOperationException) → 400` in the `/delegate` POST same-tree guard (Layer 2 already fails-closed on the same condition, so authz stays safe). Scoped suite 30/30 green.** **W2 completeness tests ADDED: `ByMonth_CrossAfdelingDesignated_Sees_AndCrossStyrelse_DoesNot` (the by-month read carries the same single-effective-approver semantics; cross-styrelse blocked) + `AdminActing_DirectEdge_TakesPrecedence_Sees_AndApproves_CrossAfdeling` (resolver precedence step-1 admin-ACTING grants visibility+authority cross-afdeling). DesignatedApproverAuthority 17/17 + ManagerVikar 15/15 green. 7402 ACCEPTED — both lenses cleared (Reviewer CLEAN; Codex c1 BLOCKED(2)→B1 two-layer fix→c2 confirmed-closed+1 robustness one-liner). B2 TOCTOU + W1 asOf-midnight → owner-deferred follow-up (in-lock approval-authorization hardening; ROADMAP at close).**

- **7403 ✅ ACCEPTED 2026-06-13** (after the deepest review of the program — Codex c1 5B → c2 4B → c3 3B → c4 2B → c5 1B, each cycle narrower, every fix confirmed correct; the headline was a concurrency crucible). FINAL: the drift-reacquire deadlock REMOVED (uniform order advisory→id-ordered-rows→cycle-guard→slot, no inversion; c5 confirmed gone); the c5 R10-wrong-lock gap (R10 could reassign a cross-styrelse-transferred report under the removed person's lock) CLOSED with a `repTreeRoot==removedTreeRoot` reject-guard (Orchestrator direct, 409 surfaces the transferred report — consistent with the owner's reject-the-rare-transfer ruling; 17/17 + 70/70 + 4/4 green, build 0/0). DEFERRED to the in-lock hardening follow-up: perfect serialization of reporting-line assign/delete under a concurrent cross-styrelse org-transfer (needs a stable tree id or an org-transfer lock — NON-corrupting residual). **SAFETY INVARIANTS ALL SOUND + VERIFIED: no orphan (in-tx census under lock), no cross-tree edge (id-ordered two-row FOR UPDATE + post-pin same-tree reject), cycle guard (tree advisory lock + bounded path-array walk, deep cycles caught), atomic create+assign + delete (ADR-018 D3), audit (ADR-026 trio for the vikar close + UserUpdated), airtight concurrency tests (pg_locks waiter barrier).** ───── Original delivery: R8 `AcquireTreeLockAsync` (pg_advisory_xact_lock on tree root) + `GuardNoCycleAsync` (bounded descendant walk) on both assigns; R9 atomic create+assign (in-tx `ValidateSameTree`/`ResolveTreeRoot` overloads + `ReportingLineAssigned`); R10 preflight-409 delete + 5-step closure matrix (reassign incoming PRIMARY / close incoming ACTING / close own edges / close manager_vikar both-sides / soft-deactivate). 12 new + 78 regression green; build 0E/0W production. **Step-5a Codex BLOCKED(5) — five REAL concurrency/correctness defects (the isolation one is the marquee — passes every sequential test, only bites under concurrency):** **B1** `RepeatableRead` defeats the phantom-gap closure (the advisory-lock SELECT pins T2's snapshot BEFORE it waits → after T1 commits, T2's descendant walk reads the pre-T1 snapshot, misses T1's edge, admits A→B+B→A) → the cycle-guard txs must be **READ COMMITTED** so the in-lock guard read sees post-lock committed state; **B2** the bulk-import path (`ReportingLineEndpoints.cs:871,916`) bypasses BOTH the tree lock and the cycle guard → can create cycles + race guarded assigns → apply lock+cycle-validation to the import; **B3** the `depth<10` cap is a FALSE-NEGATIVE source (a cycle at descendant depth 11+ is missed; the path-array guard already guarantees termination) → REMOVE the cap; **B4** R10's report census runs BEFORE the tx/lock → a concurrent assign can slip a new report onto the removed person after the preflight (orphan), or wait behind the delete then insert an edge to the now-inactive user → census + closure must be INSIDE the locked tx + the assign must re-validate manager-active AFTER the lock (the S70-B1 in-lock pattern); **B5** R10's soft-deactivate emits registered `UserUpdated` via plain `EnqueueAsync` → NO audit_projection row (same dead-audit class as 7401-B1) → use the `EnqueueAndReturnIdAsync`+mapper+`InsertAsync` trio. WARNINGs: W1 missing/inactive R9 approver + R10 replacement throw `InvalidOperationException`→500 (catch→4xx, the 7402-c2 robustness class again); W2 R8 tests are SEQUENTIAL (don't prove the advisory-lock serialization / phantom-gap) + the rollback test omits the reporting-line/users audits + the endpoint choreography (non-tautological but weak). **Theme: the guard reads (cycle, census, manager-active) must ALL be in-tx, after the lock, under READ COMMITTED.** **Reviewer BLOCKED(1): independently found B4 (the R10 census-before-lock orphan race — the SAME defect, with a detailed interleaving trace + the fix = mirror the sibling deactivation `AdminEndpoints.cs:1412` in-tx re-read; this time the steered Reviewer DID hunt the edge case) and CONFIRMED SOUND the parts NOT to touch (the advisory-lock primitive + taken-before-walk; R9 in-tx overloads see the just-inserted user; the vikar-close trio; the forced-rollback test is REAL not tautological). [[review-lens-complementarity]]: Codex caught B1/B2/B3/B5 the Reviewer's structural pass missed — esp. B1 the RepeatableRead snapshot-timing (the Reviewer checked "lock before walk" but not the snapshot pinning).** **ONE comprehensive concurrency FIX-FORWARD dispatched** (agent below): B1 READ COMMITTED for the 4 guard txs + move the assign same-tree/manager-active validation in-tx after the lock; B2 lock+per-edge-cycle-guard the bulk import; B3 remove the depth cap (path-array terminates); B4 R10 lock-first + in-tx re-read of incoming edges + roll-back-409 on a newly-appeared unreplaced report; B5 UserUpdated via the audit trio; W1 catch→4xx; W2 a concurrent assign↔assign serialization test + a concurrent assign-vs-delete no-orphan test + strengthen the rollback test. **FIX-FORWARD LANDED (all 7 addressed; build prod 0/0; 14/14 + 67/67 + 4/4). Step-5a c2 (Codex) — CORE FIXES CONFIRMED SOUND (ReadCommitted+post-lock-FOR-UPDATE safe; R10 authoritative census/rollback; B5 trio; W1; batch-internal cycle visibility; re-entrant advisory lock OK) — but BLOCKED(4) on DEEPER edges (we are at the 2-cycle CAP):** (c2-1) the lock KEY is racy — `ResolveEmployeeTreeRootInTxAsync` reads `primary_org_id` WITHOUT a row lock → a concurrent cross-styrelse org-transfer could make two same-tree assigns take DIFFERENT advisory keys (NARROW — needs a concurrent cross-styrelse transfer mid-assign); (c2-2) the bulk IMPORT's manager-active/tree validation stayed PRE-LOCK → a concurrent R10-delete → the import inserts an edge to a just-deactivated manager (the B1-companion fix wasn't applied to import) — a narrow D9 orphan; (c2-3) the PLAIN DELETE-PRIMARY endpoint's multi-root census changed to ReadCommitted but never takes the tree lock → its root-invariant races a concurrent assign (PRE-EXISTING — that endpoint never had the lock); (c2-4) the W2 "concurrent" tests use `Task.WhenAll` with NO barrier → can run sequentially + still pass (don't PROVE serialization); no deep-cycle termination test. **→ OWNER RULED "FIX ALL 4 NOW".** Fix-2 landed: C2-1 FOR-UPDATE key pinning (employee row) + consistent lock order; C2-2 import in-tx validation; C2-3 DELETE-PRIMARY takes the tree lock; C2-4 deterministic held-lock tests (the held-lock-blocks-assign test + the deep-14-level-cycle test + the pre-existing-loop termination test). Build 0/0; 16/16+69/69+4/4+27/27. **Step-5a c3 (Codex) BLOCKED(3) — CONVERGING (c1 5B → c2 4B → c3 3B, each narrower; the FIXES confirmed correct: no deadlock, DELETE-PRIMARY serialization correct, employee pinning stable, R9 reasoning correct, 14-level test valid):** (c3-1) the MANAGER row stayed UNPINNED → a concurrent cross-styrelse manager transfer between validation and insert → a cross-tree edge (D2) — the unfinished half of C2-1; (c3-2) the import skips matching edges BEFORE the in-tx validation → a skipped edge bypasses the active/tree check; (c3-3) the held-lock tests lack a `pg_locks` barrier proving the request REACHED the lock (a slow request satisfies the 2s negative assertion) + the loop-termination test is masked by the depth ceiling. **OWNER's "fix it right" directive extended → CONVERGING fix-3 dispatched (agent `aed0e9f23fc7f8b76`): c3-1 lock BOTH user rows FOR UPDATE in id-sorted order (deadlock-safe, pins both trees); c3-2 validate every inserted import edge; c3-3 a `pg_locks` waiter-barrier in the held-lock tests + isolate the loop-test from the depth ceiling. Off-ramp offered to the owner. Then FINAL re-verify → accept.** **Step-5a c4 (Codex) BLOCKED(2) — but B2(import)+B3(tests) CONFIRMED FIXED; the 2 BLOCKERs are a NEW deadlock the c3 drift-reacquire introduced (it takes the advisory AFTER pinning rows → inversion with an assign holding that advisory; R10's per-report re-lock repeats it). KEY: the drift-reacquire ONLY fires on a concurrent cross-styrelse org-transfer → the deadlock is EXTREMELY NARROW + NON-CORRUPTING (a deadlock aborts+rolls back, no bad data). The SAFETY invariants are all sound (no orphan, no cross-tree edge, validated import, airtight tests).** **ROOT TENSION (deep): the advisory key is derived from MUTABLE org data, and R10 must hold the tree advisory before touching rows (so the global order is advisory→rows), which forces the key to be read UNLOCKED → drift → the re-acquire-after-pin inversion. Perfect serialization against a SIMULTANEOUS cross-styrelse transfer is genuinely hard (needs a stable tree id or an org-transfer serialization lock — a follow-up design). 4 review cycles (5B→4B→3B→2B), each narrower, the chase spawning new subtleties on the same rare corner. → OWNER DECISION (cycle 4, deep diminishing returns).** **OWNER RULED "Remove deadlock, defer deep race" (2026-06-13): strip the drift-reacquire (assigns) + R10's inner per-report advisory re-acquire (the deadlock causes); rely on the id-ordered two-row FOR UPDATE + ValidateSameTreeAsync (post-pin) to REJECT the cross-tree-drift with a clean 400; DEFER the deep residual (perfect serialization when BOTH parties transfer to the SAME new styrelse simultaneously with an assign — astronomically rare, NON-corrupting) to the in-lock concurrency-hardening follow-up (needs a stable tree id or an org-transfer serialization lock). Removal fix-forward dispatched (agent `ab27d88e4eded027c`) → uniform order advisory→id-ordered-rows→cycle-guard→slot, no inversion. Then ONE final verify → accept. FOLLOW-UP (ROADMAP at close, bundled with the 7402-B2 in-lock hardening): reporting-line assign/delete serialization under a concurrent cross-styrelse org-transfer (stable-tree-id or org-transfer lock).**

- **7404 ✅ ACCEPTED 2026-06-13.** Backend reads (A5/R11): the per-styrelse period-status projection (`GetPeriodStatusProjectionForTreeAsync` — last-closed-month status OPEN/SUBMITTED/APPROVED via LEFT JOIN LATERAL + per-manager pending tally; new `idx_approval_employee_period_end` segment, db-schema 65 tables, check_docs green) + the server-side person-search (`SearchPeopleAsync` — real paginated COUNT(*) OVER() query, scope-filtered via `GetAccessibleOrgsAsync`, ILIKE-escaped, self+descendant exclusion via a NEW read-only `GetDescendantIdsAsync` sibling mirroring 7403's CTE). 2 GET endpoints (LocalAdminOrAbove). 8 new + 32 + 54 green; build 0/0. **Step-5a Codex BLOCKED(1) — D2 scope-filtering/parameterization/RBAC/cross-org-negative CONFIRMED SOUND (no data leak):** **B1** the per-manager pending TALLY used the raw resolver, bypassing the dashboard's canonical predicate (active+LeaderOrAbove+same-tree) → a role-revoked manager could show a non-zero tile while the dashboard shows zero → gate the tally with `DesignatedApproverAuthorizer.IsEffectiveDesignatedApproverAsync` so the tile MATCHES the dashboard. W1 `COUNT(*) OVER()` reports total=0 on an empty trailing page; W2 descendant-exclusion test only covers a direct child (add a deep chain); NOTE the index segment lacks the SEGMENT-END marker. **FIX-FORWARD (agent `a1bdbeeb578081379`): all 4. Then re-verify → accept.**

- **DOCS (task #12) — ADR amendments DRAFTED 2026-06-13:** ADR-027 D13 (designated-edge approve-authority, the A3 expansion + the deferred TOCTOU) / D14 (approver-owned vikar + resolver vikar-consult + same-tree + inclusive date) / D15 (write-time integrity: cycle guard + atomic create+assign + no-orphan delete + the concurrency model + the deferred cross-styrelse-transfer serialization) / D16 (enhed_label + the ManagerVikar events + ReportingLineSelfDelegated retire-from-emission). ADR-012 (manager-transition authorization OR-branch — approve/reject/reopen-Leader-arm, employee-approve/submit excluded). Remaining docs at close: ROADMAP done-row + follow-ups + INDEX + QUALITY + the sprint header/Test-Summary.

## Step-7a (sprint-level, 2026-06-13) — CLEAN both lenses

Whole-diff dual-lens over the integrated 5-task slice (each task already passed its own per-task Step-5a; 7402 + 7403 had multi-cycle security/concurrency reviews). **Codex CLEAN (0B, 3W docs-consistency); Reviewer CLEAN (0B/0W/3N).** Both confirmed the cross-task integration is SOUND: the canonical `DesignatedApproverAuthorizer` predicate is consumed CONSISTENTLY by all 5 surfaces (the 3 actions + both dashboard reads + the 7404 tile tally — no raw-resolver drift; see==act holds); the styrelse boundary (D2) holds uniformly (predicate structural re-check + vikar-creation same-tree); the vikar lifecycle composes (7400 table → 7401 repo/events/resolver → 7402 predicate → 7403 R10 both-sides closure → 7404 tally); event-sourcing/replay integrity preserved (ReportingLineSelfDelegated retained; the ManagerVikar family audited; 66-row parity in lockstep); the org/RBAC/audit/settlement backbone byte-unchanged; the R7 route→read→approve→reopen integration test landed THIS sprint. **3 docs-consistency findings (both lenses converged) — ALL FIXED at close:** the KB INDEX ADR-027 entry stale (→ D11-D16 added); `ManagerVikarEnded.EndReason="APPROVER_REMOVED"` (R10) implemented-but-undocumented (→ event doc + catalog updated); the ADR-027 D13 stale-premise sentence the next bullet walked back (→ reconciled). The deferred follow-ups (in-lock approval-auth TOCTOU; cross-styrelse-transfer serialization) confirmed the ONLY load-bearing open items, NON-launch-blocking. Artifacts: `.claude/reviews/SPRINT-74-step7a-{codex,reviewer}.md`.

## Test Summary

**824u (+7) + 837r (+61, single CLEAN run 837/837 0-fail, 43m9s — no FAIL-002 sheds) + 6s (fresh greenfield volume — `docker compose down -v` + rebuilt S74 images, 6/6) + 385fe = 2052 (+68 vs S73's 1984).** Build 0E/0W; check_docs green (65 tables). Step-7a CLEAN both lenses. **Greenfield init.sql with the S74 segments (enhed_label + manager_vikar + the SELF_DELEGATION→manager_vikar migration + the 7404 index) applied CLEANLY on a fresh compose volume (postgres healthy, no init errors); all 8 services healthy; smoke 6/6.** The regression run was slow (testcontainer-per-class churn from ~70 new Docker-gated tests) but clean on the first pass.
