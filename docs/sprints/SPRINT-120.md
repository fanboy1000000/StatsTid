# Sprint 120 — Typed API Contract retrofit Pass 7: bucket C complete (the final bulk drain)

| Field | Value |
|-------|-------|
| **Sprint** | 120 |
| **Status** | complete |
| **Start Date** | 2026-07-21 |
| **End Date** | 2026-07-23 (both Phase-2 agents session-limit-interrupted 2026-07-21 mid-validation; Orchestrator-recovered and closed 2026-07-23) |
| **Orchestrator Approved** | yes (2026-07-23) |
| **Build Verified** | yes — `dotnet build` 0 errors ×3 (post-12000, post-nullable-items-fix, final tree) |
| **Test Verified** | yes — 2939 green (see Test Validation Report; 46+6 environmental sheds isolation-cleared per FAIL-002) |

## Sprint Goal
Retrofit Pass 7 (PAT-012, [[typed-api-contract-program]]): drain **bucket C COMPLETE — the final 17-op employee-facing surface** (time-entries 2, absences 1, flex 1, balance 3, compliance 2, overtime 5, skema 3). **Manifest 20→3; typed 117→134; declared body-less 9 unchanged** (no candidates). After this pass only the deliberate residue remains grandfathered: `GET /health` + the 2 standing flag-and-defer ops — **the program's bulk retrofit is COMPLETE.**

**THREE OWNER-RULED WIRE CHANGES (ruled 2026-07-21, each restated with its operational cost; NOT precedent for less-degenerate cases):**
1. **Flex normalization (branch-normalization class, 1st instance):** the flex-balance GET's no-history branch (`{employeeId, balance:0, message}` — omitting `previousBalance/delta/reason`) normalizes to the ONE shape: all fields always present, the 3 history fields null when no history, the vestigial `message` DROPPED (no reader exists). **The named FE companion edit (sanctioned, behavior-preserving): `FlexBalanceCard.tsx:38-47`'s two presence-guards (`!== undefined`) become value-guards (`!= null`)** — the ONLY consumer of the branch difference (Step-4 Reviewer W1); residual risk = a stale-bundle client during the deploy window, on that card only.
2. **Year-overview settlement null-fill (branch-normalization class, 2nd instance):** the empty-config category branch (`BalanceEndpoints.cs:900-908`) gains `settlement: null` (configured rows always emit it — `:1145-1156`); zero consumers read the key today (the FE types omit it — that omission is itself lie-audit scope).
3. **The compliance null→502 guard (dead-branch class, the S118-ruling-#1 lineage — ONE ruling, TWO ops):** `GET /api/compliance/{employeeId}/period` + `GET /api/overtime/{employeeId}/governance` can serve a literal-null 200 ONLY if `ReadFromJsonAsync` yields null — PROVEN defensive dead code (the Rule Engine's evaluators return non-nullable results, `Program.cs:115-132`/`RestPeriodRule.cs:40`/`OvertimeGovernanceRule.cs:36`; a garbled body throws → 500; unavailability already → 503). The guard: null → **502 upstream-invalid**, making the 200 STRUCTURALLY always the full `ComplianceCheckResult` — no top-level-nullable machinery exists or is built (the Step-4 Codex BLOCKER correction: this is NOT an allOf case).

Refinement: `.claude/refinements/REFINEMENT-retrofit-pass7.md` — READY; Step-4 closed 2 cycles both lenses (Codex 1B/2W/3N — the top-level-vs-property misclassification, the per-ruling fallback arithmetic, the enum count/citations; Reviewer 0B/2W/4N — the FlexBalanceCard consumer correction, the pre-agreed split seam, the settlement/approval sibling rules, approval.status onto the declare list, the citation fixes; all absorbed, count-remnant class killed by making the AC count-free). All 3 rulings owner-granted 2026-07-21.

**Explicit exclusions:** error bodies stay untyped (incl. the skema save's 17-site 422/400/409 fan + the row-preferences 422 — runtime-guard narrowing preserved); NO request-class changes (the request-side lie detector expected NOT to fire — fact-verified all `required` members already sent); NO If-Match introduction (all 3 mutations unconditioned today, pinned so); the REFUSED enum sets stay refused (**both overtime compensation vocabularies** — PAYOUT/AFSPADSERING vs AFSPADSERING/UDBETALING, raw strings, inline validation only, NO DB CHECK [init.sql:1293/:1842] — flagged to the owner as a future P6 authority gap; `source` inline literals; skema absence-type strings per S119); the compliance `TimeEntry+VoluntaryUnsocialHours` payload is a rule-engine REQUEST, out of scope; `GET /health` + the 2 flag-and-defer ops untouched — the program residue.

## Entropy Scan Findings (Step 0a) — the fact-sheet digest
| Check | Result | Detail |
|-------|--------|--------|
| Gate baseline | GREEN | 117 typed / 20 grandfathered / 9 declared at `9926bbf` (S119 close `d3fb32c` CI GREEN `29839270400`) |
| The bucket-C cut | exact | manifest 20 − `GET /health` − 2 flag-and-defer = 17; end state 134/3/9 (all rulings granted); fallback arithmetic ruled moot |
| Success shapes | mapped | time-entries POST = 201 `{eventId, streamId}` (`TimeEndpoints.cs:113`); time-entries GET = bare array of the NAMED `TimeEntry` model; absences GET = bare array of NAMED `AbsenceEntry`; flex GET = the ruled ONE shape (5 members, 3 nullable); summary = the large composite (12 scalars + `entitlements[]` 10-member rows + nullable-complex `settlement` + nullable-complex `overtimeBalance`); series = `{employeeId, year, month, series[]}` with nested `points[]`; year-overview = the composite (header/tiles/months[]/categories[] w/ ruled `settlement` always-present); compliance period + governance = `ComplianceCheckResult` (200 structurally non-null post-ruling); compensatory-rest = bare array 7-member rows; overtime balance = 10-member; compensation-choice GET = 4-member (`source` value differs by branch — same keys, NOT polymorphic); choice PUT = 3-member; compensate POST = **the 5-member 200 echo** `{employeeId, periodYear, hours, compensationType, applied}` (both branches shape-uniform); skema month = THE LARGEST composite (~12 sub-records: projects rows ×3 surfaces, absenceTypes, entries, absences, workTime+boundaryWorkTime, dailyNorm, nullable-complex `approval`, rowPreferences, catalogs, consumptionBasis + nullable scalars); skema save = `{saved:int}`; row-preferences PUT = the month GET's rowPreferences shape |
| The allOf-wrapper scope | 4 property members, machinery COVERS list-nesting | `summary.entitlements[].settlement`, `summary.overtimeBalance`, `yearOverview.categories[].settlement`, `skemaMonth.approval`. Reviewer-verified: `ComputeResponseReachableClosure` collects array `items` refs (`ResponseStrictTypesFilter.cs:229-242`) + the matcher recurses into array elements (`SpecRuntimeMatcher.cs:276-278`) — list-nested members ride the EXISTING machinery, no extension. The compliance body is TOP-LEVEL-nullable = ruling #3's guard, never allOf |
| Sibling rules | 3 applications | ONE project-row record (month `projects`/`catalogs`/`rowPreferences` + row-prefs PUT — the S119 4-member shape); ONE settlement-disposition record (summary + year-overview list-nested `settlement` — IDENTICAL 7-key shapes, `BalanceEndpoints.cs:332-341`/`:353-362`/`:1127-1142`); the skema `approval` member (6 keys) sibling-CHECKED against S116 `ApprovalResponses.cs` before minting new |
| Enums (authorities cited) | 6 declare / 3+ refuse | DECLARE: compensatory-rest `status` (init.sql:1337); settlement `state` (init.sql:2918); settlement `reviewDisposition` (the NAMED constraint `vacation_settlements_review_disposition` init.sql:2980–2983 — FORFEIT/DEFER/MODREGNING/WAIVED/FERIEHINDRING; + the legacy convergence :3672–3680); compliance `violationType` (CLR, 6) + `severity` (CLR, 2); skema `approval.status` (init.sql:1103 + the S116 ApprovalResponses `[AllowedValues]` precedent — re-cite). REFUSE: the 2 compensation vocabularies, `source`, absence-type strings |
| P3/P6 fencing | verified ×3 | events built + enqueued unconditionally BEFORE response shaping: time-entries `TimeEndpoints.cs:62-113`; skema save in-tx emits w/ receipt at `:1740`; compensate `OvertimeEndpoints.cs:538-580` (receipt `:582-589`, `LeaderOrAbove` `:590`); row-preferences documented UN-EVENTED (`SkemaEndpoints.cs:1749-1751`). Typing receipts touches ZERO event/outbox/audit construction |
| FE consumers + the lie audit pre-scan | mapped | `useTimeEntries`, `useFlexBalance` (+`FlexBalanceCard` — the ruled guard edit), `useBalanceSummary` (**omits `settlement`** — FE-blind), `useYearOverview` (**omits category `settlement`**), `useCompliance` (**the false-exhaustive `violationType` union: 4 of 6 CLR values — missing OVERTIME_EXCEEDED/OVERTIME_UNAPPROVED**), `useCompensationChoice`, `useSkema` (the 2 sanctioned legacy calls = exactly `SKEMA_MONTH_PATH`/`SKEMA_SAVE_PATH`; nothing else blocks full tier), `api.ts putSkemaRowPreferences`→`SkemaProjectManager.tsx`. **4 ZERO-caller ops** (absences GET, overtime balance + governance GETs, compensate POST) — typed anyway (greenfield precedent). The S119 skema-owned `Project` interface + `SkemaMonthData`+subs + the balance/compliance/choice/flex/time interfaces + unused `AbsenceEntry` all DIE this pass |
| If-Match census | ZERO | no ETag/If-Match anywhere in the 6 endpoint files; all 3 mutations unconditioned — PINNED (the S119 doubly-pinned precedent) |
| Test homes | mapped | NO endpoint contract coverage for any of the 17. Tripwires (UNMODIFIED): the 10 skema suites (`tests/.../Skema/`), the outbox atomic suites (SkemaProjectionAtomic, TimeProjectionAtomic, OvertimeAtomic, the guard suites), `Balance/YearOverviewTests` + `BalanceSeriesTests`. Determinism: the year-overview suites use TimeProvider overrides (`BalanceEndpoints.cs:711` note) — new tests follow the pattern. The settlement-populated allOf branches need REAL settled-year choreography (the S117 settlement suites hold the pattern) |
| The split seam (pre-agreed) | named | if mid-sprint sizing forces a descope: the SKEMA FAMILY severs (month+save+row-prefs+the graduation+the mock re-anchoring) as its own follow-up bucket; the 14-op remainder still closes all 3 rulings; both S116 carve-outs defer WITH it |

## Plan Review (Step 0b)
| Field | Value |
|-------|-------|
| **Trigger** | MANDATORY — 3 owner-ruled wire changes + P3-adjacent (the event-sourced write's receipt) + P6-adjacent (compensate) + the program's heaviest pass (4 stacked maxima) |
| **External Codex** | cycle 1 (2026-07-21): **CLEAN — no findings.** Verified against code/config incl. the year-overview composite transcription, the ruled-fix scoping, the gate math, the carve-out deletion claims |
| **Internal Reviewer** | cycle 1 (2026-07-21): **0 BLOCKER / 2 WARNING / 2 NOTE** — W1 the enum AC lost in plan translation ("untouched" ≠ "provably absent") → a 12000 checkbox (spec-grep, count-free); W2 the mock re-anchoring unverifiable as written → the 12001 mock-edit inventory checkbox (per-file expect counts, reductions justified); N1 the 502-guard stub idiom NAMED (`RuleEngineAuthForwardingTests.cs:33-39` handler replacement — confirmed executable, no Hosting change) + the enum-fidelity criterion split (wire shape via stub round-trip; value sets spec-vs-CLR); N2 the 3-mutations-unconditioned pin promoted to checkbox. ALSO verified: all ruled-fix citations accurate (the FlexBalanceCard guards load-bearing), the 7-key disposition identity, the P3/P6 fence triangulation, Phase-2 disjointness, zero scope creep. Cycle 2: **"Verification clean — the absorption is faithful" — all 4 absorptions verified (W1 stronger in mechanism; N2 strengthened to cover If-None-Match); 0 new findings — CONVERGED (2 cycles, cap honored)** |
| **BLOCKERs resolved before Step 1** | **yes — ZERO BLOCKERs both lenses; all W/N absorbed as task criteria** |

---

### TASK-12000 — Backend: the 17-op drain + the three ruled fixes
| Field | Value |
|-------|-------|
| **ID** | TASK-12000 |
| **Status** | complete (2026-07-21) — **134 typed / 3 grandfathered / 9 declared / 0 stale, exactly the endgame target; build 0 err ×2; drift in-sync (102 paths/187 schemas); sha-idempotent ×2 (`cfc9da6c…`/`d3513bb1…`); tripwires 128/128 UNMODIFIED.** The 3 ruled deltas exact (flex `message` dead + null-fill, no-history branch only; year-overview empty-config rows gain `settlement: null`; the null→502 guards matching the SkemaEndpoints 502 idiom — all 200 paths byte-identical). 12 minted skema records + `ProjectResponse` REUSED (byte-compare evidence ×4 surfaces); ONE `SettlementDispositionInfo` ×3 sites; `SkemaApprovalInfo` minted after the S116 sibling-check (no byte-identical record exists). The 4 allOf members ride the EXISTING machinery (zero filter/matcher changes; list-nesting confirmed covered; `T \| null` in the generated TS). 6 enum sets cited; the refused sets spec-grep-proven absent. P3/P6 fences grep-proven (zero event/outbox/audit hunks; the entire skema in-lock region zero hunks). **ONE DECLARED CHECKSUM DISCREPANCY (the code wins — feeds 12001/12002): the compliance `violationType`/`severity` enums are INTEGERS on the wire** (no `JsonStringEnumConverter` in the HTTP path; spec truthfully emits `type: integer`) — the FE's hand-written string union is wrong-TYPED, so `ComplianceWarnings.tsx`'s `severity === 'VIOLATION'` CAN NEVER MATCH on the real wire = a masked prod-bug-class find for the lie audit; making the wire strings would be an unruled wire change + SharedKernel touch — declared, not fixed. |
| **Agent** | Backend/API (extended into docs/api/openapi.json + generated FE types via regen — cross-domain AUTHORIZED, the standing PAT-012 pipeline pattern) |
| **S120 addendum (2026-07-23)** | **THE NULLABLE-ITEMS SIBLING CLASS FIRED — instance #1 fixed at FIRST firing** (vs the S113 class's deferred escalation — because a runtime pin REDded on it, leaving no honest deferral). The ruling-#2 pin exposed `YearOverviewCategory.saldo` (`IReadOnlyList<decimal?>`, the empty-config all-null 12-array) emitted as plain `items: {type: number}` — Swashbuckle 6.6.2 drops array-ITEM nullability. A second Backend agent extended `ResponseStrictTypesFilter` generically (`ApplyElementNullability`: scalar nullable element → `items.nullable: true`; complex nullable element → the S117 wrapper on Items, defensive; nested arrays descended; fail-conservative) + 5 new filter unit tests (14/14). Spec delta = exactly ONE hunk repo-wide (grep-verified single-member blast radius); sha-idempotent ×2 (`E017B1FB…`); gates held 134/3/9; generated TS `saldo: (number \| null)[]`. **The agent also caught the Orchestrator's task-premise error: the matcher's array-ELEMENT path bypassed null admission** (`AssertScalarKind` never consults `nullable`) — the Orchestrator added element-position null admission in `SpecRuntimeMatcher` (the S113 rule at element position: null admitted iff `items` carries `nullable:true` directly or through the S117 wrapper; non-nullable items keep the existing REDs; small-task exception). The FE's interim `Omit`-widening in `useYearOverview.ts` collapsed to the plain spec types (the declared discrepancy RESOLVED in-sprint at the source). |
| **Components** | TimeEndpoints.cs, BalanceEndpoints.cs, ComplianceEndpoints.cs, OvertimeEndpoints.cs, SkemaEndpoints.cs, Contracts/ (new per-family files), tools/openapi-convention-exempt.txt, regen |
| **KB Refs** | PAT-012 (paved road; sibling rules; the allOf wrapper; the transcription-slip class), PAT-010 |

**Description**: Records as EXACT shape-copies per the fact-sheet digest (the member trees are CHECKSUMS — the code wins, discrepancies DECLARED). The 3 ruled fixes: (1) flex ONE-shape normalization (drop `message`, null-fill the 3 history fields — the no-history branch only; the normal branch byte-identical); (2) year-overview empty-config rows gain `settlement: null` (all other rows byte-identical); (3) the null→502 guard at both compliance sites (the 200 path byte-identical; `.Produces<ComplianceCheckResult>(200)` now honest). The 4 allOf members through the EXISTING wrapper machinery (emission + matcher-resolution per member as ONE change, inner-violation REDs prove each — the S117 rule). The 3 sibling-rule applications (project row ×4 surfaces; settlement disposition ×2; approval sibling-checked vs S116 before minting). The enum sets per the Step-0a Enums row (the row is the single source of truth), cited; the refused sets provably absent from the spec. `.Produces<T>` per op (bare arrays as `IEnumerable<T>`; time-entries create 201; everything else 200). Manifest 20→3 (health + 2 f-a-d survive WITH their cause comments). Regen ×2 sha-idempotent; field-mapping tables per op incl. the full skema tree.

**Validation Criteria**:
- [ ] Build 0 err; convention **134 typed / 3 grandfathered / 9 declared / 0 stale**; drift + freshness green; sha-idempotent ×2
- [ ] Wire-byte identity on every op EXCEPT the ruled deltas: flex no-history branch, year-overview empty-config rows, the null→502 guard — each DELTA-pinned (never byte-asserted); ALL reachable 200 paths byte-identical everywhere
- [ ] The 4 allOf members emitted + matcher-resolved as paired changes; zero handler-logic hunks beyond the 3 ruled fixes; P3/P6: zero event/outbox/audit construction changes (grep-provable); policy strings byte-identical ×17 (incl. `LeaderOrAbove` compensate, self-only row-preferences)
- [ ] **Enums: ALL declared sets per the Step-0a Enums row present in the spec with cited authorities; the refused sets PROVABLY ABSENT (no `enum` member on any compensationType/source/absence-type property in openapi.json — grep the spec, not the C#)** (Step-0b Reviewer W1)
- [ ] The named tripwire suites UNMODIFIED and green (scoped)

---

### TASK-12001 — FE: the 7-hook switch + the skema graduation + the lie audit (depends: 12000)
| Field | Value |
|-------|-------|
| **ID** | TASK-12001 |
| **Status** | complete (2026-07-23) — **agent session-limit-interrupted mid-close (2026-07-21, after its final Writes but BEFORE validation); Orchestrator-recovered and validated 2026-07-23.** tsc 0 / lint 0 / vitest 660/660 (+23). All 7 hooks + api.ts switched; the useSkema GRADUATION landed (FULL tier; both S116 carve-out rules + the partial-tier entry deleted — grep-proven, and the `as`-ban RED-probe FIRED in the graduated file); all 11 hand-written interfaces deleted; the FlexBalanceCard guards flipped `!= null` (test-pinned, new 13-assert component test); the ruled settlement members surfaced additively. **The mock-edit inventory (Orchestrator-reconstructed): 8 of 9 re-anchored files assertion-count IDENTICAL (41/138/69/60/127/58/56/35); ComplianceWarnings.test.tsx GREW 6→14; +2 new files (13 + 54 asserts); ZERO reductions.** One file the agent never reached (`useYearOverview.test.ts` — the stale-year-guard) was Orchestrator-re-anchored (small-task exception; URL-string asserts → typed template + query-object asserts; the behavior pin byte-preserved). **PROD BUG #8 (the integer-enum repair, the declared-discrepancy payoff): `ComplianceWarnings.tsx` compared wire INTEGERS against STRINGS — `severity === 'VIOLATION'` could never match (every item rendered with WARNING styling + the "Advarsel" badge) and the type column rendered raw integers; repaired FE-only via CLR-order constants covering all 6 violation types (the deleted union was false-exhaustive at 4) — the S119 `Project.isActive` FE-only-fix precedent.** |
| **Agent** | UX/Frontend |
| **Components** | useTimeEntries.ts, useFlexBalance.ts, FlexBalanceCard.tsx, useBalanceSummary.ts, useYearOverview.ts, useCompliance.ts, useCompensationChoice.ts, useSkema.ts, api.ts (putSkemaRowPreferences), types.ts, the skema/balance test mocks (re-anchoring), eslint.config.mjs, package.json |
| **KB Refs** | PAT-012 (call-form audit; the partial-tier graduation) |

**Description**: Switch all consuming call sites to the typed forms. **The `useSkema.ts` GRADUATION: both legacy explicit-T calls (month GET, save POST) go typed; the file joins the FULL tier; BOTH S116 carve-out rules (`NO_GET_TYPEARG_RULE_EXCEPT_SKEMA_MONTH`, `NO_BODY_VERB_TYPEARG_RULE_EXCEPT_SKEMA_SAVE`) and the partial-tier entry are DELETED from eslint.config.mjs** — the S116 promise honored. DELETE the hand-written interfaces (`SkemaMonthData`+subs, the S119 skema-owned `Project`, `BalanceSummary`/`EntitlementInfo`/`OvertimeBalanceInfo`, `YearOverview`+5 subs, `ComplianceCheckResult`/`ComplianceViolation` — **the spec enum REPAIRS the false-exhaustive `violationType` union (4→6 values)**, `CompensationChoice`, `FlexBalanceInfo`, `TimeEntry`, the unused `AbsenceEntry`); `settlement` surfaces additively on summary/year-overview types (display-only read side — no consumer branches on absence). **The ruled FlexBalanceCard companion edit: the two presence-guards become `!= null` (behavior-preserving, test-pinned).** Mock re-anchoring in the skema/balance test files (mock-shape edits only; behavior pins must not weaken). Lint tiers: the switched files join FULL; RED-probe every newly-tiered file (scratchpad-copy reverts — NEVER `git checkout` on uncommitted work). Wire pins: the 3 mutations unconditioned; request key sets byte-unchanged; the flex normalization delta-pinned FE-side.

**Validation Criteria**:
- [ ] tsc 0; lint 0 — `useSkema.ts` FULL tier, both S116 carve-outs + the partial-tier entry DELETED (grep-proof); tiers RED-probed; vitest green (delta counted); zero unsanctioned explicit-T/`as`
- [ ] The FlexBalanceCard guards flipped `!= null` + component-test-pinned (no-history renders the zero state, never throws)
- [ ] **The mock-edit inventory delivered: per re-anchored test file, the assertion (`expect`) count BEFORE/AFTER, any reduction individually justified — Step 7a reviews a declared delta, never hunts for silent weakening** (Step-0b Reviewer W2)
- [ ] The lie audit reported: the violationType union repair; the settlement surfacing; every deleted interface's faithfulness verdict or declared discrepancy; the request-side lie detector's non-firing confirmed

---

### TASK-12002 — Test & QA: per-route assertions for the 17 ops (depends: 12000)
| Field | Value |
|-------|-------|
| **ID** | TASK-12002 |
| **Status** | complete (2026-07-23) — **agent session-limit-interrupted 2026-07-21 while its scoped run was still executing; Orchestrator-recovered.** All 23 per-family Docker assertions written; tripwires untouched. First full scoped run (2026-07-23): 21/23 — the 2 REDs both PRODUCTIVE: **(1) a test-fact error** (the month GET's legacy `absenceTypes` asserted to empty under B1 — it is the CATALOG projection, selection-INDEPENDENT by design, SkemaEndpoints.cs:250-256; Orchestrator-corrected to the catalog-identity pin); **(2) a REAL spec lie caught by the ruling-#2 pin — the nullable-ITEMS class fired** (see the S120 addendum under TASK-12000). Post-fix: **23/23 green**, incl. the 3 ruling pins, the 4 allOf both-branch proofs, the null→502 stub proof at both sites, the 3 mutations pinned UNCONDITIONED, and the RED-on-lie proof. |
| **Agent** | Test & QA |
| **Components** | tests/StatsTid.Tests.Regression/Contracts/ (new per-family S120 classes) |
| **KB Refs** | PAT-012 (gate #1), FAIL-002 |

**Description**: Per-route spec≡runtime Docker assertions, per-family classes (~22-26 tests): time-entries (the 201 receipt; the GET array), absences (greenfield), flex (**the ruling-#1 DELTA pins: the no-history branch serves the normalized shape with nulls + NO `message`; the with-history branch byte-faithful**), balance summary + series + year-overview (**the ruling-#2 pin: empty-config rows carry `settlement: null`; the allOf both-branch proofs: each of the 4 members driven NULL and POPULATED via real choreography — the settled-year branches via the S117 settlement choreography pattern**), compliance period + governance (**the ruling-#3 pins: the 200 structurally-full path Docker-proven; the null→502 guard exercised via the ESTABLISHED stub idiom — `RuleEngineAuthForwardingTests.cs:33-39`'s named-client primary-handler replacement via `WithWebHostBuilder` + `HttpClientFactoryOptions` (keeps the agent inside Contracts/, NO factory/Hosting change) — returning a literal-null body, NOT left untested; the 200 structurally-full path for the same 2 ops necessarily ALSO runs against the stub (no rule engine boots in the WAF harness) — honest because the handler round-trips through the CLR `ComplianceCheckResult`; the violationType/severity VALUE-SET fidelity is therefore asserted spec-vs-CLR-enum (or via a stub body enumerating all 6 values through the real CLR contract), never inferred from a canned single-violation body (Step-0b Reviewer N1)**; the 6-value violationType enum fidelity), compensatory-rest (the enum), overtime balance/choice/compensate (the 5-member echo; the `LeaderOrAbove` floor pins), skema month (THE composite — the full tree asserted; the approval member both branches)/save/row-preferences. The 3 mutations asserted PRECONDITION-FREE. Per-op policy pins. TimeProvider determinism per the year-overview pattern. Ordering independence per the S119 rule (per-fact self-creation or single-method chains). Seeds disjoint (S120-prefixed); the named tripwire suites UNMODIFIED. One RED-on-lie proof.

**Validation Criteria**:
- [ ] All new Docker assertions green (exact counts); the 3 ruling pins + the 4 allOf both-branch proofs + the RED proof demonstrated; seed census disjoint; composed Contracts green; the tripwires byte-unchanged
- [ ] **The 3 mutations pinned UNCONDITIONED (no If-Match/If-None-Match — the S119 doubly-pinned precedent)** (Step-0b Reviewer N2)

---

### TASK-12003 — Orchestrator: PAT-012 endgame + validation + Step 7a + close
| Field | Value |
|-------|-------|
| **ID** | TASK-12003 |
| **Status** | complete (2026-07-23) — **Step 7a CONVERGED both lenses (cycle cap 2 honored): External Codex cycle 1 CLEAN — "No findings" (0B/0W/0N, converged at 1 cycle). Internal Reviewer cycle 1: 0B/1W/2N** — W1 the integer-enum name↔value correspondence had NO automated pin anywhere (the spec's integer set is ORDER-invariant → a CLR transposition regenerates byte-identically; the Docker enum test is set-based/order-blind; the vitest pins compare FE constants to FE literals) AND the useCompliance.ts comment claimed a tripwire that didn't exist → absorbed as `ComplianceEnumWireOrderTests` (8 named-value pins + member-count pins) + the corrected comment; N1 the row-preferences docstring overstated a legacy-`projects` assertion → trimmed to ground truth; N2 = the Target-7 catalog-identity confirmation (no action). The Reviewer ALSO verified member-by-member that the shipped repair is correct today (CLR order = the FE constants), all 4 pre-existing null-element RED paths preserved by the matcher extension, the enum scan (6 declared sets present/cited, refused sets absent), and the mock inventory exact. **Cycle 2: "converged — the absorption is faithful", 0 new findings.** For the record: one pre-existing informational (a free-form items schema never REDs on null elements — pre-S120, no such schema in the closure). |
| **Agent** | Orchestrator |
| **Components** | PAT-012, sprint log, INDEX, close gates |
| **KB Refs** | PAT-012, FAIL-002/003 |

**Description**: PAT-012: scope → **134 typed / 3 grandfathered / 9 declared — the bulk retrofit COMPLETE; the residue named** (health + the 2 f-a-d, each with cause); the branch-normalization ruling class recorded (2 instances) + the dead-branch 3rd instance; the lie tally update; the P6 compensation-vocabulary authority gap flagged as a future-sprint candidate; the S116 partial-tier pattern recorded CLOSED (all carve-outs retired). Validation (`sprint-test-validation`). Step 7a dual-lens (named targets: the 3 ruled deltas' pin quality; wire-byte identity on the 13 unchanged ops; the allOf 4-member exercise; the graduation's carve-out deletion; the mock re-anchoring's behavior-pin preservation). Close per the 5 gates; explicit file set; `design_handoff_*` untracked with cause. **If the split seam fired mid-sprint: the descope recorded honestly (the skema family → its own bucket; the S116 carve-outs stay with cause).**

**Validation Criteria**:
- [ ] PAT-012 updated; delta table; Step 7a converged; close + push + CI green all 7 jobs

---

## Test Validation Report (Step 4/5, sprint-test-validation — run 2026-07-23)
| Suite | Previous (S119) | Current | Delta |
|-------|----------|---------|-------|
| Unit | 861 | 868 | +7 (5 `ResponseStrictTypesFilter` nullable-items pins + 2 `ComplianceEnumWireOrderTests` wire-order pins) |
| Regression | 1327 | 1350 | +23 (the 6 S120 per-family contract suites) |
| Smoke | 6 | 6 | 0 (green against the freshly built composed stack) |
| DemoSeed | 55 | 55 | 0 |
| Frontend | 637 | 660 | +23 (FlexBalanceCard.test 5 new + employeeTypedWire.test 14 new + ComplianceWarnings.test 3→7; zero removals) |
| **Total** | **2886** | **2939** | **+53** |

**Run honesty:** the central regression run (1h22m, quiet verbosity) shed 46 — ALL isolation-adjudicated, none S120-caused: 42 = the `ReportingLine` fixed-port class REPRODUCING in isolation with `Failed to connect to 127.0.0.1:5432` (the stack was down per the FAIL-002 teardown rule — this suite family NEEDS a live :5432; the S118 same-42 precedent), cleared **103/103** against a fresh compose Postgres; the 4 stragglers (2 Concurrency, 1 Settlement/SpecialHoliday, 1 Outbox/EmployeeProfile) cleared 4/4 scoped. **ZERO fails in any Contracts suite across S112–S120 — the matcher element-position extension is regression-clean against every prior contract pin.** Smoke's in-chain 0/6 was the same environmental cause (stack down, connection-refused in 4s intervals); 6/6 green post `up -d --build`. Unit re-run green at 868 AFTER the Step-7a absorption (the 2 new pins counted). Frontend re-run green on the FINAL tree (post-absorption comment edits) — the FAIL-003 disk-vs-committed rule.

## Phase Plan
- **Phase 1:** TASK-12000 (one Backend agent; internal sequencing: the 3 ruled fixes + their families first → regen checkpoint → the skema composite last [the largest shape, freshest attention]; the single-agent shape per precedent — Contracts/manifest/regen coupled; a forced split is BY FAMILY with contracts/manifest/regen staying with ONE agent).
- **Phase 2 (parallel):** TASK-12001 (FE) ∥ TASK-12002 (assertions) — file-disjoint (frontend/ vs tests/).
- **Phase 3:** TASK-12003 (docs + validation + close).
- **The pre-agreed seam:** the skema family severs cleanly if sizing forces it (Step-4 Reviewer W2) — a mechanical descope, never improvised.

**Atomicity pin:** ONE close commit; explicit file set; gates at close.

## Legal & Payroll Verification
| Check | Status |
|-------|--------|
| Agreement rule compliance | ACTIVE-WATCH (P4) — the compliance/balance/skema surfaces read rule outputs but this pass changes only response construction + the 3 ruled degenerate-branch fixes; no rule value, evaluation path, or quota logic changes |
| Wage type mapping correctness | N/A — untouched |
| Overtime/payroll (P6) | PASSIVE-WATCH — the compensate POST's event/outbox construction untouched (fenced criterion); the compensation-vocabulary authority gap FLAGGED for a future sprint, not touched |
| Event sourcing / audit (P3) | ACTIVE-WATCH — the time-entries + skema-save receipts typed with ZERO event/outbox/audit hunks (grep-provable criterion) |
| Security (P7) | PASSIVE — no new endpoint; per-op policy pins incl. LeaderOrAbove compensate + self-only row-preferences |
