# SPRINT 76b — Medarbejder administration, Phase 3b: the unified-editor FE

Status: **CLOSED 2026-06-15.** All 4 tasks (7602–7604 + docs) ACCEPTED; Step-7a dual-lens CONVERGED CLEAN; full pyramid green; the recurring `SkemaRowPreferencesPut` flake ROOT-FIXED.
Program: `.claude/plans/PLAN-s74-medarbejder-administration.md` (the 4-phase program). S76b = the FE half of Phase 3 — the unified-editor FE that S76 (Phase 3a, the backend foundation `ee7d8a8`) deferred per the gated split.
Plan + gate: **NO new Step-0b** — S76b is the FE half of the S76 plan, which converged dual-lens on the WHOLE unified-editor (the R3–R5 FE pins + the owner rulings). The R-pins + OWNER RULINGS live in `docs/sprints/SPRINT-76.md` (R3–R5) + `.claude/refinements/REFINEMENT-s76-write-flows.md` (OQ-1=unified/full-re-house, OQ-2=admin-on-behalf-vikar [the API shipped in S76/7601], OQ-4=thin-HR-enhed, OQ-5=accept-honest-403).

## Premise
Build the **unified EditPersonDrawer** — THE single create/edit-everything person surface (owner ruling: full re-house). It SUBSUMES `UserManagement`'s create/edit (name/email/org/agreement + part-time/position/**enhed** + DOB/employment-start/CHILD_SICK), re-using the wiring that already exists in `UserManagement.tsx`, PLUS the new approver assign/remove + the admin-on-behalf vikar (the S76/7601 API) + the delete-with-reassignment dialog — opened from the S75 tree AND from the UserManagement list (whose own create/edit DIALOGS are retired + routed to the drawer; its list stays). Per-section server-role-gated (LocalAdmin ledelseslinje / HROrAbove HR fields; honest 403). The backend is fully present (S74 + S76); this is FE wiring.

## Task decomposition (the S76 plan's deferred FE half)
| Task | Title | Depends on | Review |
|------|-------|-----------|--------|
| **7602** | The EditPersonDrawer SHELL + the STAMDATA re-house — built MODULARLY (shell + per-section field modules + a save-orchestration hook with per-section committed/failed state + authoritative refetch); create + users/employee-profiles/DOB/employment-start/CHILD_SICK PUTs with their EXACT per-endpoint contracts (CHILD_SICK = If-None-Match:*/409/If-Match, NOT 412/428); per-section role-gating + honest-403; create-path HR-field handling; Norm read-only; effectiveFrom=today | — (existing endpoints) | discretionary Step-5a + Step-7a |
| **7603** | The ledelseslinje + vikar + delete sections in the drawer — approver `PersonPickerDialog` (assign/remove via the S74 reporting-line endpoints), the admin-on-behalf vikar create/end (the S76/7601 `/api/admin/reporting-lines/{managerId}/vikar`), the delete-with-reassignment dialog (BOTH the preflight-409 AND the in-lock-census second 409) | 7602 | Step-7a |
| **7604** | Page integration — wire the drawer into the S75 `MedarbejderAdministration` tree (row→edit, Tilføj→create) + the enforcement toggle + the hooks + **retire UserManagement's create/edit DIALOGS, route its "Opret bruger"/row-click to THE drawer** (list stays) + roster-refetch-on-write | 7602, 7603 | Step-7a |
| **docs** | QUALITY/INDEX/ROADMAP/anchors + any FE-pattern note | 7602–7604 | Step-7a |

**Sequencing:** 7602 → 7603 → 7604 → docs (the drawer is one component built up; 7603 adds sections to 7602's shell, 7604 wires it in).

## Step-0b plan-gate
N/A — covered by the S76 plan gate (the unified-editor was gated whole). The per-endpoint contracts + the role model + the UserManagement-dialog retirement are pinned in SPRINT-76.md R3–R5 (Codex c1/c2 absorbed).

## Implementation Record
- **7602 ✅ ACCEPTED 2026-06-14.** The unified `EditPersonDrawer` SHELL + the STAMDATA re-house, built modularly (the kit a11y Drawer shell + `editPerson/` section modules [Stamdata / Profile / Entitlement] + `employeeProfileApi.ts` [extracted from UserManagement, extended with `enhedLabel`] + the `useEditPerson.ts` save-orchestration hook + 14 vitest tests). The multi-PUT save = 5 INDEPENDENT sequential PUTs (users → employee-profiles[+enhed] → DOB → employment-start → CHILD_SICK) each with its own If-Match + per-section committed/failed state + read-your-write re-stamp; **CHILD_SICK's distinct contract ported verbatim** (If-None-Match:* create / 409 race / If-Match update — NOT the uniform 412/428); the 412 staleConflict "Genindlæs" banner; `effectiveFrom`=today; Norm read-only; create hides the HR fields (POST seeds defaults). A `extraSections` SLOT left for 7603 (the ledelseslinje/vikar/delete sections). **Orchestrator caught + fixed a role-gating over-restriction:** the agent set `isHrCapable = {GlobalAdmin, LocalHR}` (excluding LocalAdmin) on the premise that a pure LocalAdmin is 403'd on the HR PUTs — but `StatsTidRoles`: `LocalAdmin=2`, `LocalHR=3`, `IsAtLeast(a,r)=level(a)<=level(r)` → `IsAtLeast(LocalAdmin, LocalHR)=TRUE`, so a LocalAdmin's scope DOES satisfy the S76 B1 LocalHR floor and a LocalAdmin CAN edit the HR fields (the out-of-scope/mixed-role case is the server's honest-403, OQ-5b — not a reason to hide for every admin). Fixed `isHrCapable` → LocalHR-and-above (incl. LocalAdmin); the role-gating test now hides for a genuinely-non-HR LocalLeader + shows for LocalHR/LocalAdmin/GlobalAdmin. Validation: `tsc --noEmit` 0E; `EditPersonDrawer.test.tsx` 14/14; full FE suite **429** (415 + 14). **For 7603/7604 (agent-noted):** `useAdmin.createUser`'s body type omits the optional `approverId` — 7603 extends it to thread the atomic create+approver; `handleStaleRefresh` re-binds the profile snapshot only (adequate for 7602).

- **7603 ✅ ACCEPTED 2026-06-14.** The ledelseslinje (approver) + vikar + delete sections of the drawer (rendered in the 7602 `data-ep-slot="lifecycle"` region via a `LifecycleSections` coordinator): `ApproverSection` ("Godkendes af" — assign/reassign[If-None-Match:* first / If-Match reassign]/remove via the reporting-line endpoints; CREATE-mode picker sets the draft `approverId` threaded into the atomic create POST; root → "Øverste godkendelseslinje"); `VikarSection` (the S76/7601 admin-on-behalf `POST/DELETE .../{managerId}/vikar`, no If-Match, **honest 409[already-active]/400[same-styrelse/coverage/cycle]/404** messages, shown only for a person who approves ≥1 report); `DangerSection` (the delete-with-reassignment — a single `submit(map)` path that **re-renders the gap list on EVERY 409**, handling BOTH the preflight-409 AND the in-lock-census second 409 [N-round, selections persist], honest 400/422/403). New `PersonPickerDialog` (server-search `GET /api/admin/users/search`, debounced, self+descendants forbidden). Hooks: `useReportingLines` +`searchPeople`/`createVikar`/`endVikar`/`deletePersonWithReassignment` (typed 409 gap); `useAdmin.createUser` + `useEditPerson.createPerson` +`approverId`. Orchestrator-verified the both-409 delete contract (correct). **CREATE-FLOW BUG CAUGHT + FIXED end-to-end:** the agent flagged `createUser` omits the **backend-required** `CreateUserRequest.OkVersion` (AdminEndpoints.cs:452) → the create POST would 400 against the real backend (the tests mock it). Root: the org-list GET's `MapOrgResponse` didn't serve `okVersion`, so the FE couldn't derive it. Fixed: (a) backend — `MapOrgResponse` now serves `okVersion = org.OkVersion` (additive read-gap fill); (b) FE — `createUser`/`createPerson` accept `okVersion`, the drawer derives it from the selected org's `okVersion` and sends it. Build 0E (backend) + 0E (FE tsc); FE suite **445** (+16). **NOTE: S76b now carries a 1-line additive BACKEND read change** (the declared `okVersion` org-list gap) — covered by CI regression; a focused "org list serves okVersion" assertion is a nice-to-have at close.

- **7604 ✅ ACCEPTED 2026-06-14.** The integration — the unified `EditPersonDrawer` wired into BOTH entry points + UserManagement's create/edit DIALOGS RETIRED into the drawer (Codex c1-B4: ONE save path). **MedarbejderAdministration (the S75 tree):** a "Tilføj medarbejder" header button → CREATE drawer; each tree/orphan person NAME is now a button → EDIT drawer (the page `fetchUser(employeeId)`s the full `User` on row-click, then mounts the drawer; a tree-derived `lifecycleContext` [approver name / approves-others / active-vikar / self+descendants-forbidden] renders immediately); the **enforcement-mode toggle restored** (PREFERRED↔REQUIRED via `updateTreeSettings` If-Match; the **population-gate 409** surfaces the `unassignedEmployeeIds` honestly; 412 → reload); **roster-refetch-on-write** (drawer `onSaved` → `fetchRoster`). **UserManagement REWRITTEN to list+drawer-only:** removed the create + edit dialog JSX + `handleCreateSubmit`/`handleEditSubmit` + the in-dialog reporting-line/manager section + the DOB/employment-start/CHILD_SICK/profile dialog state + the 412 banner (0 old-dialog markers remain — verified); KEPT the org-selector + the user LIST (Deltid column / profileMap) + `useOrgUsers`; "Opret bruger"/row-click → the SAME drawer; `onSaved` → `fetchUsers` + profile refresh. Validation: tsc 0E; full FE suite **447/447** (UserManagement 8→5 [the deep per-dialog save-contract tests now covered by `EditPersonDrawer.test.tsx`], MedarbejderAdministration 8→13 [+Tilføj/row-edit/enforcement/population-409/refetch]; the dead `ReportingLineTree.test` stays green). **S76b IMPLEMENTATION COMPLETE.**

## The recurring-flake test-isolation fix (S76b)
**FIXED 2026-06-14** — `SkemaRowPreferencesPutTests.PreferenceWrite_IsUnEvented_NoOutboxRow_NoAuditProjectionRow` (the cross-test outbox/audit-contamination flake that recurred at S72/S73/S74/S76 — 5 hits in the S76 close cycle, every docs-only 2nd push flaking it). Root cause: the pin bracketed a **GLOBAL** before/after `COUNT(*)` of `events`/`outbox_events`/`audit_projection`, so a concurrent sibling's insert into CI's SHARED postgres between the two reads tripped the delta. Fix: assert **ZERO rows referencing THIS test's freshly-seeded `emp`** after the un-evented write — `events`/`outbox_events` scoped by `actor_id = emp OR stream_id LIKE '%emp%'`, `audit_projection` by `actor_id = emp` (the PUT is self-only, so a wrongly-emitted row carries actor=emp). Order-independent under the shared DB; a sibling's rows (a different employee) can't interfere. Verified `SkemaRowPreferencesPutTests` 11/11 local. (Commits with the S76b close.)

## Step-7a — sprint-end dual-lens (the unified-editor FE + the additive `okVersion` read + the flake fix)
Scope: the `EditPersonDrawer` + `editPerson/` modules + `useEditPerson` + the tree/UserManagement integration + the additive `okVersion` org-list read + the `SkemaRowPreferencesPut` flake fix. Diff vs HEAD `a87ee70`. Artifacts: `.claude/reviews/SPRINT-76b-step7a-{codex,reviewer}.md`.

- **Reviewer (internal lens): CLEAN.** UserManagement's create/edit genuinely SUBSUMED (one save path via `useEditPerson`, the c1-B4 divergence hazard closed); per-section role-gating consistent with the backend floor (`isHrCapable(LocalAdmin)=true`, no client-trusted authorization); CHILD_SICK's distinct contract + the both-409 delete + the create-`okVersion` derivation all verified against source; the additive `okVersion` read + the scoped flake fix clean. (3 NOTEs, none load-bearing.)
- **Codex (external lens): c1 BLOCKED(4)+1W → c2 CLEAN.** [[review-lens-complementarity]] on the **FE-CONTRACT layer** — the internal CLEAN had asserted "partial-failure honesty is implemented and tested"; the external code-trace found the **multi-write-to-one-versioned-row** contract bug it missed:
  - **BLOCKER — shared `users.version` multi-PUT staleness.** The users PUT + DOB + employment-start all bump the SAME `users.version`, but DOB/employment-start used the dialog-open version → every real multi-field save 412s. → FIXED: a running `usersRowVersion` cursor seeded from the users-PUT re-stamp, threaded users→DOB→employment-start with read-your-write re-stamps; untouched fields skipped (`useEditPerson.ts:219-267`).
  - **BLOCKER — "Genindlæs" didn't reload the user row** (only the profile → the next users PUT 412s again). → FIXED: `handleStaleRefresh` now `fetchUser`s FIRST, re-binds the fresh version/ETag + form, then the profile (`EditPersonDrawer.tsx:287-313`).
  - **BLOCKER — the vikar couldn't hydrate from the UserManagement list** (`activeVikar` came only from the tree context → an away-manager showed no vikar + couldn't revoke from the list). → FIXED: new additive backend `GET /api/admin/reporting-lines/{managerId}/vikar` (floored LocalAdmin scope; repo `GetActiveByApproverWithVikarNameAsync` LEFT-JOINing the stand-in's display_name) + FE `fetchActiveVikar`; `LifecycleSections` self-resolves the vikar when no tree context, re-hydrates after a mutation, context still wins (`ReportingLineEndpoints.cs:2165-2190`, `LifecycleSections.tsx:83-119`).
  - **BLOCKER — green-but-weak save tests** (the mock accepted every PUT independently + never modeled the shared `users.version`, masking the first two). → FIXED: a `setupSharedUsersVersionRouter` modeling the ONE shared version (stale If-Match → 412); the save tests assert the threaded "1"→"2"→"3" chain + the skip-untouched + the stale-refresh re-fetch; **discrimination proven by reverting each fix**.
  - **WARNING — the create `okVersion` derivation was untested.** → the mock org now carries `okVersion` + the create test asserts the POST body carries it.
  - **c2: CLEAN** — all four BLOCKERs + the WARNING confirmed resolved; the new vikar GET uses the floored scope; the shared-version tests discriminate.

## Test Summary
824u + 898r (effective; single run 897/898 + 1 FAIL-002 infra-flake isolation-cleared 13/13) + 6s (fresh greenfield volume, init.sql unchanged from S74) + 452fe = **2180 (+41)**. Baseline from S76 close: 824u + 894r + 6s + 415fe = 2139.

- **Unit:** 824/824 (`dotnet test StatsTid.Tests.Unit`, 1s).
- **Frontend:** 452/452 (`npx vitest run`, 36 files; `tsc --noEmit` 0E). +37 vs S76 (415) — the EditPersonDrawer/section/hook suites + the strengthened shared-version save tests.
- **Regression:** 898 effective (+4 vs S76 — the 4 new admin-on-behalf vikar GET tests in `AdminVikarOnBehalfTests`, 22/22). The single full run reported 897/898 with **1 non-pass — a FAIL-002 testcontainer-start socket-abort** (`Npgsql … connection aborted` in `DockerHarness.StartAsync()` fixture init for `SkemaEntitlementEligibilityGuardTests.GetEligibility_NonHr_And_CrossOrgHr_AreForbidden_InScopeAllowed`; the authorization assertions never ran). **Isolation-cleared:** re-running the whole `SkemaEntitlementEligibilityGuardTests` class passed **13/13** (44s) — confirming an environmental Docker-churn flake, NOT a regression (the S68 precedent: count the run, isolation-clear the shed start). The recurring `SkemaRowPreferencesPut` flake (the S76b root-fix) did NOT recur — verified 11/11 local pre-run + green in the full run.
- **Smoke:** 6/6 (fresh greenfield volume — `docker compose down -v` + `up -d --build` with the S76b backend, init.sql unchanged from S74; no schema delta in S76b).
