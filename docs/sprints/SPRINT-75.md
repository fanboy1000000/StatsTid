# SPRINT 75 — Medarbejder administration, Phase 2: the tree surface (read-mostly FE + the effective-approver projection)

Status: **CLOSED (ready to commit) — all 3 tasks ACCEPTED; Step-7a CONVERGED CLEAN (both lenses); full pyramid green. Awaiting owner commit/push.** 2026-06-14.
Program: `.claude/plans/PLAN-s74-medarbejder-administration.md` (owner-approved 4-phase program; Phase 1 / S74 CLOSED + CI-verified at `de9bd87`). This sprint = **Phase 2 (the tree surface)**.
Refinement: `.claude/refinements/REFINEMENT-s75-tree-surface.md` (READY; converged Codex 3B→CLEAN + Reviewer 2B-absorbed; owner OQ-A=Vikar, OQ-B=read-only-interim).

## Premise

Phase 2 renders the redesigned Medarbejder-administration screen — the derived tree + filter tiles + orphan card — hifi-faithful to the prototype, READ-MOSTLY. **THE CORRECTED MODEL (Step-0b plan-gate, verified against the prototype `data.jsx:7-18`): the tree is STRUCTURAL — keyed on the assigned manager (`approverId` = the person's leder), the raw PRIMARY edges the existing tree read ALREADY serves — and the vikar is a PER-AWAY-MANAGER ANNOTATION** (the `vikar` object lives on the absent manager, e.g. Pernille holds `vikar:{uid:henrik,until,reason}`; her reports KEEP `approverId:"pernille"` — the tree does NOT re-root under the vikar). The refinement's "key on the EFFECTIVE approver" reframe was an OVER-CORRECTION (it conflated approval-ROUTING with the tree SHAPE) — **dissolved: no per-person effective-approver projection, no resolver parity, no provenance.** Phase 2 ships a consolidated read that ENRICHES the structural roster/edges with `enhedLabel??org` + `position` + period-status + the per-away-manager OUTGOING-vikar marker (a direct `manager_vikar` read) + the root/orphan classification, and the FE builds the STRUCTURAL tree over it. Reads-only; no approval-engine/RBAC/schema change. Owner rulings hold: OQ-A the 3rd tile = **Vikar** (counts AWAY-MANAGERS = active vikarieringer — MORE prototype-faithful than the routed-reports framing); OQ-B = **accept the read-only interim** (repoint, no temp route; assign/remove/ACTING/bulk-import/the enforcement population gate return in Phase 3/S76).

## Grounded current state (Explore sweep 2026-06-14)

- The flat tree read `GET /api/admin/reporting-lines/tree/{root}` (`ReportingLineEndpoints.cs:388-432`, LocalAdminOrAbove + `ValidateOrgAccessAsync`) serves raw active `reporting_lines` rows enriched with `employeeId/employeeDisplayName/managerId/managerDisplayName/relationship/effectiveFrom/source/version` — **flat edges; NO effective-approver resolution, NO vikar, NO is_active, NO enhed_label/org**.
- The S74 period-status read `GET .../tree/{root}/period-status` (`AdminEndpoints.cs:1853-1887`) serves `{employees:[{employeeId,displayName,status}], pendingCountByManager}` over the styrelse roster (the roster source for edge-less roots/orphans).
- The resolver `ResolveDesignatedApproverAsync` (`ReportingLineRepository.cs:~769`, S74 vikar-aware) = the effective-approver source; `enhed_label` is served on `/api/admin/users/search` + the profile read (NOT the tree read).
- FE: `ReportingLineTree.tsx` + `useReportingLines.ts` at `admin/ledelseslinjer` (App.tsx:78, LocalAdmin); sidebar "Ledelseslinjer" (Sidebar.tsx:38). The prototype: `ApprovalTree/PersonRow/ApproverControl/OrphanList` + `childrenOf/rootsOf/orphansOf/descendantsOf/depthMap/collapsedForLevel` (keyed on `approverId`=effective approver) + the 3 tiles (Ikke indsendt/Ikke godkendt=managers-with-pending/Vikar). The ui-kit (Card/Badge/Input/Tabs), tokens (0px corners, oes-green, focus-visible), AppLayout, CSS Modules.

## Pinned requirements (R1–R8) — STRUCTURAL-TREE model

- **R1 (the consolidated medarbejder-roster read — the backend):** a NEW read (`GET /api/admin/reporting-lines/tree/{treeRootOrgId}/medarbejdere`) serving the full styrelse ROSTER — per person `{ employeeId, displayName, enhedLabel ?? primaryOrgName, position, structuralApproverId (the person's assigned active PRIMARY manager — the TREE KEY; from the raw edge, NOT a resolver call), periodStatus (OPEN/SUBMITTED/APPROVED), outgoingVikar { vikarUserId, vikarDisplayName, untilDate, reason } | null (the person's OWN active manager_vikar row — present iff THIS person is an away-manager covered by a vikar; drives the badge + the Vikar tile), isRoot, isOrphan }` + the styrelse `pendingCountByManager` (the existing S74 gated tally). LocalAdminOrAbove + `ValidateOrgAccessAsync(treeRootOrgId)`. Reads-only, additive. **NO effective-approver resolution** — the structural manager comes straight from the active PRIMARY `reporting_lines` edge.
- **R2 (composition — no resolver for the TREE; the existing tally for the tile):** compose the read from the structural roster (every styrelse-scoped active user) + their active PRIMARY edge (`structuralApproverId` — a raw-edge join, **NO resolver call** for the tree key/construction) + a LEFT JOIN to `employee_profiles` (`enhedLabel`, `position`) + the period-status (`periodStatus`) + a LEFT JOIN to active `manager_vikar` keyed on the person as `absent_approver_id` (`outgoingVikar`). **The `pendingCountByManager` is the EXISTING S74-7404 gated tally REUSED as-is** — it resolves `ResolveDesignatedApproverAsync` + the authorizer per PENDING employee (`ApprovalPeriodRepository.cs:451`), a BOUNDED subset (only SUBMITTED/EMPLOYEE_APPROVED periods), NOT a new whole-roster resolver loop — that's the existing, accepted computation and stays. So: the no-resolver rule applies to TREE CONSTRUCTION (the `structuralApproverId` roster); the tile reuses the existing bounded tally. One styrelse-bounded read; the effective-approver tree projection is dissolved.
- **R3 (root vs orphan — DETERMINISTIC structural rule):** with no persisted root marker (ADR-027 D9 has none), classify from the structural edges: **`isRoot` = no active PRIMARY approver AND is the `structuralApproverId` of ≥1 other person** (a people-hierarchy top — renders at the tree top); **`isOrphan` = no active PRIMARY approver AND approves NO one** (a legacy no-approver leaf gap — renders in the OrphanList). Multiple roots may exist in messy/Phase-1 trees (ADR-027 D9's ≤1-root is a Phase-4 enforcement concern, not enforced here) — each renders as a tree top. (Replaces the prototype's authored `root` boolean, which the real schema lacks.)
- **R4 (tree correctness + cycle-safety — the FE invariant):** the client tree is `childrenOf(id) = people.filter(p => p.structuralApproverId === id)` (STRUCTURAL — reports stay under their assigned manager, incl. an away-manager whose row carries the vikar badge; NOT re-rooted under the vikar). The ported helpers (`childrenOf/rootsOf/orphansOf/descendantsOf/depthMap/collapsedForLevel`) MUST carry **visited-set guards** — the structural PRIMARY tree is S74-cycle-guarded for ASSIGN, but legacy data and the un-cycle-guarded vikar path mean a defensive guard is required so `descendantsOf`/`depthMap`/the render walk cannot infinite-loop. **FOLLOW-UP (S76/backend): the `/delegate` vikar creation validates same-tree but NOT descendant-exclusion (`ReportingLineEndpoints.cs:1902`) — a subordinate can be their manager's vikar; harden it when the vikar write flows land.**
- **R5 (the FE page):** NEW `pages/admin/MedarbejderAdministration.tsx` + `.module.css` on the ui-kit (Card/Badge/Input + tokens, 0px, focus-visible). Renders: every roster row (the Enhed tag from `enhedLabel??org`; the **vikar badge on the AWAY-MANAGER's row** when `outgoingVikar` is set — "Vikar: <name> til <until>"; the status badge ONLY-when-filtered), the depth rails + collapse, the OrphanList card (`isOrphan`), the 3 tiles (**Ikke indsendt** = count of OPEN-status people; **Ikke godkendt** = count of managers-with-pending [`pendingCountByManager` keys]; **Vikar** = count of people with `outgoingVikar` = active vikarieringer) + their tree-narrowing filters, the level segmented control (`collapsedForLevel`) + search (over name + **position** + enhed). `ApproverControl` is DISPLAY-ONLY (shows the structural manager). Real Danish copy from the prototype.
- **R6 (the hook):** a NEW hook consuming R1 (+ loading/error states, apiFetch); REUSE the existing org/tree-root Styrelse selector (GlobalAdmin cross-styrelse).
- **R7 (route + sidebar swap — NO temp route, OQ-B):** repoint `admin/ledelseslinjer` (App.tsx:78) → the new page; sidebar "Ledelseslinjer" → "Medarbejder administration" (Sidebar.tsx:38); minRole LocalAdmin. The old `ReportingLineTree`/`useReportingLines` code stays in-tree (dead, unrouted) until Phase 4; **its test `ReportingLineTree.test.tsx` stays green-but-dead (kept, harmless — NOT deleted this phase).** The lost write surfaces (assign/remove/ACTING/bulk-import/the enforcement-mode population gate, incl. STY02 already running REQUIRED) are owner-accepted unavailable until S76 — the S76 first-obligation.
- **R8 (the dead CSS):** map the live `.ll-prow/.ll-stats/.ll-levelbar/.ll-orphans/.ll-seg/.ll-orgtag` blocks to CSS-Module classes (tokens, no hardcoded colors); do NOT port the dead `.ll-tbl/.ll-hybrid` blocks.

## Task decomposition

| Task | Title | Agent | Depends on | Review |
|------|-------|-------|-----------|--------|
| **7500** | The consolidated medarbejder-roster read (R1-R3): structural roster + structuralApproverId (raw edge) + enhedLabel/position + status + outgoingVikar (manager_vikar) + isRoot/isOrphan + pendingCountByManager | Approval / Data | — | Step-5a discretionary (reads-only composition; the isRoot rule) |
| **7501** | FE: the hook (R6) + the pure-TS STRUCTURAL tree-derivation helpers (keyed on structuralApproverId) with visited-set cycle guards (R4) + helper unit tests | UX / Frontend | 7500 (the contract) | rides Step-7a |
| **7502** | FE: the page (R5) — the structural tree + 3 tiles (OPEN/managers-pending/Vikar=away-managers) + orphan card + level/search(name+position+enhed)/collapse + the Enhed tag + the away-manager vikar badge + status-when-filtered, hifi on the kit (R8) + the route/sidebar repoint (R7) | UX / Frontend | 7501 | rides Step-7a |

**Sequencing:** 7500 (the read contract) → 7501 (hook + helpers) → 7502 (the page + route). 7501+7502 are both FE and may share an agent/session.

**Reshape note:** the effective-approver projection (the refinement's reframe) is DISSOLVED — verified against the prototype `data.jsx:7-18` (the tree keys on the structural `approverId`; the vikar lives on the away manager as an annotation). This SIMPLIFIES the backend (no resolver projection, no provenance, no parity fixtures — Codex B1 + the resolver-flattening concern moot) and is MORE prototype-faithful (the Vikar tile counts away-managers = "aktive vikarieringer"). The owner OQ-A/OQ-B rulings are unaffected.

## Step-0b plan-gate

**Cycle 1 — Codex BLOCKED(3) + 3W (Reviewer pending).** B1: the projection needs vikar-PROVENANCE which the resolver FLATTENS (vikar + admin-ACTING both return `ApprovalMethod="ACTING_MANAGER"`, `ReportingLineRepository.cs:747`) → 7500 introduces ONE canonical resolution WITH provenance (VIKAR/ADMIN_ACTING/PRIMARY/ESCALATED + vikar detail) used by BOTH the scalar path AND the batch projection — NOT a mirrored SQL CTE (the second-source trap). B2: `isRoot` has NO schema marker (ADR-027 D9: any no-PRIMARY person is a root; existing code infers root=manager-without-own-edge, `TreeSettingsRepository.cs:222/253`) → R3 needs a DETERMINISTIC RULE: root = no-effective-approver AND is-an-effective-approver-of-≥1; orphan = no-effective-approver AND approves-no-one. B3: the EFFECTIVE-approver graph is NOT guaranteed acyclic — vikar creation validates same-tree but NOT descendant-exclusion (`ReportingLineEndpoints.cs:1902`), so a subordinate-as-their-manager's-vikar makes an effective cycle → the ported helpers MUST be cycle-safe (visited guards); flag the backend vikar-cycle-guard gap as an S76/follow-up. W1: expand R4 fixtures (inactive-admin-ACTING fallback, escalation-level ACTING/vikar, depth-10 termination, legacy-orphan-null, + assert vikar provenance/detail not just ID). W2: the batch resolution must cover the pending-tally's per-employee resolver+authorizer calls too (`ApprovalPeriodRepository.cs:451`) preserving the LeaderOrAbove/same-tree gate. W3: add `position` to the roster (the prototype searches name+position+enhed). NOTE: the resolver-PARITY tests belong to BACKEND 7500 (7501 tests TS-derivation vs projection fixtures). **Reviewer BLOCKED(1) — the DEEPEST finding: the prototype's tree is STRUCTURAL (keyed on `approverId`=the assigned manager), the vikar lives on the AWAY-MANAGER (not the routed report); the tile counts away-managers, the badge is on the away-manager's row, the tree shape is UNCHANGED. The refinement's effective-approver reframe was an over-correction for the vikar sub-case** (`data.jsx:7-18`, `tree.jsx:47,67-74,148`, `app.jsx:25,57`). Reviewer W: R2's reuse-model is itself the N-call anti-pattern (don't re-encode as a CTE; the structural model dissolves this); W: isRoot uncomputable without a rule (= Codex B2); W: the enforcement-gate loss (governance, owner-affirmed). NOTEs: R1-consolidation defensible; the repoint surface is 2 sites + a green-but-dead test.

**ABSORBED → THE STRUCTURAL-TREE RESHAPE (verified against the prototype source by the Orchestrator):** the tree keys on the structural `approverId` (the raw PRIMARY edge the tree read already serves); the vikar is a per-away-manager annotation (`outgoingVikar` = the person's own active `manager_vikar` row). DISSOLVES Codex B1 (no effective-approver resolution → no provenance/parity), R2's N-call concern (no resolver loop), and the Reviewer's vikar-tree-shape blocker. RETAINED: B2→R3 the deterministic isRoot/isOrphan rule; B3→R4 cycle-safe helpers + the `/delegate` descendant-guard S76 follow-up; W3→R1 position; the enforcement-gate loss owner-affirmed (R7). **Cycle-2: Codex "all other reshape points are sound", 1 clarity BLOCKER (R2's "no resolver" must except the EXISTING pendingCountByManager bounded tally — clarified inline) → FIXED. STEP-0b CONVERGED 2026-06-14** (Codex c1 BLOCKED(3, effective-approver model) → reshape → c2 1-clarity-fixed; Reviewer c1 BLOCKED(1) = the structural-model finding the reshape implements). Artifacts: `.claude/s75-stepb-codex{,-c2}.log` + the Reviewer transcript.

## Implementation Record

- **7500 ✅ ACCEPTED 2026-06-14.** The consolidated roster read delivered + self-validated (build 0/0 `-warnaserror`; the composition/isRoot-isOrphan/outgoingVikar/contract Codex-CONFIRMED correct per the spec). **Step-5a Codex BLOCKED(2) — BOTH are PRE-EXISTING cross-cutting gaps in the SHARED admin-read scoping (NOT 7500-specific; the period-status/tree/person-search reads share them):** **B1 (mixed-role scope leak):** `LocalAdminOrAbove` requires SOME admin role; `ValidateOrgAccessAsync` (OrgScopeValidator.cs:222-239) grants if ANY scope covers the target WITHOUT checking the scope's role → a LocalAdmin@STY-A + any-role@STY-B reads STY-B's admin roster via the non-admin scope (a real cross-styrelse data-leak; requires a cross-styrelse mixed-role assignment). **B2 (LIKE-metacharacter leak):** the `materialized_path LIKE prefix||'%'` scoping treats `%`/`_` in org IDs as wildcards (latent — depends on org-id content; the seed ids are clean). W: the endpoint success-test didn't assert the cross-styrelse user is absent (only the repo test did).
  - **OWNER RULING 2026-06-14: fix B2 + the no-leak test now; DEFER B1, recorded for S76 pickup.**
  - **B2 FIXED:** both tree-scoped reads (`GetMedarbejderRosterForTreeAsync` :572, `GetPeriodStatusProjectionForTreeAsync` :414 — the two reads that feed the S75 page; identical idiom) now escape the prefix via the existing `EscapeLike` helper + `LIKE … ESCAPE '\'`. Behavior-preserving for metacharacter-free paths (the S74 tests stay green). **The two older sibling sites (`GetPendingByOrgPathAsync`-family :95/:113, a different caller) carry the same idiom → folded into the B1 S76 item** (the consolidated admin-read-scoping hardening).
  - **W FIXED:** `RosterEndpoint_…` now asserts EmpX (cross-styrelse STY05) is absent from the served array; + a new `Roster_PathPrefix_EscapesLikeMetacharacters_NoWildcardOverMatch` repo test (a `/MIN01/STY0_/` prefix → EMPTY roster, would leak the STY02 roster on the pre-fix code → discriminating).
  - **B1 DEFERRED → S76 (recorded, ROADMAP follow-up):** the mixed-role admin-scope leak is cross-cutting (shared `ValidateOrgAccessAsync` + ALL admin-read siblings, incl. the older `LIKE` sites above) → a proper fix = an admin-scope-aware access check (the actor's ADMIN-role scope must cover the target) + literal-prefix path matching everywhere + no-leak tests; designed as its own pre-launch security-hardening task, NOT bolted onto a read-only FE phase. **PRE-LAUNCH security item — must be resolved before launch.**
  - **Validation:** build 0E; `MedarbejderRosterReadTests` 7/7 (6 + the new escape test); sibling Approval/ReportingLine reads unaffected.
  - **NOTE:** Step-5a closed by the owner ruling (the in-scope blocker B2 is fixed + test-proven, build/tests green) rather than burning a Codex cycle re-confirming the deliberately-deferred B1 — consistent with the S74 pre-existing-residual handling.

- **7501 ✅ ACCEPTED 2026-06-14.** The FE data layer (R6 hook + R4 pure structural-tree helpers + vitest). Files: `frontend/src/hooks/useMedarbejderRoster.ts` (the hook + the verbatim 7500 contract types `OutgoingVikar`/`MedarbejderRosterRow`/`MedarbejderRosterResponse`; mirrors `useReportingLines.ts` — stateless `fetchRoster(treeRootOrgId)→ApiResult`), `frontend/src/pages/admin/medarbejderTree.ts` (pure helpers over `structuralApproverId`, consuming the served `isRoot`/`isOrphan`: `indexBy/childrenOf/rootsOf/orphansOf/isManager/descendantsOf/depthMap/collapsedForLevel/defaultCollapsed/queryHit` + the cycle-safe `visibleTreeRows` render-walk), `frontend/src/pages/admin/__tests__/medarbejderTree.test.ts`. **R4 cycle-safety:** `depthMap` + `visibleTreeRows` carry a `visited`-set guard (the prototype's `walk` had none → would infinite-loop on a structural cycle); `descendantsOf` keeps the prototype's `out`-set guard — proven by 3 cycle-fixture tests that TERMINATE (would hang on the unguarded version). Orchestrator-verified both source files (structural semantics correct, served flags consumed not recomputed). Validation: `tsc --noEmit` 0E; targeted vitest 22/22; full FE suite **407/407** (385 baseline + 22). **For 7502 (agent-noted):** imports from `pages/admin/__tests__/` need `../../../hooks/`; `queryHit` expects an already-lowercased `q`.

- **7502 ✅ ACCEPTED 2026-06-14.** The page (R5) + CSS (R8) + the route/sidebar repoint (R7) — READ-ONLY this phase. Files created: `frontend/src/pages/admin/MedarbejderAdministration.tsx` + `.module.css` + `__tests__/MedarbejderAdministration.test.tsx`; modified (repoint only): `App.tsx` (import swap `ReportingLineTree`→`MedarbejderAdministration` + route element; the `RequireRole minRole=LocalAdmin` block unchanged), `Sidebar.tsx:38` (label `Ledelseslinjer`→`Medarbejder administration`; path + minRole kept). The page composes the 7501 hook + helpers: Styrelse selector (`useOrganizations` filtered MINISTRY/STYRELSE, default first, view-state reset on switch); the 3 toggle tiles — **Ikke indsendt** = `periodStatus==='OPEN' && !isOrphan`, **Ikke godkendt** = `Object.keys(pendingCountByManager).length`, **Vikar** = `outgoingVikar!=null` count; the structural tree (depth rails, per-row collapse, level segmented control from `depthMap`, search over name+position+enhed, status badges only-when-filtered, the away-manager vikar line, the Enhed tag); the OrphanList card (`isOrphan`); the matches+ancestor-chain `visibleSet` walk (cycle-guarded). **Every write affordance stripped** (no Tilføj-medarbejder / approver picker / +Vikar / Afslut / record drawer / orphan assign / enforcement toggle / import — owner-accepted OQ-B read-only interim; S76 restores them). Orchestrator-verified: genuinely reads-only (only `fetchRoster` GET), structural semantics faithful, both repoints correct. **`ReportingLineTree.tsx`/`useReportingLines.ts`/its test KEPT green-but-dead** (R7; test imports the component directly → 10/10 still green). Validation: `tsc --noEmit` 0E; full FE suite **414/414** (385 baseline + 22 [7501] + 7 [7502]); new page test 7/7. Agent scope-edge notes (all accepted): native `<select>` mirrors the existing selector (R6); state-reset-on-switch ≡ key-remount; period copy hardcoded literals (no served label this phase); `Card` uses `header` not `title`.

## Step-7a (dual-lens, CONVERGED 2026-06-14)

Scope: the full S75 working-tree change set (the 7500 roster read + B2 fix; the FE 7501 hook/helpers + 7502 page/CSS/repoint). Artifacts: `.claude/reviews/SPRINT-75-step7a-{codex,reviewer}.md` (gitignored); logs `.claude/s75-step7a-codex{,-c2}.log`.

- **Reviewer (internal lens): CLEAN.** Verified the structural model is single-source-clean (no resolver re-encoding; `pendingCountByManager` reuses the S74 gated tally), D2 cross-styrelse bound held identically to the sibling reads (no NEW exposure beyond the deferred B1), R3 isRoot/isOrphan sound, the read-only stripping complete + the old `ReportingLineTree` correctly dead-but-present, and the docs/ROADMAP B1-deferral note accurate. 2 NOTEs (both folded into the Codex-fix below): the cycle tests weren't discriminating; a latent `!isOrphan` badge guard.
- **Codex (external lens): c1 BLOCKED(1) + 2W → c2 CLEAN.**
  - **BLOCKER (`MedarbejderAdministration.tsx`): the orphan card ignored `statusFilter` (narrowed only by search)** → under indsend/godkend/vikar it still showed all orphans (which those tile classifications exclude — the prototype reproduced the wrinkle; its own search×orphan `orphanMatches` accounting implies the card should track the active match set). FIXED: the orphan card now narrows by the SHARED `matchIds` (statusFilter+query) — hides under a filter that excludes orphans, search still narrows by query, "alle" shows all. + a discriminating page test (the "Vikar" filter hides the orphan card AND narrows the tree — fails on pre-fix code) + the defensive `!isOrphan` badge guard (the Reviewer NOTE).
  - **W1: the cycle tests contained no root-reachable cycle** (the guards weren't discriminating) → rewritten to a genuinely root-reachable cycle (a node mis-flagged `isRoot` inside an A↔B cycle — the only shape that exercises the defensive guard, since valid server flags + single-parent edges can't produce a reachable cycle; without the guard `depthMap`/`visibleTreeRows` hang).
  - **W2: the B2 escape test covered only the roster read, not the period-status read (`:414`)** → the escape test now also asserts `GetPeriodStatusProjectionForTreeAsync("/MIN01/STY0_/")` returns empty (the second escaped site).
  - **c2: CLEAN — no findings** (all three resolved, no new defect).

## Test Summary

**Full pyramid GREEN — 824u + 844r + 6s + 415fe = 2089 (+37 over S74's 2052).**
- **Unit 824/824** (unchanged — S75 has no unit-level change; all backend logic is integration-tested).
- **Regression 844/844** — a SINGLE clean run, 0 failures, 44m23s, no FAIL-002 flake (+7: the new `MedarbejderRosterReadTests` — the 7500 roster read composition, isRoot/isOrphan, outgoingVikar, the cross-styrelse no-leak, + the B2 escape on BOTH tree reads). Note: the full 8-container compose stack was up during the run (competing for resources) → ~44m vs S74's ~43m.
- **Smoke 6/6** — fresh greenfield volume (`docker compose down -v` + `up -d --build`), S75-rebuilt images, greenfield `init.sql` revalidated (byte-unchanged from S74 — no S75 schema change), all 8 services healthy.
- **FE 415/415** + `tsc --noEmit` clean (385 S74 baseline + 30: 22 helper [7501] + 7 page [7502] + 1 Step-7a orphan-card discrimination).
- Build: 0E across the solution.

## Follow-ups (recorded → ROADMAP)
- **B1 admin-read-scoping security hardening (PRE-LAUNCH, S76):** the mixed-role scope leak in `OrgScopeValidator.ValidateOrgAccessAsync` (grants via ANY scope without a role check) — cross-cutting across all admin-read siblings, incl. the two older unescaped `LIKE` sites (`ApprovalPeriodRepository.cs:95/:113`). Owner-deferred 2026-06-14.
- **The `/delegate` vikar descendant-cycle-guard (S76):** vikar creation validates same-tree but NOT descendant-exclusion (`ReportingLineEndpoints.cs:1902`) — harden when the S76 vikar write flows land; until then the FE `medarbejderTree` helpers' defensive cycle guards are the safety net (they become load-bearing once write flows can introduce a cycle).
- The phase-3/S76 write flows (assign/picker/+Vikar/Afslut/record-drawer/import/enforcement) — the read-only interim's restoration (OQ-B).
