# SPRINT-73 — the UI-testing fix bundle (rule-engine auth · full-day omsorg/senior · rejected-save honesty · the composed-stack smoke gap)

- **Status:** **CLOSED 2026-06-13** (all 4 tasks + the Step-7a fix-forward chain landed; Step-7a dual-lens terminal: Reviewer CLEAN, Codex CLEAN at cycle 4). Gate trails: refinement (Codex c1 BLOCKED(4)/3W → c2 2-drift → c3 CLEAN; Reviewer BLOCKED(1)/7W/5N; OQ-1 owner-ruled); Step-0b plan (Codex c1 BLOCKED(5) → c2 BLOCKED(3) → c3 CLEAN; Reviewer BLOCKED(1)/4W/4N). Artifacts: `.claude/s73-{refinement,step0b,step5a-7300,step5a-7301,step7a}-codex*.log` + the Reviewer transcripts.

| Field | Value |
|-------|-------|
| **Build Verified** | yes — `dotnet build` 0 errors / 0 warnings on the final tree; `npx tsc --noEmit` clean |
| **Test Verified** | **local full-pyramid GREEN** — 817 unit + **776 regression** (fresh EXCLUSIVE re-run, 0 failures, 40m11s — after a FAIL-002 13-shed run-1, isolation-cleared per protocol, all 13 the 1-ms `DockerHarness.StartAsync()` signature in S73-untouched classes) + **6 smoke** (FRESH greenfield compose volume + rebuilt final-tree images — re-validates the S73 `init.sql` segment-before-seed ordering + the full-day seeds + the CHECK from scratch) + 385 FE (tsc clean) = **1984** (+66 vs S72). CI: watched post-push; URL backfilled close-polish per the S72 convention. |
- **Branch:** master · **Start commit:** `83e789b` (S72 close-polish, CI-green run 27437029244)
- **Opened:** 2026-06-13
- **Refinement:** `.claude/refinements/REFINEMENT-s73-ui-testing-fix-bundle.md` (v3 READY — the verification evidence, owner ruling, and full absorption trail live there; THIS file pins the implementation contract)
- **Origin:** owner UI testing of the S72 redesign (2026-06-13) + Orchestrator API probes. The probes proved: the >norm cap HOLDS server-side (422), the observed "registered" value was the optimistic cell; valid registrations 503 in compose (the backend→rule-engine calls carry no bearer → 401); the full-day rule does not exist.

## Sprint Goal

Four fixes, smallest-correct-shape each:
1. **Service-call auth (the severe one):** the Backend's rule-engine calls forward the incoming bearer — quota validation and compliance warnings work in the composed stack again.
2. **The full-day rule (owner product ruling 2026-06-13):** CARE_DAY + SENIOR_DAY registrations must equal exactly the day's ADR-032 consumption basis; config-driven, evented, version-survivable.
3. **Rejected-save honesty:** a 422 reverts the cell to server truth (with the overlap guard); credential/transient failures keep the retry posture.
4. **The smoke gap:** an absence-registration + compliance probe in the composed stack (5→6) — the test that would have caught the 401/503.

## Owner ruling (refinement gate, 2026-06-13)

- **D-A · CARE_DAY + SENIOR_DAY are FULL-DAY-ONLY** — an owner PRODUCT ruling (the agreement sources establish quotas, not day-shape; the handoff's "hele dage" marks + this ruling are the authority — a provenance row goes into the agreement-source register at close). CHILD_SICK stays hours-based. **The ratified arithmetic consequence:** the existing per-day cap totals ALL absence types, so a full omsorgsdag/seniordag is the day's ONLY absence; same-day work hours remain legal.

## Pinned rules

**R1a — Handler scoping (Step-0b Reviewer W1).** The DelegatingHandler attaches ONLY to the NAMED rule-engine client — never the default client builder; pinned NEGATIVELY: a non-rule-engine outbound client carries NO forwarded bearer (a global registration would leak user bearers to every outbound host). TASK-7300 gets a per-task Step-5a Codex review (P7 — it rewires the authentication carriage of four cross-service families, the same wiring-drift class that caused the incident).

**R1 — The auth handler (ONE mechanism).** A named rule-engine HttpClient + DelegatingHandler forwarding the current request's `Authorization` AND `X-Correlation-Id` (the `WeeklyCalculationPipeline.cs:50` precedent; P3 cross-hop traces). ALL FOUR Backend call families migrate onto it: validate-entitlement ×2 (`SkemaEndpoints.cs:747/:887`), check-compliance (`ComplianceEndpoints.cs:72`), check-overtime-governance (`OvertimeEndpoints.cs:105` — its ad-hoc forwarder is REPLACED; two coexisting mechanisms would recreate the drift; the working site must not regress). The rule engine is UNTOUCHED (no endpoint relaxed). The established partition documented in-code: FORWARD when an actor exists; MINT when no HttpContext exists (the Payroll `HttpRuleClassificationProvider` precedent, S20) — the first BACKEND background caller follows the minting precedent. Pin: all four families carry bearer + correlation (test-handler capture).

**R2 — The full-day rule (config-driven, basis-exact, two-site).** `entitlement_configs.full_day_only BOOLEAN NOT NULL DEFAULT FALSE` (inline + legacy-guarded ALTER, the S72 segment precedent) + type-keyed idempotent seed UPDATE for CARE_DAY + SENIOR_DAY across all agreement/OK pairs. **The flag threads the FULL config surface** (repository read/create/update/supersession SQL; admin POST/PUT request/response DTOs; **the ACTUAL event family — `EntitlementConfigCreated` + `EntitlementConfigSuperseded` (Step-0b B1 correction: there is NO `EntitlementConfigUpdated`; same-day updates emit Created)** — additive-nullable; the TEST-PARSED audit catalog rows): version-survival is PINNED (an admin PUT editing an unrelated field produces a successor whose flag persists — the S68-B1 uniform-by-construction lesson). **The version-survival contract extends to the ADMIN FRONTEND (Step-0b B2): `useEntitlementConfig.ts` + `EntitlementConfigEditor.tsx` PUT the full config shape and MUST carry `fullDayOnly` round-trip (owned by 7302), with the admin-UI round-trip pin (edit an unrelated field via the EDITOR → the flag persists).** **Construction-enforcement (Step-0b Reviewer W2 — the FULL S68-B1 lesson, proposed-not-asked; owner may override):** the D-A ruling is a PRODUCT RULE, not a default — a type-conditional DB CHECK + an admin POST/PUT 422 guard enforce that CARE_DAY/SENIOR_DAY config rows carry `full_day_only = TRUE` (an admin cannot silently un-rule it; flipping it later is a deliberate schema/owner change, mirroring the S68 `reset_month` enforcement). **Ordering pins (Step-0b c2): the GREENFIELD seed INSERTs carry `full_day_only = TRUE` inline (the CHECK must never reject init.sql's own seeds); the LEGACY DO-block orders backfill-UPDATE BEFORE ADD CONSTRAINT (remediate-then-constrain, the S68 precedent); double-apply idempotent on both paths.** The guard: for a full_day_only type, the day's hours for that type = **the ADR-032 consumption basis EXACTLY (2-dec) — `ConsumptionCalculator.fullDayHours` INCLUDING the deliberate ANNUAL_ACTIVITY `7.4 × fraction` fallback (academics KEEP their omsorgsdage; fail-closing them out was rejected)** — else typed 422 `absence_full_day_only` carrying the required value. TWO-site like its D3 sibling: the IN-LOCK site REQUIRED (the S70 B1 lesson), the pre-tx mirror consistent. The flag reads DATED per-row at the absence date (the SENIOR_DAY `GetByTypeAtAsync` anchor precedent, P4). Zero-norm days stay covered by the existing 422.

**R3 — Served contract for the FE (PINNED here, not task-designed — Step-0b Reviewer B1).** The month GET serves `fullDayOnly` on the absence-type DTOs (catalog AND visible rows) AND a **PER-DAY dated array `consumptionBasis: [{date, hours|null}]`** — the ADR-032 consumption basis for every day of the month (per-weekday norms vary within a month; the academic fallback included; null where no dated profile covers), derived from THE SAME `ConsumptionCalculator` path the guard demands. **The served==guard IDENTITY is pinned with a test driving both surfaces from one fixture** (the S72-B1 cross-surface-drift class). Neither `dailyNorm` (the display basis, null for academics) nor the D-A `fullDayNormAtMonthEnd` scalar is overloaded. The FE snap reads `consumptionBasis[date]`; never a hardcoded list, never client norm math. (Pinning this in-plan also means a 7301 session-limit death cannot strand 7302's contract.)

**R4 — Rejected-save policy (the explicit split).** Revert-set = **exactly {422}**: the affected cells revert to server truth + the alert — guarded by the c3 overlap shape (revert ONLY when the cell still holds the value the rejected save SENT; a newer edit owns it otherwise). **Everything else non-2xx (400/401/403/404/409/5xx/network) keeps local edits + the S72 B2 re-queue/retry posture + the alert** (401/403 are credential-shaped — a mid-edit session expiry never discards typed cells). **The flush contract: a 422-reverted delta counts RESOLVED** (dropped + reverted leaves nothing stale → preference PUTs proceed); non-422 keeps returning flush-false. Implemented INSIDE `fireCellSave`'s failure handler; the existing B2/c2/c3 pins mock 500-shaped failures and stay green unchanged. The new `absence_full_day_only` 422 body (and the `absence_type_not_eligible` family) map into the FE error surface (the quota parser today only matches the quota shape).

**R5 — FE snap.** Any entry in a full-day cell SNAPS to the served `consumptionBasis[date]` value; **when that value is NULL (no dated profile covers the day) there is NO snap and NO invented value — the typed entry stands locally and the SERVER rejects through the existing anchor-422 family (fail-closed server-side; pinned)**; blank stays blank; the "hele dage" notes render from the served flag (the panel + modal included); the prefill behavior for non-full-day types is UNCHANGED (ADR-032 D3). Ferie partial-below-norm behavior is pinned UNCHANGED.

**R6 — Smoke (5→6) + the standing principle.** One composed-stack test: a valid partial ferie registration persists (200 + projection row + the quota hop executed — **the ferie-200 leg IS the hop proof: the incident-verified fail-closed 503 mapping means 200 ⇒ the rule engine answered 2xx; the partial-CARE_DAY 422 leg is backend-local and does NOT discriminate the hop — stated so the test is never misread or weakened by leg reordering (Reviewer N4)**) + a compliance-endpoint probe (non-503 — the SILENT half of the incident). Re-run posture pinned: the skema save is upsert-shaped, so same-payload re-runs are idempotent — the test asserts accordingly (plus unique-date keying), green twice consecutively. **The standing principle's venues NAMED (Reviewer W4): WORKFLOW.md gets the every-cross-service-hop-gets-a-composed-probe principle (Orchestrator-written at close); ROADMAP gets the check-overtime-governance composed-unproved follow-up row by name.** The close smoke runs on a FRESH volume per the standing schema-sprint protocol (init.sql changes this sprint), which also disposes of the 2026-06-13 probe/test residue.

**R7 — Regression-fixture changes are behavior-change-cited.** Existing tests registering partial care/senior days update against THIS sprint's rules (cite the refinement — legitimate behavior change, not laundering; census at task time).

## Task decomposition

Dependency order: **7300 → 7301 → 7302 → 7303**. Tests woven per task.

| Task | Agent | Scope (single-owner) | Acceptance (sketch) |
|------|-------|---------------------|---------------------|
| **TASK-7300** | Backend API **[Step-5a Codex — P7 auth carriage, R1a]** | The R1 handler + the 4-family migration (SkemaEndpoints, ComplianceEndpoints, OvertimeEndpoints call sites) + `Program.cs` NAMED-client registration | the 4-family bearer+correlation pins; **the R1a negative pin (no bearer on non-rule-engine clients; named-client-only registration)**; OvertimeEndpoints behavior byte-unchanged; rule engine untouched |
| **TASK-7301** | Backend API + Data Model (declared init.sql authorization) **[Step-5a Codex per R18-style discretion: evented config + legal rule + schema]** | init.sql column/ALTER/seeds; the R2 config-surface threading (repo/DTOs/the Created+Superseded events/catalog rows — catalog edits Orchestrator-written); the construction-enforcement CHECK + admin guard (R2); the two-site guard; the R3 served fields (`fullDayOnly` on BOTH DTOs + the per-day `consumptionBasis` array); db-schema regen + catalog rows by Orchestrator (sequenced as in prior sprints — the event-payload extension fails the TEST-PARSED catalog until the rows land) | BACKEND version-survival pin; **the R3 served==guard identity pin + both-surfaces field pins**; the construction-enforcement pins (admin POST/PUT of a CARE_DAY/SENIOR_DAY config with the flag false/absent → 422; the CHECK on legacy paths); **migration double-apply idempotence + EXHAUSTIVE seed assertions (CARE_DAY + SENIOR_DAY × ALL agreement/OK pairs — Step-0b B3)**; the guard matrix (exact/under/over/academic-fallback/zero-norm/dated-read/**the D-A pins: a full-day type + ANY other same-day absence 422s; same-day WORK hours stay legal**); 422 shape pin; R3 fields pinned on BOTH surfaces; existing suites green per R7 census (refinement-citation REQUIRED in every fixture change) |
| **TASK-7302** | UX | `SkemaGrid` full-day snap + the served-flag notes (panel/modal) + the R4 revert/retry split in `SkemaPage`/`useSkema` + the error-surface mapping + **the admin-editor `fullDayOnly` round-trip (`useEntitlementConfig.ts`/`EntitlementConfigEditor.tsx` — Step-0b B2)** | snap pins (incl. the academic served-basis case + **R5: blank-stays-blank; the panel/modal notes from the served flag; non-full-day prefill UNCHANGED**); the R4 matrix (422-revert w/ overlap guard; 401-keeps; flush-RESOLVED-on-422; B2/c2/c3 pins green); ferie-unchanged pin; the ADMIN-UI round-trip pin |
| **TASK-7303** | Test/QA | The R6 smoke test (tests/StatsTid.Tests.Smoke) — **SELF-ISOLATING (Step-0b B4): unique per-run keys/dates + idempotent assertions so reruns on a lived-in dev stack never collide** | smoke 6/6 green on the composed stack, twice consecutively (the rerun proof) |

**Orchestrator close obligations (named here per Step-0b W1/B4):** the agreement-source-register D-A provenance row; the standing smoke principle into **WORKFLOW.md** + the check-overtime-governance composed-unproved follow-up as a **ROADMAP row by name** (the R6-pinned venues); the dev-volume reset (`docker compose down -v` + reseed) which also clears the emp005 probe rows — the owner is testing on the current volume, so the reset happens AT CLOSE, not before, and is announced.

## Step 0 — KB consult

ADR-032 (D3 basis incl. the academic fallback; D4 lock) · ADR-021 (config append-only versioning) · ADR-029 (senior age gate — the full-day rule composes with, never replaces, eligibility) · ADR-002 (rule-engine purity — why the guard lives in the save path) · SECURITY.md (shared-secret JWT) · the S68-B1 lesson (uniform-by-construction) · the S70 B1 lesson (in-lock re-evaluation) · the S72 Step-7a B2/c2/c3 chain (the flush machinery this touches) · [[cross-process-caller-census]].

## Implementation Record

- **TASK-7300 ✅ ACCEPTED (2026-06-13; Step-5a Codex CLEAN first pass, zero findings):** NEW `Http/RuleEngineClient.cs` (the named-client constants; the pre-S73 inline fallback preserved verbatim) + `Http/RuleEngineHeaderForwardingHandler.cs` (Authorization + X-Correlation-Id from the ambient `IHttpContextAccessor`; absent/empty ⇒ no header; caller-set wins; no-HttpContext ⇒ forwards nothing — the partition documented in-class w/ the S20 Payroll precedent); Program.cs +19 (the handler registered ONLY on the named builder — R1a); the 4-family migration (SkemaEndpoints ×2, ComplianceEndpoints, OvertimeEndpoints **w/ its ad-hoc forwarder DELETED** — one mechanism; the dead `IConfiguration` params removed); 7 test fixtures gained the stub BaseAddress (the relative-URI cutover; each cited). NEW `Hosting/RuleEngineAuthForwardingTests` (7: the 4 positive family pins incl. the payload-discriminated senior site; the R1a DI-level negative pin w/ the named client as in-test positive control; absent-correlation omission; the no-ambient-context posture). Validation: build 0E; 144 green across the new + scoped suites. **LIVE-PROVEN in compose (Orchestrator): the backend-api image rebuilt + restarted — both incident probes flipped 503→200 (`VACATION 3h` saved; `CARE_DAY 3.7h` saved — the latter correctly PENDING the 7301 rule); the owner's testing session unblocked.** Codex: "no endpoint egress path bypasses or misuses the named client."
- **TASK-7301 ✅ ACCEPTED (2026-06-13; Step-5a Codex BLOCKED(4)→fix-forward→cycle-2 CLEAN):** the FULL-DAY-ONLY rule for CARE_DAY + SENIOR_DAY. **Schema:** `entitlement_configs.full_day_only` + the named type-conditional CHECK (construction-enforcement, R2) + 20 CARE/SENIOR seeds TRUE inline / 30 others FALSE; marker-fenced S73 segment (deviation ACCEPTED: placed BEFORE the seed INSERT, not at-EOF — on a legacy DB the seed references the new column, so column-first ordering is required; documented in-file); legacy block remediate-then-constrain. **Config surface (the full evented thread):** repository read/insert/supersession; BOTH admin write surfaces guarded (`EntitlementConfigEndpoints` AND — agent-caught — `AgreementEntitlementEndpoints`, else a 500-via-CHECK hole; the FullDayOnlyGuard 422s flag-false/absent for the two types); the boot seeder threaded (CHECK evaluates pre-ON-CONFLICT); `EntitlementConfigCreated`/`Superseded` payloads + both audit mappers project the field (M3); the catalog doc rows updated (Orchestrator). **Guard:** TWO-site (pre-tx mirror + the REQUIRED in-lock, S70 B1); dated per-row at the absence date; the ADR-032 consumption basis EXACTLY incl. the academic 7.4×fraction fallback (academics keep omsorgsdage); typed `absence_full_day_only` 422. **R3 served contract:** `fullDayOnly` on BOTH absence-DTO surfaces + the per-day `consumptionBasis:[{date,hours|null}]` array from the SAME ConsumptionCalculator path + the served==guard identity test. **Step-5a cycle-1 BLOCKED(4) — all Orchestrator-verified REAL:** H1 (the rounded full-day value 2.48 unbookable under the RAW D3 cap 2.479 — a self-defeating rule; the cap was the ADR-032-D1-inconsistent side → aligned to RoundBasis), H2 (the guard/served reads used live `user.OkVersion` not `ResolveVersion(absence.Date)` — wrong config across the OK24→OK26 boundary; the emission path 700 lines away already did it right), M3 (mappers didn't project the legal field), M4 (test gaps: 2nd admin surface, rounded-academic, cross-OK, mapper). **Both HIGHs were invisible to the 19 green guard tests because every fixture used fraction 0.5 [rounding inert] and OK24 [no version skew]** — the adversarial lens found them, not the suite. Fix-forward: first attempt died at a session limit zero-edits (diff-verified), retry delivered fully + honored the scoping guard (the PRE-EXISTING quota reads `:1043/:1061` + SENIOR_DAY eligibility `:928` + min_age catalog `:1929` share the same `user.OkVersion` temporal inconsistency but predate 7301 — left untouched, **recorded as a follow-up**). Cycle-2 CLEAN. Validation: build 0E; the served fields anchor matched to `dailyNorm`/`consumptionBasis` (DailyNormCalculator resolves OK per-day via ResolveVersion, evidence-backed); unit 29 + scoped regression 148 + catalog-parity 12 green; db-schema regen (64 tables) + check_docs green. **Served shapes for 7302 (verbatim):** `absenceTypes:[{type,label,fullDayOnly}]`, `catalogs.absenceTypes:[{type,label,fullDayOnly}]`, `rowPreferences.absenceTypes:[{type,label,fullDayOnly,sortOrder}]` (+ the PUT response), `consumptionBasis:[{date:"yyyy-MM-dd",hours:number|null}]`; guard 422 `{error:"absence_full_day_only",absenceType,date,requiredHours,message}`.
- **TASK-7302 ✅ ACCEPTED (2026-06-13; FE, rides Step-7a):** types.ts additive (`fullDayOnly?` on all 3 absence-DTO surfaces + `consumptionBasis`); the full-day SNAP in SkemaGrid (commit→served `consumptionBasis[date].hours`; **null-basis = NO snap, the typed value stands, server anchor-422 rejects — R5**; blank-stays-blank; non-full-day D3 prefill UNCHANGED; the "hele dage" note from the served flag — grid + modal, NOT the panel [deviation ACCEPTED: SkemaDayPanel has no absence section per R11, so no absence row to host the note — not forced]); the **R4 split inside `fireCellSave`**: revert-set = exactly 422 (revert to `serverCellsRef` truth, GUARDED by the S72-c3 overlap shape — revert only when the cell still holds the SENT value) + the new `absence_full_day_only`/`absence_type_not_eligible` 422 bodies mapped to Danish alerts; non-422 (incl. 401/403 credential-shaped) keeps the B2 retry posture; the flush counts a 422-revert RESOLVED (preference PUTs proceed) while non-422 keeps flush-false — `saveMonth` return widened `boolean`→`SaveMonthResult`, both callers updated; the admin-editor round-trip (`useEntitlementConfig` + `EntitlementConfigEditor` carry `fullDayOnly` on POST/PUT — an unrelated edit preserves it, pinned). **Validation: FE 360→381 (+21; 31 files) + tsc clean — Orchestrator-independent re-run.** S72 B2/c2/c3 + ferie partial-day pins green unchanged.
- **TASK-7303 ✅ ACCEPTED (2026-06-13):** the composed-stack smoke test `Backend_RuleEngineHop_AbsenceAndCompliance_ComposedStack` (suite 5→6) — three R6 assertions on seeded emp001: (1) the HOP PROOF — a partial VACATION 3.0h save → 200 + retrievable (the incident's fail-closed 503 mapping makes 200 ⇒ the rule engine answered 2xx through the forwarded bearer; the hop-discriminating leg); (2) a partial CARE_DAY 3.7h → 422 `absence_full_day_only` (backend-local, non-discriminating, commented as such); (3) the COMPLIANCE hop (the SILENT half) — `GET /api/compliance/emp001/period` → NOT 503. **Isolation finding (agent-caught, R6 refined): the absence save is NOT an upsert** — `AbsenceProjectionRepository.InsertAsync` appends a projection row per save (fresh event_id) + accumulates quota; the R6 "upsert-idempotent" assumption was WRONG, so the test uses unique-future-weekday isolation (anchored 2099, a fresh 25-day ferieår quota; ~0.4 days/run, green across many reruns). **Validation: 6/6 green TWICE consecutively (3 runs total) on the live composed stack; the running backend-api image already carried 7301 (the 422 leg passed live).** Only the smoke test file touched.

## Test Summary

| Suite | Count | Status |
|-------|-------|--------|
| Unit tests | 817 | all passing (+5: 7301's audit-mapper + default-config tests) |
| Regression tests | 776 | **fresh EXCLUSIVE re-run GREEN (0 failures, 40m11s)** (+35: 7300 auth-forwarding 7, 7301 full-day guard/migration/admin ~22 + the Step-7a B1 pins, 7302/Step-7a FE-adjacent backend none, fixture-cutover net). Run-1 shed 13 (the FAIL-002 1-ms `DockerHarness.StartAsync()` signature — the 8-container smoke stack was the churn; isolation-cleared on the postgres-only exclusive re-run) |
| Smoke tests | 6 | GREEN on a FRESH greenfield compose volume + rebuilt final-tree images (+1: `Backend_RuleEngineHop_AbsenceAndCompliance_ComposedStack` — the composed-stack hop probe; re-validates the S73 greenfield `init.sql`) |
| Frontend | 385 | all passing, 31 files, tsc clean (+25 vs S72's 360: 7302 snap/revert/admin-roundtrip + the Step-7a B2 work-time-422 + the c2 stale-revert pins) |
| **Total** | **1984** | **+66 vs S72 (1918 + 66 = 1984 ✓)** |

Baseline (S72): 812u + 741r + 5s + 360fe = 1918. FAIL-002 adjudication: ONE occurrence at the regression run-1 (13 shed starts, signature-exact, S73-untouched classes, isolation-cleared 776/776 on the exclusive re-run).

## Step-7a — sprint-end dual-lens (2026-06-13): Reviewer CLEAN (1 NOTE) · Codex CLEAN at cycle 4 (3B → 3B → 1B-halt → CLEAN)

Artifacts: `.claude/reviews/SPRINT-73-step7a-{codex,reviewer}.md` (both `reviewed-against-commit: 83e789b`); logs `.claude/s73-step7a-codex{,-c2,-c3,-c4}.log`.

**The dual-lens earned its keep dramatically — SIX genuine defects on a "simple" fix bundle, two only the adversarial lens caught:**
- **Internal Reviewer: CLEAN (0B/0W/1N)** — full checklist verified (P7 auth/named-client scoping; P2 save-path placement + config-driven + two-site + in-lock S70-B1; the S68-B1 construction-enforcement genuinely closed across BOTH admin surfaces; P3 additive-replay-safe; P4 the H2 fix + the deferred pre-existing reads correctly scoped). NOTE = the close obligations (pending, correctly close-time-scoped). **Its `served==guard` trace declared the consumption path clean — the [[review-lens-complementarity]] lesson: the structural trace missed `guard != consumption`.**
- **Codex cycle 1 BLOCKED(3):** **B1** a full-day booking consumed 1.0004 days (guard required `RoundBasis(2dp)`, `ToFeriedage` divided by RAW norm → 2 bookings breach the 2-day quota; the H1 fix-forward saw it and dropped the `==1.0` pin) · **B2** `fireWorkTimeSave` treated a work-time 422 as resolved without revert (silent discard) · **B3** smoke cycled 10 fixed dates + shared one quota. → fix-forward: full-day rows divide by the rounded basis (1.0 exact, partials byte-identical); work-time mirrors the cell 422-revert; unique far-future year per run.
- **Codex cycle 2 BLOCKED(3) — deeper edges:** **B1 TOCTOU** (index set + guard read the GLOBAL config separately under READ COMMITTED → a config-PUT race re-opens the divergence — the S70 B1 lesson) → the in-lock guard REUSES the one `authoritativeFullDayOnly` snapshot · **B2 stale revert** (server-truth refs advanced only on load → a 422 after a good save reverted to the original; the cell path shared it) → advance the refs on each successful save, overlap-guarded · **B3 birthday collision** (Guid-hash-mod-800) → monotonic ticks-year.
- **Codex cycle 3 BLOCKED(1) — halt-and-prompt:** B1-TOCTOU + B2-stale-revert VERIFIED RESOLVED; new residual: the cycle-2 "monotonic year" was `ticks/TicksPerSecond % 6900` → repeats every ~6900 SECONDS not years. Per the cap discipline the halt fired; **owner chose "fix it (one cycle)"** → Orchestrator one-liner `ticks/TicksPerDay % 6900` (~19-year repeat; same-day reruns harmless).
- **Codex cycle 4 (scoped to the smoke math) CLEAN.**

**Final post-fix validation:** build 0E/0W; scoped backend `SkemaFullDayOnly|Adr032Consumption` 29/29; FE 385 (+4 over 7302's 381: the B2 work-time-422 + the c2 work-time/cell stale-revert pins) + tsc clean; smoke 6/6 ×2 composed-stack. **Backend-api image is STALE after the B1/F1 source changes — the Orchestrator rebuilds it at the close-time fresh-volume run (the smoke's full-day leg is a guard-rejected partial CARE_DAY, a path the B1 fix does not change, so the ×2 proof stands against the stale image).**

## Step-7a fix-forward record (Implementation Record addendum)

- **Cycle-1 fixes:** B1 → `ConsumptionCalculator.ComputeAsync` optional `fullDayOnlyRowIndices` (those rows divide by `RoundBasisTwoDp`; the global `ToFeriedage` raw-norm path untouched for partials) + `SkemaEndpoints.RoundBasis` delegates to the shared `RoundBasisTwoDp` + `ResolveFullDayOnlyIndicesAsync` threaded into BOTH ComputeAsync calls; restored the `feriedage==1.0` pin + a 2-bookings-no-quota-breach pin. B2 → `fireWorkTimeSave` mirrors `fireCellSave`'s overlap-guarded 422-revert. B3 → unique far-future year.
- **Cycle-2 fixes:** B1-TOCTOU → the in-lock guard reuses `authoritativeFullDayOnly` (deleted its second `GetByTypeAtAsync`; one config snapshot). B2-stale → server-truth refs (work-time + cell) advance on each successful save, overlap-guarded; +2 pins. B3 → ticks-monotonic year.
- **Cycle-3 fix (Orchestrator one-liner):** `TicksPerSecond`→`TicksPerDay` in the smoke year derivation; comment corrected (the ~6900-day/~19-year repeat; append-harmless same-day reruns).
