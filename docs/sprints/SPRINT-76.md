# SPRINT 76 — Medarbejder administration, Phase 3: the write flows (the unified person editor)

Status: **CLOSED + PUSHED + SEALED 2026-06-14 (`ee7d8a8`; CI whole-workflow GREEN run `27511352157`, all 6 jobs — 13th consecutive CI-verified close). The build-and-test job FLAKED on the FIRST attempt (the known `SkemaRowPreferencesPutTests.PreferenceWrite_IsUnEvented` cross-test outbox-contamination flake under CI shared-postgres, 1/894; local was a clean 894/894; 4TH RECURRENCE — S72/S73/S74/S76) → re-ran the failed job (NOT waived) → GREEN. S76 = {7600, 7601} the backend security + vikar-API foundation; the full unified-editor FE {7602, 7603, 7604} + docs → S76b. Full pyramid GREEN (824u + 894r + 6s + 415fe = 2139).** 2026-06-14.
Program: `.claude/plans/PLAN-s74-medarbejder-administration.md` (the owner-approved 4-phase program; Phase 1/S74 + Phase 2/S75 CLOSED + CI-verified at master `b89ab29`). This sprint = **Phase 3 (the write flows)**.
Refinement: `.claude/refinements/REFINEMENT-s76-write-flows.md` (READY; Reviewer CLEAN + Codex c1 BLOCKED(4)→c2 BLOCKED(2,editorial)→absorbed; OQ-1=UNIFIED/full-re-house, OQ-2=admin-on-behalf-vikar, OQ-3=B1-in-S76, OQ-4=thin-HR-enhed, OQ-5=accept-honest-403).

## Premise
Phase 3 restores the WRITE flows the S75 read-only interim (OQ-B) deferred, as a **UNIFIED person editor** (owner ruling): the `EditPersonDrawer` becomes THE single create/edit surface, **re-housing ALL of `UserManagement`'s create/edit** (name/email/org/agreement + part-time/position/enhed + DOB/employment-start/CHILD_SICK) — re-using the wiring that already exists — PLUS the new **approver assign/remove + vikar-on-behalf + delete-with-reassignment**, opened from the S75 tree (and from UserManagement's list, which STAYS as an entry point; its retirement → Phase-4/S77). Per-section role-gated at the SERVER (LocalAdmin ledelseslinje / HROrAbove HR fields). Plus the two pre-launch backend hardening adds that the write flows make reachable: **B1** (the mixed-role scope leak in BOTH `ValidateOrgAccessAsync` + `ValidateEmployeeAccessAsync`) and the **`/delegate` D15 lock discipline + descendant-cycle-guard**. NO schema change (every write hits an existing table; the new vikar endpoint reuses `ManagerVikarCreated/Ended`).

## Grounded current state (Explore ×2, 2026-06-14)
- **The ledelseslinje/vikar/delete backend ALL exists (S74):** assign `POST /api/admin/reporting-lines` (LocalAdmin, If-Match/If-None-Match, in-tx same-tree+cycle+tree-lock); remove `DELETE .../{id}` (If-Match, root-409); ACTING `.../{id}/acting`; create-person `POST /api/admin/users` (LocalAdmin; userId/username/password/displayName/email?/primaryOrgId/agreementCode/okVersion/**approverId?** atomic create+assign; profile created with defaults part-time=1.0/position=null/enhed=null; effective_from=today); delete-with-reassignment `POST .../{id}/remove` (LocalAdmin; `{replacements}`; **preflight-409 AND an in-lock-census second 409** at ReportingLineEndpoints:1226); enforcement `PUT .../tree/{root}/settings` (LocalAdmin, If-Match, population-409 `unassignedEmployeeIds`).
- **The stamdata-edit backend ALL exists:** `PUT /api/admin/users/{id}` (LocalAdmin, **admin-strict If-Match → 412 stale / 428 missing**: displayName/email/primaryOrgId/agreementCode/isActive; agreement routes through `user_agreement_codes` ADR-020-D2 supersession); `PUT /api/admin/employee-profiles/{id}` (**HROrAbove**, If-Match 412/428: partTimeFraction/position/enhedLabel; effectiveFrom=today; ADR-020-D2 Case B/C; revaluation if fraction/position changes); DOB/employment-start/CHILD_SICK HR-only PUTs (read-then-If-Match). **Each is an INDEPENDENT PUT with its own ETag** — UserManagement saves them sequentially (users → profile → DOB → employment-start → CHILD_SICK), partial-failure-tolerant.
- **The vikar GAP:** `/delegate` is SELF-delegation only (LeaderOrAbove; `absent_approver_id=actorId`); NO admin-on-behalf endpoint. `/delegate` validates same-tree OUTSIDE the tx (no advisory lock) and has NO cycle guard.
- **B1 site:** `OrgScopeValidator.ValidateOrgAccessAsync` (:209) + `ValidateEmployeeAccessAsync` (:59) both grant via ANY covering scope without a role floor. The role-floor pattern already exists in `ValidateEmployeeAccessIncludingTerminatedAsync` (:172-203). The older `ApprovalPeriodRepository.cs:95/:113` LIKE sites are still unescaped (S75 fixed only :414/:572).
- **"Norm" is derived (not editable);** create needs username/password; `UserManagement` (admin/medarbejdere, LocalHR) owns the org user LIST + the create/edit dialogs + reporting-line display.
- **FE:** S75 shipped `MedarbejderAdministration.tsx` + `medarbejderTree.ts` + `useMedarbejderRoster.ts` at admin/ledelseslinjer; `useReportingLines`/`useAdmin`/`useEntitlementEligibility` hooks carry the write wiring; the prototype `ledelseslinjer-record.jsx` = the EditPersonDrawer + `VikarForm`/`PersonPickerDialog` in `ledelseslinjer-data.jsx`.

## Pinned requirements (R1–R10)
- **R1 (B1 hardening — backend, 7600):** add per-scope admin-role-floor variants of the scope-admission paths (reuse the `IsAtLeast(scope.Role, floor)` pattern at `OrgScopeValidator.cs:182`; ADD variants, do NOT mutate the shared methods → Employee/Leader endpoints `ReportingLineEndpoints.cs:438/:471` need the existing self/leader behavior). **THREE paths, not two (Codex c1 B1):** (i) `ValidateOrgAccessAsync` (:209), (ii) `ValidateEmployeeAccessAsync` (:59), (iii) **`GetAccessibleOrgsAsync` (:267) — the person-search picker `AdminEndpoints.cs:1975` unions EVERY scope unfloored → the drawer PersonPicker still leaks B-org users**. PLUS **the bulk-import gap (`ReportingLineEndpoints.cs:797`) calls NEITHER validator despite LocalAdminOrAbove → add an actor-floor check on `TreeRootOrgId`**. **FLOOR-PER-POLICY map (Reviewer W1):** the floor VALUE matches each caller's policy — `LocalAdmin` floor for LocalAdmin endpoints (reporting-lines assign/remove/`/remove`, `PUT /api/admin/users`), `LocalHR` floor for HR endpoints (`employee-profiles` PUT, the DOB/employment-start/CHILD_SICK PUTs) — enumerate the per-caller floor map at implementation; a uniform floor over/under-restricts. Literal-prefix the `ApprovalPeriodRepository.cs:95/:113` LIKE sites (tail item — defense-in-depth, system-derived path, NOT gating the floor work, Reviewer N1). **No-leak tests at BOTH levels:** admin@A + non-admin@B DENIED B's roster/writes/PICKER; HR@A + leader@B DENIED B's HR writes.
- **R2 (vikar-on-behalf + /delegate D15 — backend, 7601):** new `POST/DELETE /api/admin/reporting-lines/{managerId}/vikar` (LocalAdmin). **CREATE (POST) = the FULL D13/D14 authority grant:** (i) actor admin scope covers the absent manager's org; (ii) vikar active + LeaderOrAbove-eligible; (iii) vikar scope covers ALL the manager's current reports; (iv) same-tree IN-TX under the tree advisory lock; (v) `uq_manager_vikar_active` one-active 409; (vi) atomic `ManagerVikarCreated` + ADR-026 audit. **END (DELETE) = SPLIT, REVOKE-SAFE authorization (Codex c1 B2):** ONLY actor admin scope over the absent manager + same-tree + the active row exists — and the same-tree check must validate the absent manager's current root against the **PERSISTED `manager_vikar.tree_root_org_id`** (NOT `ValidateSameTreeAsync`, which requires ACTIVE users — a now-inactive vikar/manager MUST stay revocable; `CloseByApproverAsync` already keys on `absent_approver_id`, supporting this). **Audit actor-org (Reviewer W3, the S71 lesson):** the admin endpoint builds the `AuditProjectionContext` from the ADMIN's `actor.OrgId`/`actor.ActorId` (the operator), NOT the absent manager's — with a discriminating test where admin-org ≠ manager-tree (Codex confirms the existing context at `ReportingLineEndpoints.cs:2002` does this for self; the new path must too). **Restructure `/delegate` (self) to the SAME D15 lock discipline** (advisory lock → in-tx same-tree → `GuardNoCycleAsync` cycle walk) — **with `/delegate`'s request/response/error contract BYTE-STABLE as a pinned AC + a contract-pin test (Reviewer W4 + Codex W; ADR-027:140 — the live S51 self-delegation UI), and confirm the cycle guard does NOT reject a previously-valid self-delegation to a non-descendant leader (the normal case).** Reuse `ManagerVikarCreated/Ended` (no schema change). **Step-5a with the EXTERNAL lens (authorization surface).**
- **R3 (the EditPersonDrawer SHELL + stamdata re-house — FE, 7602):** the unified drawer (hifi `ledelseslinjer-record.jsx`, the named-overlay/a11y Drawer set from S72) housing CREATE (`POST /api/admin/users` incl. username/password + optional approver) + EDIT of ALL UserManagement stamdata fields, re-using the existing wiring ported from `UserManagement.tsx`. **7602 is BUILT AS MULTIPLE MODULES/HOOKS within the one task (Codex/Reviewer W — do NOT recreate the UserManagement god-component):** a drawer shell + per-section field modules + a save-orchestration hook with **per-section committed/failed state + an authoritative refetch** after each PUT. **Per-endpoint concurrency contract (NOT uniform — Codex c1 B3):** users PUT + employee-profiles PUT use **If-Match → 412 stale / 428 missing** + the staleConflict "Genindlæs" banner (S35); the DOB/employment-start PUTs use admin-strict If-Match; **CHILD_SICK has its OWN distinct read/create/update contract — `If-None-Match: *` on CREATE (absent row), 409 on a create race, If-Match on UPDATE (`EntitlementEligibilityEndpoints.cs:113/:163`) — port UserManagement's `childSickRowExists`/version logic verbatim, do NOT collapse it to 412/428.** Each PUT is INDEPENDENT (sequential save, partial-failure honesty — a later PUT failing leaves earlier commits, surfaced honestly per-section). **CREATE-path HR fields (Reviewer W2):** `POST /api/admin/users` is LocalAdmin + creates the profile with DEFAULTS (part-time=1.0/position=null/enhed=null) — a non-HR LocalAdmin creating a person CANNOT set the HR fields (they'd 403 on the follow-up profile PUT). PIN: at CREATE, HR-only fields are hidden/disabled for a non-HR actor (defaults stand); an HROrAbove actor may set them via the post-create profile PUT — rendered as an honest path, never a silent drop or a mid-create 403 surprise. **Per-section SERVER role-gating** (OQ-5: accept honest 403, Danish message, not a silent fail). "Norm" read-only/derived; `effectiveFrom`=today stamped. **7602 may run PARALLEL with 7600/7601** (it consumes existing endpoints; Codex W).
- **R4 (the ledelseslinje + vikar + delete sections in the drawer — FE, 7603):** the **approver** section (`PersonPickerDialog` over the S74 person-search → assign/reassign `POST /api/admin/reporting-lines` [If-Match/If-None-Match], remove `DELETE`); the **vikar** section (the 7601 admin-on-behalf endpoint — create with the VikarForm, end/Afslut; NO If-Match — 409 one-active); the **delete-with-reassignment** dialog (`POST .../{id}/remove`, **handles BOTH the preflight-409 AND the in-lock-census second 409** → refetch/merge the gap list, re-prompt — NOT just the first preflight). The PersonPickerDialog `forbidden` set = self + descendants (cycle-safe, reuse `medarbejderTree.descendantsOf`).
- **R5 (page integration — FE, 7604):** wire the drawer into the S75 `MedarbejderAdministration` tree (row-click → edit-drawer; the restored "Tilføj medarbejder" → create-drawer); restore the **enforcement-mode toggle** (population-409 `unassignedEmployeeIds` surfaced); the hooks (extend `useReportingLines`/`useAdmin` or a new `useMedarbejderAdmin`). **UserManagement: its LIST + org-selector STAY, but its CREATE/EDIT DIALOGS are RETIRED and its "Opret bruger" + row-click route to THE SAME drawer (Codex c1 B4 + Reviewer N5 — keeping the dialogs live = two divergent save implementations, contradicting the unified single-editor ruling).** So one save path (the drawer) reached from both the tree AND the UserManagement list; only the old `UserManagement` create/edit dialog JSX + handlers are removed (`UserManagement.tsx:291/:500`). The LIST/page retirement (the tree subsuming the list) is the deferred Phase-4/S77 item. On any successful mutation, refetch the roster (the S75 read) so the tree reflects the write.
- **R6 (the optimistic/honesty contract):** every mutation reverts to server truth on failure with an honest Danish alert; the per-endpoint status semantics (412/428 profile+user PUTs; 409 delegate/vikar/delete/population/preflight + the second-409; 422 validation; 403 role) are each handled distinctly — NOT a uniform 409/422 assumption (Codex c2).
- **R7 (no schema/event change):** every write hits an existing table; the vikar-on-behalf reuses `ManagerVikarCreated/Ended`; no new event, no new table, no migration. db-schema stays 65 tables.
- **R8 (reviews):** 7600 + 7601 = backend authorization surfaces → mandatory **Step-5a with the external lens** ([[review-lens-complementarity]]); the FE tasks (7602/7603/7604) ride **Step-7a**; 7602's stamdata re-house may take a discretionary Step-5a (the multi-PUT save orchestration + the role-gating).
- **R9 (the role model):** the page stays `minRole LocalAdmin` for entry; the drawer's HR-field sections are server-gated HROrAbove; the FE may DISABLE/hide the HR sections for a non-HR actor as polish (OQ-5b accepts a residual see-then-403, rendered honestly) — a tested scope-aware FE helper is OPTIONAL (deferred).
- **R10 (verification):** full pyramid (unit + regression + fresh-volume smoke + FE/tsc) + CI-verified close; the new backend tests prove B1 no-leak + the vikar-on-behalf authority (cross-org/cross-tree denied, the create-vs-revoke split) + the /delegate serialization/cycle guard.

## Task decomposition
| Task | Title | Domain | Depends on | Review |
|------|-------|--------|-----------|--------|
| **7600** | B1 security hardening — role-floored `ValidateOrgAccessAsync` + `ValidateEmployeeAccessAsync` variants + route all admin callers + literal-prefix the `:95/:113` LIKE sites + no-leak tests | Security / Backend | — | **Step-5a (external lens)** |
| **7601** | The admin-on-behalf vikar endpoint (full D13/D14 create-authority + split end-authorization) + the `/delegate` D15 lock-discipline + cycle-guard hardening | Reporting-Line / Backend | — | **Step-5a (external lens)** |
| **7602** | The EditPersonDrawer shell + the STAMDATA re-house (create + users/profile/DOB/employment-start/CHILD_SICK PUTs; per-section role-gating; per-endpoint If-Match/412/428 + the staleConflict banner; the multi-PUT save orchestration) | UX / Frontend | (consumes existing endpoints) | discretionary Step-5a + Step-7a |
| **7603** | The ledelseslinje + vikar + delete sections in the drawer (PersonPickerDialog approver assign/remove; the 7601 vikar create/end; the delete-with-reassignment dialog — BOTH 409s) | UX / Frontend | 7601, 7602 | Step-7a |
| **7604** | Page integration — tree→drawer wiring (edit/create) + the enforcement toggle + the hooks + UserManagement-coexists-as-list + roster-refetch-on-write | UX / Frontend | 7602, 7603 | Step-7a |
| **docs** | ADR-027 amendment (the admin-on-behalf vikar authority + D15 /delegate hardening) + ADR-012/the B1 security note; QUALITY/INDEX/ROADMAP/anchors | Orchestrator | 7600, 7601 | Step-7a |

**Sequencing:** 7600 ‖ 7601 ‖ 7602 (all three can run in parallel — 7602 consumes existing endpoints, Codex W) → 7603 (ledelseslinje/vikar/delete, needs 7601's endpoint + 7602's shell) → 7604 (integration) → docs. **SPLIT IS THE DEFAULT (both lenses W5):** land **7600 + 7601 + 7602** (the two hardening surfaces + the stamdata drawer) as **S76**; carry **7603 + 7604 + docs** (the ledelseslinje/vikar/delete UI + integration + UserManagement-dialog-retirement) to **S76b** — UNLESS 7600 AND 7601 each clear Step-5a in one cycle (the S74 authorization-surface precedent ran 2-5 cycles), in which case continue into 7603/7604 in the same sprint. Reassess after 7601's Step-5a.

## Explicit Exclusions
- `UserManagement` RETIREMENT (it coexists as the list entry; retire at Phase-4/S77).
- ACTING-manager admin UI; bulk CSV/JSON import UI; reporting-line history UI; role administration (`RoleManagement`).
- A scope-aware FE capability helper (OQ-5a) — optional polish, deferred (OQ-5b = accept honest-403).
- The S74-deferred in-lock concurrency residuals (the approval-auth TOCTOU + the cross-styrelse-transfer serialization) — Phase-4/hardening (ADR-027 D13/D15, non-corrupting).

## Step-0b plan-gate
**Cycle 1 (2026-06-14): Reviewer CLEAN(0)+5W; Codex BLOCKED(4)+3W+2N — ALL absorbed → R1/R2/R3/R5 + sequencing updated.** Codex B1 (R1 missed `GetAccessibleOrgsAsync` person-search [picker leak] + the bulk-import gap → R1 now floors THREE paths + the import actor-floor + the Reviewer's floor-per-policy map); B2 (R2 revoke must validate the PERSISTED `manager_vikar.tree_root_org_id`, NOT `ValidateSameTreeAsync` which needs active users → R2 revoke-safe clause + `CloseByApproverAsync`); B3 (R3 CHILD_SICK is NOT uniform 412/428 — `If-None-Match:*` create / 409 race / If-Match update → R3 ports the distinct contract); B4 (R5 "stay as-is" = two divergent save paths contradicting the unified ruling → R5 retires the UserManagement DIALOGS + routes its create/rows to the drawer, list stays). Reviewer W1 floor-per-policy (→ R1), W2 create-path HR-fields (→ R3), W3 admin-vikar actor-org (→ R2, S71 lesson), W4 `/delegate` byte-stable AC (→ R2), W5 + Codex W: **the S76b split is now the DEFAULT** (7600+7601+7602 = S76; 7603+7604+docs = S76b unless 7600/7601 each clear Step-5a in one cycle); 7602 runs parallel. Codex NOTEs: R4 sound, event-reuse/actor-org sufficient, no-schema/65-tables correct. **Cycle 2: Codex CLEAN — no findings** (all 4 BLOCKERs confirmed resolved: B1 three floored paths + bulk-import + policy map + two-level picker leak tests; B2 revoke-safe persisted-root + actor-org + `/delegate` contract pin; B3 correct per-endpoint matrix + sectional saves/create-HR; B4 one drawer/save path + list retained; split-default + parallel 7602 explicit). **STEP-0b CONVERGED 2026-06-14.** Artifacts: `.claude/s76-stepb-codex{,-c2}.log` + the Reviewer transcript.

## Implementation Record
- **7600 ✅ ACCEPTED 2026-06-14 (Step-5a c1 B(3)→c2 B(1)→c3 CLEAN).** B1 mixed-role-scope-leak hardening. Added role-floored overloads of the FOUR admin scope-admission surfaces (ADD-not-mutate, reuse the `IsAtLeast(scope.Role,floor)` R9f1 pattern): `ValidateOrgAccessAsync` + `ValidateEmployeeAccessAsync` + `GetAccessibleOrgsAsync` + `ValidateEmployeeAccessIncludingTerminatedAsync` (the active+terminated loops), plus role-gated `HasGlobalScope` (require GLOBAL@GlobalAdmin). Routed EVERY admin caller through the floored variant at its policy floor (LocalAdmin / LocalHR); fixed the picker (`AdminEndpoints.cs:1984`) + the unguarded bulk-import (actor-floor on TreeRootOrgId); escaped the `ApprovalPeriodRepository.cs:95/:113` LIKE sites; left `BalanceEndpoints.cs:633/:1224` null-floored (the leader year-overview reads, pinned `YearOverviewTests.Auth_LeaderInScope_Returns200`). **Step-5a external lens c1 BLOCKED(3) → c2 BLOCKED(1) → (fix #2) → c3:** the adversarial lens drove the fix to COMPLETENESS across THREE cycles — c1 caught Config/Project writers + `HasGlobalScope` (the 4th path); the in-task completeness sweep found Audit + §21/§24 settlement reads; c2 ruled the `ValidateEmployeeAccessIncludingTerminatedAsync` active-target bypass must-fix (6 HROrAbove settlement/termination/end-date sites, incl. 2 EmploymentDate surfaces the fix-forward had missed) + caught a green-but-weak payout test (seeded no real settlement row). [[review-lens-complementarity]] at its sharpest — a structural/scoped trace cannot enumerate every scope-admission caller; the external code-trace did. Validation: build 0E/0W; `MixedRoleScopeLeakTests` **32/32** (14→24→32, the validator-direct cases prove the pre-fix leak + the floored deny) + `YearOverviewTests` 24/24 + settlement/termination/end-date siblings green.

- **7601 ✅ ACCEPTED 2026-06-14 (Step-5a c1 B(5)→c2 B(1, test-strictness)→fixed; substantive concurrency/auth c2-CLEAN).** The admin-on-behalf vikar endpoint (`POST/DELETE /api/admin/reporting-lines/{managerId}/vikar`, LocalAdmin) + the `/delegate` D15 hardening. POST = the full D13/D14 create-authority (admin-scope-over-manager + vikar active/LeaderOrAbove + vikar-covers-all-reports + same-tree + cycle guard + uq one-active 409 + `ManagerVikarCreated`+audit), DELETE = revoke-safe (persisted `tree_root_org_id` sole anchor; survives an inactive manager/vikar); `/delegate` restructured to the D15 lock discipline (advisory-lock-first, READ COMMITTED, byte-stable contract). Reuses `ManagerVikarCreated/Ended` (no schema change, 65 tables). **Step-5a external lens c1 BLOCKED(5)+1W → (fix-forward) → c2:** the adversarial lens caught the S74-7403 lesson recurring — ALL authorization/coverage ran OUTSIDE the advisory lock (a TOCTOU: a concurrent report-assign or role revocation could leave an unauthorized grant; the DELETE could close a swapped row) + two green-but-weak tests. Fix-forward moved ALL auth/census IN-LOCK (root→lock→scope→census→same-tree→cycle→insert), made DELETE atomic (lock→FOR-UPDATE→authorize-persisted-root→close, OR-fallback removed), added a held-lock interleave concurrency test. Validation: build 0E/0W; `AdminVikarOnBehalfTests` **15/15** + ManagerVikarEngine/DesignatedApprover/ReportingLine siblings 86/86 green.

## Step-7a (dual-lens, CONVERGED 2026-06-14)
Each backend task had its own deep external-lens Step-5a (7600 c1 B3→c2 B1→c3 CLEAN; 7601 c1 B5→c2 B1→CLEAN); the Step-7a focused on cross-task integration + the close-out docs. Artifacts: `.claude/reviews/SPRINT-76-step7a-{codex,reviewer}.md`.
- **Reviewer (internal): CLEAN** — re-scope coherent; D17 fits ADR-027 + characterizes the resolved-vs-remaining deferrals correctly; 7601 consumes 7600's floored check (no self-leak); the `/delegate` contract + D2/D13/D14 bounds preserved; doc totals match. 3 cosmetic doc-staleness NOTEs (ROADMAP Completed-Sprints table frozen at S69; anchor-marker lag; QUALITY "Last updated: S64").
- **Codex (external): c1 BLOCKED(2)+1W → c2 CLEAN** — the external lens caught two things the internal CLEAN missed ([[review-lens-complementarity]]): (B1) **the `/delegate` error contract was NOT byte-stable** — the D15 restructure moved the active-vikar 409 before the in-lock no-reports 400, so the combined state (active vikar + zero reports) changed 400→409 → FIXED by reordering 400-before-409 (restoring the pre-S76 order) + the combined-state pin test; (B2) **ADR D17 overclaimed** the in-tx org read "prevents transfer" (it's an unlocked SELECT) → reworded to "narrows, not eliminates" + consistent with the deferred residual; (W) the vikar endpoint lacked a DIRECT mixed-role test → added discriminating POST/DELETE mixed-role 403 tests.

## Test Summary
**Full pyramid GREEN — 824u + 894r + 6s + 415fe = 2139 (+50 over S75's 2089).**
- **Unit 824/824** (unchanged — S76 is backend-auth-only; all logic integration-tested).
- **Regression 894/894** — a SINGLE clean run, 0 failures, 48m05s, no FAIL-002, on the FINAL post-Step-7a code (+50: `MixedRoleScopeLeakTests` 32 + `AdminVikarOnBehalfTests` 18). (An earlier 891/891 run preceded the Step-7a fix-forward, which added 3 tests + the `/delegate` reorder → re-run clean on the final code.)
- **Smoke 6/6** — fresh greenfield volume (`docker compose down -v` + `up -d --build`), S76-rebuilt images, greenfield `init.sql` revalidated (byte-unchanged — no S76 schema change, 65 tables), backend healthy. (Ran on the S76 fresh-volume rebuild; the subsequent Step-7a `/delegate` check-reorder is a contained logic change with no schema/wiring/DI change [builds 0E] and is untouched by the smoke paths — CI runs the authoritative final smoke.)
- **FE 415/415** (unchanged — the FE is S76b).
- Build 0E/0W; `check_docs` green (65 tables, KB INDEX complete, freshness → S76).
- **CI: whole-workflow GREEN run `27511352157` (`ee7d8a8`) — all 6 jobs.** The build-and-test job flaked on attempt 1 (the known `SkemaRowPreferencesPut` cross-test outbox flake, 1/894, unrelated to S76) → re-ran the failed job (not waived) → green. **13th consecutive CI-verified close.** **FOLLOW-UP (4th recurrence — S72/S73/S74/S76): the `SkemaRowPreferencesPutTests.PreferenceWrite_IsUnEvented` cross-test outbox/audit contamination under CI's shared postgres is a real test-isolation debt (a sibling test's outbox row leaks into its "no outbox row" assertion) — fix it (scoped cleanup / unique stream keys / a serial collection) in S76b or a dedicated pass so the docs-only/2nd-push runs stop flaking.**
- Close-out docs: ADR-027 **D17** (admin-on-behalf vikar + `/delegate` D15 + the B1 cross-cutting note); `docs/SECURITY.md` (the mixed-role scope-floor invariant); ROADMAP done-row + the S76b/follow-ups; INDEX row; QUALITY anchor→76 + narrative.

## Follow-ups (→ S76b / recorded)
- **S76b = the full unified-editor FE** (R3–R5): the re-house EditPersonDrawer (modular, multi-PUT incl. CHILD_SICK's distinct contract, per-section role-gating, honest-403) + the approver/vikar/delete sections (the both-409 delete dialog; the new vikar endpoint) + the tree/UserManagement integration + **the UserManagement create/edit DIALOG retirement** (route its create/rows to the one drawer — Codex c1-B4). The owner rulings + the full R-pins are in `SPRINT-76.md` + `REFINEMENT-s76-write-flows.md`.
- The S74-deferred in-lock residuals: D17 RESOLVED the vikar-creation in-lock hardening; the **org-scope approval-auth TOCTOU** + the **simultaneous-cross-styrelse-transfer serialization** remain (Phase-4/hardening, ADR-027 D13/D15, non-corrupting).
