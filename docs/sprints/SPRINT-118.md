# Sprint 118 — Typed API Contract retrofit Pass 5: bucket A complete + login (the program's largest pass)

| Field | Value |
|-------|-------|
| **Sprint** | 118 |
| **Status** | complete |
| **Start Date** | 2026-07-15 |
| **End Date** | 2026-07-21 |
| **Orchestrator Approved** | yes (2026-07-21) |
| **Build Verified** | yes — build 0 err; convention gate 103/34/8/0; sha-idempotent ×2 (TASK-11800) |
| **Test Verified** | yes — 2844 total (861u + 1305r + 6s + 55ds + 617fe), all suites locally green 2026-07-20/21 (see Test Summary: the 42-test + smoke environmental rerun) |

## Sprint Goal
Retrofit Pass 5 (PAT-012, [[typed-api-contract-program]]), owner-ruled cut: drain **bucket A COMPLETE — the 29-op agreement-rule config admin surface** (agreement-configs 12 incl. the 4 child entitlement ops, entitlement-configs 5, position-overrides 7, wage-type-mappings 5) **+ `POST /api/auth/login`** (already returns the named `LoginResponse` — gains its `.Produces`). **Manifest 64→34; typed 73→103; declared body-less 3→8** (clone/publish/archive/deactivate/activate). The program's largest pass (prior max 24), owner-chosen over the survey's conservative B-first sequencing.

**TWO OWNER-RULED WIRE CHANGES (first-of-their-kind, named, self-standing — NOT precedent for less-degenerate cases):**
1. **The fallback elimination (dead-branch class):** the 3 create forks (agreement-config create + clone, position-override create: `created is not null ? entity : {id}`) are ELIMINATED — implementation: **`INSERT … RETURNING *`** in the two `CreateAsync` repos (the re-read becomes structurally infallible; DB-computed `NOW()` timestamps stay byte-identical to today's success path; the null branch — defensive dead code that would break the FE if it ever fired — ceases to exist). The 201 is ALWAYS the full entity, structurally.
2. **The child-DTO drift repair (drift-repair class):** the entitlement child shape has THREE mapper copies — two byte-identical 16-member (EntitlementConfigEndpoints + AgreementEntitlementEndpoints) and one drifted 15-member inline (`MapEntitlementToResponse`, agreement-config by-id) that OMITS `fullDayOnly`. Collapse to ONE record; the by-id embedded `entitlements[]` rows GAIN `fullDayOnly` (additive; no consumer breaks; read-side P4-clean — no FE logic branches on its absence, Reviewer-verified). **THE WRITE-SIDE PIN (Reviewer W2 — an un-ruled fork closed BEFORE code): the drift repair is DISPLAY-ONLY this pass.** The sweep found a LIVE 422 DEAD-END today: `EntitlementSection.formToUpdateBody` omits `fullDayOnly` from the child PUT body → the non-nullable DTO deserializes `false` → the guard 422s every CARE_DAY/SENIOR_DAY edit from the by-id page (both types offered in the form). Wiring the field into the PUT body would be an FE request-payload change on a RULE-BEARING flag — barred by the exclusions and covered by NEITHER ruling. **The dead-end is a NAMED DEFERRED DEFECT (owner-visible, a future deliberate fix), not S118 scope.**

Refinement: `.claude/refinements/REFINEMENT-retrofit-pass5.md` — READY; Step-4 closed in 2 cycles (Codex 0B/3W; Reviewer 0B/4W/4N — incl. making the deferral option PAT-012-legal and correcting the fallback's diagnosis to defensive dead code; all absorbed; 2 stale remnants caught at cycle 2 and fixed). OQ-1/OQ-2 owner-ruled 2026-07-15; the drift repair owner-ruled same day.

**Explicit exclusions:** NO error-shape typing (the 412 `currentState` envelopes across ~14 mutations carry the only nullable-complex members — ALL on error bodies, confirmed absent from every success body); NO request-class changes beyond the two ruled fixes' repo signatures; NO other handler-logic change; the REFUSED enum sets stay refused (`entitlementType`/`accrualModel`/`agreementCode`/`okVersion`/wage-type strings — P4-open by design; init.sql:2669 "No entitlement_type CHECK" is deliberate); `GET /health` stays grandfathered WITH a cause comment (infra probe, not a product contract); buckets B + C = Passes 6–7; the 2 standing flag-and-defer ops untouched.

## Entropy Scan Findings (Step 0a) — the fact-sheet digest
| Check | Result | Detail |
|-------|--------|--------|
| Gate baseline | GREEN | 73 typed / 64 grandfathered / 3 declared at `e034efa` (S117 close `ba95b22` CI GREEN `29171520997`) |
| The 48-member truth | pinned | `MapEntityToResponse` emits **48 members** (not the FE's 43): the FE `AgreementConfig` interface (43 fields) OMITS 5 compliance fields (`maxDailyHours`, `minimumRestHours`, `restPeriodDerogationAllowed`, `weeklyMaxHoursReferencePeriod`, `voluntaryUnsocialHoursAllowed`) — the typed switch surfaces them ADDITIVELY (they are emitted today; the FE just can't see them). The by-id variant adds `entitlements[]` + `entitlementsReadOnly`. 4 entity fields the mapper does NOT emit stay absent (mapper ≠ entity, correctly) |
| Mapper call-site enumeration | complete | `MapEntityToResponse` ×8 sites (list, by-code, create-fork, clone-fork, PUT 200, + 3 error-body `currentState` — the error sites stay anonymous/untyped); `…WithEntitlements` ×1 (by-id); child mappers ×3 copies (the drift); position mapper ×8 sites (3 error — PUT/deactivate/activate `currentState`); WTM mapper ×6 sites (3 error; the WTM POST 201 builds from locals already — the precedent for fork-free creates) |
| Success-shape census | CLEAN | NO multi-2xx anywhere; NO success-body nullable-complex member (the `currentState` nullable-complex rides 412/409 ERROR bodies only — S117-wrapper-irrelevant this pass); every bucket-A op has exactly ONE success return site (the 3 forks are one-site/two-shape ternaries — eliminated by ruling #1); login has 2 success sites, both `LoginResponse` (in-memory branch serializes `orgId: null`) |
| Body-less POST census | 5 → the declared list | clone, publish, archive, deactivate, activate (the child-ent POST + the 4 creates DO bind bodies) |
| Bespoke lifecycle envelopes | 4, single-branch | publish `{configId, status:"ACTIVE", archivedConfigId: Guid?, publishedAt: DateTime?}`; archive `{configId, status:"ARCHIVED", archivedAt: DateTime?}` — ALL keys ALWAYS emitted, values nullable (nullable-always-present, NOT optional-key — the S113 required/nullable orthogonality; Reviewer N3); deactivate/activate `{overrideId, status, deactivated/activated:true}` — each its own record |
| Enums (authorities cited) | 3 sets | agreement-config `status` {DRAFT,ACTIVE,ARCHIVED} (init.sql:1249 CHECK; CLR-enum `.ToString()` emission + the publish/archive literals); position `status` {ACTIVE,INACTIVE} (init.sql:1430; raw string); `normModel` {WEEKLY_HOURS,ANNUAL_ACTIVITY} (CLR-enum total projection). REFUSED: everything agreement-defining |
| The If-Match demand map | delivered (12 mutations) | ALL 12 admin-strict `TryParseIfMatch` (428 missing / 412 stale); the FE threads versions via `formatVersionAsIfMatch(row.version)` or the by-id ETag header (`resolveEtag`); **creates are UNCONDITIONED (no If-None-Match anywhere)**; ETag stamping: creates `"1"` (agr/pos `?? 1L`) or `"<version>"` (ent/child/wtm); 204 DELETEs stamp none |
| The lie audit (pre-switch) | 2 real finds | (1) the FE `AgreementConfig` 5-field omission (above — additive surfacing, not a wire change); (2) **the `fullDayOnly` three-way drift** (ruling #2). The other 3 primary interfaces (`PositionOverrideConfig`, `WageTypeMappingItem`, `EntitlementConfig`) are FAITHFUL — no phantom fields, no false nullability. ~19 cast expressions across the 4 hooks die |
| The fallback-elimination spec | RETURNING ruled | `CreateAsync` (agr + pos repos): plain INSERT, no RETURNING, version absent (DB DEFAULT 1), timestamps via `NOW()` literals — only `createdAt`/`updatedAt` are re-read-load-bearing (sub-second). **`INSERT … RETURNING *`** preserves them byte-identically AND eliminates the null branch structurally. No DB triggers on either table |
| Test homes | mapped | AgreementConfig (concurrency+atomic), EntitlementConfig (3 Config suites incl. the ONLY child-route coverage), PositionOverride (concurrency+atomic; init.sql seeds 4 overrides `ON CONFLICT DO NOTHING`), WTM (7 suites — the densest); login has NO endpoint suite (driven via the hosting factory). No FAIL-002-adjacent contention in these families. Zero per-route contract tests |

## Plan Review (Step 0b)
| Field | Value |
|-------|-------|
| **Trigger** | MANDATORY — P4-adjacent surface (agreement-rule config) + the two wire-change rulings + repo-signature changes |
| **External Codex** | 2 cycles (2026-07-15): cycle 1 = **0 BLOCKER / 1 WARNING / 6 NOTE** — the WARNING: the `CreateAsync`-returns-entity reading collides with the UNMODIFIED-suites criterion (TxContractTests consume `Guid`) → resolved as the SIBLING method; NOTEs verified: clone shares the repo path (ONE fix covers create+clone); the reader mappers exist for reuse; the in-tx audit/outbox ordering pin; the drift repair plan-ready (no contract test pins the 15-member shape); gate math incl. login's manifest line (103/34/8 exact); the split-only-by-family-with-single-ownership guidance; the login two-config reality. Cycle 2: **all edits verified present.** |
| **Internal Reviewer** | 2 cycles (2026-07-15): cycle 1 = **0 BLOCKER / 4 WARNING / 3 NOTE** — W1 the same signature conflict (CONVERGENT) with the full overload-consumer census; **W2 a NEW LIVE DEFECT surfaced: the CARE_DAY/SENIOR_DAY 422 dead-end on the by-id page** (the un-ruled write-side fork closed BEFORE code: the drift repair is DISPLAY-ONLY; the dead-end = a named deferred defect); W3 three transcription errors (43 fields; position ×8/3-error; **byte-identity = 26 ops, the 4 wire-changed DELTA-pinned — as written the by-id GET would have failed byte-assertion BY DESIGN**); W4 the login DB branch unreachable in the harness → the derived-factory scaffolding authorized. **The P4 probes both CLEAN: events built from request/locals (RETURNING cannot alter payloads; no triggers); `fullDayOnly` display-only on the read side.** Cycle 2: **all 7 closed, verified against code; 1 trivial residual (fixed); 0 new findings.** |
| **BLOCKERs resolved before Step 1** | **yes — ZERO BLOCKERs both lenses; the write-side pin, the sibling-method design, the 26/4 split, and the login scaffolding all encoded as task criteria; hand-off note honored (the W2 pin rides the FE agent prompt verbatim)** |

---

### TASK-11800 — Backend: the 30-op drain + the two ruled fixes
| Field | Value |
|-------|-------|
| **ID** | TASK-11800 |
| **Status** | complete (2026-07-15) — **103 typed / 34 grandfathered / 8 declared / 0 stale, exactly the target.** Ruling #1: `CreateReturningAsync` siblings in both repos with a **shared-`InsertSql`-const construction (column drift between the Guid and entity paths structurally impossible)**, hydrating via the existing `ReadEntity` mappers; the 3 endpoints switched IN-TX before the unchanged audit/outbox appends (the TX pin held); the ternaries + `?? 1L` fallbacks deleted (grep-proof: comments only). Ruling #2: ONE `EntitlementConfigResponse.FromModel` single-shape-point — all three mapper copies delegate; the by-id rows gain `fullDayOnly` (read side only; the W2 write-side pin held). **The checkpoint isolated the two wire deltas exactly** (12 family ops changed, 5 schemas added, nothing else) before the bulk drain; the FINAL full diff = exactly the 30 target ops, zero collateral. 9 new records + LoginResponse's first `.Produces`; the 3 enum sets only; manifest −30 (health stays with the cause comment); declared-bodyless 3→8. **Validated: build 0 err ×2; sha-idempotent (`661146…`); AgreementConfig 25/25 + PositionOverride 21/21 + EntitlementConfig 38/38 + WTM 40/40 — ALL UNMODIFIED (the signature freeze proven); Contracts composed 131/131 at both checkpoints; P4/P7 audit clean (policy strings byte-identical: 23× GlobalAdminOnly, 7× LocalAdminOrAbove, login anonymous).** **Declared (correct, not improvised): the position record is 14 members — the plan's "13" was a fact-sheet transcription slip (the shape-copy rule wins; flag → 11803's PAT-012 update, transcription-slip class 4th instance).** |
| **Agent** | Backend/API |
| **Components** | AgreementConfigEndpoints.cs, AgreementEntitlementEndpoints.cs, EntitlementConfigEndpoints.cs, PositionOverrideEndpoints.cs, WageTypeMappingEndpoints.cs, AuthEndpoints.cs, AgreementConfigRepository.cs + PositionOverrideRepository.cs (the RETURNING fix ONLY), Contracts/ (new per-family files), tools/openapi-convention-exempt.txt + openapi-bodyless-declared.txt, regen |
| **KB Refs** | PAT-012 (paved road; declared-bodyless; the sibling-record rules), PAT-010 |

**Description**: Records per the fact sheet — **exact shape-copies at every enumerated mapper call site** (success sites ONLY; the 412/409 `currentState` error bodies stay anonymous): the 48-member `AgreementConfigResponse` (ONE record for list/by-code/create/clone/PUT — the S112 sibling rule) + the by-id variant (`…WithEntitlements`: +`entitlements[]` + `entitlementsReadOnly`); **ruling #2: ONE 16-member `EntitlementConfigResponse` collapsing all THREE child mappers — the by-id inline mapper gains `fullDayOnly` (the additive drift repair, test-pinned)**; the 14-member position record (planned "13" — a fact-sheet transcription slip, declared at 11800; the shape-copy rule wins); the 7-member WTM record (its POST 201 already builds from locals — same record); the 4 bespoke lifecycle-envelope records; `LoginResponse` gains `.Produces<LoginResponse>(200)`. **Ruling #1: the 3 create forks eliminated via `INSERT … RETURNING *` — as a RETURNING-ENTITY SIBLING METHOD (Step-0b Codex W1): `CreateAsync` keeps its `Guid` signature (TxContractTests consume it — the UNMODIFIED-suites criterion holds); a new `CreateReturningAsync` (both repos, the IN-TX `(conn, tx, entity)` shape the endpoints consume, reusing the EXISTING private reader mappers [`AgreementConfigRepository.cs:823`, `PositionOverrideRepository.cs:629`]) returns the hydrated entity; the 3 endpoints switch to it; **ALL existing `CreateAsync` overloads (in-tx AND self-managed — consumed by TxContractTests, the atomic suites, the seeder, the concurrency suites) stay signature-identical** (Reviewer W1)** (clone shares the agreement-config repo path — one fix covers create+clone); the ternaries collapse to `MapEntityToResponse(created)` unconditionally; the `?? 1L` ETag fallbacks die. **Tx pin (Codex NOTE): the RETURNING runs INSIDE the existing transaction — the audit/outbox/audit-projection appends stay in-tx after the INSERT, byte-order unchanged; moving them would be a P3 violation.** `.Produces<T>(code)` per op (bare arrays as `IEnumerable<T>`; creates 201; DELETEs declared 204); the 5 body-less ops → the declared list (3→8); `[AllowedValues]` on the 3 cited sets ONLY. Manifest 64→34. Regen; field-mapping tables per op/site.

**Validation Criteria**:
- [ ] Build 0 err; convention **103 typed / 34 grandfathered / 8 declared / 0 stale**; drift + freshness green; sha-idempotent ×2
- [ ] The RETURNING fix: both creates + clone return the full entity ALWAYS (the fork gone — grep-provable); timestamps still DB-`NOW()` (byte-identical semantics); the existing concurrency/atomic suites green UNMODIFIED
- [ ] The drift repair: all three child emission sites serve the 16-member shape; mapping tables note the by-id rows' additive `fullDayOnly`
- [ ] P4/P7 audit: zero logic hunks beyond the two ruled fixes; all policy strings byte-identical (GlobalAdminOnly ×4 families, LocalAdminOrAbove ×position, anonymous login)

---

### TASK-11801 — FE: the 4-hook typed switch + the lie audit (depends: 11800)
| Field | Value |
|-------|-------|
| **ID** | TASK-11801 |
| **Status** | complete (2026-07-20) — **tsc 0 / lint 0 (7 full-tier + 2 partial-tier files, 9 RED-probes RED→GREEN incl. the carve-out proven both directions) / vitest 589→617 (+28, 48→51 files); Constraint Validator PASS (incl. the request-payload-invariance sweep: every mutation's key set byte-unchanged, `fullDayOnly`/`effectiveFrom` absence wire-pinned).** 10 of 12 If-Match mutations typed byte-identical; creates pinned unconditioned; no `ifNoneMatch` (grep-proof); 5 zero-caller GETs had nothing to switch; login DELIBERATELY raw-fetch w/ spec-bound `LoginRequest`/`LoginResponse` (the shared 401 handler reloads the page → would destroy the wrong-password display; behavior test-pinned). **THE DEVIATION (forced by the W2 bar, the request-side lie detector firing): 2 of the 12 If-Match PUTs REFUSED to compile — both are LIVE PROD DEAD-ENDS today (binder-required `effectiveFrom` never sent): (a) the WTM update — every wage-type-mapping edit 400s in prod (NEW find); (b) the child-entitlement PUT — 400 at binding BEFORE the W2-pinned 422 guard (the dead-end is WIDER than the pin described).** Both route-helper-pinned (`WAGE_TYPE_MAPPING_UPDATE_PATH`, `CHILD_ENTITLEMENT_PATH`) on partial lint tiers per the S115/S116 precedent; current bytes wire-pinned as NAMED DEFERRED DEFECTS (the future fix = payload + typed-form graduation in ONE deliberate change). Lie audit: the 5-field surfacing confirmed additive (no phantom consumer); the 3 faithful interfaces confirmed; `types.ts LoginResponse` omitted `orgId` (deleted); a false-optional page-local `AgreementConfigWithEntitlements` (deleted); an out-of-enum `normModel:'WEEKLY'` test mock (fixed); 19 hook casts + 13 UI casts died; error narrowing via runtime guards only. Proposed PAT-012 amendment (compile-refusal = request-side lie detector; defer-pin-never-silence) → folded at 11803. |
| **Agent** | UX/Frontend |
| **Components** | useAgreementConfigs.ts, useEntitlementConfig.ts, usePositionOverrides.ts, useWageTypeMappings.ts, AgreementConfigEditor.tsx, EntitlementConfigEditor.tsx, EntitlementSection.tsx (+ consuming pages as needed), eslint.config.mjs, package.json |
| **KB Refs** | PAT-012 (call-form audit; the typed-etag overloads) |

**Description**: Switch ALL call sites per the If-Match demand map — the 12 If-Match mutations via the typed `apiFetchWithEtag(pathKey, {method, params, ifMatch, body})` / typed verbs (each op's exact precondition preserved: admin-strict If-Match threading via `formatVersionAsIfMatch`/`resolveEtag` unchanged in semantics; creates stay unconditioned); the reads via typed `get`. DELETE the 12 hand-written interfaces (the 43-field `AgreementConfig` — its replacement surfaces the 5 emitted-but-invisible compliance fields additively; `EntitlementSection`'s 15-field `Entitlement` gains `fullDayOnly` per ruling #2; the FAITHFUL 3 die too — spec types replace them). The 412-body handling keeps its runtime-guard pattern (error shapes untyped — `ConfigMutationError` narrowing stays, converted to runtime guards where `as`-casts die). Lint: the 4 hooks + AgreementConfigEditor.tsx + EntitlementConfigEditor.tsx (pages/) + EntitlementSection.tsx (components/admin/ — a COMPONENT path, Reviewer N2) join the FULL tier; package.json list updated; RED-probe every new tier file. Vitest wire pins for the switched mutations (precondition fidelity per the demand map) + the additive-field surfacing.

**Validation Criteria**:
- [ ] tsc 0; lint 0 (tiers RED-probed); vitest green (delta counted); zero unsanctioned explicit-T/`as` on the switched surface (call-form audit sweep)
- [ ] The lie audit reported: the 5-field surfacing + the `fullDayOnly` gain named; no phantom-field consumer existed (expected per the faithful-interface finding)

---

### TASK-11802 — Test & QA: per-route assertions for the 30 ops (depends: 11800)
| Field | Value |
|-------|-------|
| **ID** | TASK-11802 |
| **Status** | complete (2026-07-20) — **28 new per-route assertions green (AgreementConfig 12, EntitlementConfig 4, PositionOverride 6, WTM 4, Login 2 — login's FIRST endpoint suite); composed Contracts 156/0/0; Constraint Validator PASS (all 8 checks incl. tripwire honesty + fixture hygiene).** Both ruling pins by named test (`…RulingOnePin_Always48MemberFullEntity` create+clone + the 14-member position variant; `…RulingTwoPin_EmbeddedEntitlementRowsCarryFullDayOnly` on a guard-forced non-vacuous CARE_DAY row); RED-on-lie via in-memory spec corruption (`s118PhantomMember`, disk untouched — the S117 technique); the 26 non-wire-changed ops byte-faithful, the 4 wire-changed DELTA-pinned. Login: BOTH branches real (in-memory `orgId:null` pinned; the W4-authorized `DbAuthWebApplicationFactory` drives the production BCrypt path end-to-end via HOST config — the TASK-3001 builder-time-flag timing; NOT deferred to smoke). Seed census delivered, disjoint (S118-prefixed codes/actors/orgs ×5 families; the 4 init.sql override seeds read-asserted, never mutated). **DECLARED CROSS-DOMAIN DEFECT (the 3rd named deferred defect, NEW prod bug): archive + supersession-publish 500 TODAY — `AgreementConfigEndpoints` passes bare strings as audit previous/new data → `::jsonb` 22P02 → tx rollback; every archive and every re-publish fails in prod; MASKED because the repo suites hand-built valid JSON (their comments admit the strings are invalid) — the first honest HTTP drive caught it. Orchestrator ruling: DEFER (the owner-ruled "no other handler-logic change" exclusion + P3 surface); covered by the labeled tripwire `PublishSupersedeAndArchive_DeclaredProductDefect_…` asserting the 22P02 signature — goes RED on the fix, forcing the envelope-assertion flip; the reachable no-prior-active publish branch pinned green.** ⚠ Count reconciliation for validation: the 11800 checkpoint's "131 composed" vs the namespace-filtered census 128 (+28=156) — 3 tests counted outside the filter; recount at `sprint-test-validation`. 2 proposed KB notes (repo-test-masking smell; builder-time-flag host-config pattern) → folded at 11803. Minor declaration: `CreateGlobalAdminClient` is sealed-factory-typed → the login suite mirrors JWT minting locally (S117 ActorClient precedent, byte-identical key). |
| **Agent** | Test & QA |
| **Components** | tests/StatsTid.Tests.Regression/Contracts/ (new per-family S118 classes) |
| **KB Refs** | PAT-012 (gate #1), FAIL-002 |

**Description**: Per-route spec≡runtime Docker assertions, per-family classes: agreement-configs (incl. **the always-entity 201 pins on create + clone — the ruling-#1 proof: assert the FULL 48-member key set, never `{configId}`**; the by-id embedded rows carry `fullDayOnly` — **the ruling-#2 pin**; the 4 lifecycle envelopes; enum fidelity on status/normModel); the child-entitlement quartet (204 DELETE status+empty); entitlement-configs; position-overrides (the always-entity create pin; activate/deactivate envelopes; status enum); WTM (the query-param-keyed DELETE; the natural-key PUT); login — **concrete branch coverage (Step-0b Codex NOTE): the in-memory branch under the default factory (`Auth:UseDatabase=false` — `orgId: null` pinned); **the DB branch is UNREACHABLE in the current harness (the factory overrides only the connection string — Reviewer W4): AUTHORIZED test-side scaffolding — a derived factory setting `Auth:UseDatabase=true` (tests-only config, not product change) covers the production auth path**; if that proves disproportionate mid-task, record the DB branch smoke/e2e-covered-only WITH cause (login has no existing endpoint suite — these are its first). If-Match flows composed as the FE does (read version → If-Match); dedicated seeds per family (fresh agreement codes/org-free surfaces — no FAIL-002 contention expected; respect the init.sql position-override seeds). One RED-on-lie proof. Scoped runs only; composed Contracts green alongside.

**Validation Criteria**:
- [ ] All new Docker assertions green (exact counts); the two ruling pins demonstrated; the RED proof demonstrated
- [ ] Seed census delivered (disjoint from the config families' existing suites)

---

### TASK-11803 — Orchestrator: PAT-012 update + validation + Step 7a + close
| Field | Value |
|-------|-------|
| **ID** | TASK-11803 |
| **Status** | complete (2026-07-21) — **PAT-012 updated** (scope → 103/34/8 + the Pass-6/7 map [B 14 → C 17 incl. the skema graduation]; the TWO wire-change rulings recorded first-of-their-kind; the request-side lie detector [compile-refusal] + the repo-test-masking smell + the builder-time-flag host-config corollary + **the transcription-slip class, 4th instance** [planned-13/shipped-14 position record — counts are checksums, the shape-copy rule wins]; lie tally → **6 prod bugs + 11 lies, honest counting**). **Validation** (`sprint-test-validation`): delta table below, +56; the Contracts census RECONCILED — namespace-filtered 156 = 128 pre-existing + 28 S118 (`--list-tests` proof; the 11800 checkpoint's "131" counted 3 contract-adjacent tests outside the namespace filter — a broader-filter artifact, 156 canonical. **Step 7a converged 2 cycles both lenses** (section below). **Operational notes:** (1) the close session hit its session limit mid-11803 (2026-07-20) — the first Reviewer attempt died 2 min in; resumed 2026-07-21, Reviewer re-run in full. (2) The 2026-07-20 central validation ran with the compose Postgres DOWN → 42 fixed-port ReportingLine/ManagerVikar FAILs + 6 smoke FAILs, all ENVIRONMENTAL (FAIL-002 class); rerun 2026-07-21 against a fresh `down -v` compose Postgres: scoped 103/103 (the whole namespace, incl. all 42) + smoke 6/6 + unit/DemoSeed re-green. (3) A Step-7a RED-probe cleanup briefly reverted `useWageTypeMappings.ts` via `git checkout` (uncommitted S118 work) — restored byte-identical from the FE agent transcript's single full Write; verified tsc 0 / lint 0 / vitest 617/617. **Carry line for the archive/re-publish fix task (Reviewer N1): when the tripwire flips to real envelope assertions, the supersede leg must ALSO gain the rollback-state assertion the archive leg already has (prior ACTIVE row + failing DRAFT unchanged).** Close via the explicit file set; `design_handoff_*` dirs left untracked with cause (owner assets). |
| **Agent** | Orchestrator |
| **Components** | PAT-012, sprint log, INDEX, close gates |
| **KB Refs** | PAT-012, FAIL-003 |

**Description**: PAT-012: scope → **103 typed / 34 grandfathered / 8 declared**; the TWO wire-change rulings recorded first-of-their-kind (dead-branch class + drift-repair class — each self-standing, explicitly NOT precedent for less-degenerate cases); the lie-tally update (the 5-field omission + the drift = FE-blindness finds; tally per the honest counting); the Pass-6/7 map (B 14 → C1 ~10 → C2 composites + the skema graduation + the flex-balance deferral). Validation (`sprint-test-validation`); Step 7a dual-lens (named targets: the RETURNING semantics, the drift repair's additive delta, wire-byte identity on the 26 non-wire-changed ops (the 4 wire-changed = the 3 fork-creates [ruling #1] + the by-id GET's additive rows [ruling #2] get DELTA-pinned instead, never byte-asserted), the 12-mutation precondition fidelity); close per the 5 gates; explicit file set.

**Validation Criteria**:
- [ ] PAT-012 updated; delta table; Step 7a converged; close + push + CI green all 7 jobs

---

## External Review (Step 7a)

Dual-lens, both cycle-1 → cycle-2 converged (2026-07-20 → 2026-07-21). Artifacts: `.claude/reviews/SPRINT-118-step7a-{codex,reviewer}.md`.

| Lens | Cycle 1 | Cycle 2 (fix verification) |
|------|---------|---------------------------|
| External Codex (`codex exec`, prompt-steered) | **0 BLOCKER / 2 WARNING / 0 NOTE** — both against the two S118 eslint carve-outs: route-helper-only pins are not airtight (the WTM helper could sanction a future non-PUT explicit-T call; `CHILD_ENTITLEMENT_PATH` doubles as the child DELETE route) | **"Verified — no findings."** |
| Internal Reviewer (re-run 2026-07-21 after the session-limit kill of the first attempt) | **0 BLOCKER / 1 WARNING / 4 NOTE** — W: the promised PAT-012 transcription-slip note had not landed (+ the sprint-log "13-member" remnant); N1 supersede-leg rollback assertion (→ the fix-task carry line, TASK-11803); N2 repo binding asymmetry (accepted); N3 `buildUrl` empty-string invariant lives in callers (recorded); N4 `AgreementConfigList` legacy cast (future pass). ALL checklist verdicts held: P3 append order byte-unchanged, P4/P7 boundaries held, wire-byte identity faithful on all four families, `CreateReturningAsync` sound (name-based hydration — `RETURNING *` order-immune), precondition fidelity held, both carry-overs RESOLVED (`buildUrl` skips undefined → clone wire URL unchanged) | **"Verification clean — the absorption is faithful"** (+ the N1 carry-line reminder, honored above) |

**The fix set (all absorbed before close):** (1) both S118 carve-out selectors tightened to pin the exact sanctioned call shape — arity 2 + the named route helper + an options object-literal whose FIRST property is `method: 'PUT'` — fail-closed in every escape direction (other method/arity/helper, raw URL, reordered options); lint green + two RED probes (method flipped → ban fires) demonstrated. (2) The PAT-012 transcription-slip-class paragraph added (4th instance) + the sprint-log "13-member" remnant corrected. [[review-lens-complementarity]]: the external lens attacked the lint-tier escape surface; the internal lens verified the wire/tx/policy invariants member-by-member and resolved both carry-overs — disjoint classes again.

## Test Summary (close, 2026-07-21)

| Suite | Previous (S117) | Current | Delta |
|-------|-----------------|---------|-------|
| Unit | 861 | 861 | 0 |
| Regression | 1277 | 1305 | +28 |
| Smoke | 6 | 6 | 0 |
| DemoSeed | 55 | 55 | 0 |
| Frontend | 589 | 617 | +28 |
| **Total** | **2788** | **2844** | **+56** |

Delta composition: +28 regression = the S118 per-route Contracts assertions (AgreementConfig 12, EntitlementConfig 4, PositionOverride 6, WTM 4, Login 2 — login's first endpoint suite); +28 FE = the wire pins + additive-field surfacing across 3 new test files (48→51). Full-run integrity: the 2026-07-20 FULL regression ran 1305 total with 1263 green + 42 fixed-port ENVIRONMENTAL fails (compose Postgres down — FAIL-002 class, smoke 0/6 same cause; unit 861/861 + DemoSeed 55/55 green same run); 2026-07-21 against a fresh compose Postgres the entire affected namespace reran **103/103 (all 42 included)**, smoke **6/6** against the healthy 8-service stack, unit + DemoSeed re-green. Composed Contracts census: **156** namespace-filtered (128 + 28, `--list-tests`-proven). FE vitest **617/617** (51 files).

## Phase Plan
- **Phase 1:** TASK-11800 (one Backend agent — the records + the two ruled fixes + regen). **Split guidance (Step-0b Codex NOTE): the single-agent shape is correct (Contracts/ + manifest + regen are coupled — two agents would collide); if mid-task sizing forces a split, it is BY FAMILY with the contracts/manifest/regen ownership staying with ONE of them, never shared.** Internal sequencing: the two ruled fixes + their family first, regen-checkpoint, then the bulk drain.
- **Phase 2 (parallel):** TASK-11801 (FE — needs the regen'd types) ∥ TASK-11802 (assertions — needs the spec; file-disjoint: frontend/ vs tests/)
- **Phase 3:** TASK-11803 (docs + validation + close)

**Atomicity pin:** ONE close commit; explicit file set; gates at close.

## Legal & Payroll Verification
| Check | Status |
|-------|--------|
| Agreement rule compliance | ACTIVE-WATCH (P4) — the config surface DEFINES agreement rules but this pass changes only response construction + the two ruled fixes (a repo RETURNING clause + an additive field); NO rule value, validation path, or supersession logic changes; the REFUSED enum boundary keeps OK-version/agreement-code/entitlement-type semantics open as designed |
| Wage type mapping correctness | N/A — response typing only; the WTM Case-A/B/C write machinery untouched |
| Event sourcing / audit | N/A — no event/audit-path change |
| Security (P7) | PASSIVE — no new endpoint; all policy strings byte-identical; login stays anonymous by design |
