# SPRINT-80 — ADR-033 Slice 2: særlige feriedage (D11 model correction + §15 stk.2/§17 godtgørelse)

**Status:** CLOSED LOCALLY 2026-06-16 (local commit; push + CI-verify deferred — owner out of GitHub Actions minutes). Refinement: `.claude/refinements/REFINEMENT-s80-saerlige-feriedage-godtgoerelse.md` (READY, owner-ratified 2026-06-16). Building LOCALLY (owner out of GitHub Actions — local commit, push deferred).

## Premise
The last remaining ADR-033 settlement slice. Særlige feriedage (`SPECIAL_HOLIDAY`, the agreement-based "6th week", 5 days) are mis-modeled today with `ResetMonth=9` (the statutory Sep–Aug ferieår); the verified law (Cirkulære 021-24 §12) is **calendar-year accrual (1 Jan–31 Dec) + a 1 May–30 Apr taking window**, with unused-untransferred days **paid out (§15 stk.2/§17, 2½% godtgørelse)** at the **30 Apr** boundary — NO §34 forfeiture, NO §22 feriehindring, NO carryover. D11 corrects the model; the godtgørelse settlement makes the precise window meaningful. Money stays out (SLS owns 2½%); D13-gated.

## Owner decisions (2026-06-16)
- **OQ-1 = full model** (forced — the minimal reset_month flip settles ~4 months early + mis-keys booking).
- **OQ-2 = payout-only + GATED:** build the D11 correction + the godtgørelse machinery; the SPECIAL_HOLIDAY auto-close stays DORMANT until §15 stk.1 (the carry-forward agreement) is modeled — a documented go-live dependency (wrongful-payout safety: "no modeled agreement" must not == "no agreement"). §15 stk.1 = a recorded follow-on.
- **SLS = `SLS_TBD_*` placeholder line + record the verified 5017/5027/5037** in the ADR/Step-0 trail.
- **OQ-3 = pre-launch reseed + re-derive** (no production data).

## Source-verified ground truth (dual-lens grounded + Step-4 reviewed, cited)
- **Law (Cirkulære 021-24):** §12 calendar accrual (1 Jan–31 Dec, 0.42/md=5/yr) + §12 stk.2 taking window 1 May–30 Apr; §15 stk.1 written transfer agreement OR §15 stk.2 default cash godtgørelse at afholdelsesperiode end; §17 = 2½% (≠ §10's 2,02%, D12). Worked example: **accrual 2022 → taken 1 May 2023–30 Apr 2024 → godtgørelse at 30 Apr (accrual-year + 2)**.
- **Current code:** `SPECIAL_HOLIDAY` seeded `ResetMonth=9`, MONTHLY_ACCRUAL, carryover 0 (`DefaultEntitlementConfigs.cs:81`, `init.sql:1538`) — a deliberate simplification D11 corrects. `SettlementCloseService` is VACATION-hardcoded (`:109/:667` SQL anti-join, `IsBoundaryPassed` 31-Dec `:759`, `ResolveLeaverFerieaar` `:789`). `SaerligeFeriedagePaidOut` define-only (`EventSerializer.cs:154`, no mapper/emit). Booking/balance year keys off `reset_month` (`SkemaEndpoints.cs:96/:1095`, `BalanceEndpoints.cs:210/:940`). The §34 partition: `Partition` `overCap = disposable − CarryoverMax` (`:1362`) → with CarryoverMax=0, the whole balance becomes a §34 candidate → PENDING_REVIEW (`:320`). The existing `SPECIAL_HOLIDAY_ALLOWANCE → SLS_0570` mapping (`init.sql:261`) is the CONSUMPTION lønart (not the godtgørelse line). The 4–5-site ferieår-resolution duplication (`SettlementCloseService:789`, `VacationSettlementService:791`, `VacationSettlementEndpoints:165`, `EmploymentDateEndpoints`, + the SQL year-cap).

## Pinned requirements
- **R1 (D11 accrual model).** SPECIAL_HOLIDAY accrual anchored to the calendar year (the accrual-year keying becomes calendar; `AccrualMath.EarnedToDate` fed a 1-Jan `ferieaarStart` everywhere SPECIAL_HOLIDAY accrues — Reviewer N2 caveat). Reseed `DefaultEntitlementConfigs` + the init.sql seed (the S60/S63 idempotent type-keyed UPDATE + sentinel-reseed; the VACATION-only reset_month CHECK `:1393` does NOT constrain SPECIAL_HOLIDAY).
- **R2 (the taking window — the third period; CORRECTED at Step-0b — the window spans TWO calendar years).** The taking window is **1 May Y+1 – 30 Apr Y+2** for accrual year Y, so the usage→accrual-year mapping has BOTH halves (the first text named only the Jan–Apr tail — Reviewer BLOCKER): for a usage date `d`, `takingPeriodStart = d.Month >= 5 ? (d.Year, 5, 1) : (d.Year−1, 5, 1)`; **`accrualYear = takingPeriodStart.Year − 1`** — i.e. **May–Dec T → accrual year T−1; Jan–Apr T → accrual year T−2**. NOT expressible by raw `reset_month` (even `reset_month=5` keys Jan–Apr Y+2 to Y+1, still wrong). The booking/balance keying (`SkemaEndpoints.cs:96/:1095`, `BalanceEndpoints.cs:210/:940`) must use this taking-window mapping (via the R10 resolver). **AC must pin BOTH a May–Dec T and a Jan–Apr T usage date.** Highest-architectural-risk item.
- **R3 (settlement boundary).** SPECIAL_HOLIDAY accrual year Y settles at **30 Apr (Y+2)** — distinct from VACATION's 31 Dec. A discriminating AC pins the exact boundary date.
- **R4 (the §34-free partition branch — Reviewer B1, HARD constraint).** A **DEDICATED SPECIAL_HOLIDAY partition + settle method** that NEVER threads through the shared VACATION `Partition()`/`SettleActiveYearEndAsync` (`:1362/:320` — which would inherit `overCap=disposable−CarryoverMax`=whole-balance → forfeit → PENDING_REVIEW + the carryover-write). Godtgørelse-only: NO over-cap/§34/§22, NO PENDING_REVIEW; the remainder IS `disposable` (no cap). NOT an `if type==SPECIAL_HOLIDAY` shortcut inside the VACATION partition (Step-0b W3). Test: an unused 5-day balance → SETTLED + `payout_days = remainder`, NEVER PENDING_REVIEW/forfeit_days; + a regression test that VACATION's partition is unchanged.
- **R5 (the §15 stk.1 safety gate — Codex BLOCKER).** The SPECIAL_HOLIDAY auto-close stays DORMANT (behind D13 AND an explicit "§15 stk.1 not modeled" gate) — it must NOT auto-pay a godtgørelse until the transfer-agreement surface exists, else an employee with a carry-forward agreement is wrongly paid. Recorded as a go-live dependency. (Pre-launch nothing auto-runs, so the machinery is built + gated.)
- **R6 (the second due-enumeration).** `SettlementCloseService` gains a SPECIAL_HOLIDAY enumeration pass with its OWN 30 Apr boundary geometry (via the R10 resolver), separate from the VACATION 31 Dec pass (NOT a parameter flip). **Do NOT reuse the VACATION `resetMonthCache` without a type dimension** (Step-0b — `:700-710`); the SQL anti-join is already type-scoped (`:664-670`) and the advisory lock employee-scoped, so the shared-state risk is manageable but the cache must be type-keyed.
- **R7 (the godtgørelse line).** `SaerligeFeriedagePaidOut` is **consumed AS-IS** (the event payload is frozen — `payout_days` + snapshot; no schema change to the event — Step-0b W2). A NEW settlement `time_type` (e.g. `SPECIAL_HOLIDAY_SETTLEMENT_PAYOUT`) → a `SLS_TBD_*` sentinel (delivery-disabled, triple-locked, the §24 `SLS_TBD_S24` pattern) lives in the Payroll `wage_type_mappings` — MUST NOT reuse the consumption `SLS_0570` (`init.sql:261`). **Widen BOTH the emitter switch (`SettlementExportEmitter.cs:174`) AND the repository event filter (`SettlementInboxLineRepository.cs:91`)** (Step-0b). Snapshot-keyed (the §24 pattern); exactly-once, replay-deterministic, money-free. Record 5017/5027/5037 in the ADR (not wired).
- **R8 (event/audit).** `SaerligeFeriedagePaidOut` emit + the ADR-026 mapper + DI registration + the catalog row (test-parsed) + round-trip coverage.
- **R9 (migration).** Pre-launch reseed of the SPECIAL_HOLIDAY config + re-derive any existing SPECIAL_HOLIDAY balances; the legacy-DB-upgrade-runbook note; a discriminating before/after test.
- **R10 (ferieår-resolution — Step-0b DECIDED: HOIST now, in 8001).** Both lenses ruled hoist (the SPECIAL_HOLIDAY taking window is a genuine THIRD geometry — accrual-year ≠ taking-window-year; neither existing `reset_month` branch expresses it — so the 5-site Month≥9 duplication would become a 5-site THREE-geometry divergence, [[cross-process-caller-census]]). Hoist ONE shared resolver returning **`(entitlementYear, accrualStart, takingStart, boundary)` by `(entitlementType, resetMonth)`** + cut the 5-6 sites (`SkemaEndpoints.cs:96`, `BalanceEndpoints.cs:210/:940`, `SettlementCloseService.cs:759/:789`, `VacationSettlementService` window) over to it. R2 depends on it → the resolver lands in **8001** before 8002 consumes it.
- **R11 (invariants).** Money-isolation (day-count only — SLS owns 2½%; §17 ≠ §10's 2,02%, D12); the close tx under `pg_advisory_xact_lock`; no `used` mutation; replay-deterministic; D13-gated.
- **R12 (non-goals — recorded follow-ups).** §15 stk.1 transfer-agreement surface (+ the SPECIAL_HOLIDAY auto-close go-live gate it unblocks); wiring the verified 5017/5027/5037; any §26-termination interaction for særlige feriedage (not in this slice).

## Task decomposition
| Task | Scope | Review |
|------|-------|--------|
| **8001** | The D11 model correction (R1/R2/R3/R9): calendar accrual + the 1 May–30 Apr taking-window keying + the reseed/migration | **Step-5a** (the booking/accrual keying) + Step-7a |
| **8002** | The SPECIAL_HOLIDAY settlement close + godtgørelse (R4/R5/R6/R8): the second 30 Apr due-pass + the §34-free partition branch + the gated dormancy + the event/audit | **mandatory Step-5a** (legal-sensitive) + Step-7a |
| **8003** | The godtgørelse staged line (R7): the new time_type + SLS_TBD_* sentinel (≠ SLS_0570) + the §24-pattern consumer | **Step-5a** + Step-7a |
| **8004** | Step-7a dual-lens + docs (ADR-033 amendment, SPRINT-80, INDEX/ROADMAP/QUALITY, the R10 decision) + the LOCAL close | — |

**Sequencing:** 8001 (the model correction — the foundation + the highest-risk keying) → 8002 (the close, consuming the corrected model) → 8003 (the line) → 8004 (docs/close). The R10 ferieår-resolution decision is taken at Step-0b and shapes 8001.

## Step-0b plan-gate
**Cycle 1 — [[review-lens-complementarity]]: both lenses converged.**
- **BLOCKER (Reviewer; Codex NOTE'd the same substance): R2 was under-specified — the taking window spans TWO calendar years** (1 May Y+1 – 30 Apr Y+2), so the usage→accrual-year mapping needs BOTH halves (the first text named only the Jan–Apr Y+2 tail, omitting the larger May–Dec Y+1 head). → **R2 CORRECTED** to the full function (`May–Dec T → accrual T−1; Jan–Apr T → accrual T−2`) + an AC pinning BOTH halves.
- **WARNING (both): R10 = HOIST the resolver NOW** (the third geometry) → R10 decided (the resolver in 8001, returning the 4-tuple by type).
- WARNING (Reviewer W2): the `SaerligeFeriedagePaidOut` payload is FROZEN → R7 pins "consumed as-is; the time_type lives in the mappings, not the event".
- WARNING (Reviewer W3): forbid threading SPECIAL_HOLIDAY through the shared §34 `Partition`/`SettleActiveYearEndAsync` → R4 pins a DEDICATED settle method + a VACATION-unchanged regression test.
- NOTEs (Codex): R6 — type-dimension the `resetMonthCache`; R7 — widen BOTH the emitter switch AND the repo event filter; R3 (30 Apr Y+2), R4 (separate godtgørelse-only branch), R5 (operation-level dormant gate, NOT per-row PENDING_REVIEW — writing PENDING_REVIEW would consume due-detection), R9 (reseed safe), money-isolation/§17≠§10 all confirmed; sequencing right (resolver in 8001).
- **Cycle 2 — Codex re-verification: CLEAN — no findings.** R2 COMPLETE (the full mapping covers every month, no residual mis-keying; matches the law worked example accrual-2022→boundary-2024-04-30); R10/R4/R6/R7 correct and sufficient. **Step-0b CONVERGED → 0 BLOCKERs. Ready for implementation (8001 → 8002 → 8003 → 8004).**

## Implementation Record
- **8001 ✅ ACCEPTED 2026-06-16 (fix + re-Step-5a CLEAN both lenses; WIP commits `2ddd6b5`→`767305b`[fix]→`ac833d3`[cosmetic], superseded at the close).** The build landed the hoisted `EntitlementPeriodResolver` (VACATION behavior-identical; SPECIAL_HOLIDAY calendar accrual + the 1 May–30 Apr taking-window keying [May–Dec T→T−1, Jan–Apr T→T−2] + the 30 Apr Y+2 boundary), the SPECIAL_HOLIDAY config reseed (reset_month 9→1, idempotent), and cut over Balance/Skema/VacationSettlement endpoints + the close service + service. Build 0E; unit 848/848; the resolver tests discriminate both halves + the VACATION-identical pin. **BUT Step-5a — [[review-lens-complementarity]] (Codex caught what the Reviewer false-cleared):**
  - **BLOCKER 1 (a VACATION REGRESSION — Codex; the Reviewer false-cleared it "byte-identical"; Orchestrator VERIFIED against source = REAL).** `BalanceEndpoints.cs:~1013` YearOverview uses the resolver's `.Boundary` (the §21 31-Dec deadline) as the `EarnedToDate` asOf instead of the ferieår END (31 Aug). `AccrualMath.EarnedToDate` (`AccrualMath.cs:67-75`) clamps `monthsElapsed` FROM THE ACCRUAL START (=hire), capped at 12 — NOT capping the asOf at the ferieår end. So a VACATION employee hired MID-ferieår (e.g. Jan 2025, ferieår Sep'24–Aug'25): old 31-Aug asOf → 8/12; new 31-Dec asOf → 12/12 (over-count). **FIX:** the resolver must expose the **accrual-period-END** (31 Aug VACATION / 31 Dec-Y SPECIAL_HOLIDAY accrual) SEPARATELY from the settlement boundary (§21 31-Dec / 30 Apr Y+2); YearOverview's earned-at-boundary asOf must use the accrual-period-end. + a mid-ferieår-hire VACATION discriminating test.
  - **BLOCKER 2 (SPECIAL_HOLIDAY saldo split-brain — Codex).** `BalanceEndpoints.cs:~967` subtracts `AfholdtWithin(type, ferieaarStart, monthEnd)` by DATE RANGE, not by resolved accrual year — wrong for SPECIAL_HOLIDAY (its taking window spans two calendar years; a Mar-2025 booking keyed to accrual 2023 is wrongly subtracted from a Nov-2025/accrual-2024 view). **FIX:** subtract the afholdt keyed to the resolved accrual year.
  - **BLOCKER 3 (revaluation split-brain — BOTH lenses).** `EmployeeProfileEndpoints.cs:835` (`ApplyRevaluationAsync`, local helper `:881`) was NOT cut over — still raw `Month>=resetMonth`; after the reseed it keys SPECIAL_HOLIDAY to the raw calendar year while booking uses the taking-window mapping → revaluation hits the WRONG balance row. **FIX:** route `:835` through the resolver; remove the local helper; + a Jan–Apr-SPECIAL_HOLIDAY revaluation discriminating test.
  - WARNING (defer to 8002): `VacationSettlementService.cs:852` (`IsYearEndSupersedeDueUnderLock`) leftover inline `Month>=resetMonth` — VACATION-only today, but type-guard it before 8002 introduces SPECIAL_HOLIDAY settlements. NOTE: the init.sql AC_RESEARCH/AC_TEACHING SPECIAL_HOLIDAY seed literals still say `reset_month=9` (the later idempotent UPDATE flips them to 1 — end-state OK but brittle; align the seed literal).
  - **COVERAGE GAP (important):** the affected regression passed **398/398 DESPITE all 3 BLOCKERs** — i.e. NO existing test discriminates them (and the agent's touched `YearOverviewTests` may have been adjusted to the regressed behavior). So each of the 3 fixes MUST ship with a test that is **RED on the current (pre-fix) code** (the all-session discipline). Do NOT trust the green 398 — it's blind to these paths.
  - **FIXED + re-Step-5a CLEAN (2026-06-16) — [[review-lens-complementarity]]: the cycle-1 Codex catch of the VACATION regression (the Reviewer false-cleared it; verified vs source) was the decisive save.** The two fix-agents both died on transient API errors (500/529) after landing the 3 code-fixes + the seed alignment; the Orchestrator completed it directly (the incomplete BLOCKER-3 test's 3 helpers). FIXES: (1) the resolver gained an **`AccrualEnd`** field (= the accrual-period END: ferieår-end 31 Aug E+1 / 31 Dec E for calendar / 31 Dec Y for SPECIAL_HOLIDAY) DISTINCT from the settlement `Boundary`; YearOverview earned-at-boundary uses `AccrualEnd` (VACATION byte-identical to pre-S80; SPECIAL_HOLIDAY correct). (2) the SPECIAL_HOLIDAY saldo subtracts over `[TakingStart, min(monthEnd, Boundary)]`, gated to SPECIAL_HOLIDAY only. (3) the revaluation routes through the resolver (local helper deleted). + the init.sql seed-literal alignment. **3 discriminating tests**, BLOCKER-1 EMPIRICALLY PROVEN RED-on-`2ddd6b5` (reverted BalanceEndpoints → the test failed: Actual 20.0 vs the 8/12 the fix gives); BLOCKER-2/3 discriminate by construction + verified by both lenses. Re-Step-5a: **Codex CLEAN; Reviewer "all 3 CLOSED" + 2 cosmetic NOTEs (a dangling `<see cref>` + missing direct `AccrualEnd` unit assertions) — both fixed (`ac833d3`).** Build 0E; unit 848/848 (resolver 24/24 incl. AccrualEnd pins); affected regression 56/56. **NEXT: 8002** (type-guard the WARNING site `VacationSettlementService.cs:852` there).
- **8002 ✅ ACCEPTED 2026-06-16 (Codex re-Step-5a CLEAN; internal Reviewer DEFERRED to Step-7a — Anthropic API outage; WIP `df41a7a`).** The SPECIAL_HOLIDAY settlement close + the §15 stk.2/§17 godtgørelse: **R6** a SECOND due-enumeration on the 30-Apr-(Y+2) boundary (type-scoped, resolver-driven, separate from the VACATION 31-Dec pass); **R4** a DEDICATED `SettleSpecialHolidayGodtgoerelseAsync`/`PartitionSpecialHoliday` that NEVER threads the §34 `Partition()` (remainder = earned[via `AccrualEnd` 31 Dec Y, the 8001 lesson] + carryoverIn − used[taking-window] − planned → all to `payout_days`; SETTLED, never PENDING_REVIEW); **R5** the `Settlement:SpecialHolidaySettlementEnabled` operation-level dormant gate (default OFF + fail-closed, behind D13 too — the §15-stk.1 wrongful-payout safety; no PENDING_REVIEW rows that would consume due-detection); **R8** `SaerligeFeriedagePaidOut` emit + the ADR-026 mapper + Program.cs DI + the catalog row (67→68); + the `:852` `IsYearEndSupersedeDueUnderLock` type-guard fix. **Step-5a — [[review-lens-complementarity]]: Codex c1 BLOCKED(2)→c2 CLEAN** (the Reviewer lens died on the API outage both cycles): (BL1) the SPECIAL_HOLIDAY branch was placed BEFORE the in-lock leaver/not-due guards → a now-leaver could be paid (TOCTOU) → MOVED after the guards + a now-leaver SPECIAL_HOLIDAY fails closed (R12 termination-interaction non-goal); (BL2) the SPECIAL_HOLIDAY supersede path routed to `SettleActiveYearEndAsync` (the §34 Partition) → fail-closed at the top of `ResettleSupersedingAsync` (unreachable while R5-gated; a dedicated supersede path is a recorded follow-up). + a BLOCKER-1 leaver discriminating test (9/9 SPECIAL_HOLIDAY suite; discriminates by construction). Build 0E; unit 854/854; affected regression 107/107. **The internal Reviewer lens on 8002 is DEFERRED to the program Step-7a (8004), which reviews the full S80 diff incl. 8002 — recorded because the Anthropic API was overloaded during 8002.** **NEXT: 8003** (the SLS_TBD_* godtgørelse line + the §24-pattern consumer).
- **8003 ✅ ACCEPTED 2026-06-16 (Step-5a dual-lens: Codex CLEAN; Reviewer WARNING+NOTE, no BLOCKER; WIP next).** The §15 stk.2/§17 godtgørelse staged payroll LINE (R7), mirroring the §24/§69 + §26/§71 pattern: the poll filter widened for `SaerligeFeriedagePaidOut` (`SettlementInboxLineRepository`); the emitter branch `ProcessSaerligeFeriedagePaidOutAsync` (`SettlementExportEmitter`) stages ONE `line_kind='ORIGINAL'` money-free line (`hours=PayoutDays`, `amount=0`) off the IMMUTABLE snapshot, dated lønart at `asOf=Snapshot.SettlementBoundaryDate` (30 Apr Y+2), a NEW inbox bucket `GODTGOERELSE_S15S17`, exactly-once via `(source_event_id, bucket)`; a NEW `wage_type_mappings` row `SPECIAL_HOLIDAY_SETTLEMENT_PAYOUT → SLS_TBD_S15S17` (the 10-pair matrix; DISTINCT from the consumption `SPECIAL_HOLIDAY_ALLOWANCE → SLS_0570`; the `SLS_TBD_*` prefix → never delivered). 11 tests (2 unit sentinel + 5 emitter + 4 schema), discriminating. Build 0E; sentinel 6/6; emitter+schema regression 20/20. **Step-5a Reviewer WARNING (RECORDED follow-up, NOT blocking — non-corrupting + R5-dormant):** the §15 consumer OMITS the §24 under-lock REVERSED-row probe (step 0b) → a SPECIAL_HOLIDAY line staged AFTER a reversal interleaved could orphan (the `SettlementReversed` consumer derives targets bucket-agnostically). Non-corrupting in-slice (SPECIAL_HOLIDAY close R5-gated dormant → no event emitted; `SLS_TBD_*` never delivered). PARITY NOTE in the consumer code. **GO-LIVE FOLLOW-UP (with the §15-stk.1 gate + the 8002 supersede path): the SPECIAL_HOLIDAY reversal-handling cluster — the §24 REVERSED-probe retrofit on the §15 consumer + the dedicated supersede partition — MUST land before the §15-stk.1 auto-close gate opens.** (Codex NOTE: the §24 reconciled-skip omission is correct — no §15 reconciliation surface.) **The verified SLS codes 5017/5027/5037 to record in the ADR amendment (8004).** **NEXT: 8004** (Step-7a — incl. the internal lens on the FULL diff [8002+8003] — + docs + LOCAL close).

## Step-7a — program-level dual-lens
**CONVERGED — no BLOCKER (both lenses); the deferred 8002+8003 internal lens caught up here.** Reviewed the full S80 diff `699cc3a..726c26c`.
- **Reviewer (internal): CLEAN — no findings (2 NOTEs = already-recorded follow-ups).** Verified end-to-end: SPECIAL_HOLIDAY NEVER reaches the §34 `Partition()`/PENDING_REVIEW on ANY path (direct/leaver/supersede/reversal); the VACATION-unchanged invariant byte-for-byte across all 3 tasks (the resolver hoist, the close branch-reorder + the `:852` fix, the line); the 8002 dormant gate airtight + the leaver/supersede fail-closes correct + the system-actor audit-org convention + the catalog 67→68; 8003 exactly-once + money-isolation + replay + distinct-from-SLS_0570; no split-brain (accrual→taking→close→line all key by the resolver's accrual year).
- **Codex (external): no BLOCKER; 1 WARNING + 1 NOTE — both non-corrupting + R5-dormant + NEW go-live follow-ups:**
  - **WARNING (snapshot-capture strictness):** the SPECIAL_HOLIDAY snapshot capture (`VacationSettlementService.cs:817/:862`) is LESS strict than §24 — a missing dated agreement falls back to `user.AgreementCode`/live config, a missing dated profile → `Position=null` → `""` in the emitter's lønart lookup — so a §15/§17 line could durable-stage under a live/empty wage-key where §24 fails closed (`:1403/:1508`). Non-corrupting in-slice (R5-dormant + `SLS_TBD_*` never-delivered). **GO-LIVE FOLLOW-UP** (with the §15-stk.1 gate): make the SPECIAL_HOLIDAY snapshot capture fail-closed on a missing dated agreement/profile, matching §24.
  - **NOTE (zero-day emission):** `SaerligeFeriedagePaidOut` is emitted even when `PayoutDays == 0` (§24 only emits when payout > 0). Non-corrupting (`SLS_TBD_*` undeliverable). **GO-LIVE DECISION (recorded):** before real SLS codes, either pin it as intended zero-day audit behavior or skip the event/line at 0 (the §24 precedent).
- **Disposition:** no BLOCKER → no close gate. The WARNING + NOTE join the SPECIAL_HOLIDAY go-live follow-up cluster (below). NOT fixed in-slice (non-corrupting + dormant; fixing would expand the close + invalidate the running pyramid).

### Go-live follow-up cluster (the SPECIAL_HOLIDAY auto-close §15-stk.1 gate dependencies — ALL must land before `SpecialHolidaySettlementEnabled` opens)
1. The **§15 stk.1 transfer-agreement surface** (else the godtgørelse wrongly pays a carry-forward-agreement holder — the R5 gate's reason).
2. The **SPECIAL_HOLIDAY reversal-handling cluster:** the §24 under-lock REVERSED-row probe retrofit on the §15 consumer (8003 Step-5a) + the dedicated SPECIAL_HOLIDAY supersede partition (8002 Step-5a).
3. The **snapshot-capture fail-closed** retrofit (Step-7a WARNING — match §24's dated-agreement/profile strictness).
4. The **zero-day-emission** decision (Step-7a NOTE — pin vs skip-at-0).
5. The **verified SLS codes 5017/5027/5037** swap for `SLS_TBD_S15S17` (the SLS-dialogue).

## Test Summary
Full local pyramid (S80 is backend-only — no FE diff):
- **Unit:** 856/856 (fresh run; +32 vs S79's 824 — the new `EntitlementPeriodResolverTests` incl. AccrualEnd pins, `SpecialHolidayPartitionTests` (6), the `Slice3bSentinelCoverageTests` SaerligeFeriedagePaidOut sentinel, + `DefaultEntitlementConfigTests`/`ReversalSequenceArithmeticTests` extensions).
- **Regression (Docker-gated):** **956/956 (single clean run, 0-fail, 51m31s, no FAIL-002)** [+22 vs S79's 934; log `.claude/s80-full-regression.log`]. The S80 regression-side additions: `SpecialHolidaySettlementTests` (incl. the leaver-fails-closed discriminator), `Settlement15S17GodtgoerelseEmitterTests`, `Settlement15S17WageMappingSchemaTests`, the `YearOverviewTests` BLOCKER-1 RED-on-old pin, the `Adr032RevaluationTests` BLOCKER-3 accrual-year pin, + `BalanceSeriesTests`/`SkemaMonthlyAccrualGuardTests`/`SettlementEmitterFixture` extensions.
- **Smoke (fresh greenfield):** 6/6 — `docker compose down -v` + `up --build` from a clean volume (init.sql changed: the SPECIAL_HOLIDAY `reset_month` 9→1 reseed + the new `SPECIAL_HOLIDAY_SETTLEMENT_PAYOUT → SLS_TBD_S15S17` wage-type-mapping), then the smoke suite.
- **Frontend:** 459/459 (unchanged — S80 touched no FE file).
- **Pyramid total: 856u + 956r + 6s + 459fe = 2277 (+54 vs S79's 2223).**

**CI status:** CI-pending — the owner is out of GitHub Actions minutes (the S78 close `27596242792` was the last CI-verified close, 16th consecutive). The full pyramid is verified LOCALLY green; the push (the held S79 `699cc3a` + this S80 close) + CI-verify ride the next push when the Actions allowance returns. The consecutive-CI-pending close gate is waived for S80 via `.claude/reviews/SPRINT-80-ci-pending-WAIVED.md` (S79 was the first CI-pending close; this is the second consecutive — Actions-exhaustion, NOT a Docker-down/locally-unverified close).


## Close
**S80 CLOSED LOCALLY 2026-06-16.** ADR-033 Slice 2 (særlige feriedage) — the LAST remaining ADR-033 settlement slice — is feature-complete and gated.

- **Delivered (4 tasks):** 8001 the D11 calendar-accrual correction + the hoisted `EntitlementPeriodResolver` (the two-armed taking-window mapping; VACATION behavior-identical); 8002 the SPECIAL_HOLIDAY settlement close + the §15 stk.2/§17 godtgørelse (the dedicated §34-free partition, the `SpecialHolidaySettlementEnabled` dormant gate, the `SaerligeFeriedagePaidOut` emit + ADR-026 mapper); 8003 the `SLS_TBD_S15S17` staged payroll line (the §24-pattern consumer, distinct from the consumption `SLS_0570`); 8004 the program Step-7a + docs + this close.
- **Gates:** Step-0b CONVERGED (2 cycles, 0 BLOCKER); per-task Step-5a dual-lens on every task (8001 + 8002 the legal-sensitive ones — [[review-lens-complementarity]] decisive: Codex caught the VACATION over-count regression + the leaver-TOCTOU the internal Reviewer false-cleared); program Step-7a CONVERGED CLEAN (Reviewer CLEAN incl. the caught-up 8002/8003 internal lens; Codex no-BLOCKER + 1 WARNING + 1 NOTE → the go-live cluster).
- **Architectural posture:** money stays OUT (SLS owns the 2½% godtgørelse); the auto-close is DORMANT behind D13 + the `SpecialHolidaySettlementEnabled` §15-stk.1 safety gate; the `SLS_TBD_*` line is never delivered. **No launch-blocking item.** The 5-item go-live follow-up cluster (above) gates the eventual `SpecialHolidaySettlementEnabled` opening; the manual fallback remains the recorded launch posture.
- **Commit:** the 6 S80 WIP commits (`2ddd6b5`..`726c26c`) squashed into ONE S80 close commit on `699cc3a` (S79). **Push HELD** (owner out of Actions); the S79+S80 stack rides the next push + CI-verify.
- **ADR-033 D6 / D11:** the slice-2 amendment section records the D11 calendar-accrual correction, the two-armed taking window, the §15 stk.2/§17 godtgørelse, the verified SLS codes 5017/5027/5037 (recorded, not wired), and the go-live follow-up cluster.
