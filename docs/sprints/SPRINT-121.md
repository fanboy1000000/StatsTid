# Sprint 121 — The three S118-deferred prod defects repaired (config admin write flows)

| Field | Value |
|-------|-------|
| **Sprint** | 121 |
| **Status** | complete |
| **Start Date** | 2026-07-23 |
| **End Date** | 2026-07-23 |
| **Orchestrator Approved** | yes (2026-07-23) |
| **Build Verified** | yes — `dotnet build` 0 errors (post-12100 + the final tree) |
| **Test Verified** | yes — 2951 green (see Test Validation Report; 45 environmental sheds isolation-cleared per FAIL-002) |

## Sprint Goal
Repair the three S118-deferred LIVE prod defects on the config admin surface — **(1)** the wage-type-mapping update dead write flow (binder-400: spec-required `effectiveFrom` never sent), **(2)** the child-entitlement create+update dead-ends (the S118 pin was UPDATE-only; the grounding sweep proved CREATE also 422s for CARE_DAY/SENIOR_DAY — the create body omits `fullDayOnly` too and `FullDayOnlyGuard` sits on both verbs), **(3)** the agreement-config archive + supersession re-publish 500 (bare strings into `::jsonb` audit params → 22P02 → rollback on EVERY archive/re-publish; the audit rows have NEVER landed). Every S118 defer-pin flips DELIBERATELY (defer-pin-never-silence); the two surviving legacy explicit-T FE calls graduate to the typed forms — **completing the FE side of the PAT-012 program** (post-S121 FE-lint residue = exactly the S115 eligibility helper pin; the OpenAPI manifest stays 134/3/9 — no op changes tier).

**THREE OWNER RULINGS (2026-07-23, "Go with your recommendations" on the refinement's recommended options):**
1. **`effectiveFrom` → server-defaulted optional across the WHOLE three-PUT family** (WTM PUT `WageTypeMappingEndpoints.cs:743`, child-entitlement PUT `AgreementEntitlementEndpoints.cs:791`, primary entitlement-config PUT `EntitlementConfigEndpoints.cs:885` — the Reviewer-W1 family framing: ruling only the two dead PUTs would leave the midnight race live on the primary surface, whose FE already client-computes today). Request-wire LOOSENING (`required` → optional; backward-compatible). **The compute-once contract (Codex W1): `var requestedEffectiveFrom = body.EffectiveFrom ?? today;` at the TOP of each handler, used at EVERY downstream site** — the WTM PUT reads the body value at FOUR sites (validator `:417`, **the 422-payload `suppliedEffectiveFrom` echo `:422`**, persisted mapping `:449`, cross-day branch discriminator `:469`); the child PUT at `:311` + **the `:316` 422-payload echo**; the primary PUT at `:393` + **the `:398` echo** (Step-0b Reviewer N1 — the echo reads must ALSO use the computed value); a partial substitution desynchronizes the routing. The same-day 422 validator RETAINED for explicitly-sent non-today values (S29 semantics unchanged).
2. **`fullDayOnly` preserve-only in the child form** — derived `true` for CARE_DAY/SENIOR_DAY (the D-A ruling + the DB CHECK `entitlement_configs_full_day_only_types`), the row's current value round-tripped for non-forced types; NO new UI control (an editable checkbox = a new product capability on a rule-bearing flag — out of scope, deferred to a product decision).
3. **`FullDayOnly` → `required` on all 4 entitlement DTOs** (child POST/PUT + primary POST/PUT — the Reviewer-W2 structural closure): omission becomes a binder-400 (the request-side lie detector now covers the trap that shipped this defect) instead of a silent guard-422. Request-wire TIGHTENING; both known FE surfaces send the member post-S121 (the primary editor already does — create checkbox `EntitlementConfigEditor.tsx:524`, PUT preserve `:237`).

Refinement: `.claude/refinements/REFINEMENT-s121-deferred-defects.md` — READY; Step-4 closed 2 cycles Codex (1B — the DRAFT-archive audit-lie; c2 "Clean — absorption verified") + 1 cycle Reviewer (0B/2W/4N — the third-PUT family framing, the structural trap, the P3 caller census CLEAN, the throw-on-second harness note; all absorbed). All 3 rulings owner-granted 2026-07-23.

**Explicit exclusions:** NO rule-semantics change (the `FullDayOnlyGuard`, the DB CHECK, and the D-A ruling untouched — P4); NO editable `fullDayOnly` control; NO other handler-logic change beyond the two audit sites + the compute-once substitutions + the sanctioned `SaveAgreementConfigResult.PreviousStatus` result-member; the repo suites that hand-built valid audit JSON stay UNTOUCHED (they become consistent with the endpoint once it serializes properly — do not "fix" their inputs); NO response-envelope change on any op (previously-REACHABLE 200s byte-identical; the never-succeeded archive/supersession 200s asserted against their declared contract shapes — no runtime baseline exists); the masking-class repo tests are NOT the anti-masking layer — the new HTTP-driven assertions are; `GET /health` + the 2 flag-and-defer ops untouched.

## Ground Facts (Step 0a digest — refinement-grounded, Reviewer-verified)
| Check | Result | Detail |
|-------|--------|--------|
| Defect #3 sites | exactly 2 | `AgreementConfigEndpoints.cs:510-512` (supersession-publish archive leg: bare `"ACTIVE"`,`"ARCHIVED"`) + `:619-621` (direct archive: bare `existing.Status.ToString()`,`"ARCHIVED"`); all 3 `AppendAuditAsync` overloads cast `::jsonb` (`src/Infrastructure/StatsTid.Infrastructure/AgreementConfigRepository.cs` — overloads `:732/:755/:783`, casts `:741/:763/:795`). The healthy sites' convention = hand-built single-key JSON objects (`:264` CLONED, `:477` PUBLISHED) |
| The DRAFT-archive shape (Codex B) | 2 previous-status shapes | direct archive admits ACTIVE **or DRAFT** (`:574`); previous data = the TRUE status from the FOR-UPDATE-locked row (Reviewer N3 — not the pre-flight read); the supersession leg's previous is structurally ACTIVE |
| P3 caller census | CLEAN | zero production readers of `agreement_config_audit` (INSERT sites only; sole reader = a test); `AgreementConfigArchivedAuditMapper` exists, Program.cs-registered, cataloged (audit-projection-catalog.md:94, GLOBAL_TENANT_VISIBLE); `AuditEndpoints` filters event types generically — first-time-landing rows surprise nothing; NO migration needed (the rows never landed) |
| The 3-PUT `effectiveFrom` family | verified | all three `required DateOnly` + same-day-422; the child PUT already stamps the new row with server today (`AgreementEntitlementEndpoints.cs:407` — body value read only in the `:311` validator + its `:316` 422-echo); the WTM POST precedent `:101-104` has the compute-once shape; nothing downstream distinguishes client-sent from server-defaulted today (Reviewer-verified) |
| The 4-DTO `fullDayOnly` set | verified | child POST `:768` / child PUT `:790` / primary POST `:852` / primary PUT `:882` — all non-required `bool` today; the guard fires on false-or-absent for the 2 forced types on all 4 verbs; the row data the FE needs for preserve is present post-S118 (`fullDayOnly` REQUIRED on the response row — wire-pinned `EntitlementSection.wire.test.tsx:111-112`) |
| FE surfaces | 2 to fix, 1 to align | `frontend/src/components/admin/EntitlementSection.tsx` (the ONLY broken surface — full path per Reviewer N2; a same-named file under `pages/admin/editPerson/` is unrelated and clean) + `useWageTypeMappings.ts`; `EntitlementConfigEditor.tsx` is LIVE-working on client-computed today (`:234`) → aligns to omission under ruling #1 |
| Defer-pins to flip | named | the S118 absence wire-pins (`EntitlementSection.wire.test.tsx:139-140/:157-158` + the WTM equivalents), the 2 eslint carve-outs (`eslint.config.mjs:193/:205`) + partial tiers, the labeled 22P02 tripwire (`S118AgreementConfigSpecRuntimeTests.cs:354`), the Constraint-Validator absence sweep lines |
| Rollback-assertion home | atomic-mirror suite | the `AgreementConfigAtomicTests` convention; `ForcedRollbackHarness.ThrowingOutboxEnqueue` throws on EVERY call → a throw-on-SECOND-call decorator variant needed to reach the archive leg (the harness header bars a WebApplicationFactory route) — Reviewer N1 |
| Spec impact | request-side only | regen: `effectiveFrom` leaves 3 request `required` arrays; `fullDayOnly` enters 4; generated TS request types shift accordingly; NO response schema change; convention gate unchanged 134/3/9 |

## Plan Review (Step 0b)
| Field | Value |
|-------|-------|
| **Trigger** | MANDATORY — owner-ruled wire changes (a 3-op loosening + a 4-DTO tightening) + P3 surface (the audit fix makes never-landed audit rows land atomically) |
| **External Codex** | cycle 1 (2026-07-23): **1 BLOCKER / 2 WARNING / 3 NOTE** — B: the locked-row previous status is repo-local (the `ArchiveAsync` result carries no pre-archive status) → the sanctioned `PreviousStatus` result-member extension; W1 the byte-identity criterion unverifiable for never-succeeding flows → contract-shape-identity wording; W2 the primary absent→422 doctrine test also flips → named. N: the compute-once site lists confirmed complete; the supersession-leg structurally-ACTIVE claim verified (repo `:536`); the throw-on-second decorator fits the harness. Cycle 2: pending verification of the absorption. |
| **Internal Reviewer** | cycle 1 (2026-07-23): **2 BLOCKER / 1 WARNING / 2 NOTE** — B1 CONVERGENT with Codex (+ the repo file joins TASK-12100 Components; the path citation corrected); B2 the tightening blast radius = EXACTLY 5 named `Config/` tests (2 semantic flips of the D-A absent→422 doctrine + 2 collateral body fixes + 1 re-expression), everything else sweep-verified SAFE and the loosening radius CLEAN; W1 the pin-flip inventory made precise (the `effectiveFrom` absence pins REMAIN, the key-set pins FLIP, one stale presence pin found); N1 the 422-payload echo reads named; N2 all other plan claims verified accurate. ALL absorbed into the tasks. Cycle 2: pending verification of the absorption. |
| **BLOCKERs resolved before Step 1** | **yes — the convergent locked-row BLOCKER resolved via the sanctioned repo-result extension; the blast-radius BLOCKER resolved by naming all 5 tests + adding Config/ to TASK-12102; cycle-2 verification of both lenses precedes agent launch** |

---

### TASK-12100 — Backend: the two audit sites + the 3-PUT loosening + the 4-DTO tightening + regen
| Field | Value |
|-------|-------|
| **ID** | TASK-12100 |
| **Status** | complete (2026-07-23) — build 0 err; regen sha-idempotent ×2 (`3AB50594…`); convention 134/3/9; drift in sync (102/187). **The spec delta EXACT: `effectiveFrom` out of the 3 PUT `required` arrays (+ the inherent `nullable:true` on those property schemas, byte-matching the POST-optional precedents); `fullDayOnly` into the 4; ZERO response hunks.** `SaveAgreementConfigResult.PreviousStatus` (trailing, defaulted — zero construction-site churn) populated from the locked `:691` read; the direct-archive audit uses it (NOT the pre-flight); the supersession leg `{"status":"ACTIVE"}`→`{"status":"ARCHIVED"}`. Compute-once grep-proven: `body.EffectiveFrom` exactly once per handler (WTM `:419`, child `:314`, primary `:396`). **The expected-RED census EXACT: Config/WTM scoped 29/34 with the 5 named blast-radius REDs matched 1:1 to the plan entries (no strays; all WTM retained-422 pins green); S118 scoped 27/28 with the single RED = the labeled 22P02 tripwire (the fix un-broke what it pins).** Append order/guard/envelopes/policies: zero hunks (grep-proven). No test modified. |
| **Agent** | Backend/API (cross-domain AUTHORIZED into docs/api/openapi.json + generated api-types.ts via regen — the standing PAT-012 pipeline pattern) |
| **Components** | AgreementConfigEndpoints.cs (2 audit sites), **src/Infrastructure/StatsTid.Infrastructure/AgreementConfigRepository.cs (the sanctioned `SaveAgreementConfigResult.PreviousStatus` result-member extension — Step-0b convergent BLOCKER both lenses: the locked `currentStatus` at `:691` is repo-local; SQL/locking/version semantics byte-unchanged)**, WageTypeMappingEndpoints.cs (PUT DTO + compute-once), AgreementEntitlementEndpoints.cs (PUT DTO + compute-once; POST/PUT FullDayOnly required), EntitlementConfigEndpoints.cs (PUT DTO + compute-once; POST/PUT FullDayOnly required), regen |
| **KB Refs** | PAT-012 (paved road, regen), ADR-018 D3 (atomic write — the append ORDER inside the tx is byte-unchanged), ADR-020 |

**Description**: (1) The two audit sites serialize honest single-key JSON: direct archive = `{"status": <locked-row status>}` → `{"status":"ARCHIVED"}` — **the locked status is NOT currently available at the call site (Step-0b Codex BLOCKER: `ArchiveAsync` reads `currentStatus` internally, `AgreementConfigRepository.cs:729`, but its result `:953-958` does not carry it) — the SANCTIONED additive repo change: `ArchiveAsync`'s result gains the locked `PreviousStatus` member (result-shape extension only; the SQL, locking, and version semantics byte-unchanged), and the audit site uses it**; supersession leg = `{"status":"ACTIVE"}` → `{"status":"ARCHIVED"}` (structurally correct — the repo archives only `status = 'ACTIVE'` rows there, `:536`). The tx append ORDER (state → audit → outbox) byte-unchanged; no other handler hunks at these sites beyond the sanctioned result-member consumption. (2) The three PUT DTOs: `EffectiveFrom` → `DateOnly?`; compute-once `?? today` at the top of each handler substituted at EVERY downstream body-read (the FULL four/two/two-site lists incl. the 422-payload echoes — per ruling #1 above); the same-day 422 retained for explicit values; 200 envelopes byte-identical. (3) The four DTOs: `FullDayOnly` → `required bool`. (4) Regen ×2 sha-idempotent; convention 134/3/9; drift + freshness green; the request-schema deltas exactly: `effectiveFrom` out of 3 `required` arrays, `fullDayOnly` into 4.

**Validation Criteria**:
- [ ] Build 0 err; regen sha-idempotent ×2; convention 134/3/9; drift green; the spec request-delta EXACTLY the 3-out/4-in members (no other schema hunk)
- [ ] Zero event/outbox/audit-ORDER hunks (grep-provable) beyond the two data-param fixes + the sanctioned `ArchiveAsync` result-member; **response contract-shape identity: the previously-REACHABLE flows (all three PUTs' 200s, the no-prior-active publish 200) byte-identical to baseline; the previously-500ing archive/supersession flows asserted against their declared contract shapes (no runtime baseline exists — Step-0b Codex W1)**; policy strings byte-identical
- [ ] The compute-once contract verifiable by inspection: exactly ONE `body.EffectiveFrom` read per handler (the `?? today` site)

---

### TASK-12101 — FE: the two graduations + the primary-editor alignment + the pin flips (depends: 12100)
| Field | Value |
|-------|-------|
| **ID** | TASK-12101 |
| **Status** | complete (2026-07-23) — tsc 0 / lint 0 / vitest **663/663** (56 files; +3 tests). Both graduations landed (typed structured forms; `CHILD_ENTITLEMENT_PATH` deleted outright — the typed form interpolates via `params.path`; the WTM payload bytes unchanged as planned); the derived/preserved `fullDayOnly` via `isFullDayForcedType()` + the `currentFullDayOnly` floor; the editor's client-`today` dropped (ruling #1 alignment). Both carve-outs + partial tiers DELETED — grep-proof zero hits; FE-lint residue = exactly the S115 `ELIGIBILITY_PATH` rule (def `:159`, usage `:195`). **The pin-disposition inventory delivered per the Step-0b W1 ground truth: 3 FLIPs (the 2 `fullDayOnly` absence pins + the 9→10 key-set), 3 REMAINs (assertions byte-unchanged, comments-only), 1 UPDATE (the stale `:558` presence pin inverted to omission), 3 NEW pins (derived-true, preserve-round-trip via a legal non-forced `fullDayOnly:true` row, editor omission); expect counts non-decreasing (24→36 / +2 / 8→12); zero deletions.** RED-probes fired both graduated files (scratchpad-backup pattern; restored + re-verified green). The defective `Omit` types + both deferred-defect header narratives died. |
| **Agent** | UX/Frontend |
| **Components** | frontend/src/components/admin/EntitlementSection.tsx, frontend/src/hooks/useWageTypeMappings.ts, frontend/src/pages/admin/EntitlementConfigEditor.tsx, eslint.config.mjs, the S118 wire-pin test files |
| **KB Refs** | PAT-012 (call-form audit; the graduation pattern; the S118 useWageTypeMappings scratchpad-backup incident) |

**Description**: EntitlementSection (the admin/ one — NOT pages/admin/editPerson/): create body gains derived `fullDayOnly` (type-forced), update body gains preserved `fullDayOnly` (row value, type-forced floor) — both omit `effectiveFrom`; both calls graduate to the typed forms; `CHILD_ENTITLEMENT_PATH` carve-out + partial tier DELETED. useWageTypeMappings: update omits `effectiveFrom`, graduates typed; `WAGE_TYPE_MAPPING_UPDATE_PATH` carve-out + partial tier DELETED. EntitlementConfigEditor: the client-computed `effectiveFrom: today` DROPPED (ruling #1 alignment — the server owns today); `fullDayOnly` sends unchanged (already does; now binder-required). **The pin-flip inventory (Step-0b Reviewer W1 ground truth): FLIP to presence = the `fullDayOnly` absence pins (`EntitlementSection.wire.test.tsx:139` POST / `:157` PUT) AND the `EXPECTED_BODY_KEYS` exact key-set pins (`:90-93`, asserted `:138`/`:156` — 9 keys → 10). REMAIN absence pins (rationale comments rewritten dead-end→deliberate-server-default, assertions unchanged) = the `effectiveFrom` pins (`:140` POST / `:158` PUT) and the WTM 6-key set + no-`effectiveFrom` pin (`configTypedWire.test.ts:481-484` — the WTM payload BYTES do not change in S121, only the call form graduates; titles/comments rewritten). UPDATE = the STALE presence pin `configTypedWire.test.ts:558/:570-573` (the primary hook PUT carrying `effectiveFrom` "so a regression toward the child-PUT defect is caught" — ruling #1 inverts that rationale; the editor stops sending it; the `EntitlementConfigEditor.test.tsx:116-119` `putBodies` pins survive unchanged). Also in the mechanical inventory: the defective-payload `Omit` types (`EntitlementSection.tsx:59-62`, `useWageTypeMappings.ts:35-38`) and both deferred-defect header narratives (`EntitlementSection.tsx:29-42`, `useWageTypeMappings.ts:11-23`) DIE.** FULL-tier RED-probes on both graduated files (scratchpad-backup pattern — NEVER `git checkout` on uncommitted work). tsc 0 / lint 0 / vitest green.

**Validation Criteria**:
- [ ] Both carve-outs + partial tiers deleted (grep-proof); FE-lint residue = exactly the S115 eligibility pin; RED-probes fired both graduated files; zero explicit-T/`as`
- [ ] The pin-flip inventory: every S118 absence pin DISPOSITIONED per the FLIP/REMAIN/UPDATE inventory (no silent deletions; the REMAIN pins keep their absence assertions — ruling #1 omission); assertion counts non-decreasing per file
- [ ] The primary editor's omission alignment test-pinned (request body carries NO `effectiveFrom`; `fullDayOnly` still present)

---

### TASK-12102 — Test & QA: the tripwire rewrite + the success-path Docker assertions (depends: 12100)
| Field | Value |
|-------|-------|
| **ID** | TASK-12102 |
| **Status** | complete (2026-07-23) — **composed scoped run 76/76 (4m39s, fresh testcontainers, stack down), the arithmetic decomposing EXACTLY against the TASK-12100 expected-RED census** (Config/WTM 29/34→34/34; S118 27/28→28/28; Atomic 6/6 (+1); S121 8/8 new). The tripwire REWRITTEN (not deleted) as `…S118DefectFixed_AuditJsonLandsAndSuccessPathsMatchContract`: DRAFT-archive AND ACTIVE-archive 200s with jsonb-equality audit assertions (`{"status":"DRAFT"|"ACTIVE"}`→`{"status":"ARCHIVED"}`), supersession re-publish 200 with `archivedConfigId` populated + BOTH audit rows. The rollback proof: `ThrowOnSecondCallOutboxEnqueue` (real inner enqueue, Interlocked counter; call #1 genuinely inserts in-tx — the vanished first-enqueue row is the load-bearing assertion) in the atomic-mirror suite per the harness bar; full-tx rollback pinned (no rows on EITHER stream). The 5 blast-radius dispositions per the named plan (2 semantic flips renamed honestly with in-test guard-422 re-pins; the doctrine docstring rewritten to the post-ruling-#3 truth). New: `S121WageTypeMappingUpdateSpecRuntimeTests` (same-day in-place v1→2 / cross-day supersede via SQL-backdated predecessor / retained-validator 422 — the first success-path coverage the op has ever had) + `S121EntitlementWriteSpecRuntimeTests` (the repaired CARE_DAY dead-ends both verbs; preserve round-trip; binder-400 omission pins both verbs; explicit-false guard-422; the third-family primary PUT omission). Seed census S121-prefixed, disjoint (verified against boot seeders + all legacy prefixes). Unflipped tripwires byte-unchanged; footprint exactly the in-scope test files. |
| **Agent** | Test & QA |
| **Components** | tests/StatsTid.Tests.Regression/Contracts/ (S118 tripwire rewrite + new S121 assertions), tests/StatsTid.Tests.Regression/Outbox/ (the throw-on-second decorator + the atomic-mirror rollback assertion), **tests/StatsTid.Tests.Regression/Config/ (the 5 named ruling-#3 blast-radius tests — Step-0b Reviewer B2)** |
| **KB Refs** | PAT-012 (gate #1), FAIL-002, ADR-018 D3 |

**Description**: (1) The labeled 22P02 tripwire REWRITTEN (not deleted) to the green assertions: archive 200 (BOTH previous-status shapes — an ACTIVE archive AND a DRAFT archive — with the landed audit rows' `{"status":…}` JSON content asserted), supersession re-publish 200 with `archivedConfigId` populated + its two audit rows. (2) The supersede-leg rollback assertion in the atomic-mirror suite via a throw-on-SECOND-call outbox decorator (the archive leg reached, then fault → NO archived row / NO audit rows / NO outbox events — the full-tx-rollback proof). (3) WTM update success paths: same-day in-place (version bump + ETag) AND cross-day supersede (close+insert) — the branch discriminator driven by predecessor age; omission-of-`effectiveFrom` succeeds; explicit non-today still 422 (the retained validator pinned). (4) Child create+update success paths: CARE_DAY (forced true) + a non-forced type (preserve round-trip); omission-of-`fullDayOnly` now binder-400 (the ruling-#3 pin — the structural closure proven); the guard-422 still reachable via an explicit false flag on a forced type. **The COMPLETE ruling-#3 blast radius (Step-0b Codex W2 + Reviewer B2 — the full-tree sweep; named up front, not discovered mid-sprint) = exactly 5 tests, all in `Config/`:** (1) `EntitlementConfigEndpointTests.cs:120-135` `Post_CreatesNewConfig_…` (collateral: the VACATION body gains explicit `fullDayOnly:false`, stays 201); (2) `:197-213` `Post_VacationConfig_NonNineResetMonth_Returns422` (collateral: the body gains the member so the reset-month 422 concern is NOT silently re-routed to a binder-400); (3) `EntitlementConfigFullDayOnlyAdminTests.cs:75-100` `Post_SeniorDay_FlagAbsent_Returns422_NothingPersisted` (SEMANTIC FLIP: absent now 400 at the binder — deliberately rewritten to pin absent→400, the guard-422 re-pinned via an explicit-false body); (4) `:131-156` `Post_ChildSick_FlagAbsent_NotGuarded_CreatedWithFalse` (re-expressed as explicit `fullDayOnly:false` → 201 — the type-scoped-unguarded concern preserved); (5) `:266-287` `SubResource_Post_SeniorDay_FlagAbsent_Returns422` (the same semantic flip on the child surface). **The Reviewer sweep verified EVERYTHING ELSE SAFE** (every other body sends the member; the S118 spec-runtime suites send it; repo-level and DemoSeed/Smoke/Unit unaffected) **and the loosening blast radius CLEAN** (no test asserts 400-on-missing-`effectiveFrom`; the retained-validator 422 pins — `WageTypeMappingEndpointTests.cs:211-237`, `EntitlementConfigEndpointTests.cs:337-371` — pin explicit-non-today and survive unchanged).** (5) Primary PUT: omission-of-`effectiveFrom` succeeds. Seeds S121-prefixed, disjoint; the named tripwire suites (minus the deliberate flips) UNMODIFIED.

**Validation Criteria**:
- [ ] All new Docker assertions green (exact counts); the tripwire flip + the rollback proof + the binder-400 pin + the retained-422 pins demonstrated; seed census disjoint; composed Contracts green; unflipped tripwires byte-unchanged

---

### TASK-12103 — Orchestrator: validation + Step 7a + close
| Field | Value |
|-------|-------|
| **ID** | TASK-12103 |
| **Status** | complete (2026-07-23) — **Step 7a CONVERGED both lenses at CYCLE 1 (zero absorption needed — a program first for a wire-changing sprint): External Codex "No findings" (0B/0W/0N); Internal Reviewer 0B/0W/5N, all record-only:** N1 the interpolated previous-status JSON is safe today (DB-constrained values; the jsonb-equality tripwire covers both known shapes) — `JsonSerializer.Serialize` suggested if the file is ever touched again; N2 the atomic mirror omits the audit-projection legs per the suite's pre-existing convention (fault-point equivalence holds); N3 the Ground-Facts "Constraint-Validator absence sweep lines" = a run-time checklist item, VACUOUSLY SATISFIED (no tracked file encodes them); N4 the 2 new S121 test files untracked at review time — included in the explicit close file set (the FAIL-003 gate backstops); N5 the run-level claims (76/76, 663/663, sha ×2) deferred to the central validation run as the authority. The Reviewer additionally verified: exactly one `body.EffectiveFrom` read per handler with the echo on the computed value; the WTM discriminator semantics identical for the only previously-legal input; the repo diff touches ONLY the return + comments; the 44-site AppendAuditAsync sweep clean; the pin dispositions 1:1 with the Step-0b inventory, zero deletions; the preserve-vs-derive fixture genuinely distinguishing (a derivation would send false). Validation run + close pending. |
| **Agent** | Orchestrator |
| **Components** | sprint log, INDEX, PAT-012 (the wire-change classes' 4th/5th instances recorded), close gates |
| **KB Refs** | PAT-012, FAIL-002/003 |

**Description**: Validation (`sprint-test-validation`; FAIL-002 sequencing — ReportingLine needs compose :5432). Step 7a dual-lens (named targets: the compute-once substitution completeness ×3 handlers; the audit-content honesty both shapes; the pin-flip inventory; the graduation carve-out deletions; the binder-400 structural-closure pin; response byte-identity ×all touched ops). PAT-012: the loosening + tightening recorded as ruled wire-change instances; the S118 deferred-defect ledger CLOSED. Close per the 5 gates; explicit file set.

**Validation Criteria**:
- [ ] Delta table; Step 7a converged; close + push + CI green all 7 jobs; PAT-012 + deferred-defect ledger updated

---

## Test Validation Report (Step 4/5, sprint-test-validation — run 2026-07-23)
| Suite | Previous (S120) | Current | Delta |
|-------|----------|---------|-------|
| Unit | 868 | 868 | 0 |
| Regression | 1350 | 1359 | +9 (the 8 S121 per-route Contracts tests + 1 atomic rollback test) |
| Smoke | 6 | 6 | 0 (green against the freshly built composed stack) |
| DemoSeed | 55 | 55 | 0 |
| Frontend | 660 | 663 | +3 (2 wire-test pins + 1 editor omission pin; zero removals) |
| **Total** | **2939** | **2951** | **+12** |

**Run honesty:** the central chain (unit 868/868 green; regression 1h20m quiet run 1314/1359) shed 45 — ALL isolation-adjudicated environmental, none S121-caused: 42 = the ReportingLine fixed-port class (stack down per the FAIL-002 teardown rule; the S118/S120 precedent) cleared **103/103** on a fresh compose Postgres; 3 = long-run contention flakes (2 S118 list GETs + 1 new S121 test — all green in the agent's scoped 76/76 AND in the 3/3 isolation rerun). Smoke's 6/6 against the healthy composed stack. The FE 663/663 is the TASK-12101 agent's full-suite run on the final FE tree (no FE change after it). The rewritten tripwire, the 5 blast-radius flips, and all 9 new tests ran green INSIDE the central run (the 45 sheds contain none of them beyond the 1 contention flake, which cleared).

## Phase Plan
- **Phase 1:** TASK-12100 (one Backend agent — the DTO changes + audit sites + regen are coupled through the pipeline).
- **Phase 2 (parallel):** TASK-12101 (FE) ∥ TASK-12102 (assertions) — file-disjoint (frontend/ vs tests/).
- **Phase 3:** TASK-12103 (validation + close).

**Atomicity pin:** ONE close commit; explicit file set; gates at close.

## Legal & Payroll Verification
| Check | Status |
|-------|--------|
| Agreement rule compliance | PASSIVE — the guard, the DB CHECK, and the D-A ruling untouched (P4); the same-day-only S29 rule semantics unchanged (server-defaulted today ≡ the only previously-legal value) |
| Wage type mapping correctness (P6) | ACTIVE-WATCH — the WTM update flow becomes REACHABLE for the first time; both repo-dispatch branches Docker-asserted; the persisted mapping's EffectiveFrom = the same today it always had to be |
| Event sourcing / audit (P3) | ACTIVE-WATCH — the defect IS a P3 defect (audit rows never landed); the fix makes the archive/publish audit+outbox+state commit atomically for the first time; append ORDER byte-unchanged; the rollback proof extended to the supersede leg |
| Security (P7) | PASSIVE — no policy or endpoint surface change |
